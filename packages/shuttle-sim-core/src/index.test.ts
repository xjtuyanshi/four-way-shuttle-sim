import { ShuttleScenarioSchema, ShuttleSimStateSchema, type Reservation, type ShuttleScenario } from '@four-way-shuttle/schemas';

import goldenStaticSceneContract from '../../../config/shuttle/static-scene-contract.golden.json';
import {
  ShuttleSimCore,
  REQUIRED_CALIBRATION_DIMENSION_KEYS,
  calculateTravelTimeSec,
  createDefaultShuttleScenario,
  createInboundOutboundDemoScenario,
  createInboundMvpBaselineScenario,
  hashEventLog,
  hashEngineSnapshot,
  motionProfileAt,
  runFixedStep,
  staticObstacleFootprintViolations,
  summarizeScenarioStaticSceneContract,
  topLiftWorkcellRouteContractViolations,
  verticalStorageFootprintEdgeViolations
} from './index.js';

function testScenario(overrides: Partial<ShuttleScenario>): ShuttleScenario {
  const base: ShuttleScenario = {
    schemaVersion: 'shuttle.phase0.v0',
    id: 'test-scenario',
    name: 'Test Scenario',
    seed: 7,
    durationSec: 60,
    timeStepSec: 0.2,
    vehicles: {
      count: 2,
      lengthM: 1,
      widthM: 1,
      heightM: 0.2,
      emptySpeedMps: 1,
      loadedSpeedMps: 0.7,
      accelerationMps2: 1,
      switchDirectionSec: 0,
      liftTimeSec: 0,
      lowerTimeSec: 0,
      maxLoadKg: 1000,
      safetyRadiusM: 0.4,
      batteryEnabled: false,
      initialSoc: 1
    },
    layout: {
      units: 'meter',
      calibrationProfile: null,
      nodes: [
        { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
        { id: 'B', type: 'parking', x: 4, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
      ],
      edges: [
        { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true }
      ],
      zones: []
    },
    taskGeneration: {
      inboundRatePerHour: 0,
      outboundRatePerHour: 0,
      inboundOutboundMix: 0.5,
      arrivalDistribution: 'deterministic',
      maxTasks: 1,
      initialStorageFillPolicy: 'full-columns',
      initialOutboundFullColumns: 0
    },
    physicsParams: {
      emptySpeedMps: 1,
      loadedSpeedMps: 0.7,
      accelerationMps2: 1,
      switchDirectionSec: 0,
      liftTimeSec: 0,
      lowerTimeSec: 0,
      loadedClearanceM: 0.2,
      reservationClearanceSec: 0.2
    },
    routingPolicy: {
      algorithm: 'astar',
      allowReplan: true,
      routeTimeoutSec: 12,
      maxReplansPerTask: 3
    },
    trafficPolicy: {
      controllerMode: 'reservation-v2',
      edgeCapacity: 1,
      nodeCapacity: 1,
      zoneCapacity: 1,
      liftApproachCapacity: 1,
      sourceBufferCapacity: 4,
      collisionAvoidanceEnabled: true,
      minimumClearanceSec: 0.2,
      dynamicAvoidanceClearanceM: 0.5,
      priorityAgingSec: 20,
      deadlockDetectSec: 1,
      deadlockBreakPolicy: 'oldest-waits-wins'
    }
  };
  return ShuttleScenarioSchema.parse({
    ...base,
    ...overrides,
    vehicles: { ...base.vehicles, ...overrides.vehicles },
    layout: { ...base.layout, ...overrides.layout },
    taskGeneration: { ...base.taskGeneration, ...overrides.taskGeneration },
    physicsParams: { ...base.physicsParams, ...overrides.physicsParams },
    routingPolicy: { ...base.routingPolicy, ...overrides.routingPolicy },
    trafficPolicy: { ...base.trafficPolicy, ...overrides.trafficPolicy }
  });
}

function runFor(sim: ShuttleSimCore, durationSec: number, dtSec = 0.2) {
  sim.start();
  for (let elapsedSec = 0; elapsedSec < durationSec; elapsedSec = Number((elapsedSec + dtSec).toFixed(6))) {
    sim.step(dtSec);
  }
  return sim.getState();
}

function addStoredLoadsForAllStorageExcept(
  sim: ShuttleSimCore,
  scenario: ShuttleScenario,
  excludedNodeIds: Set<string>,
  idPrefix: string
) {
  for (const node of scenario.layout.nodes.filter((candidate) => candidate.type === 'storage')) {
    if (excludedNodeIds.has(node.id)) {
      continue;
    }
    sim.addLoadForTest({
      id: `${idPrefix}-${node.id}`,
      state: 'stored',
      nodeId: node.id,
      vehicleId: null,
      weightKg: 100
    });
  }
}

function runForWithStoragePathAssertions(sim: ShuttleSimCore, scenario: ShuttleScenario, durationSec: number, dtSec = 0.2, sampleSec = 5) {
  sim.start();
  let state = sim.getState();
  let nextSampleSec = 0;
  for (let elapsedSec = 0; elapsedSec < durationSec; elapsedSec = Number((elapsedSec + dtSec).toFixed(6))) {
    state = sim.step(dtSec);
    if (state.simTimeSec + 1e-6 >= nextSampleSec) {
      expectNoVerticalStorageFootprintTravel(scenario, state);
      expectNoLoadedStorageTransitThroughStoredLoads(state);
      nextSampleSec = Number((nextSampleSec + sampleSec).toFixed(6));
    }
  }
  return state;
}

function expectNoTrafficSafetyFailures(state: ReturnType<ShuttleSimCore['getState']>) {
  expect(state.kpis.deadlockCount).toBe(0);
  expect(state.kpis.livelockCount).toBe(0);
  expect(state.traffic.physicalViolationCount).toBe(0);
}

function addStoredLoads(sim: ShuttleSimCore, nodeIds: string[]) {
  nodeIds.forEach((nodeId, index) => {
    sim.addLoadForTest({
      id: `stored-${String(index + 1).padStart(4, '0')}`,
      state: 'stored',
      nodeId,
      vehicleId: null,
      weightKg: 100
    });
  });
}

function crossRowStorageHops(routeNodeIds: string[]): Array<[string, string]> {
  const storageRow = (nodeId: string): string | null => /^storage-r(\d+)-c\d+$/.exec(nodeId)?.[1] ?? null;
  const hops: Array<[string, string]> = [];
  for (let index = 1; index < routeNodeIds.length; index += 1) {
    const from = routeNodeIds[index - 1]!;
    const to = routeNodeIds[index]!;
    const fromRow = storageRow(from);
    const toRow = storageRow(to);
    if (fromRow && toRow && fromRow !== toRow) {
      hops.push([from, to]);
    }
  }
  return hops;
}

function storageRowsInRoute(routeNodeIds: string[]): string[] {
  return [...new Set(routeNodeIds.flatMap((nodeId) => {
    const match = /^storage-r(\d+)-c\d+$/.exec(nodeId);
    return match ? [match[1]!] : [];
  }))];
}

function storageColumn(nodeId: string): number | null {
  return Number(/^storage-r\d+-c(\d+)$/.exec(nodeId)?.[1] ?? NaN) || null;
}

function isTopLiftColumnAccessNodeIdForTest(nodeId: string): boolean {
  return /^column-(?:top-a|top-b|middle|bottom-a|bottom-b)-c\d+$/.test(nodeId);
}

function storageFillHoles(nodeIds: string[]): string[] {
  const rows = new Map<string, number[]>();
  for (const nodeId of nodeIds) {
    const match = /^storage-r(\d+)-c(\d+)$/.exec(nodeId);
    if (!match) {
      continue;
    }
    rows.set(match[1]!, [...(rows.get(match[1]!) ?? []), Number(match[2])]);
  }

  const holes: string[] = [];
  for (const [row, columns] of rows) {
    const columnSet = new Set(columns);
    const maxColumn = Math.max(...columns);
    for (let column = 1; column <= maxColumn; column += 1) {
      if (!columnSet.has(column)) {
        holes.push(`r${row}-c${String(column).padStart(2, '0')}`);
      }
    }
  }
  return holes.sort();
}

function expectStorageTraversalOnlyHorizontal(routeNodeIds: string[]) {
  for (let index = 1; index < routeNodeIds.length; index += 1) {
    const from = /^storage-r(\d+)-c(\d+)$/.exec(routeNodeIds[index - 1]!);
    const to = /^storage-r(\d+)-c(\d+)$/.exec(routeNodeIds[index]!);
    if (from && to) {
      expect(to[1]).toBe(from[1]);
    }
  }
}

function edgeIdForRouteHop(scenario: ShuttleScenario, fromNodeId: string, toNodeId: string): string | null {
  const edge = scenario.layout.edges.find((candidate) =>
    (candidate.from === fromNodeId && candidate.to === toNodeId) ||
    (candidate.directionMode === 'twoWay' && candidate.from === toNodeId && candidate.to === fromNodeId)
  );
  return edge?.id ?? null;
}

function expectRouteHopsAdjacent(scenario: ShuttleScenario, routeNodeIds: string[]) {
  for (let index = 1; index < routeNodeIds.length; index += 1) {
    expect(edgeIdForRouteHop(scenario, routeNodeIds[index - 1]!, routeNodeIds[index]!)).not.toBeNull();
  }
}

function expectNoVerticalStorageFootprintTravel(scenario: ShuttleScenario, state: ReturnType<ShuttleSimCore['getState']>) {
  const forbiddenEdgeIds = new Set(verticalStorageFootprintEdgeViolations(scenario).map((violation) => violation.edgeId));
  const movingViolations = state.vehicles
    .filter((vehicle) => vehicle.currentEdgeId && forbiddenEdgeIds.has(vehicle.currentEdgeId))
    .map((vehicle) => `${vehicle.id}:${vehicle.currentEdgeId}`)
    .sort();
  const routeViolations = state.vehicles.flatMap((vehicle) => {
    const route = vehicle.routeNodeIds.slice(Math.max(0, vehicle.routeIndex));
    return route.flatMap((nodeId, index) => {
      const nextNodeId = route[index + 1];
      if (!nextNodeId) {
        return [];
      }
      const edgeId = edgeIdForRouteHop(scenario, nodeId, nextNodeId);
      return edgeId && forbiddenEdgeIds.has(edgeId) ? [`${vehicle.id}:${edgeId}`] : [];
    });
  }).sort();

  expect({ movingViolations, routeViolations }).toEqual({ movingViolations: [], routeViolations: [] });
}

function expectNoLoadedStorageTransitThroughStoredLoads(state: ReturnType<ShuttleSimCore['getState']>) {
  const storedNodeIds = new Set(
    state.loads
      .filter((load) => load.state === 'stored' && load.nodeId?.startsWith('storage-'))
      .flatMap((load) => load.nodeId ? [load.nodeId] : [])
  );
  const taskById = new Map(state.tasks.map((task) => [task.id, task]));

  for (const vehicle of state.vehicles.filter((candidate) => candidate.loaded && candidate.taskId)) {
    const task = taskById.get(vehicle.taskId!);
    if (!task) {
      continue;
    }
    const dropoffIndex = vehicle.routeNodeIds.indexOf(task.dropoffNodeId, vehicle.routeIndex);
    const loadedPath = dropoffIndex >= 0
      ? vehicle.routeNodeIds.slice(vehicle.routeIndex + 1, dropoffIndex)
      : vehicle.routeNodeIds.slice(vehicle.routeIndex + 1);
    expect(loadedPath.filter((nodeId) => storedNodeIds.has(nodeId))).toEqual([]);
  }
}

function expectLoadedInboundRoutesApproachDropoffFromRight(state: ReturnType<ShuttleSimCore['getState']>) {
  const taskById = new Map(state.tasks.map((task) => [task.id, task]));
  for (const vehicle of state.vehicles.filter((candidate) => candidate.loaded && candidate.taskId)) {
    const task = taskById.get(vehicle.taskId!);
    if (!task || task.kind !== 'inbound') {
      continue;
    }
    const dropoffMatch = /^storage-r(\d+)-c\d+$/.exec(task.dropoffNodeId);
    if (!dropoffMatch) {
      continue;
    }
    const row = dropoffMatch[1]!;
    for (const route of [
      vehicle.routeNodeIds.slice(Math.max(0, vehicle.routeIndex)),
      vehicle.plannedRouteNodeIds
    ]) {
      const dropoffIndex = route.indexOf(task.dropoffNodeId);
      if (dropoffIndex < 0) {
        continue;
      }
      const routeToDropoff = route.slice(0, dropoffIndex + 1);
      expect(routeToDropoff).not.toContain(`left-row-${row}`);
      const sameRowColumns = routeToDropoff
        .filter((nodeId) => nodeId.startsWith(`storage-r${row}-c`))
        .map((nodeId) => storageColumn(nodeId))
        .filter((column): column is number => column !== null);
      for (let index = 1; index < sameRowColumns.length; index += 1) {
        expect(sameRowColumns[index]).toBeLessThanOrEqual(sameRowColumns[index - 1]!);
      }
    }
  }
}

function expectIdleVehiclesParkedOnlyOnParkableNodes(scenario: ShuttleScenario, state: ReturnType<ShuttleSimCore['getState']>) {
  const nodesById = new Map(scenario.layout.nodes.map((node) => [node.id, node]));
  for (const vehicle of state.vehicles.filter((candidate) => candidate.state === 'idle' && candidate.taskId === null)) {
    const node = nodesById.get(vehicle.currentNodeId);
    expect(node).toBeDefined();
    expect(node?.noParking).toBe(false);
    expect(node?.noStop).toBe(false);
    expect(['parking', 'storage']).toContain(node?.type);
  }
}

function verifiedCalibrationDimensions(): NonNullable<ShuttleScenario['layout']['calibrationProfile']>['dimensions'] {
  const currentAssumptionValues = new Map(
    createDefaultShuttleScenario().layout.calibrationProfile?.dimensions.map((dimension) => [dimension.key, dimension.valueM])
  );
  return REQUIRED_CALIBRATION_DIMENSION_KEYS.map((key) => ({
    key,
    label: key,
    valueM: currentAssumptionValues.get(key) ?? 1,
    source: 'cad' as const,
    confidence: 'high' as const
  }));
}

describe('shuttle phase 0 SimCore', () => {
  it('validates the default phase 0 scenario schema', () => {
    const scenario = createDefaultShuttleScenario();
    const parsed = ShuttleScenarioSchema.parse(scenario);

    expect(parsed.schemaVersion).toBe('shuttle.phase0.v0');
    expect(parsed.vehicles.safetyRadiusM).toBe(0.1);
    expect(parsed.physicsParams.liftTimeSec).toBe(0.05);
    expect(parsed.physicsParams.lowerTimeSec).toBe(0.05);
    expect(parsed.layout.nodes.filter((node) => node.type === 'lift-blackbox').map((node) => node.id).sort()).toEqual([
      'inbound-lift-bottom-01',
      'inbound-lift-bottom-02',
      'inbound-lift-top-01',
      'inbound-lift-top-02',
      'outbound-lift-bottom-01',
      'outbound-lift-bottom-02',
      'outbound-lift-top-01',
      'outbound-lift-top-02'
    ]);
    expect(parsed.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'inbound')).toHaveLength(4);
    expect(parsed.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'outbound')).toHaveLength(4);
    expect(parsed.layout.nodes.filter((node) => node.type === 'parking' && !node.noParking).map((node) => node.id).sort()).toEqual([
      'parking-a',
      'parking-b',
      'parking-c',
      'parking-d',
      'parking-e',
      'parking-f',
      'parking-g',
      'parking-h'
    ]);
    expect(parsed.layout.nodes.filter((node) => node.id.includes('-stage-'))).toHaveLength(0);
    expect(parsed.layout.zones.some((zone) => zone.noStop && zone.noParking)).toBe(true);
    expect(parsed.layout.calibrationProfile).toMatchObject({
      id: 'phase0-cad-assumption-v1',
      status: 'assumption',
      units: 'meter'
    });
    expect(parsed.layout.calibrationProfile?.blockedCells).toEqual([]);
    expect(parsed.layout.calibrationProfile?.dimensions.map((dimension) => dimension.key)).toEqual([
      'storageCellPitchX',
      'storageCellPitchZ',
      'storageBayGapX',
      'mainLaneCenterSpacingZ',
      'innerStorageBankGapZ',
      'liftStandoffZ',
      'sideClearanceX'
    ]);
  });

  it('does not compact inbound source buffers while reading public state', () => {
    const sim = new ShuttleSimCore(
      createInboundOutboundDemoScenario({
        durationSec: 1800,
        vehicles: { count: 8 },
        taskGeneration: {
          inboundRatePerHour: 3600,
          outboundRatePerHour: 3600,
          inboundOutboundMix: 0.5,
          initialOutboundFullColumns: 4,
          initialStorageFillPolicy: 'zone-balanced-50'
        },
        layoutProfile: { layoutKind: 'top-lift-column', liftPairCount: 2 },
        trafficPolicy: { collisionAvoidanceEnabled: true }
      })
    );

    sim.start();
    for (let timeSec = 30; timeSec <= 90; timeSec += 30) {
      sim.advanceByInPlace(30);
      const loadsBeforeRead = sim.createSnapshot().loads;

      sim.getState();

      expect(sim.createSnapshot().loads).toEqual(loadsBeforeRead);
    }
  }, 20000);

  it('can create an all-inbound lift layout without connector staging queues', () => {
    const scenario = createDefaultShuttleScenario({ liftMode: 'all-inbound', vehicles: { count: 8 } });
    const parsed = ShuttleScenarioSchema.parse(scenario);
    const liftNodes = parsed.layout.nodes.filter((node) => node.type === 'lift-blackbox');

    expect(liftNodes).toHaveLength(8);
    expect(liftNodes.every((node) => node.liftKind === 'inbound')).toBe(true);
    expect(parsed.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'outbound')).toHaveLength(0);
    expect(parsed.layout.nodes.filter((node) => /-stage-\d\d$/.test(node.id))).toHaveLength(0);
    expect(parsed.layout.edges.filter((edge) => edge.from.includes('-stage-') || edge.to.includes('-stage-'))).toHaveLength(0);

    const state = new ShuttleSimCore(scenario).getState();
    expect(state.traffic.liftPorts.filter((port) => port.kind === 'inbound')).toHaveLength(8);
    expect(state.traffic.liftPorts.filter((port) => port.kind === 'outbound')).toHaveLength(0);
  });

  it('publishes 90-degree shuttle yaw during direction-change delay', () => {
    const scenario = testScenario({
      vehicles: {
        count: 1,
        lengthM: 1,
        widthM: 1,
        heightM: 0.2,
        emptySpeedMps: 1,
        loadedSpeedMps: 0.7,
        accelerationMps2: 1,
        switchDirectionSec: 1,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        maxLoadKg: 1000,
        safetyRadiusM: 0.4,
        batteryEnabled: false,
        initialSoc: 1
      },
      physicsParams: {
        emptySpeedMps: 1,
        loadedSpeedMps: 0.7,
        accelerationMps2: 1,
        switchDirectionSec: 1,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.2
      },
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'parking', x: 1, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 1, y: 0, z: 1, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'B-C', noParking: true }
        ],
        zones: []
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C']);
    sim.start();

    let state = sim.step(2);
    expect(state.vehicles[0]!.currentNodeId).toBe('B');
    state = sim.step(0.2);

    const vehicle = state.vehicles[0]!;
    expect(vehicle.currentNodeId).toBe('B');
    expect(vehicle.targetNodeId).toBe('C');
    expect(vehicle.x).toBe(1);
    expect(vehicle.z).toBe(0);
    expect(vehicle.yaw).toBeLessThan(0);
    expect(vehicle.yaw).toBeGreaterThan(-Math.PI / 2);
    expect(vehicle.speedMps).toBe(0);
  });

  it('can create the top-lift column-fill layout with fixed inbound/outbound ports', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 8 },
      trafficPolicy: { controllerMode: 'agent-refresh' }
    });
    const parsed = ShuttleScenarioSchema.parse(scenario);
    const contract = summarizeScenarioStaticSceneContract(parsed);

    expect(parsed.layout.calibrationProfile?.id).toBe('top-lift-column-v1');
    expect(contract.storageCellCount).toBe(196);
    expect(contract.storageIslandCount).toBe(4);
    expect(contract.storageRows).toBe(14);
    expect(contract.storageColumns).toBe(14);
    expect(contract.storagePolicy).toBe('columnContiguousBottomToTopFill');
    expect(contract.inboundStorageFlow).toBe('bottomToTop');
    expect(contract.diagonalTrackCount).toBe(0);
    const normalColumnPitchM = parsed.layout.nodes.find((node) => node.id === 'storage-r01-c02')!.x -
      parsed.layout.nodes.find((node) => node.id === 'storage-r01-c01')!.x;
    const bayBoundaryPitchM = parsed.layout.nodes.find((node) => node.id === 'storage-r01-c08')!.x -
      parsed.layout.nodes.find((node) => node.id === 'storage-r01-c07')!.x;
    expect(bayBoundaryPitchM).toBeCloseTo(normalColumnPitchM * 2, 6);
    expect(parsed.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'inbound').map((node) => node.id)).toEqual([
      'lift-01-inbound'
    ]);
    expect(parsed.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'outbound').map((node) => node.id)).toEqual([
      'lift-01-outbound'
    ]);
    for (const liftId of ['lift-01-inbound', 'lift-01-outbound']) {
      const liftNode = parsed.layout.nodes.find((node) => node.id === liftId)!;
      const accessNode = parsed.layout.nodes.find((node) => node.id === `${liftId}-buffer-access`);
      const serviceExitNode = parsed.layout.nodes.find((node) => node.id === `${liftId}-queue-01-service-exit`);
      const topALaneNode = parsed.layout.nodes.find((node) => node.id === 'column-top-a-c04')!;
      expect(accessNode?.type).toBe('intersection');
      expect(serviceExitNode?.type).toBe('intersection');
      expect(Math.abs((serviceExitNode?.z ?? 0) - topALaneNode.z)).toBeGreaterThanOrEqual(
        parsed.vehicles.widthM + parsed.vehicles.safetyRadiusM
      );
      if (liftId.endsWith('inbound')) {
        const queueNode = parsed.layout.nodes.find((node) => node.id === `parking-${liftId}-queue`);
        const queueAccessNode = parsed.layout.nodes.find((node) => node.id === `${liftId}-queue-access`);
        const queueLaneNode = parsed.layout.nodes.find((node) => node.id === `${liftId}-queue-01-access`);
        const queuePickupAccessNode = parsed.layout.nodes.find((node) => node.id === `${liftId}-queue-pickup-access`);
        expect(queueNode?.type).toBe('parking');
        expect(queueAccessNode?.type).toBe('intersection');
        expect(queueLaneNode?.type).toBe('intersection');
        expect(queuePickupAccessNode).toBeUndefined();
        expect(queueNode?.x).toBeGreaterThan(accessNode?.x ?? Number.POSITIVE_INFINITY);
        expect(queueNode?.z).toBeLessThan(accessNode?.z ?? Number.NEGATIVE_INFINITY);
        expect(parsed.layout.edges.some((edge) =>
          (edge.from === queueLaneNode?.id && edge.to === queueNode?.id) ||
          (edge.to === queueLaneNode?.id && edge.from === queueNode?.id)
        )).toBe(true);
      }
      const bufferNodes = parsed.layout.nodes
        .filter((node) => node.id.startsWith(`${liftId}-buffer-`))
        .filter((node) => !node.id.endsWith('-access'))
        .sort((left, right) => left.id.localeCompare(right.id));
      expect(bufferNodes.map((node) => node.id)).toEqual([
        `${liftId}-buffer-01`,
        `${liftId}-buffer-02`,
        `${liftId}-buffer-03`
      ]);
      expect(bufferNodes.every((node) => node.type === (liftId.endsWith('inbound') ? 'inbound' : 'outbound'))).toBe(true);
      const direction = liftId.endsWith('inbound') ? 1 : -1;
      bufferNodes.forEach((node) => {
        expect(node.x).toBeCloseTo(liftNode.x + direction * 0.7, 6);
      });
      const zDirection = (accessNode?.z ?? liftNode.z) >= liftNode.z ? 1 : -1;
      bufferNodes.forEach((node, index) => {
        if (index > 0) {
          expect((node.z - bufferNodes[index - 1]!.z) * zDirection).toBeGreaterThan(0);
        }
        expect((node.z - liftNode.z) * zDirection).toBeGreaterThanOrEqual(0);
        expect((node.z - (accessNode?.z ?? node.z)) * zDirection).toBeLessThanOrEqual(0);
      });
      expect(accessNode?.x).toBeCloseTo(bufferNodes.at(-1)?.x ?? 0, 6);
      expect(((accessNode?.z ?? liftNode.z) - bufferNodes.at(-1)!.z) * zDirection).toBeGreaterThanOrEqual(0);
    }
    const hasEdge = (left: string, right: string): boolean => parsed.layout.edges.some((edge) =>
      (edge.from === left && edge.to === right) || (edge.from === right && edge.to === left)
    );
    expect(hasEdge('column-top-a-c04', 'lift-01-inbound-buffer-access')).toBe(false);
    expect(hasEdge('lift-01-inbound-buffer-access', 'column-top-a-c05')).toBe(false);
    expect(verticalStorageFootprintEdgeViolations(parsed)).toEqual([]);
  });

  it('primes the top-lift inbound source buffers and keeps SKU columns clear of idle parking', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        liftApproachCapacity: 4,
        sourceBufferCapacity: 4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const state = sim.getState();
    const inboundLiftIds = scenario.layout.nodes
      .filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'inbound')
      .map((node) => node.id)
      .sort();
    const sourceSlotIdsByLift = new Map(inboundLiftIds.map((liftId) => [
      liftId,
      [liftId, `${liftId}-buffer-01`, `${liftId}-buffer-02`, `${liftId}-buffer-03`]
    ]));
    const sourceSlotIds = new Set([...sourceSlotIdsByLift.values()].flat());
    const waitingSourceLoads = state.loads.filter((load) =>
      load.state === 'waiting' &&
      load.nodeId !== null &&
      sourceSlotIds.has(load.nodeId)
    );

    expect(inboundLiftIds).toHaveLength(2);
    expect(waitingSourceLoads).toHaveLength(8);
    for (const liftId of inboundLiftIds) {
      const occupiedSlotIds = waitingSourceLoads
        .map((load) => load.nodeId)
        .filter((nodeId): nodeId is string => nodeId !== null && sourceSlotIdsByLift.get(liftId)!.includes(nodeId))
        .sort();
      expect(occupiedSlotIds).toEqual(sourceSlotIdsByLift.get(liftId)!.sort());
    }
    const inboundTasks = state.tasks.filter((task) => task.kind === 'inbound');
    expect(inboundTasks).toHaveLength(2);
    expect(inboundTasks.map((task) => task.pickupNodeId)).toEqual([
      'column-top-a-c08',
      'column-top-a-c22'
    ]);
    expect(inboundTasks.map((task) => task.dropoffNodeId)).toEqual([
      'storage-r14-c01',
      'storage-r14-c15'
    ]);
    expect(state.vehicles.map((vehicle) => vehicle.currentNodeId)).toEqual([
      'storage-r02-c07',
      'storage-r02-c21',
      'storage-r13-c03',
      'storage-r13-c04',
      'storage-r13-c05',
      'storage-r13-c06',
      'storage-r13-c07',
      'storage-r13-c08'
    ]);
    expect(state.vehicles.every((vehicle) => !vehicle.currentNodeId.startsWith('parking-'))).toBe(true);
    expect(state.vehicles.every((vehicle) => !state.loads.some((load) => load.nodeId === vehicle.currentNodeId))).toBe(true);
    expect(state.traffic.liftPorts.filter((port) => port.kind === 'inbound').map((port) => port.sourceBufferOccupancy)).toEqual([4, 4]);
  });

  it('starts mixed top-lift demo shuttles inside the feasible grid instead of the exterior grey parking pads', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({ durationSec: 2400 }));
    const state = sim.getState();

    expect(state.vehicles.map((vehicle) => vehicle.currentNodeId)).toEqual([
      'column-top-a-c09',
      'column-top-a-c10',
      'column-top-a-c11',
      'column-top-a-c23',
      'column-top-a-c24',
      'column-top-a-c25',
      'storage-r13-c03',
      'storage-r13-c04'
    ]);
    expect(state.vehicles.slice(0, 6).every((vehicle) =>
      vehicle.localRouteReason === 'inbound-queue-standby' &&
      vehicle.plannedGoalNodeId === vehicle.currentNodeId
    )).toBe(true);
    expect(state.vehicles.slice(6).every((vehicle) => vehicle.localRouteReason === null)).toBe(true);
    expect(state.vehicles.every((vehicle) => !vehicle.currentNodeId.startsWith('parking-'))).toBe(true);
    expect(state.vehicles.every((vehicle) => !state.loads.some((load) => load.nodeId === vehicle.currentNodeId))).toBe(true);
  });

  it('routes inbound pickup to the lift-side yellow-grid edge stop instead of lift internals', () => {
    const scenario = createInboundMvpBaselineScenario();
    const sim = new ShuttleSimCore(scenario);
    sim.start();
    sim.step(0.25);
    const state = sim.getState();
    const vehicle = state.vehicles.find((candidate) => candidate.taskId === 'task-0001');
    const task = state.tasks.find((candidate) => candidate.id === 'task-0001');

    expect(task?.pickupNodeId).toBe('column-top-a-c08');
    expect(vehicle?.routeNodeIds.at(-1)).toBe('column-top-a-c08');
    expect(task?.pickupNodeId).not.toContain('service-exit');
    const route = vehicle?.routeNodeIds ?? [];
    expect(route).toContain('column-top-a-c08');
    expect(route).not.toContain('lift-01-inbound-queue-01-entry-access');
    expect(route).not.toContain('lift-01-inbound-queue-01-service-exit');
    expect(route).not.toContain('column-top-a-c09');
    expect(route).not.toContain('module-01-spine-top-a');
    expect(route).not.toContain('lift-01-inbound-buffer-access');
    expect(route).not.toContain('lift-01-inbound-queue-access');
    expect(route).not.toContain('lift-01-inbound-buffer-03');
    expect(route).not.toContain('lift-01-inbound-queue-pickup-access');
    expect(route).not.toContain('parking-lift-01-inbound-queue');
    expect(vehicle?.routeNodeIds).not.toContain('lift-01-inbound');
  }, 15000);

  it('allows projected inbound queue vehicles to stop on their planned top-a queue point', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        initialStorageFillPolicy: 'zone-balanced-50',
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'projected-queue-stop-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-02',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'projected-queue-stop-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'projected-queue-stop-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-01', 'projected-queue-stop-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null }>;
      tasks: Array<{ id: string }>;
      mustStopAtNode(vehicle: unknown, task: unknown, nodeId: string): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'projected-queue-stop-task')!;
    vehicle.plannedGoalNodeId = 'column-top-a-c10';

    expect(internals.mustStopAtNode(vehicle, task, 'column-top-a-c10')).toBe(true);
  });

  it('compresses projected inbound queue vehicles toward pickup on the yellow top-a queue line', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 4,
        initialStorageFillPolicy: 'zone-balanced-50',
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'projected-queue-compress-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-02',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'projected-queue-compress-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'projected-queue-compress-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-01', 'projected-queue-compress-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; waitReason: string | null; plannedGoalNodeId: string | null }>;
      tasks: Array<{ id: string }>;
      taskAssignmentRoute(vehicle: unknown, task: unknown): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'projected-queue-compress-task')!;

    expect(internals.taskAssignmentRoute(vehicle, task).slice(0, 3)).toEqual([
      'column-top-a-c10',
      'column-top-a-c09',
      'column-top-a-c08'
    ]);
  });

  it('keeps upstream inbound queue slots available while a vehicle travels to the queue tail', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'queue-tail-follow-load',
      state: 'waiting',
      nodeId: 'lift-02-inbound-buffer-02',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'queue-tail-follow-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r02-c23',
      loadId: 'queue-tail-follow-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-02-spine-top-a',
      'column-top-a-c22',
      'column-top-a-c23',
      'column-top-a-c24',
      'column-top-a-c25'
    ]);
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c25']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      tasks: Array<{ id: string }>;
      topLiftInboundOpenApproachQueueSlotCount(liftNodeId: string): number;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
    };
    const tailVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    tailVehicle.plannedGoalNodeId = 'column-top-a-c25';
    tailVehicle.plannedRouteNodeIds = [
      'module-02-spine-top-a',
      'column-top-a-c22',
      'column-top-a-c23',
      'column-top-a-c24',
      'column-top-a-c25'
    ];
    tailVehicle.localRouteNodeIds = [...tailVehicle.plannedRouteNodeIds];
    tailVehicle.localRouteReason = 'inbound-queue-standby';
    const followerVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;
    const followerTask = internals.tasks.find((candidate) => candidate.id === 'queue-tail-follow-task')!;

    expect(internals.topLiftInboundOpenApproachQueueSlotCount('lift-02-inbound')).toBeGreaterThanOrEqual(2);
    expect(internals.topLiftInboundQueueNodeIdForTask(followerTask, followerVehicle)).toBe('column-top-a-c23');
  });

  it('reassigns a route-unavailable inbound pickup to a same-lift queue resource', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'route-unavailable-reassign-load',
      state: 'waiting',
      nodeId: 'lift-02-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'route-unavailable-reassign-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r02-c23',
      loadId: 'route-unavailable-reassign-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-01', 'route-unavailable-reassign-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-a-c11',
      waitReason: 'route-unavailable',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c24']);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleRouteForTest('SH-03', ['module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        taskId: string | null;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        routeNodeIds: string[];
      }>;
      tasks: Array<{ id: string; vehicleId: string | null }>;
      topLiftInboundLiftContextForVehicle(vehicle: unknown): string | null;
      tryReassignRouteUnavailableInboundPickupTask(vehicle: unknown, task: unknown, goalNodeId: string): boolean;
    };
    const stuckVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const sameLiftVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'route-unavailable-reassign-task')!;

    expect(internals.topLiftInboundLiftContextForVehicle(stuckVehicle)).toBe('lift-01-inbound');
    expect(internals.topLiftInboundLiftContextForVehicle(sameLiftVehicle)).toBe('lift-02-inbound');
    expect(internals.tryReassignRouteUnavailableInboundPickupTask(stuckVehicle, task, 'column-top-a-c22')).toBe(true);

    const state = sim.getState();
    const releasedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const reassignedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-02');
    const reassignedTask = state.tasks.find((candidate) => candidate.id === 'route-unavailable-reassign-task');

    expect(releasedVehicle?.taskId).toBeNull();
    expect(releasedVehicle?.plannedGoalNodeId).toBeNull();
    expect(reassignedTask?.vehicleId).toBe('SH-02');
    expect(reassignedVehicle?.taskId).toBe('route-unavailable-reassign-task');
    expect(reassignedVehicle?.plannedGoalNodeId).toBe('column-top-a-c22');
    expect(reassignedVehicle?.routeNodeIds).not.toContain('column-top-a-c10');
    expect(reassignedVehicle?.routeNodeIds).not.toContain('column-top-a-c11');
    expectRouteHopsAdjacent(scenario, reassignedVehicle?.routeNodeIds ?? []);
  });

  it('reassigns a route-unavailable inbound queue approach off a stale storage target', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'queue-route-unavailable-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'queue-route-unavailable-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c02',
      loadId: 'queue-route-unavailable-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c01', 'storage-r07-c01']);
    sim.setVehicleTaskForTest('SH-01', 'queue-route-unavailable-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r07-c01',
      waitReason: 'route-unavailable',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleRouteForTest('SH-03', ['module-02-spine-top-b']);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        taskId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
      }>;
      tasks: Array<{ id: string; vehicleId: string | null }>;
      tryReassignRouteUnavailableInboundPickupTask(vehicle: unknown, task: unknown, goalNodeId: string): boolean;
      routeUnavailableWaitTargetNodeId(vehicle: unknown, task: unknown, goalNodeId: string): string;
    };
    const stuckVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'queue-route-unavailable-task')!;

    expect(internals.routeUnavailableWaitTargetNodeId(stuckVehicle, task, 'column-top-a-c09')).toBe('column-top-a-c09');
    expect(internals.tryReassignRouteUnavailableInboundPickupTask(stuckVehicle, task, 'column-top-a-c09')).toBe(true);

    const state = sim.getState();
    const releasedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const reassignedVehicle = state.vehicles.find((candidate) => candidate.id === state.tasks.find((candidate) => candidate.id === 'queue-route-unavailable-task')?.vehicleId);
    const reassignedTask = state.tasks.find((candidate) => candidate.id === 'queue-route-unavailable-task');

    expect(releasedVehicle?.taskId).toBeNull();
    expect(reassignedTask?.vehicleId).not.toBe('SH-01');
    expect(reassignedVehicle?.taskId).toBe('queue-route-unavailable-task');
    expect(reassignedVehicle?.routeNodeIds).not.toContain('storage-r07-c01');
    expectRouteHopsAdjacent(scenario, reassignedVehicle?.routeNodeIds ?? []);
  });

  it('hands a route-unavailable inbound queue predecessor to the next same-lift FIFO waiter', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 3
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'fifo-handoff-early-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'fifo-handoff-early-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c02',
      loadId: 'fifo-handoff-early-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'fifo-handoff-later-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-04',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'fifo-handoff-later-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r05-c01',
      loadId: 'fifo-handoff-later-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c01', 'storage-r07-c01']);
    sim.setVehicleTaskForTest('SH-01', 'fifo-handoff-early-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-a-c09',
      waitReason: 'route-unavailable',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-02', 'fifo-handoff-later-task', false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-top-a-c10',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-03', ['module-02-spine-top-b']);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        taskId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
      }>;
      tasks: Array<{ id: string; state: string; vehicleId: string | null; waitReason: string | null }>;
      tryReassignRouteUnavailableInboundPickupTask(vehicle: unknown, task: unknown, goalNodeId: string): boolean;
    };
    const stuckVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const earlyTask = internals.tasks.find((candidate) => candidate.id === 'fifo-handoff-early-task')!;

    expect(internals.tryReassignRouteUnavailableInboundPickupTask(stuckVehicle, earlyTask, 'column-top-a-c09')).toBe(true);

    const state = sim.getState();
    const releasedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const replacementVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-02');
    const reassignedTask = state.tasks.find((candidate) => candidate.id === 'fifo-handoff-early-task');
    const displacedTask = state.tasks.find((candidate) => candidate.id === 'fifo-handoff-later-task');

    expect(releasedVehicle?.taskId).toBeNull();
    expect(reassignedTask?.vehicleId).toBe('SH-02');
    expect(replacementVehicle?.taskId).toBe('fifo-handoff-early-task');
    expect(replacementVehicle?.routeNodeIds).not.toContain('storage-r07-c01');
    expect(displacedTask?.state).toBe('queued');
    expect(displacedTask?.vehicleId).toBeNull();
    expect(displacedTask?.waitReason).toBe('inbound-lift-fifo-wait');
    expectRouteHopsAdjacent(scenario, replacementVehicle?.routeNodeIds ?? []);
  });

  it('stops boosting inbound queue replenishment after the target queue depth is covered', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 3
      }
    }));
    for (const [index, taskId, loadId, vehicleId, nodeId] of [
      [1, 'covered-queue-a', 'covered-load-a', 'SH-01', 'column-top-a-c09'],
      [2, 'covered-queue-b', 'covered-load-b', 'SH-02', 'column-top-a-c10']
    ] as const) {
      sim.addLoadForTest({ id: loadId, state: 'waiting', nodeId: `lift-01-inbound-buffer-0${index}`, vehicleId: null, weightKg: 100 });
      sim.addTaskForTest({
        id: taskId,
        kind: 'inbound',
        state: 'assigned',
        createdAtSec: index,
        assignedAtSec: index,
        startedAtSec: null,
        completedAtSec: null,
        pickupNodeId: 'column-top-a-c08',
        dropoffNodeId: `storage-r0${index}-c08`,
        loadId,
        vehicleId,
        replanCount: 0,
        waitReason: null
      });
      sim.setVehicleRouteForTest(vehicleId, [nodeId]);
      sim.setVehicleTaskForTest(vehicleId, taskId, false);
    }
    sim.addLoadForTest({ id: 'covered-load-c', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'covered-queue-c',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 3,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r03-c08',
      loadId: 'covered-load-c',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', ['column-bottom-b-c11']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; waitReason: string | null; plannedGoalNodeId: string | null }>;
      topLiftInboundActiveEmptyQueueDepth(liftNodeId: string): number;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      queuedTaskAssignmentPriority(task: unknown): number;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'covered-queue-c')!;
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-03')!;

    expect(internals.topLiftInboundActiveEmptyQueueDepth('lift-01-inbound')).toBe(2);
    expect(internals.topLiftInboundQueueNodeIdForTask(task, vehicle)).toBe('column-top-a-c11');
    expect(internals.queuedTaskAssignmentPriority(task)).toBe(0);
  });

  it('lets real inbound work override far taskless standby queue claims', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'soft-standby-claim-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-02',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'soft-standby-claim-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'soft-standby-claim-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      topLiftInboundOpenApproachQueueSlotCount(liftNodeId: string): number;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      topLiftInboundQueueSlotAvailableForTask(task: unknown): boolean;
    };
    const standbyClaims = [
      ['SH-01', 'column-top-a-c09', 'column-bottom-b-c01'],
      ['SH-02', 'column-top-a-c10', 'column-bottom-b-c02'],
      ['SH-03', 'column-top-a-c11', 'column-bottom-b-c03']
    ] as const;
    for (const [vehicleId, nodeId, currentNodeId] of standbyClaims) {
      sim.setVehicleRouteForTest(vehicleId, [currentNodeId]);
      const vehicle = internals.vehicles.find((candidate) => candidate.id === vehicleId)!;
      vehicle.plannedGoalNodeId = nodeId;
      vehicle.plannedRouteNodeIds = [vehicle.currentNodeId, nodeId];
      vehicle.localRouteNodeIds = [vehicle.currentNodeId, nodeId];
      vehicle.localRouteReason = 'inbound-queue-standby';
    }
    const task = internals.tasks.find((candidate) => candidate.id === 'soft-standby-claim-task')!;
    const availableVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;

    expect(internals.topLiftInboundOpenApproachQueueSlotCount('lift-01-inbound')).toBe(3);
    expect(internals.topLiftInboundQueueSlotAvailableForTask(task)).toBe(true);
    expect(internals.topLiftInboundQueueNodeIdForTask(task, availableVehicle)).toBe('column-top-a-c09');
  });

  it('lets an occupied projected inbound queue reserve adopt the task trying to enter its yellow slot', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'queue-adopt-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-02',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'queue-adopt-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c13',
      loadId: 'queue-adopt-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c10']);
    sim.setVehicleRouteForTest('SH-03', ['column-top-a-c11']);
    sim.setVehicleRouteForTest('SH-04', ['column-top-b-c09', 'column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-04', 'queue-adopt-task', false);
    sim.setVehicleWaitingForTest('SH-04', {
      targetNodeId: 'column-top-a-c09',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        taskId: string | null;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      tasks: Array<{ id: string; vehicleId: string | null }>;
      tryAdoptInboundQueueTaskAtCurrentSlot(vehicle: unknown): boolean;
    };
    for (const [vehicleId, nodeId] of [
      ['SH-01', 'column-top-a-c09'],
      ['SH-02', 'column-top-a-c10'],
      ['SH-03', 'column-top-a-c11']
    ] as const) {
      const vehicle = internals.vehicles.find((candidate) => candidate.id === vehicleId)!;
      vehicle.plannedGoalNodeId = nodeId;
      vehicle.plannedRouteNodeIds = [nodeId];
      vehicle.localRouteNodeIds = [];
      vehicle.localRouteReason = 'inbound-queue-standby';
    }

    const queueLeader = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    expect(internals.tryAdoptInboundQueueTaskAtCurrentSlot(queueLeader)).toBe(true);

    const state = sim.getState();
    const adoptedTask = state.tasks.find((candidate) => candidate.id === 'queue-adopt-task');
    const adoptedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const releasedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-04');

    expect(adoptedTask?.vehicleId).toBe('SH-01');
    expect(adoptedVehicle?.taskId).toBe('queue-adopt-task');
    expect(adoptedVehicle?.routeNodeIds).toEqual(['column-top-a-c09', 'column-top-a-c08']);
    expect(releasedVehicle?.taskId).toBeNull();
    expect(releasedVehicle?.targetNodeId).toBeNull();
    expectRouteHopsAdjacent(scenario, adoptedVehicle?.routeNodeIds ?? []);
  });

  it('does not let outbound work steal a demanded inbound queue standby reserve', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'inbound-demand-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'inbound-demand-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r01-c08',
      loadId: 'inbound-demand-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'outbound-reserve-load',
      state: 'stored',
      nodeId: 'storage-r01-c01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'outbound-reserve-task',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'column-bottom-a-c08',
      loadId: 'outbound-reserve-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c01']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string } } | null;
    };
    const standbyVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    standbyVehicle.plannedGoalNodeId = 'column-top-a-c09';
    standbyVehicle.plannedRouteNodeIds = ['column-top-a-c09'];
    standbyVehicle.localRouteNodeIds = [];
    standbyVehicle.localRouteReason = 'inbound-queue-standby';
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-reserve-task')!;

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01', 'SH-02']))?.vehicle.id).toBe('SH-02');
    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01']))).toBeNull();
  });

  it('lets outbound work use a stale inbound queue standby when no inbound source demand exists', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'outbound-stale-reserve-load',
      state: 'stored',
      nodeId: 'storage-r01-c01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'outbound-stale-reserve-task',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'column-bottom-a-c08',
      loadId: 'outbound-stale-reserve-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c01']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string } } | null;
    };
    const standbyVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    standbyVehicle.plannedGoalNodeId = 'column-top-a-c09';
    standbyVehicle.plannedRouteNodeIds = ['column-top-a-c09'];
    standbyVehicle.localRouteNodeIds = [];
    standbyVehicle.localRouteReason = 'inbound-queue-standby';
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-stale-reserve-task')!;

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01']))?.vehicle.id).toBe('SH-01');
  });

  it('pre-stages one local inbound queue resource before the source load arrives', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'planned-reserve-outbound-load',
      state: 'stored',
      nodeId: 'storage-r14-c01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'planned-reserve-outbound-task',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'planned-reserve-outbound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c12']);
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c01']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; localRouteReason: string | null; plannedGoalNodeId: string | null }>;
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string } } | null;
      dispatchTasklessToInboundQueueStandby(vehicle: unknown): boolean;
      topLiftTasklessVehicleShouldReserveForInboundQueue(vehicle: unknown): boolean;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'planned-reserve-outbound-task')!;
    const topVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const lowerVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;

    expect(internals.topLiftTasklessVehicleShouldReserveForInboundQueue(topVehicle)).toBe(true);
    expect(internals.topLiftTasklessVehicleShouldReserveForInboundQueue(lowerVehicle)).toBe(false);
    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01']))).toBeNull();
    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-02']))?.vehicle.id).toBe('SH-02');
    expect(internals.dispatchTasklessToInboundQueueStandby(topVehicle)).toBe(true);
    expect(topVehicle.localRouteReason).toBe('inbound-queue-standby');
    expect(topVehicle.plannedGoalNodeId).toMatch(/^column-top-a-c(?:09|10|11)$/);
  });

  it('reports station-owned shadow contracts for inbound demand, queue reservation, and active service', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'shadow-contract-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'shadow-contract-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'shadow-contract-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-02', 'shadow-contract-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
    };
    const reserveVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    reserveVehicle.plannedGoalNodeId = 'column-top-a-c09';
    reserveVehicle.plannedRouteNodeIds = ['column-top-a-c09'];
    reserveVehicle.localRouteNodeIds = [];
    reserveVehicle.localRouteReason = 'inbound-queue-standby';

    const state = ShuttleSimStateSchema.parse(sim.getState());
    const stationContracts = state.traffic.shadowLedger.stationContracts;
    const station = stationContracts.stations.find((candidate) => candidate.stationId === 'lift-01-inbound');

    expect(stationContracts.enabled).toBe(true);
    expect(station).toMatchObject({
      stationId: 'lift-01-inbound',
      demandCount: 1,
      claimedDemandCount: 1,
      queueReservationCount: 1,
      activeServiceDepth: 1,
      physicalDepth: 2,
      nearCoveredDepth: 2,
      activeAssignmentQueueLeaseCount: 1,
      tasklessStandbySoftReserveCount: 1,
      physicalQueueSlotLeaseCount: 2
    });
    expect(station?.demands).toContainEqual(expect.objectContaining({
      kind: 'source-load',
      status: 'claimed',
      loadId: 'shadow-contract-load',
      taskId: 'shadow-contract-task'
    }));
    expect(station?.vehicleCommitments).toContainEqual(expect.objectContaining({
      vehicleId: 'SH-01',
      kind: 'queueReservation',
      currentQueueSlot: 1
    }));
    expect(station?.vehicleCommitments).toContainEqual(expect.objectContaining({
      vehicleId: 'SH-02',
      kind: 'activeInboundService',
      taskId: 'shadow-contract-task'
    }));
    expect(station?.routeLeaseCount).toBeGreaterThanOrEqual(2);
    expect(station?.routeLeases).toContainEqual(expect.objectContaining({
      vehicleId: 'SH-01',
      kind: 'physicalQueueSlot',
      phase: 'occupied',
      resourceKey: 'station:lift-01-inbound:queue-slot:1',
      slotIndex: 1
    }));
    expect(station?.routeLeases).toContainEqual(expect.objectContaining({
      vehicleId: 'SH-02',
      kind: 'physicalQueueSlot',
      phase: 'occupied',
      resourceKey: 'station:lift-01-inbound:queue-slot:2',
      slotIndex: 2
    }));
  });

  it('reports duplicate station route leases before they become physical queue conflicts', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c05']);
    sim.setVehicleRouteForTest('SH-02', ['column-middle-c06']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
    };
    for (const vehicleId of ['SH-01', 'SH-02']) {
      const vehicle = internals.vehicles.find((candidate) => candidate.id === vehicleId)!;
      vehicle.plannedGoalNodeId = 'column-top-a-c09';
      vehicle.plannedRouteNodeIds = [vehicle.currentNodeId, 'column-top-a-c09'];
      vehicle.localRouteNodeIds = [];
      vehicle.localRouteReason = 'inbound-queue-standby';
    }

    const stationContracts = ShuttleSimStateSchema.parse(sim.getState()).traffic.shadowLedger.stationContracts;

    expect(stationContracts.invariantCounts.duplicateRouteLease).toBeGreaterThanOrEqual(1);
    expect(stationContracts.violations).toContainEqual(expect.objectContaining({
      code: 'station-duplicate-route-lease',
      stationId: 'lift-01-inbound'
    }));
  });

  it('keeps station shadow contract sampling read-only within a 3D tick', () => {
    const scenario = createInboundOutboundDemoScenario({
      durationSec: 12,
      timeStepSec: 0.2,
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 2,
        initialStorageFillPolicy: 'zone-balanced-50',
        maxTasks: 24
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();
    sim.advanceByInPlace(1);

    const beforeHash = hashEngineSnapshot(sim.createSnapshot());
    const state = ShuttleSimStateSchema.parse(sim.getState());
    const afterHash = hashEngineSnapshot(sim.createSnapshot());

    expect(state.traffic.shadowLedger.stationContracts.enabled).toBe(true);
    expect(state.traffic.shadowLedger.stationContracts.stationCount).toBeGreaterThan(0);
    expect(afterHash).toBe(beforeHash);
  });

  it('protects taskless shuttles for inbound queue reserve before ordinary outbound work', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'inbound-queue-protection-source',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addLoadForTest({
      id: 'outbound-queue-protection-load',
      state: 'stored',
      nodeId: 'storage-r14-c01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'outbound-queue-protection-task',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'outbound-queue-protection-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c12']);
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c01']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; localRouteReason: string | null; plannedGoalNodeId: string | null }>;
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string } } | null;
      dispatchTasklessToInboundQueueStandby(vehicle: unknown): boolean;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-queue-protection-task')!;
    const topVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const lowerVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01', 'SH-02']))?.vehicle.id).toBe('SH-02');
    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01']))).toBeNull();
    expect(internals.dispatchTasklessToInboundQueueStandby(topVehicle)).toBe(true);
    expect(topVehicle.localRouteReason).toBe('inbound-queue-standby');
    expect(topVehicle.plannedGoalNodeId).toMatch(/^column-top-a-c(?:09|10|11)$/);
    expect(lowerVehicle.localRouteReason).toBeNull();
    expect(lowerVehicle.plannedGoalNodeId).toBeNull();
  });

  it('does not let inbound work steal an en-route inbound queue reserve before it reaches the queue', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'inbound-reserve-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'inbound-reserve-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'inbound-reserve-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-b-c11', 'column-bottom-a-c11']);
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c01']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string } } | null;
    };
    const reserveVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    reserveVehicle.plannedGoalNodeId = 'column-top-a-c09';
    reserveVehicle.plannedRouteNodeIds = ['column-bottom-b-c11', 'column-bottom-a-c11', 'column-top-a-c09'];
    reserveVehicle.localRouteNodeIds = ['column-bottom-b-c11', 'column-bottom-a-c11', 'column-top-a-c09'];
    reserveVehicle.localRouteReason = 'inbound-queue-standby';
    const task = internals.tasks.find((candidate) => candidate.id === 'inbound-reserve-task')!;

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01']))).toBeNull();
    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01', 'SH-02']))).toBeNull();

    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    reserveVehicle.plannedGoalNodeId = 'column-top-a-c09';
    reserveVehicle.plannedRouteNodeIds = ['column-top-a-c09'];
    reserveVehicle.localRouteNodeIds = [];
    reserveVehicle.localRouteReason = 'inbound-queue-standby';

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01']))?.vehicle.id).toBe('SH-01');
  });

  it('only lets a ready same-lift queue resource hold lower-level inbound fallback', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'local-reserve-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'local-reserve-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'local-reserve-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c25']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string } } | null;
    };
    const reserveVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    reserveVehicle.plannedGoalNodeId = 'column-top-a-c09';
    reserveVehicle.plannedRouteNodeIds = ['column-top-a-c09'];
    reserveVehicle.localRouteNodeIds = [];
    reserveVehicle.localRouteReason = 'inbound-queue-standby';
    const task = internals.tasks.find((candidate) => candidate.id === 'local-reserve-task')!;

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-02']))).toBeNull();

    reserveVehicle.localRouteReason = null;

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-02']))?.vehicle.id).toBe('SH-02');

    sim.setVehicleRouteForTest('SH-01', ['column-bottom-b-c25']);
    reserveVehicle.plannedGoalNodeId = null;
    reserveVehicle.plannedRouteNodeIds = [];

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-02']))?.vehicle.id).toBe('SH-02');
  });

  it('assigns a priority-held inbound task when a specific available shuttle can take the queue slot', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 2
      }
    }));
    sim.addLoadForTest({ id: 'priority-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'priority-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'priority-earlier-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'priority-earlier-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'priority-later-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 1,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r03-c08',
      loadId: 'priority-later-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c08', 'module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-01', 'priority-earlier-task', false);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-top-b', 'column-top-a-c09']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string; state: string; vehicleId: string | null }>;
      vehicles: Array<{
        id: string;
        taskId: string | null;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      topLiftInboundQueueSlotAvailableForTask(task: unknown): boolean;
      queuedTaskAssignmentPriority(task: unknown): number;
    };
    const laterTask = internals.tasks.find((task) => task.id === 'priority-later-task')!;
    const availableVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    availableVehicle.plannedGoalNodeId = 'column-top-a-c09';
    availableVehicle.plannedRouteNodeIds = ['module-01-spine-top-b', 'column-top-a-c09'];
    availableVehicle.localRouteReason = null;
    const c10Claimant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    c10Claimant.plannedGoalNodeId = 'column-top-a-c10';
    c10Claimant.plannedRouteNodeIds = ['column-bottom-b-c10', 'column-top-a-c10'];
    c10Claimant.localRouteReason = null;
    c10Claimant.taskId = 'existing-task-c10';
    const c11Claimant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-04')!;
    c11Claimant.plannedGoalNodeId = 'column-top-a-c11';
    c11Claimant.plannedRouteNodeIds = ['column-bottom-b-c11', 'column-top-a-c11'];
    c11Claimant.localRouteReason = null;
    c11Claimant.taskId = 'existing-task-c11';

    expect(internals.topLiftInboundQueueSlotAvailableForTask(laterTask)).toBe(false);
    expect(internals.topLiftInboundQueueNodeIdForTask(laterTask, availableVehicle)).toBe('column-top-a-c09');
    expect(internals.queuedTaskAssignmentPriority(laterTask)).toBe(2);

    const state = sim.step(0.2);
    const assignedTask = state.tasks.find((task) => task.id === 'priority-later-task');
    const assignedVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(assignedTask?.state).toBe('assigned');
    expect(assignedTask?.vehicleId).toBe('SH-02');
    expect(assignedVehicle?.plannedGoalNodeId).toBe('column-top-a-c09');
  });

  it('sends taskless inbound queue standbys to the nearest available yellow queue point first', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'under-covered-lift-load',
      state: 'waiting',
      nodeId: 'lift-02-inbound-buffer-01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'under-covered-lift-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r01-c22',
      loadId: 'under-covered-lift-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-b-c11']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c09']);
    sim.setVehicleRouteForTest('SH-03', ['column-top-a-c10']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      topLiftInboundQueueStandbyTargetNodeId(vehicle: unknown): string | null;
    };
    for (const vehicleId of ['SH-02', 'SH-03']) {
      const vehicle = internals.vehicles.find((candidate) => candidate.id === vehicleId)!;
      vehicle.plannedGoalNodeId = vehicle.currentNodeId;
      vehicle.plannedRouteNodeIds = [vehicle.currentNodeId];
      vehicle.localRouteNodeIds = [];
      vehicle.localRouteReason = 'inbound-queue-standby';
    }
    const availableVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.topLiftInboundQueueStandbyTargetNodeId(availableVehicle)).toBe('column-top-a-c23');
  });

  it('does not count lower-level inbound approaches as visible inbound queue coverage', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({ id: 'lower-coverage-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'lower-coverage-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r08-c08',
      loadId: 'lower-coverage-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-bottom-b-c25',
      'column-bottom-a-c25',
      'module-02-spine-bottom-a',
      'module-02-spine-middle',
      'module-02-spine-top-a',
      'column-top-a-c11'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'lower-coverage-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; plannedRouteNodeIds: string[] }>;
      topLiftInboundActiveEmptyQueueDepth(liftNodeId: string): number;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    vehicle.plannedGoalNodeId = 'column-top-a-c11';
    vehicle.plannedRouteNodeIds = [
      'column-bottom-b-c25',
      'column-bottom-a-c25',
      'module-02-spine-bottom-a',
      'module-02-spine-middle',
      'module-02-spine-top-a',
      'column-top-a-c11'
    ];

    expect(internals.topLiftInboundActiveEmptyQueueDepth('lift-01-inbound')).toBe(0);
  });

  it('binds top-level standby travel to queued inbound work instead of taskless reserve', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'queue-bound-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'queue-bound-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c11',
      loadId: 'queue-bound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c12']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string; state: string; vehicleId: string | null }>;
      vehicles: Array<{
        id: string;
        taskId: string | null;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      dispatchTasklessInboundQueueStandbys(): void;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    vehicle.plannedGoalNodeId = 'storage-r01-c12';
    vehicle.plannedRouteNodeIds = ['column-top-b-c12', 'storage-r01-c12'];
    vehicle.localRouteNodeIds = ['column-top-b-c12', 'storage-r01-c12'];
    vehicle.localRouteReason = 'post-dropoff-column-exit';

    internals.dispatchTasklessInboundQueueStandbys();
    const stateVehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');
    const stateTask = internals.tasks.find((candidate) => candidate.id === 'queue-bound-task');

    expect(stateTask?.state).toBe('assigned');
    expect(stateTask?.vehicleId).toBe('SH-01');
    expect(stateVehicle?.taskId).toBe('queue-bound-task');
    expect(stateVehicle?.localRouteReason).toBeNull();
    expect(stateVehicle?.plannedGoalNodeId).toMatch(/^column-top-a-c(?:09|10|11)$/);
    expect(stateVehicle?.routeNodeIds.at(-1)).toBe(stateVehicle?.plannedGoalNodeId);
  });

  it('binds an inbound dropoff vehicle from storage to queued lift work in mixed flow', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 4,
        initialStorageFillPolicy: 'zone-balanced-50',
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'dropoff-bound-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'dropoff-bound-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c11',
      loadId: 'dropoff-bound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r14-c11']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string; state: string; vehicleId: string | null }>;
      vehicles: Array<{ id: string }>;
      dispatchVehicleAfterInboundDropoff(vehicle: unknown): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.dispatchVehicleAfterInboundDropoff(vehicle)).toBe(true);
    const stateVehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');
    const stateTask = internals.tasks.find((candidate) => candidate.id === 'dropoff-bound-task');

    expect(stateTask?.state).toBe('assigned');
    expect(stateTask?.vehicleId).toBe('SH-01');
    expect(stateVehicle?.taskId).toBe('dropoff-bound-task');
    expect(stateVehicle?.localRouteReason).toBeNull();
    expect(stateVehicle?.plannedGoalNodeId).toMatch(/^column-top-a-c(?:09|10|11)$/);
    expect(stateVehicle?.routeNodeIds.at(-1)).toBe(stateVehicle?.plannedGoalNodeId);
  });

  it('keeps an inbound approach moving to a reachable prefix when the pickup path is temporarily protected', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'approach-prefix-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-02',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'approach-prefix-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c12',
      loadId: 'approach-prefix-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'loaded-clearance-load',
      state: 'carried',
      nodeId: null,
      vehicleId: 'SH-02',
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'loaded-clearance-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r01-c09',
      loadId: 'loaded-clearance-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['module-boundary-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-01', 'approach-prefix-task', false);
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-a-c08',
      'column-top-b-c08',
      'column-top-b-c09',
      'storage-r01-c09'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'loaded-clearance-task', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; waitReason: string | null; plannedGoalNodeId: string | null }>;
      tasks: Array<{ id: string }>;
      taskAssignmentRoute(vehicle: unknown, task: unknown): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'approach-prefix-task')!;
    const route = internals.taskAssignmentRoute(vehicle, task);

    expect(route[0]).toBe('module-boundary-01-spine-top-b');
    expect(route.at(-1)).toBe('column-top-b-c10');
    expect(route).not.toContain('column-top-a-c08');
  });

  it('does not pre-stage taskless mixed-flow shuttles without concrete inbound work', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.start();
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c12']);

    const internals = sim as unknown as {
      scenario: {
        taskGeneration: {
          inboundRatePerHour: number;
          outboundRatePerHour: number;
        };
      };
      dispatchTasklessInboundQueueStandbys(): void;
    };
    internals.scenario.taskGeneration.inboundRatePerHour = 3600;
    internals.scenario.taskGeneration.outboundRatePerHour = 3600;
    internals.dispatchTasklessInboundQueueStandbys();

    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(vehicle?.taskId).toBeNull();
    expect(vehicle?.localRouteReason).toBeNull();
  });

  it('holds taskless inbound queue standby vehicles instead of shuffling them between queue points', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.start();
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    const internals = sim as unknown as {
      scenario: {
        taskGeneration: {
          inboundRatePerHour: number;
          outboundRatePerHour: number;
        };
      };
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
    };
    internals.scenario.taskGeneration.inboundRatePerHour = 3600;
    internals.scenario.taskGeneration.outboundRatePerHour = 3600;
    const mutableVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    mutableVehicle.plannedGoalNodeId = 'column-top-a-c09';
    mutableVehicle.plannedRouteNodeIds = ['column-top-a-c09'];
    mutableVehicle.localRouteReason = 'inbound-queue-standby';

    sim.step(0.2);

    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(vehicle?.currentNodeId).toBe('column-top-a-c09');
    expect(vehicle?.targetNodeId).toBeNull();
    expect(vehicle?.plannedGoalNodeId).toBe('column-top-a-c09');
    expect(vehicle?.localRouteReason).toBe('inbound-queue-standby');
    expect(vehicle?.waitReason).toBeNull();
  });

  it('compresses a taskless inbound queue standby forward when front yellow queue slots open', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.start();
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c11']);
    const internals = sim as unknown as {
      scenario: {
        taskGeneration: {
          inboundRatePerHour: number;
          outboundRatePerHour: number;
        };
      };
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
    };
    internals.scenario.taskGeneration.inboundRatePerHour = 3600;
    internals.scenario.taskGeneration.outboundRatePerHour = 3600;
    const mutableVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    mutableVehicle.plannedGoalNodeId = 'column-top-a-c11';
    mutableVehicle.plannedRouteNodeIds = ['column-top-a-c11'];
    mutableVehicle.localRouteReason = 'inbound-queue-standby';

    sim.step(0.2);

    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(vehicle?.currentNodeId).toBe('column-top-a-c11');
    expect(vehicle?.targetNodeId).toBe('column-top-a-c10');
    expect(vehicle?.plannedGoalNodeId).toBe('column-top-a-c09');
    expect(vehicle?.routeNodeIds).toEqual(['column-top-a-c11', 'column-top-a-c10', 'column-top-a-c09']);
    expect(vehicle?.localRouteReason).toBe('inbound-queue-standby');
    expect(vehicle?.waitReason).toBeNull();
  });

  it('stages the next inbound column after the current FIFO column pickup is released', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 4
      }
    }));
    sim.start();
    sim.addLoadForTest({
      id: 'handoff-active-load',
      state: 'carried',
      nodeId: null,
      vehicleId: 'SH-01',
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'handoff-active-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r01-c08',
      loadId: 'handoff-active-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c08', 'storage-r01-c08']);
    sim.setVehicleTaskForTest('SH-01', 'handoff-active-task', true);

    const internals = sim as unknown as {
      selectTopLiftInboundStorageNodeForLift(liftNodeId: string): string | null;
    };

    expect(internals.selectTopLiftInboundStorageNodeForLift('lift-01-inbound')).toMatch(/^storage-r\d{2}-c09$/);
  });

  it('can assign an inbound task to a shuttle already standing on its queue goal', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({ id: 'already-queued-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'already-queued-earlier-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c04',
      loadId: 'already-queued-earlier-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'already-queued-follower-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'already-queued-follower-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 1,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r13-c04',
      loadId: 'already-queued-follower-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-02', 'already-queued-earlier-task', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      taskDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      taskAssignmentRoute(vehicle: unknown, task: unknown): string[];
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string }; route: string[] } | null;
    };
    const followerTask = internals.tasks.find((candidate) => candidate.id === 'already-queued-follower-task')!;
    const queuedVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.taskDispatchGoalNodeId(followerTask, queuedVehicle)).toBe('column-top-a-c09');
    expect(internals.taskAssignmentRoute(queuedVehicle, followerTask)).toEqual(['column-top-a-c09']);
    expect(internals.bestAvailableVehicleForTask(followerTask, new Set(['SH-01']))).toMatchObject({
      vehicle: { id: 'SH-01' },
      route: ['column-top-a-c09']
    });
  });

  it('treats same-lift inbound queue vehicles as FIFO shuttle resources before global nearest vehicles', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({ id: 'fifo-resource-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'fifo-resource-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'fifo-resource-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c11']);
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c08']);
    sim.setVehicleRouteForTest('SH-03', ['column-top-a-c09']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string }; route: string[] } | null;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'fifo-resource-task')!;

    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01', 'SH-02', 'SH-03']))).toMatchObject({
      vehicle: { id: 'SH-03' },
      route: ['column-top-a-c09', 'column-top-a-c08']
    });
    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-02']))?.vehicle.id).toBe('SH-02');
  });

  it('does not send a taskless mixed-flow shuttle on a long standby tour while queued work is waiting', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({ durationSec: 2400 }));
    sim.start();

    sim.advanceByInPlace(65);

    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-03');
    if (!vehicle?.taskId && !vehicle?.loaded) {
      expect(vehicle?.plannedRouteNodeIds.length ?? 0).toBeLessThanOrEqual(4);
      expect(vehicle?.routeNodeIds).not.toContain('module-01-spine-middle');
      expect(vehicle?.routeNodeIds).not.toContain('storage-r01-c04');
    }
  }, 20000);

  it('preempts taskless standby travel when queued work is waiting', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        initialOutboundFullColumns: 0,
        maxTasks: 8
      }
    }));

    sim.addLoadForTest({
      id: 'queued-source-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'queued-inbound-after-standby',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r01-c08',
      loadId: 'queued-source-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    const tasklessRoute = ['column-top-b-c12', 'column-top-b-c13', 'column-top-b-c14'];
    sim.setVehicleRouteForTest('SH-01', tasklessRoute);
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    vehicle.plannedGoalNodeId = tasklessRoute.at(-1)!;
    vehicle.plannedRouteNodeIds = [...tasklessRoute];
    vehicle.localRouteNodeIds = [...tasklessRoute];
    vehicle.localRouteReason = 'post-dropoff-column-exit';

    sim.start();
    sim.step(0.25);

    const state = sim.getState();
    const task = state.tasks.find((candidate) => candidate.id === 'queued-inbound-after-standby');
    const reassignedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(task?.state).toBe('assigned');
    expect(task?.vehicleId).toBe('SH-01');
    expect(reassignedVehicle?.taskId).toBe('queued-inbound-after-standby');
    expect(reassignedVehicle?.plannedGoalNodeId).toBe('column-top-a-c08');
    expect(reassignedVehicle?.localRouteReason).toBeNull();
  });

  it('assigns queued work immediately after outbound unload before standby clearance', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        initialOutboundFullColumns: 0,
        maxTasks: 8
      }
    }));

    sim.addLoadForTest({
      id: 'finished-outbound-load',
      state: 'carried',
      nodeId: null,
      vehicleId: 'SH-01',
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'finished-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c08',
      dropoffNodeId: 'column-bottom-a-c08',
      loadId: 'finished-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'next-source-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'next-inbound',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r01-c08',
      loadId: 'next-source-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-a-c08']);
    sim.setVehicleTaskForTest('SH-01', 'finished-outbound', true);
    const internals = sim as unknown as {
      vehicles: Array<{ id: string; state: string; phaseRemainingSec: number }>;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    vehicle.state = 'lowering';
    vehicle.phaseRemainingSec = 0.01;

    sim.start();
    sim.step(0.05);

    const state = sim.getState();
    const completedTask = state.tasks.find((candidate) => candidate.id === 'finished-outbound');
    const nextTask = state.tasks.find((candidate) => candidate.id === 'next-inbound');
    const reassignedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(completedTask?.state).toBe('completed');
    expect(nextTask?.state).toBe('assigned');
    expect(nextTask?.vehicleId).toBe('SH-01');
    expect(reassignedVehicle?.taskId).toBe('next-inbound');
    expect(sim.getEventLog().some((event) =>
      event.eventType === 'vehicle-standby-dispatched' &&
      event.vehicleId === 'SH-01' &&
      event.reason === 'outbound-lift-dropoff-clearance'
    )).toBe(false);
  });

  it('routes a lower-side outbound finisher to the inbound queue before mixed-flow pickup', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 8
      }
    }));

    sim.addLoadForTest({
      id: 'mixed-finished-outbound-load',
      state: 'carried',
      nodeId: null,
      vehicleId: 'SH-01',
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'mixed-finished-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c08',
      dropoffNodeId: 'column-bottom-a-c08',
      loadId: 'mixed-finished-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'mixed-next-source-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'mixed-next-inbound',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r01-c08',
      loadId: 'mixed-next-source-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-a-c08']);
    sim.setVehicleTaskForTest('SH-01', 'mixed-finished-outbound', true);
    const internals = sim as unknown as {
      vehicles: Array<{ id: string; state: string; phaseRemainingSec: number }>;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    vehicle.state = 'lowering';
    vehicle.phaseRemainingSec = 0.01;

    sim.start();
    sim.step(0.05);

    const state = sim.getState();
    const completedTask = state.tasks.find((candidate) => candidate.id === 'mixed-finished-outbound');
    const nextTask = state.tasks.find((candidate) => candidate.id === 'mixed-next-inbound');
    const queueVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(completedTask?.state).toBe('completed');
    expect(nextTask?.state).toBe('assigned');
    expect(nextTask?.vehicleId).toBe('SH-01');
    expect(nextTask?.waitReason).toBeNull();
    expect(queueVehicle?.taskId).toBe('mixed-next-inbound');
    expect(queueVehicle?.localRouteReason).toBeNull();
    expect(queueVehicle?.plannedGoalNodeId).toMatch(/^column-top-a-c(?:09|10|11)$/);
    expect(queueVehicle?.plannedGoalNodeId).not.toBe('column-top-a-c08');
    expect(queueVehicle?.routeNodeIds.at(-1)).toBe(queueVehicle?.plannedGoalNodeId);
  });

  it('prefers top-side shuttles over lower-side shuttles for mixed-flow inbound dispatch', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));

    sim.addLoadForTest({
      id: 'manual-inbound-source',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'manual-mixed-inbound',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r01-c08',
      loadId: 'manual-inbound-source',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-b-c08']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c14']);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      taskAssignmentRoute(vehicle: unknown, task: unknown): string[];
      topLiftInboundLowerLevelAssignmentPenaltyM(vehicle: unknown, task: unknown, route: string[]): number;
      bestAvailableVehicleForTask(task: unknown, vehicleIds: Set<string>): { vehicle: { id: string } } | null;
    };
    const bottomSideVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const topSideVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'manual-mixed-inbound')!;
    const bottomRoute = internals.taskAssignmentRoute(bottomSideVehicle, task);
    const topRoute = internals.taskAssignmentRoute(topSideVehicle, task);

    expect(internals.topLiftInboundLowerLevelAssignmentPenaltyM(bottomSideVehicle, task, bottomRoute)).toBeGreaterThan(0);
    expect(internals.topLiftInboundLowerLevelAssignmentPenaltyM(topSideVehicle, task, topRoute)).toBe(0);
    expect(internals.bestAvailableVehicleForTask(task, new Set(['SH-01', 'SH-02']))?.vehicle.id).toBe('SH-02');
  });

  it('prioritizes inbound lift queue replenishment before ordinary outbound work', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));

    sim.addLoadForTest({
      id: 'queue-replenish-inbound-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'queue-replenish-inbound',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c01',
      loadId: 'queue-replenish-inbound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'ordinary-outbound-load',
      state: 'stored',
      nodeId: 'storage-r14-c15',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'ordinary-outbound',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c15',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'ordinary-outbound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      queuedTaskAssignmentPriority(task: unknown): number;
      queuedTasksForAssignment(): Array<{ task: { id: string }; priority: number }>;
    };
    const inboundTask = internals.tasks.find((task) => task.id === 'queue-replenish-inbound')!;
    const outboundTask = internals.tasks.find((task) => task.id === 'ordinary-outbound')!;

    expect(internals.queuedTaskAssignmentPriority(inboundTask)).toBeLessThan(internals.queuedTaskAssignmentPriority(outboundTask));
    expect(internals.queuedTasksForAssignment().map(({ task }) => task.id).slice(0, 2)).toEqual([
      'queue-replenish-inbound',
      'ordinary-outbound'
    ]);
  });

  it('reserves a newly free shuttle for active inbound queue work before ordinary outbound assignment', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c12']);
    sim.addLoadForTest({
      id: 'active-inbound-reserve-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'active-inbound-reserve',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c01',
      loadId: 'active-inbound-reserve-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'ordinary-outbound-reserve-load',
      state: 'stored',
      nodeId: 'storage-r14-c15',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'ordinary-outbound-reserve',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c15',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'ordinary-outbound-reserve-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        taskId: string | null;
        localRouteReason: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
      }>;
      tasks: Array<{ id: string; state: string; vehicleId: string | null }>;
      tryAssignQueuedTaskToVehicle(vehicle: unknown): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.tryAssignQueuedTaskToVehicle(vehicle)).toBe(true);
    expect(vehicle.taskId).toBe('active-inbound-reserve');
    expect(vehicle.localRouteReason).toBeNull();
    expect(vehicle.plannedGoalNodeId).toMatch(/^column-top-a-c(?:09|10|11)$/);
    expect(vehicle.routeNodeIds.at(-1)).toBe(vehicle.plannedGoalNodeId);
    expect(internals.tasks.find((task) => task.id === 'active-inbound-reserve')).toMatchObject({
      state: 'assigned',
      vehicleId: 'SH-01'
    });
    expect(internals.tasks.find((task) => task.id === 'ordinary-outbound-reserve')).toMatchObject({
      state: 'queued',
      vehicleId: null
    });
  });

  it('lets an inbound successor reserve the queue tail as the predecessor exits toward pickup', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));

    sim.addLoadForTest({
      id: 'predecessor-exiting-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-01',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'predecessor-exiting',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r12-c11',
      loadId: 'predecessor-exiting-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'successor-tail-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-02',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'successor-tail',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 1,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r11-c11',
      loadId: 'successor-tail-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-a-c11',
      'column-top-b-c11',
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08',
      'column-top-a-c08'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'predecessor-exiting', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null }>;
      tasks: Array<{ id: string }>;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      topLiftInboundQueueSlotAvailableForTask(task: unknown): boolean;
    };
    const predecessorVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    predecessorVehicle.plannedGoalNodeId = 'column-top-a-c08';
    const successorTask = internals.tasks.find((task) => task.id === 'successor-tail')!;

    expect(internals.topLiftInboundQueueNodeIdForTask(successorTask, null)).toBe('column-top-a-c11');
    expect(internals.topLiftInboundQueueSlotAvailableForTask(successorTask)).toBe(true);
  });

  it('allows same-direction top-lift spine following when the leading shuttle has enough headway', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      durationSec: 2400,
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 32,
        initialOutboundFullColumns: 4
      }
    }));

    sim.start();
    sim.advanceByInPlace(18);

    const state = sim.getState();
    const loadedFollower = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    expect(loadedFollower?.routeNodeIds.at(-1)).toBe(loadedFollower?.plannedGoalNodeId);
    expect(loadedFollower?.routeNodeIds.every((nodeId) => !nodeId.includes('-top-b'))).toBe(true);
    expect(loadedFollower?.waitReason).toBeNull();
    expect(loadedFollower?.currentEdgeId).not.toBeNull();
    expect(state.traffic.waitingVehicles.filter((vehicle) => vehicle.vehicleId === loadedFollower?.id)).toEqual([]);
    expect(state.traffic.physicalViolationCount).toBe(0);
  });

  it('dispatches top-lift inbound work into lift-side pickup and queue positions at startup', () => {
    const scenario = createInboundMvpBaselineScenario();
    const sim = new ShuttleSimCore(scenario);

    expect(sim.getState().tasks.filter((task) => task.kind === 'inbound')).toHaveLength(8);

    sim.start();
    const state = sim.step(0.25);
    const assignedInboundTasks = state.tasks.filter((task) => task.kind === 'inbound' && task.state === 'assigned');
    const queuedInboundTasks = state.tasks.filter((task) => task.kind === 'inbound' && task.state === 'queued');
    const movingVehicles = state.vehicles.filter((vehicle) => vehicle.currentEdgeId !== null);

    expect(assignedInboundTasks).toHaveLength(6);
    expect(queuedInboundTasks).toHaveLength(2);
    expect(assignedInboundTasks.map((task) => task.dropoffNodeId)).toEqual([
      'storage-r14-c01',
      'storage-r14-c15',
      'storage-r13-c01',
      'storage-r13-c15',
      'storage-r12-c01',
      'storage-r12-c15'
    ]);
    expect(queuedInboundTasks.map((task) => task.dropoffNodeId)).toEqual([
      'storage-r11-c01',
      'storage-r11-c15'
    ]);
    expect(queuedInboundTasks.every((task) => task.waitReason === 'inbound-lift-fifo-wait')).toBe(true);
    expect(movingVehicles).toHaveLength(6);
    const taskGoals = new Map(state.vehicles.map((vehicle) => [vehicle.taskId, vehicle.plannedGoalNodeId]));
    expect(taskGoals.get('task-0001')).toBe('column-top-a-c08');
    expect(taskGoals.get('task-0002')).toBe('column-top-a-c22');
    expect(taskGoals.get('task-0003')).toBe('column-top-a-c10');
    expect(taskGoals.get('task-0004')).toBe('column-top-a-c24');
    expect(taskGoals.get('task-0005')).toBe('column-top-a-c11');
    expect(taskGoals.get('task-0006')).toBe('column-top-a-c25');
    expect(taskGoals.get('task-0007')).toBeUndefined();
    expect(Array.from(taskGoals.values()).filter(Boolean).every((nodeId) =>
      /^column-top-a-c\d+$/.test(nodeId!)
    )).toBe(true);
  });

  it('adds compact tail wait slots beside each top-lift inbound pickup queue', () => {
    const scenario = createInboundMvpBaselineScenario();
    const queueSlotIds = [
      'parking-lift-01-inbound-queue',
      'parking-lift-01-inbound-queue-02',
      'parking-lift-01-inbound-queue-03'
    ];
    const queueAccessIds = [
      'lift-01-inbound-queue-access',
      'lift-01-inbound-queue-01-access',
      'lift-01-inbound-queue-02-access',
      'lift-01-inbound-queue-03-access'
    ];
    const queueSlots = queueSlotIds.map((nodeId) => scenario.layout.nodes.find((node) => node.id === nodeId));
    const queueAccesses = queueAccessIds.map((nodeId) => scenario.layout.nodes.find((node) => node.id === nodeId));
    const queueEdges = new Set(scenario.layout.edges.map((edge) => `${edge.from}>${edge.to}`));

    expect(queueSlots.map((node) => node?.type)).toEqual(['parking', 'parking', 'parking']);
    expect(queueAccesses.map((node) => node?.type)).toEqual(['intersection', 'intersection', 'intersection', 'intersection']);
    expect(queueSlots.map((node) => node?.noParking)).toEqual([true, true, true]);
    expect(queueSlots.map((node) => node?.noStop)).toEqual([true, true, true]);
    expect(queueEdges.has('lift-01-inbound-queue-access>lift-01-inbound-queue-01-access')).toBe(false);
    expect(queueEdges.has('lift-01-inbound-queue-01-access>parking-lift-01-inbound-queue')).toBe(true);
    expect(queueEdges.has('lift-01-inbound-queue-01-access>lift-01-inbound-queue-02-access')).toBe(true);
    expect(queueEdges.has('lift-01-inbound-queue-02-access>parking-lift-01-inbound-queue-02')).toBe(true);
    expect(queueEdges.has('lift-01-inbound-queue-02-access>lift-01-inbound-queue-03-access')).toBe(true);
    expect(queueEdges.has('lift-01-inbound-queue-03-access>parking-lift-01-inbound-queue-03')).toBe(true);
    expect(queueEdges.has('lift-01-inbound-queue-access>parking-lift-01-inbound-queue')).toBe(false);
    expect(queueEdges.has('lift-01-inbound-queue-access>parking-lift-01-inbound-queue-02')).toBe(false);
    expect(queueEdges.has('lift-01-inbound-queue-access>parking-lift-01-inbound-queue-03')).toBe(false);
    expect(queueEdges.has('parking-lift-01-inbound-queue>parking-lift-01-inbound-queue-02')).toBe(true);
    expect(queueEdges.has('parking-lift-01-inbound-queue-02>parking-lift-01-inbound-queue-03')).toBe(true);
    expect(queueEdges.has('lift-01-inbound-queue-01-entry-access>lift-01-inbound-queue-01-access')).toBe(true);
    expect(queueEdges.has('lift-01-inbound-queue-02-entry-access>lift-01-inbound-queue-02-access')).toBe(true);
    expect(queueEdges.has('lift-01-inbound-buffer-access>lift-01-inbound-queue-access')).toBe(true);
    expect(queueEdges.has('column-top-a-c09>lift-01-inbound-queue-01-entry-access')).toBe(false);
    expect(queueEdges.has('lift-01-inbound-queue-01-entry-access>column-top-a-c09')).toBe(false);
    expect(queueEdges.has('lift-01-inbound-queue-01-entry-access>column-top-b-c09')).toBe(true);
    expect(scenario.layout.edges.some((edge) =>
      edge.from === 'lift-01-inbound-queue-01-entry-access' &&
      edge.to === 'column-top-b-c09' &&
      edge.directionMode === 'twoWay'
    )).toBe(true);
    expect(scenario.layout.edges.some((edge) =>
      (edge.from === 'lift-01-inbound-queue-access' && edge.to.startsWith('column-top-a-')) ||
      (edge.to === 'lift-01-inbound-queue-access' && edge.from.startsWith('column-top-a-'))
    )).toBe(false);
  });

  it('routes an inbound queue access shuttle directly into its parking meter instead of crossing the lift entry', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    }));
    const accessNodeId = 'lift-01-inbound-queue-01-access';
    const entryNodeId = 'lift-01-inbound-queue-01-entry-access';
    const parkingNodeId = 'parking-lift-01-inbound-queue';
    sim.setVehicleRouteForTest('SH-01', [accessNodeId, entryNodeId, accessNodeId, parkingNodeId]);
    sim.addLoadForTest({ id: 'queue-access-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queue-access-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c01',
      loadId: 'queue-access-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'queue-access-task', false);
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
      }>;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    vehicle.plannedGoalNodeId = parkingNodeId;
    vehicle.plannedRouteNodeIds = [accessNodeId, entryNodeId, accessNodeId, parkingNodeId];

    sim.step(0.2);
    const state = sim.getState();
    const corrected = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const correctionEvent = sim.getEventLog().find((event) =>
      event.vehicleId === 'SH-01' &&
      event.reason === 'agent-refresh-top-lift-queue-access-correction'
    );

    expect(corrected?.routeNodeIds).toEqual([accessNodeId, parkingNodeId]);
    expect(corrected?.targetNodeId).toBe(parkingNodeId);
    expect(corrected?.routeNodeIds).not.toContain(entryNodeId);
    expect(correctionEvent?.details.route).toBe(`${accessNodeId}>${parkingNodeId}`);
  });

  it('keeps a later same-lift inbound queue task from blocking the earlier pickup task', () => {
    const sim = new ShuttleSimCore(createInboundMvpBaselineScenario());

    const state = runFor(sim, 180);
    const task8 = state.tasks.find((task) => task.id === 'task-0008');
    const task9 = state.tasks.find((task) => task.id === 'task-0009');
    const repeatedTopRightYield = sim.getEventLog().filter((event) =>
      event.vehicleId === 'SH-05' &&
      event.eventType === 'route-replanned' &&
      event.reason === 'agent-refresh-side-yield' &&
      String(event.details.route ?? '') === 'column-top-b-c26>column-top-b-c25'
    );

    if (task8) {
      expect(task8.state).not.toBe('failed');
      if (task8.vehicleId) {
        expect(task8.vehicleId).toMatch(/^SH-\d{2}$/);
      } else {
        expect(task8.waitReason).toMatch(/^(?:inbound-lift-fifo-wait|inbound-column-predecessor-wait|vehicle-unavailable)$/);
      }
    } else {
      expect(state.kpis.completedInbound).toBeGreaterThanOrEqual(8);
    }
    expect(task9?.state).not.toBe('failed');
    if (task9?.vehicleId) {
      expect(task9.vehicleId).toMatch(/^SH-\d{2}$/);
    } else {
      expect(task9?.waitReason).not.toBe('route-unavailable');
    }
    expect(repeatedTopRightYield).toHaveLength(0);
    expect(state.kpis.deadlockCount).toBe(0);
    expect(state.traffic.physicalViolationCount).toBe(0);
  }, 20000);

  it('keeps unready top-lift inbound work from parking on the pickup stop and gridlocking traffic', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({ durationSec: 2400 }));
    sim.start();
    sim.advanceByInPlace(420);
    const state = sim.getState();
    const maxWaitSec = Math.max(
      0,
      ...state.traffic.waitingVehicles.map((vehicle) =>
        vehicle.waitingSinceSec === null ? 0 : state.simTimeSec - vehicle.waitingSinceSec
      )
    );
    const pickupSourceWaits = state.traffic.waitingVehicles.filter((vehicle) =>
      vehicle.waitReason === 'inbound-source-not-at-pickup' &&
      (
        /^column-top-b-c(?:07|21)$/.test(vehicle.currentNodeId) ||
        /^lift-\d{2}-inbound-queue-01-service-exit$/.test(vehicle.currentNodeId)
      )
    );

    expect(pickupSourceWaits).toEqual([]);
    expect(maxWaitSec).toBeLessThan(20);
    expect(state.kpis.completedInbound).toBeGreaterThanOrEqual(12);
    expect(state.kpis.completedOutbound).toBeGreaterThanOrEqual(15);
    expect(state.kpis.deadlockCount).toBe(0);
    expect(state.traffic.physicalViolationCount).toBe(0);
  }, 45000);

  it('clears a projected top-a pickup blocker even when the inbound queue slots are full', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 2
      }
    }));
    sim.addLoadForTest({ id: 'projected-blocker-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'projected-blocker-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'projected-blocker-earlier-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r13-c04',
      loadId: 'projected-blocker-earlier-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'projected-blocker-later-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r12-c04',
      loadId: 'projected-blocker-later-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-01', 'projected-blocker-later-task', false);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c09', 'column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-02', 'projected-blocker-earlier-task', false);
    sim.setVehicleRouteForTest('SH-03', ['column-top-a-c10']);
    sim.setVehicleRouteForTest('SH-04', ['column-top-a-c11']);

    sim.step(0.2);
    const state = sim.getState();
    const laterVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(laterVehicle?.waitReason).not.toBe('inbound-source-not-at-pickup');
    expect(laterVehicle?.targetNodeId).toBe('column-top-b-c08');
    expect(laterVehicle?.plannedGoalNodeId).toBe('column-top-b-c08');
    expect(laterVehicle?.localRouteReason).toBe('temporary-yield');
    expect(laterVehicle?.routeNodeIds).toEqual(['column-top-a-c08', 'column-top-b-c08']);
  });

  it('does not let a later same-column inbound task lift before its deeper predecessor completes', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 2
      }
    }));
    sim.addLoadForTest({ id: 'same-column-earlier-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'same-column-earlier-r06',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r06-c28',
      loadId: 'same-column-earlier-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'same-column-later-load', state: 'waiting', nodeId: 'column-top-a-c22', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'same-column-later-r04',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r04-c28',
      loadId: 'same-column-later-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['column-middle-c28', 'storage-r07-c28', 'storage-r06-c28']);
    sim.setVehicleTaskForTest('SH-02', 'same-column-earlier-r06', true);
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c22']);
    sim.setVehicleTaskForTest('SH-01', 'same-column-later-r04', false);

    sim.step(0.2);
    const state = sim.getState();
    const laterVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const laterTask = state.tasks.find((candidate) => candidate.id === 'same-column-later-r04');
    const laterLiftStarted = sim.getEventLog().some((entry) =>
      entry.eventType === 'lift-started' &&
      entry.vehicleId === 'SH-01' &&
      entry.taskId === 'same-column-later-r04'
    );

    expect(laterVehicle?.loaded).toBe(false);
    expect(laterVehicle?.state).not.toBe('lifting');
    expect(laterTask?.startedAtSec).toBeNull();
    expect(laterLiftStarted).toBe(false);
  });

  it('clears an inbound top-a spine requester sideways when its queue goal is temporarily unreachable', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({ id: 'spine-clear-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'spine-clear-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r06-c17',
      loadId: 'spine-clear-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['module-02-spine-top-a']);
    sim.setVehicleTaskForTest('SH-01', 'spine-clear-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      tryClearInboundTopASpineRouteUnavailable(vehicle: unknown, task: unknown): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'spine-clear-task')!;

    expect(internals.tryClearInboundTopASpineRouteUnavailable(vehicle, task)).toBe(true);
    const state = sim.getState();
    const clearedVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(clearedVehicle?.targetNodeId).toBe('column-top-a-c21');
    expect(clearedVehicle?.plannedGoalNodeId).toBe('column-top-a-c21');
    expect(clearedVehicle?.localRouteReason).toBe('temporary-yield');
  });

  it('keeps a staged same-lift inbound successor in queue until its deeper predecessor clears', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 1
      }
    }));
    sim.addLoadForTest({
      id: 'same-column-predecessor-load',
      state: 'carried',
      nodeId: null,
      vehicleId: 'SH-01',
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'same-column-predecessor-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c11',
      loadId: 'same-column-predecessor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'same-column-successor-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'same-column-successor-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r13-c11',
      loadId: 'same-column-successor-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-a-c08',
      'column-top-b-c08',
      'column-top-b-c09',
      'column-top-b-c10',
      'column-top-b-c11',
      'storage-r14-c11'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'same-column-predecessor-task', true);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-02', 'same-column-successor-task', false);

    const state = sim.step(0.2);
    const successorTask = state.tasks.find((task) => task.id === 'same-column-successor-task');
    const successorVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(successorTask?.state).toBe('assigned');
    expect(successorTask?.startedAtSec).toBeNull();
    expect(successorVehicle?.currentNodeId).toBe('column-top-a-c10');
    expect(successorVehicle?.plannedGoalNodeId).toMatch(/^column-top-a-c(?:09|10)$/);
    expect(successorVehicle?.plannedRouteNodeIds).not.toContain('column-top-a-c08');
    expect(successorVehicle?.loaded).toBe(false);
  });

  it('clears empty outbound dropoff shuttles from the lift-side service dock before reassignment', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario());
    sim.start();

    let clearanceRoute: string[] | null = null;
    let clearanceVehicleId: string | null = null;
    for (let step = 0; step < 300; step += 1) {
      sim.step(0.2);
      const clearanceEvent = sim.getEventLog().find((event) =>
        event.eventType === 'vehicle-standby-dispatched' &&
        event.reason === 'outbound-lift-dropoff-clearance' &&
        typeof event.details.route === 'string'
      );
      if (clearanceEvent) {
        clearanceRoute = String(clearanceEvent.details.route).split('>');
        clearanceVehicleId = clearanceEvent.vehicleId;
        break;
      }
    }
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        assignmentHoldUntilSec: number | null;
        localRouteReason: string | null;
      }>;
      simTimeSec: number;
    };
    const clearanceVehicle = internals.vehicles.find((vehicle) => vehicle.id === clearanceVehicleId);

    expect(clearanceRoute?.slice(0, 4)).toEqual([
      'column-bottom-b-c08',
      'column-bottom-b-c09',
      'column-bottom-b-c10',
      'column-bottom-b-c11'
    ]);
    expect(clearanceRoute?.at(-1)).toMatch(/^storage-r\d{2}-c\d{2}$/);
    expect(clearanceRoute?.slice(1)).not.toContain('column-bottom-b-c08');
    expect(clearanceRoute?.some((nodeId) => nodeId.includes('-top-b'))).toBe(false);
    expect(clearanceRoute).not.toContain('lift-01-outbound-buffer-access');
    expect(clearanceVehicle?.localRouteReason).toBe('outbound-lift-clearance');
    expect(clearanceVehicle?.assignmentHoldUntilSec ?? 0).toBeGreaterThan(internals.simTimeSec);
  });

  it('moves a later same-lift task off the pickup point when the earlier load is still first', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'earlier-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r14-c01',
      loadId: 'earlier-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c01',
      loadId: 'later-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['parking-lift-01-inbound-queue']);
    sim.setVehicleTaskForTest('SH-01', 'later-pickup', false);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(vehicle?.waitReason).not.toBe('inbound-source-not-at-pickup');
    expect(vehicle?.plannedGoalNodeId).toBe('parking-lift-01-inbound-queue-02');
    expect(vehicle?.routeNodeIds).not.toContain('lift-01-inbound-queue-01-service-exit');
  });

  it('clears an unready inbound pickup stop into adjacent storage even when that column is active', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 5 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'blocked-queue-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'blocked-queue-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'blocked-queue-earlier-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r14-c07',
      loadId: 'blocked-queue-earlier-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'blocked-queue-later-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c07',
      loadId: 'blocked-queue-later-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['parking-lift-01-inbound-queue']);
    sim.setVehicleTaskForTest('SH-01', 'blocked-queue-later-pickup', false);
    sim.setVehicleRouteForTest('SH-03', ['module-01-spine-top-b', 'column-top-b-c08']);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(vehicle?.waitReason).not.toBe('inbound-source-not-at-pickup');
    expect(vehicle?.plannedGoalNodeId).toBe('parking-lift-01-inbound-queue-02');
    expect(vehicle?.routeNodeIds).not.toContain('lift-01-inbound-queue-01-service-exit');
  });

  it('keeps a later inbound queue blocker at the queue meter so the earlier pickup can pass', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 6 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'queue-block-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'queue-block-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queue-block-earlier-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r14-c01',
      loadId: 'queue-block-earlier-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'queue-block-later-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c01',
      loadId: 'queue-block-later-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'queue-block-earlier-pickup', false);
    sim.setVehicleRouteForTest('SH-06', ['parking-lift-01-inbound-queue']);
    sim.setVehicleTaskForTest('SH-06', 'queue-block-later-pickup', false);

    const state = sim.step(0.2);
    const laterVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-06');
    expect(laterVehicle?.plannedGoalNodeId).toBe('parking-lift-01-inbound-queue');
    expect(laterVehicle?.routeNodeIds).not.toContain('lift-01-inbound-queue-01-service-exit');
    expect([null, 'projected-display-clearance', 'inbound-lift-fifo-wait']).toContain(laterVehicle?.waitReason);
  });

  it('reassigns a later top-a inbound queue hold so it no longer blocks the earlier infeed entry', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 6 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'entry-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'entry-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'entry-earlier-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r14-c01',
      loadId: 'entry-earlier-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'entry-later-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c01',
      loadId: 'entry-later-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c09',
      'lift-01-inbound-queue-01-entry-access',
      'lift-01-inbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'entry-earlier-pickup', false);
    sim.setVehicleRouteForTest('SH-06', ['column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-06', 'entry-later-pickup', false);
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: null,
      waitReason: 'inbound-lift-source-assigned',
      waitingSinceSec: 0
    });
    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      topLiftInboundQueueHoldReason(task: unknown): string;
    };
    const laterTask = internals.tasks.find((candidate) => candidate.id === 'entry-later-pickup')!;
    expect(internals.topLiftInboundQueueHoldReason(laterTask)).toBe('inbound-lift-fifo-wait');

    const state = sim.step(0.2);
    const requester = state.vehicles.find((candidate) => candidate.id === 'SH-03');
    const blocker = state.vehicles.find((candidate) => candidate.id === 'SH-06');
    const reassignedTask = state.tasks.find((candidate) => candidate.id === 'entry-later-pickup');

    expect(requester?.waitReason).toBeNull();
    expect(requester?.blockingVehicleId).toBeNull();
    expect(reassignedTask?.vehicleId).not.toBe('SH-06');
    expect(blocker?.taskId).toBeNull();
    expect(blocker?.targetNodeId).toBeNull();
    expect(blocker?.waitReason).toBeNull();
  });

  it('compresses a projected inbound queue hold toward the lift while a column predecessor clears', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 4
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      }
    }));

    sim.addLoadForTest({ id: 'column-predecessor-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'column-predecessor',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r09-c13',
      loadId: 'column-predecessor-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'held-successor-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'held-successor',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r08-c13',
      loadId: 'held-successor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['storage-r09-c13']);
    sim.setVehicleTaskForTest('SH-02', 'column-predecessor', true);
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c11']);
    sim.setVehicleTaskForTest('SH-01', 'held-successor', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-a-c11',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      topLiftInboundQueueHoldReason(task: unknown): string;
    };
    const heldTask = internals.tasks.find((candidate) => candidate.id === 'held-successor')!;
    expect(internals.topLiftInboundQueueHoldReason(heldTask)).toBe('inbound-column-predecessor-wait');

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.routeNodeIds).toEqual([
      'column-top-a-c11',
      'column-top-a-c10',
      'column-top-a-c09'
    ]);
    expect(vehicle?.plannedGoalNodeId).toBe('column-top-a-c09');
    expect(vehicle?.targetNodeId).toBe('column-top-a-c10');
    expect(vehicle?.waitReason).toBeNull();
  });

  it('holds inbound pickup while a same-column loaded predecessor is still active', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 4
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      }
    }));

    sim.addLoadForTest({ id: 'headed-predecessor-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'headed-predecessor-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c04',
      loadId: 'headed-predecessor-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'ready-successor-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'ready-successor-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r13-c04',
      loadId: 'ready-successor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c04', 'column-top-b-c04', 'storage-r14-c04']);
    sim.setVehicleTaskForTest('SH-02', 'headed-predecessor-task', true);
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c11']);
    sim.setVehicleTaskForTest('SH-01', 'ready-successor-task', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      topLiftInboundColumnPredecessorPending(task: unknown): boolean;
      topLiftInboundColumnPredecessorBlocksPickup(task: unknown): boolean;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
    };
    const successorTask = internals.tasks.find((candidate) => candidate.id === 'ready-successor-task')!;
    const successorVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const queueNodeId = internals.topLiftInboundQueueNodeIdForTask(successorTask, successorVehicle);

    expect(internals.topLiftInboundColumnPredecessorPending(successorTask)).toBe(true);
    expect(internals.topLiftInboundColumnPredecessorBlocksPickup(successorTask)).toBe(true);
    expect(internals.inboundTaskShouldWaitBeforePickup(successorTask)).toBe(true);
    expect(internals.inboundPickupDispatchGoalNodeId(successorTask, successorVehicle)).toBe(queueNodeId);
    expect(internals.inboundPickupDispatchGoalNodeId(successorTask, successorVehicle)).not.toBe('column-top-a-c08');
  });

  it('lets the front inbound queue successor pick once its same-column predecessor is inside storage', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        initialOutboundFullColumns: 0,
        maxTasks: 4
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      }
    }));

    sim.addLoadForTest({ id: 'inside-predecessor-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'inside-predecessor-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c04',
      loadId: 'inside-predecessor-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'front-successor-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'front-successor-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r13-c04',
      loadId: 'front-successor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: 'inbound-column-predecessor-wait'
    });
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r10-c04',
      'storage-r11-c04',
      'storage-r12-c04',
      'storage-r13-c04',
      'storage-r14-c04'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'inside-predecessor-task', true);
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-01', 'front-successor-task', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; waitReason: string | null; plannedGoalNodeId: string | null }>;
      topLiftInboundColumnPredecessorPending(task: unknown): boolean;
      topLiftInboundColumnPredecessorBlocksPickup(task: unknown): boolean;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      topLiftLoadedInboundWipCountForLift(liftNodeId: string): number;
    };
    const successorTask = internals.tasks.find((candidate) => candidate.id === 'front-successor-task')!;
    const successorVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.topLiftLoadedInboundWipCountForLift('lift-01-inbound')).toBe(1);
    expect(internals.topLiftInboundColumnPredecessorPending(successorTask)).toBe(true);
    expect(internals.topLiftInboundColumnPredecessorBlocksPickup(successorTask)).toBe(false);
    expect(internals.inboundTaskShouldWaitBeforePickup(successorTask)).toBe(false);
    expect(internals.inboundPickupDispatchGoalNodeId(successorTask, successorVehicle)).toBe('column-top-a-c08');

    const state = sim.step(0.2);
    const releasedVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const releasedTask = state.tasks.find((task) => task.id === 'front-successor-task');

    expect(releasedVehicle?.waitReason).toBeNull();
    expect(releasedVehicle?.plannedGoalNodeId).toBe('column-top-a-c08');
    expect(releasedVehicle?.targetNodeId).toBe('column-top-a-c08');
    expect(releasedTask?.startedAtSec).toBeNull();
  });

  it('moves an empty middle-aisle blocker away from a loaded inbound drop route', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-middle-drop-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-middle-drop-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r08-c25',
      loadId: 'loaded-middle-drop-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'empty-middle-blocker-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-middle-blocker-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r05-c14',
      loadId: 'empty-middle-blocker-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c22',
      'column-middle-c23',
      'column-middle-c24',
      'column-middle-c25',
      'storage-r08-c25'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-middle-drop-task', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-02-spine-middle',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-02'
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c25',
      'column-middle-c24',
      'storage-r07-c24',
      'storage-r06-c24',
      'storage-r05-c24',
      'storage-r04-c24',
      'storage-r03-c24',
      'storage-r02-c24',
      'storage-r01-c24',
      'column-top-b-c24',
      'column-top-a-c24',
      'column-top-a-c23',
      'column-top-a-c22',
      'module-02-spine-top-a',
      'column-top-a-c21',
      'column-top-a-c20',
      'column-top-a-c19',
      'column-top-a-c18',
      'column-top-a-c17',
      'column-top-a-c16',
      'column-top-a-c15',
      'module-boundary-01-spine-top-a',
      'column-top-a-c14',
      'column-top-a-c13',
      'column-top-a-c12',
      'column-top-a-c11',
      'column-top-a-c10'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'empty-middle-blocker-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const loaded = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.agentRefreshHandleMoveBlock(
      loaded,
      'module-02-spine-middle',
      { reason: 'middle-aisle-opposing-claim', blockingVehicleId: 'SH-02' }
    )).toBe(false);

    const blocker = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;
    expect(blocker.currentNodeId).toBe('column-middle-c25');
    expect(blocker.targetNodeId).not.toBe('column-middle-c25');
    expect(blocker.waitReason).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
    expect(blocker.routeNodeIds[0]).toBe('column-middle-c25');
    expect(blocker.routeNodeIds[1]).not.toBe('column-middle-c24');
    expect(blocker.routeNodeIds).not.toContain('storage-r08-c25');
  });

  it('moves a route-unavailable empty middle blocker into a storage pocket to clear loaded inbound', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'tail-loaded-drop-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'tail-loaded-drop-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r10-c28',
      loadId: 'tail-loaded-drop-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'tail-empty-blocker-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'tail-empty-blocker-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r10-c06',
      loadId: 'tail-empty-blocker-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c22',
      'column-middle-c23',
      'column-middle-c24',
      'column-middle-c25',
      'column-middle-c26',
      'column-middle-c27',
      'column-middle-c28',
      'storage-r08-c28',
      'storage-r09-c28',
      'storage-r10-c28'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'tail-loaded-drop-task', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-02-spine-middle',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-02'
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c28',
      'column-middle-c27',
      'storage-r07-c27'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'tail-empty-blocker-task', false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c27',
      waitReason: 'route-unavailable',
      blockingVehicleId: null
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const loaded = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.agentRefreshHandleMoveBlock(
      loaded,
      'module-02-spine-middle',
      { reason: 'middle-aisle-opposing-claim', blockingVehicleId: 'SH-02' }
    )).toBe(false);

    const blocker = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;
    expect(blocker.currentNodeId).toBe('column-middle-c28');
    expect(blocker.targetNodeId).toBe('storage-r07-c28');
    expect(blocker.waitReason).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
    expect(blocker.routeNodeIds).toEqual(['column-middle-c28', 'storage-r07-c28']);
  });

  it('clears an idle bottom-b no-parking blocker out of a loaded outbound queue', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'bottom-b-service-blocked-load', state: 'carried', nodeId: null, vehicleId: 'SH-03', weightKg: 100 });
    sim.addLoadForTest({ id: 'bottom-b-c06-occupied-load', state: 'stored', nodeId: 'storage-r14-c06', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'bottom-b-service-blocked-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c02',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'bottom-b-service-blocked-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'column-bottom-b-c05',
      'column-bottom-b-c06',
      'column-bottom-b-c07',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'bottom-b-service-blocked-task', true);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'column-bottom-b-c06',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-04',
      waitingSinceSec: -90
    });
    sim.setVehicleRouteForTest('SH-04', ['column-bottom-b-c06']);
    sim.setVehicleTaskForTest('SH-04', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const requester = internals.vehicles.find((candidate) => candidate.id === 'SH-03')!;

    expect(internals.agentRefreshHandleMoveBlock(
      requester,
      'column-bottom-b-c06',
      { reason: 'node-occupied', blockingVehicleId: 'SH-04' }
    )).toBe(false);

    const blocker = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;
    expect(blocker.currentNodeId).toBe('column-bottom-b-c06');
    expect(blocker.targetNodeId).not.toBeNull();
    expect(blocker.waitReason).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
    expect(blocker.routeNodeIds[0]).toBe('column-bottom-b-c06');
    expect(blocker.routeNodeIds).not.toContain('column-bottom-b-c05');
    expect(blocker.routeNodeIds.at(-1)).toMatch(/^storage-/);
    expect(blocker.routeNodeIds.at(-1)).not.toBe('storage-r14-c06');
    expectRouteHopsAdjacent(scenario, blocker.routeNodeIds);
  });

  it('clears an assigned no-target top-b no-parking blocker away from an inbound requester', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'top-b-requester-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'top-b-requester-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r12-c16',
      loadId: 'top-b-requester-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'top-b-blocker-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'top-b-blocker-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r05-c18',
      loadId: 'top-b-blocker-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', [
      'column-top-b-c22',
      'column-top-b-c23',
      'column-top-b-c24',
      'column-top-a-c24',
      'column-top-a-c23',
      'column-top-a-c22'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'top-b-requester-task', false);
    sim.setVehicleWaitingForTest('SH-08', {
      targetNodeId: 'column-top-b-c23',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-07',
      waitingSinceSec: -90
    });
    sim.setVehicleRouteForTest('SH-07', ['column-top-b-c23']);
    sim.setVehicleTaskForTest('SH-07', 'top-b-blocker-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const requester = internals.vehicles.find((candidate) => candidate.id === 'SH-08')!;

    expect(internals.agentRefreshHandleMoveBlock(
      requester,
      'column-top-b-c23',
      { reason: 'node-occupied', blockingVehicleId: 'SH-07' }
    )).toBe(false);

    const blocker = internals.vehicles.find((candidate) => candidate.id === 'SH-07')!;
    expect(blocker.currentNodeId).toBe('column-top-b-c23');
    expect(blocker.targetNodeId).not.toBeNull();
    expect(blocker.waitReason).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
    expect(blocker.routeNodeIds[0]).toBe('column-top-b-c23');
    expect(blocker.routeNodeIds).not.toContain('column-top-b-c22');
    expect(blocker.routeNodeIds.at(-1)).toMatch(/^storage-/);
    expect(blocker.routeNodeIds.at(-1)).not.toBe('column-top-b-c23');
    expectRouteHopsAdjacent(scenario, blocker.routeNodeIds);
  });

  it('releases a long route-unavailable outbound storage pickup back to the task queue', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'storage-release-load',
      state: 'stored',
      nodeId: 'storage-r08-c16',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'storage-release-task',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r08-c16',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'storage-release-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r12-c16']);
    sim.setVehicleTaskForTest('SH-01', 'storage-release-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r08-c16',
      waitReason: 'route-unavailable',
      blockingVehicleId: null,
      waitingSinceSec: -121
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        taskId: string | null;
        state: string;
        waitReason: string | null;
        localRouteReason: string | null;
        targetNodeId: string | null;
      }>;
      tasks: Array<{
        id: string;
        state: string;
        vehicleId: string | null;
        waitReason: string | null;
      }>;
      tryReleaseRouteUnavailableOutboundPickupTaskInStorage(vehicle: unknown, task: unknown, goalNodeId: string): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'storage-release-task')!;

    expect(internals.tryReleaseRouteUnavailableOutboundPickupTaskInStorage(
      vehicle,
      task,
      'storage-r08-c16'
    )).toBe(true);

    expect(task.state).toBe('queued');
    expect(task.vehicleId).toBeNull();
    expect(task.waitReason).toBe('route-unavailable');
    expect(vehicle.taskId).toBeNull();
    expect(vehicle.waitReason).toBeNull();
    expect(vehicle.state).not.toBe('waiting-blocked');
    expect(vehicle.localRouteReason).toBe('route-unavailable-storage-release-exit');
    expect(vehicle.targetNodeId).not.toBeNull();
  });

  it('allows an empty top-b shuttle to clear away from a loaded inbound service exit', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'service-exit-load', state: 'carried', nodeId: null, vehicleId: 'SH-04', weightKg: 100 });
    sim.addTaskForTest({
      id: 'service-exit-loaded-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r09-c01',
      loadId: 'service-exit-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-04', [
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c09',
      'storage-r01-c09',
      'storage-r02-c09',
      'storage-r03-c09',
      'storage-r04-c09',
      'storage-r05-c09',
      'column-middle-c09',
      'module-01-spine-middle',
      'module-01-spine-top-b',
      'column-top-b-c08',
      'column-top-b-c07',
      'column-top-b-c06',
      'column-top-b-c05',
      'column-top-b-c04',
      'column-top-b-c03',
      'column-top-b-c02',
      'column-top-b-c01',
      'storage-r01-c01',
      'storage-r02-c01',
      'storage-r03-c01',
      'storage-r04-c01',
      'storage-r05-c01',
      'storage-r06-c01',
      'storage-r07-c01',
      'storage-r08-c01',
      'storage-r09-c01'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'service-exit-loaded-inbound', true);
    sim.setVehicleRouteForTest('SH-07', ['column-top-b-c08', 'module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-07', null, false);

    const internals = sim as unknown as {
      vehicles: Array<unknown & { id: string }>;
      topLiftInboundServiceAccessAisleClearanceMoveAllowed(vehicle: unknown, toNodeId: string, serviceVehicle: unknown): boolean;
      agentRefreshMoveBlocker(vehicle: unknown, toNodeId: string, candidateRouteNodeIds?: string[] | null): { reason: string; blockingVehicleId: string | null } | null;
    };
    const serviceVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-04')!;
    const aisleVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;

    expect(internals.topLiftInboundServiceAccessAisleClearanceMoveAllowed(
      aisleVehicle,
      'module-01-spine-top-b',
      serviceVehicle
    )).toBe(true);
    expect(internals.topLiftInboundServiceAccessAisleClearanceMoveAllowed(
      serviceVehicle,
      'lift-01-inbound-queue-01-entry-access',
      aisleVehicle
    )).toBe(false);
    expect(internals.agentRefreshMoveBlocker(
      aisleVehicle,
      'module-01-spine-top-b',
      ['column-top-b-c08', 'module-01-spine-top-b']
    )).toBeNull();
  });

  it('stages later inbound work in a dedicated lift-side queue instead of the main aisle', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 6 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'earlier-service-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'later-service-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'earlier-service-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r14-c03',
      loadId: 'earlier-service-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-service-pickup',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 1,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c04',
      loadId: 'later-service-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07',
      'lift-01-inbound-queue-01-entry-access',
      'lift-01-inbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'earlier-service-pickup', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
    };
    const laterTask = internals.tasks.find((task) => task.id === 'later-service-pickup')!;
    const laterVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;

    expect(internals.topLiftInboundQueueNodeIdForTask(laterTask, laterVehicle)).toMatch(/^column-top-a-c(?:09|10|11)$/);

    const state = sim.step(0.2);
    const assignedLaterTask = state.tasks.find((task) => task.id === 'later-service-pickup');
    const assignedVehicle = state.vehicles.find((vehicle) => vehicle.id === assignedLaterTask?.vehicleId);

    expect(assignedLaterTask?.state).toBe('assigned');
    expect(assignedVehicle?.plannedGoalNodeId).toMatch(/^column-top-a-c(?:09|10|11)$/);
    expect(assignedVehicle?.routeNodeIds).not.toContain('lift-01-inbound-queue-01-service-exit');
    expect(assignedVehicle?.plannedRouteNodeIds.at(-1)).toBe(assignedVehicle?.plannedGoalNodeId);
  });

  it('keeps short top-lift side-yields from creating early traffic failures', () => {
    const sim = new ShuttleSimCore(createInboundMvpBaselineScenario());

    const state = runFor(sim, 30);

    expectNoTrafficSafetyFailures(state);
  }, 10000);

  it('keeps empty top-lift side-yields off the loaded inbound dropoff path', () => {
    const sim = new ShuttleSimCore(createInboundMvpBaselineScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    }));
    sim.addTaskForTest({
      id: 'loaded-drop-c16',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r02-c16',
      loadId: 'loaded-drop-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16',
      'storage-r01-c16',
      'storage-r02-c16'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-drop-c16', true);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c17', 'column-top-b-c18']);
    const internals = sim as unknown as {
      vehicles: any[];
      agentRefreshInstallSideYield: (
        vehicle: any,
        blockedTargetNodeId: string,
        requester: any,
        session?: null
      ) => boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshInstallSideYield(yielder, 'column-top-b-c18', requester, null)).toBe(true);
    expect(yielder.routeNodeIds).not.toContain('column-top-b-c16');
    expect(yielder.routeNodeIds).not.toContain('storage-r01-c16');
    expect(yielder.routeNodeIds).not.toContain('storage-r02-c16');
  });

  it('keeps empty top-lift side-yields off nearby loaded inbound approach routes', () => {
    const sim = new ShuttleSimCore(createInboundMvpBaselineScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    }));
    sim.addTaskForTest({
      id: 'loaded-drop-c15',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-buffer-03',
      dropoffNodeId: 'storage-r09-c15',
      loadId: 'loaded-drop-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c19',
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16',
      'column-top-b-c15',
      'storage-r08-c15',
      'storage-r09-c15'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-drop-c15', true);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c17', 'column-top-b-c16']);
    sim.setVehicleRouteForTest('SH-03', ['column-top-b-c16']);
    const internals = sim as unknown as {
      vehicles: any[];
      agentRefreshInstallSideYield: (
        vehicle: any,
        blockedTargetNodeId: string,
        requester: any,
        session?: null
      ) => boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshInstallSideYield(yielder, 'column-top-b-c16', requester, null)).toBe(true);
    expect(yielder.routeNodeIds).toEqual([
      'column-top-b-c17',
      'storage-r01-c17'
    ]);
    expect(yielder.routeNodeIds).not.toContain('storage-r02-c17');
    expect(yielder.routeNodeIds).not.toContain('column-top-b-c18');
    expect(yielder.routeNodeIds).not.toContain('column-top-b-c16');
  });

  it('does not push a top-lift storage side-yield deeper into the rack', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r01-c17', 'column-top-b-c17']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c17']);

    const internals = sim as unknown as {
      vehicles: any[];
      agentRefreshInstallSideYield: (
        vehicle: any,
        blockedTargetNodeId: string,
        requester: any,
        session?: null
      ) => boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshInstallSideYield(yielder, 'column-top-b-c17', requester, null)).toBe(false);
    expect(yielder.routeNodeIds).not.toContain('storage-r02-c17');
  });

  it('accepts top-lift yield pockets that can return through the lift-side aisle grid', () => {
    const sim = new ShuttleSimCore(createInboundMvpBaselineScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c05']);
    const internals = sim as unknown as {
      vehicles: any[];
      agentRefreshYieldPocketCanReturn: (
        vehicle: any,
        pocketNodeId: string,
        resumeNodeId: string
      ) => boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.agentRefreshYieldPocketCanReturn(
      vehicle,
      'column-top-b-c04',
      'lift-01-inbound-buffer-03'
    )).toBe(true);
    expect(internals.agentRefreshYieldPocketCanReturn(
      vehicle,
      'column-top-a-c05',
      'lift-01-inbound-buffer-03'
    )).toBe(true);
  });

  it('does not ping-pong an empty top-lift storage yielder between adjacent cells', async () => {
    const sim = new ShuttleSimCore(createInboundMvpBaselineScenario());

    for (let elapsedSec = 0; elapsedSec < 570; elapsedSec += 30) {
      runFor(sim, Math.min(30, 570 - elapsedSec));
      await new Promise<void>((resolve) => setTimeout(resolve, 0));
    }

    const singleStepStorageYields = sim.getEventLog()
      .filter((event) =>
        event.vehicleId === 'SH-01' &&
        event.eventType === 'route-replanned' &&
        event.reason === 'empty-yields-to-loaded' &&
        event.timeSec >= 540 &&
        event.timeSec <= 570
      )
      .map((event) => String(event.details.route ?? '').split('>'))
      .filter((route) => route.length === 2 && route.every((nodeId) => /^storage-r\d+-c\d+$/.test(nodeId)));
    const reverseStep = singleStepStorageYields.find((route, index) =>
      index > 0 &&
      route[0] === singleStepStorageYields[index - 1]?.[1] &&
      route[1] === singleStepStorageYields[index - 1]?.[0]
    );

    expect(reverseStep).toBeUndefined();
  }, 90000);

  it('keeps top-lift inbound allocation bounded while a SKU column is active', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    const internals = sim as unknown as {
      selectTopLiftInboundStorageNode: () => { nodeId: string; loadId: string } | null;
    };
    const addInboundTask = (row: number): void => {
      const rowLabel = String(row).padStart(2, '0');
      sim.addTaskForTest({
        id: `inbound-c01-r${rowLabel}`,
        kind: 'inbound',
        state: 'queued',
        createdAtSec: 15 - row,
        assignedAtSec: null,
        startedAtSec: null,
        completedAtSec: null,
        pickupNodeId: 'lift-01-inbound-buffer-03',
        dropoffNodeId: `storage-r${rowLabel}-c01`,
        loadId: `source-c01-r${rowLabel}`,
        vehicleId: null,
        replanCount: 0,
        waitReason: null
      });
    };

    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r14-c01');
    addInboundTask(14);
    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r13-c01');
    addInboundTask(13);
    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r12-c01');
    addInboundTask(12);
    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r11-c01');
    addInboundTask(11);
    expect(internals.selectTopLiftInboundStorageNode()).toBeNull();
  });

  it('moves top-lift inbound allocation to the next SKU column after a full stored column', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    const internals = sim as unknown as {
      selectTopLiftInboundStorageNode: () => { nodeId: string; loadId: string } | null;
    };
    for (let row = 14; row >= 1; row -= 1) {
      const rowLabel = String(row).padStart(2, '0');
      sim.addLoadForTest({ id: `stored-c01-r${rowLabel}`, state: 'stored', nodeId: `storage-r${rowLabel}-c01`, vehicleId: null, weightKg: 100 });
    }

    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r14-c02');
  });

  it('keeps top-lift inbound allocation on a reachable same-column slot when one side is open', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    const internals = sim as unknown as {
      selectTopLiftInboundStorageNode: () => { nodeId: string; loadId: string } | null;
    };
    for (const row of [14, 13, 12, 11]) {
      sim.addLoadForTest({ id: `stored-c01-r${row}`, state: 'stored', nodeId: `storage-r${row}-c01`, vehicleId: null, weightKg: 100 });
    }

    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r10-c01');
  });

  it('skips top-lift inbound holes that are blocked by stored loads from both ends', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    const internals = sim as unknown as {
      selectTopLiftInboundStorageNode: () => { nodeId: string; loadId: string } | null;
    };
    for (let row = 1; row <= 14; row += 1) {
      if (row === 10) continue;
      const rowLabel = String(row).padStart(2, '0');
      sim.addLoadForTest({ id: `stored-c01-r${rowLabel}`, state: 'stored', nodeId: `storage-r${rowLabel}-c01`, vehicleId: null, weightKg: 100 });
    }

    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r14-c02');
  });

  it('blocks queued top-lift inbound holes when later active dropoffs would seal the only open side', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    const internals = sim as unknown as {
      taskAssignmentBlockReason: (task: {
        id: string;
        kind: 'inbound';
        state: 'queued' | 'assigned' | 'in-progress' | 'completed' | 'failed';
        createdAtSec: number;
        assignedAtSec: number | null;
        startedAtSec: number | null;
        completedAtSec: number | null;
        pickupNodeId: string;
        dropoffNodeId: string;
        loadId: string;
        vehicleId: string | null;
        replanCount: number;
        waitReason: string | null;
      }) => string | null;
    };
    const blockedHoleTask = {
      id: 'blocked-hole-r05',
      kind: 'inbound' as const,
      state: 'queued' as const,
      createdAtSec: 10,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r05-c01',
      loadId: 'blocked-hole-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    };

    sim.addLoadForTest({ id: 'stored-c01-r04', state: 'stored', nodeId: 'storage-r04-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest(blockedHoleTask);
    sim.addTaskForTest({
      id: 'later-active-r06',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 20,
      assignedAtSec: 20,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-02',
      dropoffNodeId: 'storage-r06-c01',
      loadId: 'later-active-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    expect(internals.taskAssignmentBlockReason(blockedHoleTask)).toBe('storage-full');
  });

  it('blocks inbound assignment into a column with active outbound ownership', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'column-mode-outbound-load', state: 'stored', nodeId: 'storage-r03-c18', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'column-mode-inbound-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'active-outbound-c18',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c18',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'column-mode-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const inboundTask = {
      id: 'queued-inbound-c18',
      kind: 'inbound' as const,
      state: 'queued' as const,
      createdAtSec: 1,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r12-c18',
      loadId: 'column-mode-inbound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    };
    sim.addTaskForTest(inboundTask);

    const internals = sim as unknown as {
      taskAssignmentBlockReason(task: unknown): string | null;
    };

    expect(internals.taskAssignmentBlockReason(inboundTask)).toBe('column-mode-conflict:c18');
  });

  it('blocks outbound assignment from a column with active inbound ownership', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'column-mode-inbound-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'column-mode-outbound-load', state: 'stored', nodeId: 'storage-r03-c18', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'active-inbound-c18',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r12-c18',
      loadId: 'column-mode-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const outboundTask = {
      id: 'queued-outbound-c18',
      kind: 'outbound' as const,
      state: 'queued' as const,
      createdAtSec: 1,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c18',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'column-mode-outbound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    };
    sim.addTaskForTest(outboundTask);

    const internals = sim as unknown as {
      taskAssignmentBlockReason(task: unknown): string | null;
      topLiftOutboundColumnCanAcceptTask(column: number): boolean;
    };

    expect(internals.taskAssignmentBlockReason(outboundTask)).toBe('column-mode-conflict:c18');
    expect(internals.topLiftOutboundColumnCanAcceptTask(18)).toBe(false);
  });

  it('does not assign a primed top-lift inbound task after near-full seeding occupies its dropoff', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 12 },
      trafficPolicy: {
        deadlockDetectSec: 5
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 80
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const storageNodeIds = scenario.layout.nodes
      .filter((node) => node.type === 'storage')
      .sort((left, right) => left.z - right.z || left.x - right.x || left.id.localeCompare(right.id))
      .map((node) => node.id);
    for (const [index, nodeId] of storageNodeIds.slice(0, storageNodeIds.length - 4).entries()) {
      sim.addLoadForTest({
        id: `near-full-seed-${String(index + 1).padStart(4, '0')}`,
        state: 'stored',
        nodeId,
        vehicleId: null,
        weightKg: 100
      });
    }

    sim.start();
    const state = runFor(sim, 5);
    const primedTask = state.tasks.find((task) => task.id === 'task-0001');

    expect(primedTask).toMatchObject({ state: 'queued', waitReason: 'storage-full', vehicleId: null });
    expect(state.loads.some((load) =>
      load.state === 'stored' &&
      load.nodeId === primedTask?.dropoffNodeId &&
      load.id !== primedTask.loadId
    )).toBe(true);
    expectNoTrafficSafetyFailures(state);
  });

  it('holds deeper top-lift outbound picks until the front load has left storage', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    const internals = sim as unknown as {
      selectTopLiftOutboundLoad: () => { nodeId: string; loadId: string } | null;
    };
    sim.addLoadForTest({ id: 'stored-c01-r14', state: 'stored', nodeId: 'storage-r14-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-c01-r13', state: 'stored', nodeId: 'storage-r13-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-c02-r14', state: 'stored', nodeId: 'storage-r14-c02', vehicleId: null, weightKg: 100 });

    expect(internals.selectTopLiftOutboundLoad()).toEqual({ nodeId: 'storage-r14-c01', loadId: 'stored-c01-r14' });
    sim.addTaskForTest({
      id: 'outbound-c01-r14',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'lift-01-outbound',
      loadId: 'stored-c01-r14',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    expect(internals.selectTopLiftOutboundLoad()).toBeNull();
    const frontLoad = (sim as unknown as { loads: Array<{ id: string; state: 'waiting' | 'carried' | 'stored' | 'delivered'; nodeId: string | null; vehicleId: string | null }> }).loads
      .find((load) => load.id === 'stored-c01-r14');
    if (frontLoad) {
      frontLoad.state = 'carried';
      frontLoad.nodeId = null;
      frontLoad.vehicleId = 'SH-01';
    }
    const frontTask = (sim as unknown as { tasks: Array<{ id: string; state: string; completedAtSec: number | null }> }).tasks
      .find((task) => task.id === 'outbound-c01-r14');
    if (frontTask) {
      frontTask.state = 'completed';
      frontTask.completedAtSec = 1;
    }
    expect(internals.selectTopLiftOutboundLoad()).toEqual({ nodeId: 'storage-r13-c01', loadId: 'stored-c01-r13' });
    sim.addTaskForTest({
      id: 'outbound-c01-r13',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 1,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r13-c01',
      dropoffNodeId: 'lift-01-outbound',
      loadId: 'stored-c01-r13',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    expect(internals.selectTopLiftOutboundLoad()).toBeNull();
  });

  it('does not route a loaded top-lift outbound pallet through stored front loads', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    const internals = sim as unknown as {
      topLiftLoadedOutboundStorageExitRoute: (fromNodeId: string) => string[] | null;
    };
    sim.addLoadForTest({ id: 'stored-c02-r01', state: 'stored', nodeId: 'storage-r01-c02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-c02-r02', state: 'stored', nodeId: 'storage-r02-c02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-c02-r03', state: 'stored', nodeId: 'storage-r03-c02', vehicleId: null, weightKg: 100 });

    const route = internals.topLiftLoadedOutboundStorageExitRoute('storage-r03-c02');

    expect(route?.at(-1)).toBe('column-middle-c02');
    expect(route ?? []).not.toContain('storage-r01-c02');
    expect(route ?? []).not.toContain('storage-r02-c02');
  });

  it('routes empty top-lift outbound pickups around stored front loads', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'stored-c02-r08', state: 'stored', nodeId: 'storage-r08-c02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-c02-r09', state: 'stored', nodeId: 'storage-r09-c02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-c02-r10', state: 'stored', nodeId: 'storage-r10-c02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-c02-r10',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r10-c02',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'stored-c02-r10',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c03']);
    sim.setVehicleTaskForTest('SH-01', 'outbound-c02-r10', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-c02-r10')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route[0]).toBe('column-middle-c03');
    expect(route.at(-1)).toBe('storage-r10-c02');
    expect(route).not.toContain('storage-r08-c02');
    expect(route).not.toContain('storage-r09-c02');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('seeds dedicated top-lift outbound columns and releases them to inbound after empty', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      loads: Array<{ id: string; state: 'waiting' | 'carried' | 'stored' | 'delivered'; nodeId: string | null; vehicleId: string | null; weightKg: number }>;
      selectTopLiftInboundStorageNode: () => { nodeId: string; loadId: string } | null;
      selectTopLiftOutboundLoad: () => { nodeId: string; loadId: string } | null;
      topLiftOutboundLockedColumns: () => Set<number>;
    };

    expect(internals.topLiftOutboundLockedColumns()).toEqual(new Set([1]));
    expect(internals.loads.filter((load) => load.id.startsWith('outbound-seed-') && load.state === 'stored')).toHaveLength(14);
    expect(internals.selectTopLiftOutboundLoad()).toEqual({
      nodeId: 'storage-r14-c01',
      loadId: 'outbound-seed-c01-r14-0001'
    });
    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r14-c02');

    for (const load of internals.loads) {
      if (load.id.startsWith('outbound-seed-')) {
        load.state = 'delivered';
        load.nodeId = 'lift-01-outbound';
      }
    }

    expect(internals.topLiftOutboundLockedColumns()).toEqual(new Set());
    expect(internals.selectTopLiftOutboundLoad()).toBeNull();
    expect(internals.selectTopLiftInboundStorageNode()?.nodeId).toBe('storage-r14-c01');
  });

  it('seeds zone-balanced 50 percent outbound columns across each top-lift region', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialStorageFillPolicy: 'zone-balanced-50',
        initialOutboundFullColumns: 4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      loads: Array<{ id: string; state: 'waiting' | 'carried' | 'stored' | 'delivered'; nodeId: string | null }>;
      vehicles: Array<{ currentNodeId: string }>;
      topLiftOutboundLockedColumns: () => Set<number>;
    };

    const seedLoads = internals.loads.filter((load) => load.id.startsWith('outbound-seed-') && load.state === 'stored');
    const seededColumns = [...new Set(seedLoads.map((load) => Number(load.id.match(/-c(\d+)/)?.[1] ?? 0)))]
      .sort((left, right) => left - right);
    const vehicleSeedColumnStorageCells = internals.vehicles.filter((vehicle) => {
      const match = /^storage-r\d+-c(\d+)$/.exec(vehicle.currentNodeId);
      return match ? seededColumns.includes(Number(match[1])) : false;
    }).length;

    expect(scenario.taskGeneration.initialStorageFillPolicy).toBe('zone-balanced-50');
    expect(seededColumns).toEqual([1, 2, 3, 4, 5, 6, 7, 15, 16, 17, 18, 19, 20, 21]);
    expect(seedLoads).toHaveLength(seededColumns.length * 14 - vehicleSeedColumnStorageCells);
    expect(internals.topLiftOutboundLockedColumns()).toEqual(new Set(seededColumns));
  });

  it('promotes full inbound-filled top-lift SKU columns to outbound work', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const columnThreeNodeIds = scenario.layout.nodes
      .filter((node) => node.type === 'storage' && /^storage-r\d+-c03$/.test(node.id))
      .sort((left, right) => right.z - left.z || left.id.localeCompare(right.id))
      .map((node) => node.id);
    addStoredLoads(sim, columnThreeNodeIds);
    const internals = sim as unknown as {
      refreshTopLiftColumnFlowModes: () => void;
      topLiftOutboundLockedColumns: () => Set<number>;
      selectTopLiftOutboundLoad: () => { nodeId: string; loadId: string } | null;
    };

    internals.refreshTopLiftColumnFlowModes();

    expect(internals.topLiftOutboundLockedColumns()).toEqual(new Set([3]));
    expect(internals.selectTopLiftOutboundLoad()).toEqual({
      nodeId: 'storage-r14-c03',
      loadId: 'stored-0001'
    });
  });

  it('promotes partially blocked top-lift SKU columns to outbound when no inbound slot is reachable', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    addStoredLoads(sim, [
      'storage-r01-c03',
      'storage-r07-c03',
      'storage-r08-c03',
      'storage-r14-c03'
    ]);
    const internals = sim as unknown as {
      refreshTopLiftColumnFlowModes: () => void;
      topLiftOutboundLockedColumns: () => Set<number>;
      selectTopLiftOutboundLoad: () => { nodeId: string; loadId: string } | null;
    };

    internals.refreshTopLiftColumnFlowModes();

    expect(internals.topLiftOutboundLockedColumns()).toEqual(new Set([3]));
    expect(internals.selectTopLiftOutboundLoad()).toEqual({
      nodeId: 'storage-r14-c03',
      loadId: 'stored-0004'
    });
  });

  it('allows outbound to use inactive inbound-filled columns without stealing the active inbound column', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    addStoredLoads(sim, [
      'storage-r14-c03',
      'storage-r14-c04'
    ]);
    sim.addLoadForTest({ id: 'active-inbound-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'active-inbound-c03',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r13-c03',
      loadId: 'active-inbound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    const internals = sim as unknown as {
      topLiftOutboundLockedColumns: () => Set<number>;
      selectTopLiftOutboundLoad: () => { nodeId: string; loadId: string } | null;
      tasks: Array<{ id: string; state: 'queued' | 'assigned' | 'in-progress' | 'completed' | 'failed'; completedAtSec: number | null }>;
    };

    expect(internals.topLiftOutboundLockedColumns()).toEqual(new Set([4]));
    expect(internals.selectTopLiftOutboundLoad()).toEqual({
      nodeId: 'storage-r14-c04',
      loadId: 'stored-0002'
    });

    const activeTask = internals.tasks.find((task) => task.id === 'active-inbound-c03')!;
    activeTask.state = 'completed';
    activeTask.completedAtSec = 1;

    expect(internals.topLiftOutboundLockedColumns()).toEqual(new Set([3, 4]));
    expect(internals.selectTopLiftOutboundLoad()).toEqual({
      nodeId: 'storage-r14-c03',
      loadId: 'stored-0001'
    });
  });

  it('balances top-lift outbound task creation across each lift region', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      selectTopLiftOutboundTaskSelection: () => { nodeId: string; liftNodeId: string; loadId: string } | { reason: string };
    };

    const first = internals.selectTopLiftOutboundTaskSelection();
    expect(first).toMatchObject({
      liftNodeId: 'lift-01-outbound',
      nodeId: 'storage-r14-c01'
    });
    if ('reason' in first) {
      throw new Error(first.reason);
    }
    sim.addTaskForTest({
      id: 'outbound-first',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: first.nodeId,
      dropoffNodeId: first.liftNodeId,
      loadId: first.loadId,
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    expect(internals.selectTopLiftOutboundTaskSelection()).toMatchObject({
      liftNodeId: 'lift-02-outbound',
      nodeId: 'storage-r14-c15'
    });
  }, 10000);

  it('creates top-lift outbound work against the nearest yellow-grid dock', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();

    let state = sim.step(0.2);
    for (let index = 0; index < 20 && state.tasks.length === 0; index += 1) {
      state = sim.step(0.2);
    }

    const outboundTask = state.tasks.find((task) => task.kind === 'outbound');
    expect(outboundTask?.dropoffNodeId).toBe('column-bottom-b-c08');
    const outboundDropoffNode = scenario.layout.nodes.find((node) => node.id === outboundTask?.dropoffNodeId);
    const outboundLiftNode = scenario.layout.nodes.find((node) => node.id === 'lift-01-outbound');
    expect(outboundDropoffNode?.x).toBeGreaterThan(outboundLiftNode?.x ?? Number.NaN);
    expect(outboundDropoffNode?.z).toBeLessThan(outboundLiftNode?.z ?? Number.NaN);
    expect(outboundTask?.dropoffNodeId).toMatch(/^column-bottom-b-c\d+$/);
  });

  it('lets a top double-lane side-yield continue from the alternate lane instead of routing back', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      agentRefreshInstallSideYield: (
        vehicle: { id: string },
        blockedTargetNodeId: string,
        requester: { id: string },
        session: null
      ) => boolean;
    };

    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c08']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c09']);
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshInstallSideYield(
      yielder,
      'column-top-a-c09',
      requester,
      null
    )).toBe(true);
    expect(yielder.routeNodeIds).toEqual(['column-top-a-c08', 'column-top-b-c08']);
    expect(yielder.localRouteNodeIds).toEqual(['column-top-a-c08', 'column-top-b-c08']);
    expect(yielder.localRouteReason).toBe('temporary-yield');
  });

  it('parallelizes mixed top-lift outbound picks across seeded SKU columns with reachable front loads', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      selectTopLiftOutboundLoad: () => { nodeId: string; loadId: string } | null;
    };

    expect(internals.selectTopLiftOutboundLoad()).toEqual({
      nodeId: 'storage-r14-c01',
      loadId: 'outbound-seed-c01-r14-0001'
    });
    sim.addTaskForTest({
      id: 'outbound-c01-r14',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'lift-01-outbound',
      loadId: 'outbound-seed-c01-r14-0001',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    expect(internals.selectTopLiftOutboundLoad()).toEqual({
      nodeId: 'storage-r14-c02',
      loadId: 'outbound-seed-c02-r14-0015'
    });
  });

  it('routes loaded top-lift outbound exits through the open column end', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    }));
    const internals = sim as unknown as {
      agentRefreshLoadedOutboundRouteToDropoff: (
        currentNodeId: string,
        task: { kind: 'outbound'; dropoffNodeId: string }
      ) => string[] | null;
    };

    const route = internals.agentRefreshLoadedOutboundRouteToDropoff('storage-r09-c15', {
      kind: 'outbound',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit'
    });

    expect(route?.slice(0, 7)).toEqual([
      'storage-r09-c15',
      'storage-r10-c15',
      'storage-r11-c15',
      'storage-r12-c15',
      'storage-r13-c15',
      'storage-r14-c15',
      'column-bottom-a-c15'
    ]);
    expect(route).not.toContain('lift-02-outbound-queue-access');
    expect(route?.at(-1)).toBe('lift-02-outbound-queue-01-service-exit');
    expect(route).not.toContain('lift-02-outbound-buffer-access');
    expect(route).not.toContain('lift-02-outbound-buffer-03');
    expect(route).not.toContain('storage-r08-c15');
  });

  it('routes loaded top-lift inbound upper dropoffs directly off the lift top lane', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'upper-drop-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'upper-drop',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r06-c03',
      loadId: 'upper-drop-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      agentRefreshLoadedInboundRouteToDropoff(currentNodeId: string, task: unknown): string[];
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'upper-drop')!;
    const route = internals.agentRefreshLoadedInboundRouteToDropoff('lift-01-inbound-buffer-03', task);

    expect(route).toEqual([
      'lift-01-inbound-buffer-03',
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07',
      'column-top-b-c06',
      'column-top-b-c05',
      'column-top-b-c04',
      'column-top-b-c03',
      'storage-r01-c03',
      'storage-r02-c03',
      'storage-r03-c03',
      'storage-r04-c03',
      'storage-r05-c03',
      'storage-r06-c03'
    ]);
    expect(route).not.toContain('module-01-spine-middle');
    expect(route).not.toContain('lift-01-inbound-queue-02-service-exit');
  });

  it('retargets a loaded inbound task instead of crashing when lift-complete route planning is temporarily unavailable', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      physicsParams: {
        liftTimeSec: 0.01
      },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    sim.addLoadForTest({ id: 'route-recovery-load', state: 'waiting', nodeId: 'column-top-b-c02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'route-recovery-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c02',
      dropoffNodeId: 'storage-r03-c02',
      loadId: 'route-recovery-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c02']);
    sim.setVehicleTaskForTest('SH-01', 'route-recovery-task', false);

    type TestVehicle = {
      id: string;
      currentNodeId: string;
      routeNodeIds: string[];
      routeIndex: number;
      targetNodeId: string | null;
      state: string;
      loaded: boolean;
      phaseRemainingSec: number;
      waitReason: string | null;
      blockingReservationId: string | null;
      blockingVehicleId: string | null;
      waitingSinceSec: number | null;
      plannedGoalNodeId: string | null;
      plannedRouteNodeIds: string[];
      localRouteNodeIds: string[];
      localRouteReason: string | null;
    };
    const internals = sim as unknown as {
      vehicles: TestVehicle[];
      installAgentRefreshPlannedRoute: () => string[];
      tryRetargetLoadedInboundToReachableDropoff: (vehicle: TestVehicle, task: unknown, failedGoalNodeId: string) => boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    vehicle.state = 'lifting';
    vehicle.phaseRemainingSec = 0.01;

    let retargeted = false;
    internals.installAgentRefreshPlannedRoute = () => {
      throw new Error('No route between column-top-b-c02 and storage-r03-c02');
    };
    internals.tryRetargetLoadedInboundToReachableDropoff = (retargetVehicle, _task, failedGoalNodeId) => {
      retargeted = true;
      expect(failedGoalNodeId).toBe('storage-r03-c02');
      retargetVehicle.routeNodeIds = ['column-top-b-c02', 'column-top-b-c03'];
      retargetVehicle.routeIndex = 0;
      retargetVehicle.targetNodeId = 'column-top-b-c03';
      retargetVehicle.state = 'loaded-moving';
      retargetVehicle.waitReason = null;
      retargetVehicle.blockingReservationId = null;
      retargetVehicle.blockingVehicleId = null;
      retargetVehicle.waitingSinceSec = null;
      retargetVehicle.plannedGoalNodeId = 'column-top-b-c03';
      retargetVehicle.plannedRouteNodeIds = retargetVehicle.routeNodeIds;
      retargetVehicle.localRouteNodeIds = [];
      retargetVehicle.localRouteReason = null;
      return true;
    };

    sim.start();
    expect(() => sim.step(0.02)).not.toThrow();
    const state = sim.getState();
    const recoveredVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const recoveredLoad = state.loads.find((load) => load.id === 'route-recovery-load')!;

    expect(retargeted).toBe(true);
    expect(recoveredVehicle.loaded).toBe(true);
    expect(recoveredVehicle.state).toBe('loaded-moving');
    expect(recoveredVehicle.targetNodeId).toBe('column-top-b-c03');
    expect(recoveredLoad.state).toBe('carried');
    expect(recoveredLoad.vehicleId).toBe('SH-01');
  });

  it('does not retarget a loaded inbound dropoff into an outbound-locked column', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 4,
        initialStorageFillPolicy: 'zone-balanced-50'
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    addStoredLoadsForAllStorageExcept(
      sim,
      scenario,
      new Set(['storage-r07-c18', 'storage-r07-c19']),
      'retarget-fill'
    );
    sim.addTaskForTest({
      id: 'pending-outbound-c18',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 1,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c18',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'retarget-fill-storage-r05-c18',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'retarget-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'retarget-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 2,
      assignedAtSec: 2,
      startedAtSec: 2,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r02-c25',
      loadId: 'retarget-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c22']);
    sim.setVehicleTaskForTest('SH-01', 'retarget-inbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; routeNodeIds: string[] }>;
      tasks: Array<{ id: string; dropoffNodeId: string }>;
      tryRetargetLoadedInboundToReachableDropoff(vehicle: unknown, task: unknown, failedGoalNodeId: string): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'retarget-inbound')!;

    expect(internals.tryRetargetLoadedInboundToReachableDropoff(vehicle, task, 'storage-r02-c25')).toBe(true);
    expect(task.dropoffNodeId).toBe('storage-r07-c19');
    expect(task.dropoffNodeId).not.toBe('storage-r07-c18');
    expect(vehicle.plannedGoalNodeId).toBe('storage-r07-c19');
    expect(vehicle.routeNodeIds).toContain('column-middle-c19');
  });

  it('retargets a queued inbound task when its reserved storage gap is no longer reachable', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    for (const nodeId of ['storage-r01-c13', 'storage-r04-c13', 'storage-r05-c13', 'storage-r07-c13']) {
      sim.addLoadForTest({ id: `gap-block-${nodeId}`, state: 'stored', nodeId, vehicleId: null, weightKg: 100 });
    }
    sim.addLoadForTest({ id: 'queued-retarget-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queued-retarget-inbound',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c13',
      loadId: 'queued-retarget-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: 'storage-full'
    });

    const internals = sim as unknown as {
      tasks: Array<{ id: string; dropoffNodeId: string; waitReason: string | null }>;
      tryRetargetQueuedInboundStorageFullTask(task: unknown): boolean;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'queued-retarget-inbound')!;

    expect(internals.tryRetargetQueuedInboundStorageFullTask(task)).toBe(true);
    expect(task.dropoffNodeId).toBe('storage-r14-c01');
    expect(task.waitReason).toBeNull();
  });

  it('retargets a loaded inbound task when a taskless shuttle occupies its dropoff cell', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-retarget-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-retarget-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r07-c12',
      loadId: 'loaded-retarget-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c14', 'column-middle-c13', 'column-middle-c12', 'storage-r07-c12']);
    sim.setVehicleTaskForTest('SH-01', 'loaded-retarget-task', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r07-c12']);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; routeNodeIds: string[] }>;
      tasks: Array<{ id: string; dropoffNodeId: string }>;
      tryRetargetLoadedInboundAroundTasklessDropoffBlocker(vehicle: unknown, task: unknown, block: { reason: string; blockingVehicleId: string | null }): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'loaded-retarget-task')!;

    expect(internals.tryRetargetLoadedInboundAroundTasklessDropoffBlocker(
      vehicle,
      task,
      { reason: 'node-local-yield', blockingVehicleId: 'SH-02' }
    )).toBe(true);
    expect(task.dropoffNodeId).not.toBe('storage-r07-c12');
    expect(vehicle.plannedGoalNodeId).toBe(task.dropoffNodeId);
    expect(vehicle.routeNodeIds).not.toContain('storage-r07-c12');
    expectRouteHopsAdjacent(scenario, vehicle.routeNodeIds);
  });

  it('lets a mixed-flow top-lift storage shuttle accept executable queued work', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'queued-source-load', state: 'waiting', nodeId: 'column-top-a-c22', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-under-shuttle', state: 'stored', nodeId: 'storage-r07-c28', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queued-inbound-work',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r12-c14',
      loadId: 'queued-source-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r07-c28']);
    sim.setVehicleTaskForTest('SH-01', null, false);

    sim.step(0.2);
    const state = sim.getState();
    const task = state.tasks.find((candidate) => candidate.id === 'queued-inbound-work');
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(task?.state).toBe('queued');
    expect(task?.vehicleId).toBeNull();
    expect(vehicle?.taskId).not.toBeNull();
    expect(vehicle?.localRouteReason).toBeNull();
    expect(vehicle?.routeNodeIds.length).toBeGreaterThan(1);
    expect(vehicle?.routeNodeIds[0]).toBe('storage-r07-c28');
    expect(vehicle?.routeNodeIds).toContain('column-middle-c28');
    expectRouteHopsAdjacent(scenario, vehicle?.routeNodeIds ?? []);
  });

  it('keeps a trailing loaded outbound shuttle in its lift queue slot until the front dropoff clears', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      }
    }));
    sim.addTaskForTest({
      id: 'front-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'front-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'queued-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r13-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'queued-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['parking-lift-01-outbound-queue-02']);
    sim.setVehicleTaskForTest('SH-02', 'queued-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; plannedRouteNodeIds: string[] }>;
      tasks: Array<{ id: string }>;
      taskDispatchGoalNodeId: (task: { id: string }, vehicle: { id: string }) => string;
      agentRefreshInstallSideYield: (vehicle: { id: string }, blockedTargetNodeId: string, requester: { id: string }, session: null) => boolean;
    };
    const queuedVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const queuedTask = internals.tasks.find((task) => task.id === 'queued-outbound')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    queuedVehicle.plannedGoalNodeId = 'parking-lift-01-outbound-queue-02';
    queuedVehicle.plannedRouteNodeIds = ['parking-lift-01-outbound-queue-02'];

    expect(internals.taskDispatchGoalNodeId(queuedTask, queuedVehicle)).toBe('parking-lift-01-outbound-queue-02');
    expect(internals.agentRefreshInstallSideYield(queuedVehicle, 'lift-01-outbound-buffer-03', requester, null)).toBe(false);
  });

  it('clears an outbound dropoff directly onto bottom-b and holds away from the lift', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      physicsParams: {
        lowerTimeSec: 0.1
      }
    }));
    sim.addLoadForTest({ id: 'outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-clear',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['lift-01-outbound-queue-01-service-exit']);
    sim.setVehicleTaskForTest('SH-01', 'outbound-clear', true);
    sim.start();

    sim.step(0.2);
    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const task = state.tasks.find((candidate) => candidate.id === 'outbound-clear');
    const internals = sim as unknown as {
      vehicles: Array<{ id: string; assignmentHoldUntilSec: number | null; routeNodeIds: string[] }>;
      simTimeSec: number;
      canAcceptQueuedTask(vehicle: unknown): boolean;
      routeTravelEstimateSec(vehicle: unknown, route: string[]): number;
    };
    const mutableVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const clearanceEvent = sim.getEventLog().find((event) =>
      event.vehicleId === 'SH-01' &&
      event.eventType === 'vehicle-standby-dispatched' &&
      event.reason === 'outbound-lift-dropoff-clearance'
    );

    expect(task?.state).toBe('completed');
    expect(vehicle?.taskId).toBeNull();
    expect(vehicle?.routeNodeIds).toEqual([
      'lift-01-outbound-queue-01-service-exit',
      'lift-01-outbound-queue-01-entry-access',
      'column-bottom-b-c06',
      'column-bottom-b-c05',
      'column-bottom-b-c04',
      'column-bottom-b-c03'
    ]);
    expect(vehicle?.plannedGoalNodeId).toBe('column-bottom-b-c03');
    expect(vehicle?.localRouteReason).toBe('outbound-lift-clearance');
    expect(vehicle?.routeNodeIds).not.toContain('lift-01-outbound-queue-pickup-access');
    expect(vehicle?.routeNodeIds).not.toContain('lift-01-outbound-buffer-access');
    expect(vehicle?.routeNodeIds).not.toContain('lift-01-outbound-queue-02-service-exit');
    expect(vehicle?.routeNodeIds).not.toContain('lift-01-outbound-queue-03-service-exit');
    expect(clearanceEvent?.details.route).toBe('lift-01-outbound-queue-01-service-exit>lift-01-outbound-queue-01-entry-access>column-bottom-b-c06>column-bottom-b-c05>column-bottom-b-c04>column-bottom-b-c03');
    expect(internals.canAcceptQueuedTask(mutableVehicle)).toBe(false);
    expect(
      mutableVehicle.assignmentHoldUntilSec! -
        internals.simTimeSec -
        internals.routeTravelEstimateSec(mutableVehicle, mutableVehicle.routeNodeIds)
    ).toBeCloseTo(2, 2);

    let heldState = state;
    for (let index = 0; index < 200; index += 1) {
      heldState = sim.step(0.2);
    }
    const heldVehicle = heldState.vehicles.find((candidate) => candidate.id === 'SH-01');
    const heldMutableVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    expect(heldVehicle?.currentNodeId).toBe('column-bottom-b-c03');
    expect(heldVehicle?.targetNodeId).toBeNull();
    expect(heldVehicle?.state).toBe('idle');
    expect(heldVehicle?.routeNodeIds).toEqual(['column-bottom-b-c03']);
    expect(internals.canAcceptQueuedTask(heldMutableVehicle)).toBe(true);
  });

  it('extends outbound clearance to an inbound queue standby when inbound demand exists', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      }
    }));
    const task = {
      id: 'outbound-clear-to-inbound-queue',
      kind: 'outbound' as const,
      state: 'in-progress' as const,
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-clear-to-inbound-queue-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    };
    sim.addLoadForTest({
      id: 'outbound-clear-to-inbound-queue-source',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-01',
      vehicleId: null,
      weightKg: 100
    });
    sim.setVehicleRouteForTest('SH-01', ['lift-01-outbound-queue-01-service-exit']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
        assignmentHoldUntilSec: number | null;
        recentOutboundClearanceRouteNodeIds: string[];
      }>;
      outboundLiftDropoffClearanceRoute(vehicle: unknown, task: unknown): string[] | null;
      convertCompletedOutboundClearanceToInboundQueueStandby(vehicle: unknown, nodeId: string): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const route = internals.outboundLiftDropoffClearanceRoute(vehicle, task);
    const queueGoalNodeId = route?.at(-1) ?? null;

    expect(route?.slice(0, 3)).toEqual([
      'lift-01-outbound-queue-01-service-exit',
      'lift-01-outbound-queue-01-entry-access',
      'column-bottom-b-c06'
    ]);
    expect(queueGoalNodeId).toMatch(/^column-top-a-c(?:09|10|11)$/);
    expect(route).not.toContain('lift-01-outbound-buffer-access');

    vehicle.currentNodeId = queueGoalNodeId!;
    vehicle.targetNodeId = null;
    vehicle.routeNodeIds = [queueGoalNodeId!];
    vehicle.plannedGoalNodeId = queueGoalNodeId;
    vehicle.plannedRouteNodeIds = route ?? [];
    vehicle.localRouteNodeIds = route ?? [];
    vehicle.localRouteReason = 'outbound-lift-clearance';
    vehicle.assignmentHoldUntilSec = 999;
    vehicle.recentOutboundClearanceRouteNodeIds = route ?? [];

    expect(internals.convertCompletedOutboundClearanceToInboundQueueStandby(vehicle, queueGoalNodeId!)).toBe(true);
    expect(vehicle.localRouteReason).toBe('inbound-queue-standby');
    expect(vehicle.plannedGoalNodeId).toBe(queueGoalNodeId);
    expect(vehicle.assignmentHoldUntilSec).toBeNull();
    expect(vehicle.recentOutboundClearanceRouteNodeIds).toEqual([]);
  });

  it('does not send an outbound dock clearance shuttle back through the dock face to reach standby', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    }));
    const task = {
      id: 'outbound-dock-clear',
      kind: 'outbound' as const,
      state: 'in-progress' as const,
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c19',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'outbound-dock-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    };
    sim.setVehicleRouteForTest('SH-04', ['column-bottom-b-c22']);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      outboundLiftDropoffClearanceRoute(vehicle: unknown, task: unknown): string[] | null;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;
    const route = internals.outboundLiftDropoffClearanceRoute(vehicle, task);

    expect(route).toEqual([
      'column-bottom-b-c22',
      'column-bottom-b-c23',
      'column-bottom-b-c24',
      'column-bottom-b-c25'
    ]);
    expect(route?.slice(1)).not.toContain('module-02-spine-bottom-b');
    expect(route?.slice(1)).not.toContain('column-bottom-b-c22');
  });

  it('does not treat a stationary bottom-boundary blocker as already clearing a loaded outbound dock route', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    }));
    sim.addLoadForTest({ id: 'loaded-boundary-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-boundary-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r04-c17',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'loaded-boundary-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'module-boundary-01-spine-bottom-a',
      'module-boundary-01-spine-bottom-b',
      'column-bottom-b-c15',
      'column-bottom-b-c16',
      'column-bottom-b-c17',
      'column-bottom-b-c18',
      'column-bottom-b-c19',
      'column-bottom-b-c20',
      'column-bottom-b-c21',
      'module-02-spine-bottom-b',
      'column-bottom-b-c22'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'loaded-boundary-outbound', true);
    sim.setVehicleRouteForTest('SH-04', [
      'module-boundary-01-spine-bottom-b',
      'column-bottom-b-c15',
      'column-bottom-a-c15',
      'column-bottom-a-c16',
      'storage-r14-c16'
    ]);
    sim.setVehicleTaskForTest('SH-04', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      topLiftBlockerHasCommittedContinuationAway(requester: unknown, blockedTargetNodeId: string, blocker: unknown): boolean;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const requester = internals.vehicles.find((candidate) => candidate.id === 'SH-06')!;
    const blocker = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;

    expect(internals.topLiftBlockerHasCommittedContinuationAway(requester, 'module-boundary-01-spine-bottom-b', blocker)).toBe(false);
    expect(internals.agentRefreshHandleMoveBlock(requester, 'module-boundary-01-spine-bottom-b', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-04'
    })).toBe(false);
  });

  it('lets an empty inbound shuttle leave the outbound dock junction before another empty shuttle enters it', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    }));
    sim.addLoadForTest({ id: 'junction-clear-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'junction-clear-inbound',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r05-c17',
      loadId: 'junction-clear-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'junction-enter-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'junction-enter-inbound',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r07-c13',
      loadId: 'junction-enter-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'module-02-spine-bottom-b',
      'module-02-spine-bottom-a',
      'module-02-spine-middle',
      'module-02-spine-top-b',
      'module-02-spine-top-a',
      'column-top-a-c22'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'junction-clear-inbound', false);
    sim.setVehicleRouteForTest('SH-08', [
      'column-bottom-b-c22',
      'module-02-spine-bottom-b',
      'column-bottom-b-c21',
      'column-bottom-b-c20'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'junction-enter-inbound', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[] }>;
      agentRefreshMoveBlocker(
        vehicle: unknown,
        toNodeId: string,
        candidateRouteNodeIds?: string[] | null
      ): { reason: string; blockingVehicleId: string | null } | null;
    };
    const leavingJunction = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;
    const enteringJunction = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;

    expect(internals.agentRefreshMoveBlocker(
      leavingJunction,
      'module-02-spine-bottom-a',
      leavingJunction.routeNodeIds
    )).toBeNull();
    expect(internals.agentRefreshMoveBlocker(
      enteringJunction,
      'module-02-spine-bottom-b',
      enteringJunction.routeNodeIds
    )).toEqual({ reason: 'node-occupied', blockingVehicleId: 'SH-06' });
  });

  it('keeps empty inbound pickup routes out of the outbound lift face', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    }));
    sim.addLoadForTest({ id: 'pickup-load', state: 'waiting', nodeId: 'column-top-a-c08', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r11-c01',
      loadId: 'pickup-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', ['storage-r14-c09']);
    sim.setVehicleTaskForTest('SH-03', 'inbound-pickup', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      agentRefreshNominalRouteToGoal: (vehicle: unknown, task: unknown, goalNodeId: string) => string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-03')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'inbound-pickup')!;
    const route = internals.agentRefreshNominalRouteToGoal(vehicle, task, 'column-top-a-c08');

    expect(route).not.toContain('column-bottom-a-c08');
    expect(route).not.toContain('module-01-spine-bottom-a');
    expect(route).not.toContain('column-bottom-b-c06');
  });

  it('does not side-yield an empty inbound pickup shuttle into storage beside the outbound lift face', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    }));
    sim.addLoadForTest({ id: 'pickup-load', state: 'waiting', nodeId: 'column-top-b-c09', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r11-c01',
      loadId: 'pickup-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', ['column-bottom-a-c09', 'column-bottom-a-c08']);
    sim.setVehicleTaskForTest('SH-03', 'inbound-pickup', false);
    sim.setVehicleRouteForTest('SH-02', ['storage-r14-c08', 'column-bottom-a-c08']);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null }>;
      agentRefreshInstallSideYield: (vehicle: { id: string }, blockedTargetNodeId: string, requester: { id: string }, session: null) => boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshInstallSideYield(yielder, 'column-bottom-a-c08', requester, null)).toBe(false);
    expect(yielder.routeNodeIds).toEqual(['column-bottom-a-c09', 'column-bottom-a-c08']);
    expect(yielder.localRouteReason).toBeNull();
  });

  it('lets an outbound clearance shuttle exit past a loaded shuttle waiting to enter the lift queue', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    }));
    sim.addLoadForTest({ id: 'waiting-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'waiting-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r13-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'waiting-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-a-c08',
      'lift-01-outbound-queue-01-entry-access',
      'lift-01-outbound-queue-01-access',
      'parking-lift-01-outbound-queue'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'waiting-outbound', true);
    sim.setVehicleRouteForTest('SH-03', [
      'lift-01-outbound-queue-02-entry-access',
      'column-top-b-c08',
      'storage-r01-c08'
    ]);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshColumnEdgeBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
    };
    const clearer = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;

    expect(internals.agentRefreshColumnEdgeBlocker(clearer, 'column-top-b-c08')).toBeNull();
  });

  it('does not retreat an outbound clearance shuttle back into the lift workcell during a faceoff', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    }));
    sim.addLoadForTest({ id: 'waiting-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'waiting-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r13-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'waiting-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-a-c08',
      'lift-01-outbound-queue-01-entry-access',
      'lift-01-outbound-queue-01-access',
      'parking-lift-01-outbound-queue'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'waiting-outbound', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'lift-01-outbound-queue-01-entry-access',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-03'
    });
    sim.setVehicleRouteForTest('SH-03', [
      'lift-01-outbound-queue-03-entry-access',
      'column-top-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-03', null, false);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'column-top-b-c08',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-01'
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null }>;
      tryRetreatAgentRefreshNearFaceoff(vehicleIds: string[]): boolean;
    };

    expect(internals.tryRetreatAgentRefreshNearFaceoff(['SH-01', 'SH-03'])).toBe(false);
    expect(internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')?.routeNodeIds).toEqual([
      'lift-01-outbound-queue-03-entry-access',
      'column-top-b-c08'
    ]);
    expect(internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')?.localRouteReason).toBeNull();
  });

  it('moves an empty top-a blocker right instead of reversing a loaded outbound shuttle away from its lift dropoff', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      }
    }));
    sim.addLoadForTest({ id: 'service-load', state: 'carried', nodeId: null, vehicleId: 'SH-08', weightKg: 100 });
    sim.addTaskForTest({
      id: 'service-drop',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r06-c15',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'service-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', [
      'lift-02-outbound-queue-01-entry-access',
      'lift-02-outbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'service-drop', true);
    sim.setVehicleRouteForTest('SH-05', ['column-top-a-c24']);
    sim.setVehicleWaitingForTest('SH-05', {
      targetNodeId: 'column-top-a-c24',
      waitReason: 'local-yield-hold',
      blockingVehicleId: 'SH-08'
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentNodeId: string;
        targetNodeId: string | null;
        currentEdgeId: string | null;
        routeNodeIds: string[];
        routeIndex: number;
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        waitReason: string | null;
        blockingVehicleId: string | null;
        x: number;
        z: number;
      }>;
      traffic: { findEdge: (fromNodeId: string, toNodeId: string) => { id: string; lengthM: number } | null };
      tryRetreatAgentRefreshNearFaceoff: (vehicleIds: string[]) => boolean;
    };
    const loaded = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;
    const serviceEdge = internals.traffic.findEdge(
      'lift-02-outbound-queue-01-entry-access',
      'lift-02-outbound-queue-01-service-exit'
    )!;
    loaded.state = 'waiting-blocked';
    loaded.currentEdgeId = serviceEdge.id;
    loaded.targetNodeId = 'lift-02-outbound-queue-01-service-exit';
    loaded.routeIndex = 0;
    loaded.legRemainingM = 1.1;
    loaded.legElapsedSec = 1;
    loaded.legTravelSec = 2;
    loaded.x = 33.7;
    loaded.z = -0.4;
    loaded.waitReason = 'min-separation';
    loaded.blockingVehicleId = 'SH-05';

    expect(internals.tryRetreatAgentRefreshNearFaceoff(['SH-08', 'SH-05'])).toBe(true);
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;
    expect(loaded.currentNodeId).toBe('lift-02-outbound-queue-01-entry-access');
    expect(loaded.routeNodeIds).toEqual([
      'lift-02-outbound-queue-01-entry-access',
      'lift-02-outbound-queue-01-service-exit'
    ]);
    expect(blocker.routeNodeIds).toEqual(['column-top-a-c24', 'column-top-a-c25']);
    expect(blocker.targetNodeId).toBe('column-top-a-c25');
  });

  it('lets a loaded top-lift shuttle clear forward when the loaded blocker is behind it', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      }
    }));
    sim.setVehicleRouteForTest('SH-05', [
      'lift-01-inbound-queue-02-service-exit',
      'lift-01-inbound-queue-02-entry-access'
    ]);
    sim.setVehicleRouteForTest('SH-08', [
      'column-top-b-c07',
      'module-01-spine-top-b',
      'column-top-b-c08'
    ]);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentNodeId: string;
        targetNodeId: string | null;
        currentEdgeId: string | null;
        routeNodeIds: string[];
        routeIndex: number;
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        legMotionMode: string;
        speedMps: number;
        targetSpeedMps: number;
        waitReason: string | null;
        blockingVehicleId: string | null;
        waitingSinceSec: number | null;
        loaded: boolean;
        taskId: string | null;
        x: number;
        z: number;
        plannedGoalNodeId: string | null;
        localRouteReason: string | null;
        localRouteNodeIds: string[];
      }>;
      traffic: { findEdge: (fromNodeId: string, toNodeId: string) => { id: string; lengthM: number } | null };
      advanceMovement: (vehicle: unknown, dtSec: number) => void;
    };
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;
    blocker.state = 'waiting-blocked';
    blocker.currentEdgeId = internals.traffic.findEdge(
      'lift-01-inbound-queue-02-service-exit',
      'lift-01-inbound-queue-02-entry-access'
    )!.id;
    blocker.targetNodeId = 'lift-01-inbound-queue-02-entry-access';
    blocker.legRemainingM = 1.0875;
    blocker.legElapsedSec = 2.933333;
    blocker.legTravelSec = 4.283333333333333;
    blocker.speedMps = 0;
    blocker.targetSpeedMps = 1.5;
    blocker.waitReason = 'min-separation';
    blocker.blockingVehicleId = 'SH-08';
    blocker.waitingSinceSec = 4490.2;
    blocker.loaded = true;
    blocker.x = 8.9125;
    blocker.z = 0.6;

    const clearing = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;
    clearing.state = 'loaded-moving';
    clearing.loaded = true;
    clearing.currentEdgeId = internals.traffic.findEdge('column-top-b-c07', 'module-01-spine-top-b')!.id;
    clearing.targetNodeId = 'module-01-spine-top-b';
    clearing.routeIndex = 0;
    clearing.legRemainingM = 1.125;
    clearing.legElapsedSec = 0.083333;
    clearing.legTravelSec = 0.8333333333333334;
    clearing.legMotionMode = 'cruise';
    clearing.speedMps = 0;
    clearing.targetSpeedMps = 1.5;
    clearing.x = 10.125;
    clearing.z = 0.8;
    clearing.plannedGoalNodeId = 'column-top-b-c03';
    clearing.localRouteReason = 'temporary-yield';
    clearing.localRouteNodeIds = ['column-top-b-c07', 'module-01-spine-top-b', 'column-top-b-c08'];

    internals.advanceMovement(clearing, 0.2);

    expect(clearing.state).toBe('loaded-moving');
    expect(clearing.waitReason).toBeNull();
    expect(clearing.blockingVehicleId).toBeNull();
    expect(clearing.legElapsedSec).toBeGreaterThan(0.083333);
    expect(clearing.x).toBeGreaterThan(10.125);
  });

  it('lets an empty top-lift shuttle clear forward when an empty lift-exit blocker is behind it', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      }
    }));
    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-outbound-queue-03-service-exit',
      'lift-01-outbound-queue-03-entry-access',
      'column-top-b-c08'
    ]);
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentNodeId: string;
        targetNodeId: string | null;
        currentEdgeId: string | null;
        routeIndex: number;
        routeNodeIds: string[];
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        legMotionMode: string;
        speedMps: number;
        targetSpeedMps: number;
        waitReason: string | null;
        blockingVehicleId: string | null;
        waitingSinceSec: number | null;
        loaded: boolean;
        taskId: string | null;
        x: number;
        z: number;
        plannedGoalNodeId: string | null;
      }>;
      traffic: { findEdge: (fromNodeId: string, toNodeId: string) => { id: string; lengthM: number } | null };
      advanceMovement: (vehicle: unknown, dtSec: number) => void;
    };
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    blocker.state = 'waiting-blocked';
    blocker.currentEdgeId = internals.traffic.findEdge(
      'lift-01-outbound-queue-03-service-exit',
      'lift-01-outbound-queue-03-entry-access'
    )!.id;
    blocker.targetNodeId = 'lift-01-outbound-queue-03-entry-access';
    blocker.legRemainingM = 1.08;
    blocker.legElapsedSec = 2.9;
    blocker.legTravelSec = 4.2;
    blocker.speedMps = 0;
    blocker.targetSpeedMps = 2;
    blocker.waitReason = 'min-separation';
    blocker.blockingVehicleId = 'SH-03';
    blocker.waitingSinceSec = 25502.8;
    blocker.loaded = false;
    blocker.taskId = null;
    blocker.x = 13.244949;
    blocker.z = 1.6;

    const clearing = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    clearing.state = 'moving-to-pickup';
    clearing.loaded = false;
    clearing.currentEdgeId = internals.traffic.findEdge('column-top-b-c08', 'module-01-spine-top-b')!.id;
    clearing.targetNodeId = 'module-01-spine-top-b';
    clearing.routeIndex = 0;
    clearing.legRemainingM = 0.75;
    clearing.legElapsedSec = 0.1;
    clearing.legTravelSec = 0.8;
    clearing.legMotionMode = 'cruise';
    clearing.speedMps = 0;
    clearing.targetSpeedMps = 2;
    clearing.waitReason = null;
    clearing.blockingVehicleId = null;
    clearing.x = 11.916666;
    clearing.z = 0.8;
    clearing.plannedGoalNodeId = 'lift-02-inbound-queue-01-service-exit';

    internals.advanceMovement(clearing, 0.2);

    expect(clearing.state).toBe('moving-to-pickup');
    expect(clearing.waitReason).toBeNull();
    expect(clearing.blockingVehicleId).toBeNull();
    expect(clearing.legElapsedSec).toBeGreaterThan(0.1);
    expect(clearing.x).toBeLessThan(11.916666);
  });

  it('completes outbound lowering if a stale local route starts at the lift dropoff', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      physicsParams: {
        lowerTimeSec: 0.1
      }
    }));
    sim.addLoadForTest({ id: 'stale-route-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'stale-route-drop',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r06-c02',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'stale-route-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-outbound-queue-01-service-exit',
      'lift-01-outbound-queue-01-entry-access'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'stale-route-drop', true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentEdgeId: string | null;
        targetNodeId: string | null;
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        x: number;
        z: number;
      }>;
      traffic: { findEdge: (fromNodeId: string, toNodeId: string) => { id: string; lengthM: number } | null };
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const reverseEdge = internals.traffic.findEdge(
      'column-bottom-b-c07',
      'column-bottom-b-c08'
    )!;
    vehicle.state = 'loaded-moving';
    vehicle.currentEdgeId = reverseEdge.id;
    vehicle.targetNodeId = 'lift-01-outbound-queue-01-entry-access';
    vehicle.legRemainingM = 1;
    vehicle.legElapsedSec = 1;
    vehicle.legTravelSec = 2;
    vehicle.x = 8.7;
    vehicle.z = -0.4;

    sim.start();
    sim.step(0.2);
    const state = sim.step(0.2);
    const task = state.tasks.find((candidate) => candidate.id === 'stale-route-drop');
    const finalVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(task?.state).toBe('completed');
    expect(finalVehicle?.loaded).toBe(false);
    expect(finalVehicle?.taskId).toBeNull();
  });

  it('settles an edge arrival even when avoidance left zero remaining distance on the edge', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c20', 'column-top-b-c21']);
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentEdgeId: string | null;
        targetNodeId: string | null;
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        x: number;
        z: number;
      }>;
      traffic: { findEdge: (fromNodeId: string, toNodeId: string) => { id: string; lengthM: number } | null };
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const edge = internals.traffic.findEdge('column-top-b-c20', 'column-top-b-c21')!;
    vehicle.state = 'returning';
    vehicle.currentEdgeId = edge.id;
    vehicle.targetNodeId = 'column-top-b-c21';
    vehicle.legRemainingM = 0;
    vehicle.legElapsedSec = 0.625;
    vehicle.legTravelSec = 0.625;
    vehicle.x = 30;
    vehicle.z = 0.8;

    sim.start();
    const state = sim.step(0.2);
    const settled = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(settled?.currentNodeId).toBe('column-top-b-c21');
    expect(settled?.currentEdgeId).toBeNull();
  });

  it('lets an empty top-b shuttle clear away from an outbound lift exit lane', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
      }>;
      agentRefreshMoveBlocker: (
        vehicle: { id: string },
        toNodeId: string,
        candidateRouteNodeIds: string[]
      ) => { reason: string; blockingVehicleId: string | null } | null;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'lift-02-outbound-queue-03-service-exit',
      'lift-02-outbound-queue-03-entry-access',
      'column-top-b-c22'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-b-c22',
      'module-02-spine-top-b',
      'column-top-b-c21'
    ]);

    const liftClearance = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const exitLane = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(liftClearance, {
      loaded: false,
      taskId: null,
      targetNodeId: 'lift-02-outbound-queue-03-entry-access',
      plannedGoalNodeId: 'column-top-b-c22',
      plannedRouteNodeIds: liftClearance.routeNodeIds,
      x: 33.536372,
      z: 1.6,
      currentEdgeId: 'lift-02-outbound-queue-03-service-exit-lift-02-outbound-queue-03-entry-access-clearance'
    });
    Object.assign(exitLane, {
      loaded: false,
      taskId: null,
      targetNodeId: 'module-02-spine-top-b',
      plannedGoalNodeId: 'column-top-b-c21',
      plannedRouteNodeIds: exitLane.routeNodeIds,
      x: 32.253286,
      z: 0.8,
      currentEdgeId: 'module-02-spine-top-b-column-top-b-c22'
    });

    expect(internals.agentRefreshMoveBlocker(
      exitLane,
      'module-02-spine-top-b',
      exitLane.routeNodeIds
    )).toBeNull();
    expect(internals.agentRefreshMoveBlocker(
      liftClearance,
      'lift-02-outbound-queue-03-entry-access',
      liftClearance.routeNodeIds
    )).toBeNull();
  });

  it('keeps side-yield pockets out of an outbound lift clearance sweep', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
      }>;
      agentRefreshInstallSideYield: (
        vehicle: { id: string },
        blockedTargetNodeId: string,
        requester: { id: string },
        session: null
      ) => boolean;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c09',
      'column-top-b-c10'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-outbound-queue-03-service-exit',
      'lift-01-outbound-queue-03-entry-access',
      'column-top-b-c08'
    ]);

    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const clearance = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(clearance, {
      loaded: false,
      taskId: null,
      targetNodeId: 'lift-01-outbound-queue-03-entry-access',
      plannedGoalNodeId: 'column-top-b-c08',
      plannedRouteNodeIds: clearance.routeNodeIds,
      x: 15.091404,
      z: 1.6,
      currentEdgeId: 'lift-01-outbound-queue-03-service-exit-lift-01-outbound-queue-03-entry-access-clearance'
    });

    expect(internals.agentRefreshInstallSideYield(
      yielder,
      'column-top-b-c10',
      clearance,
      null
    )).toBe(true);
    expect(yielder.routeNodeIds).toEqual([
      'column-top-b-c09',
      'storage-r01-c09'
    ]);
    expect(yielder.routeNodeIds).not.toContain('column-top-a-c09');
    expect(yielder.routeNodeIds).not.toContain('storage-r02-c09');
  });

  it('breaks a loaded bottom-a adjacent node swap by yielding onto bottom-b', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 5 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    sim.addLoadForTest({ id: 'loaded-inbound-swap-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-inbound-swap',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c15',
      loadId: 'loaded-inbound-swap-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'loaded-outbound-swap-load', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-outbound-swap',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c19',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'loaded-outbound-swap-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-bottom-a-c20',
      'column-bottom-a-c19',
      'column-bottom-a-c18',
      'column-bottom-a-c17',
      'column-bottom-a-c16',
      'column-bottom-a-c15',
      'module-boundary-01-spine-bottom-a',
      'module-boundary-01-spine-middle',
      'column-middle-c15',
      'storage-r13-c15'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-inbound-swap', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-bottom-a-c19',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-05'
    });
    sim.setVehicleRouteForTest('SH-05', [
      'column-bottom-a-c19',
      'column-bottom-a-c20',
      'column-bottom-b-c20',
      'lift-02-outbound-queue-01-entry-access',
      'lift-02-outbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-05', 'loaded-outbound-swap', true);
    sim.setVehicleWaitingForTest('SH-05', {
      targetNodeId: 'column-bottom-a-c20',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01'
    });
    sim.setVehicleRouteForTest('SH-04', [
      'storage-r14-c19',
      'column-bottom-a-c19',
      'column-bottom-b-c19'
    ]);
    sim.setVehicleTaskForTest('SH-04', null, true);
    sim.setVehicleWaitingForTest('SH-04', {
      targetNodeId: 'column-bottom-a-c19',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-05'
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
      }>;
      tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair: (
        first: { id: string },
        second: { id: string }
      ) => { id: string } | null;
    };
    const first = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const second = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;

    const yielder = internals.tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(first, second);

    expect(yielder?.id).toMatch(/^SH-(?:01|05)$/);
    const yieldedVehicle = internals.vehicles.find((vehicle) => vehicle.id === yielder?.id);
    expect(yieldedVehicle?.routeNodeIds).toHaveLength(2);
    expect(yieldedVehicle?.routeNodeIds[1]).toMatch(/^column-bottom-b-c(?:19|20)$/);
    expect(yieldedVehicle?.targetNodeId).toBe(yieldedVehicle?.routeNodeIds[1]);
    expect(yieldedVehicle?.waitReason).toBeNull();
    expect(yieldedVehicle?.blockingVehicleId).toBeNull();
  });

  it('keeps forward faceoff yields out of an inbound lift clearance sweep', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
      }>;
      agentRefreshForwardYieldPocketNodeId: (
        vehicle: { id: string },
        currentNodeId: string,
        forwardNodeId: string,
        blocker: { id: string }
      ) => string | null;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c20',
      'column-top-b-c21'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-02-inbound-queue-03-service-exit',
      'lift-02-inbound-queue-03-entry-access',
      'column-top-b-c21',
      'column-top-a-c21',
      'column-top-a-c20'
    ]);

    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const clearance = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(yielder, {
      loaded: false,
      taskId: null,
      targetNodeId: 'column-top-b-c21',
      plannedGoalNodeId: 'column-top-b-c21',
      plannedRouteNodeIds: yielder.routeNodeIds,
      x: 28.8,
      z: 0.8,
      currentEdgeId: 'column-top-b-c20-column-top-b-c21'
    });
    Object.assign(clearance, {
      loaded: true,
      taskId: 'inbound-clearance',
      targetNodeId: 'lift-02-inbound-queue-03-entry-access',
      plannedGoalNodeId: 'storage-r06-c21',
      plannedRouteNodeIds: clearance.routeNodeIds,
      x: 28.573887,
      z: 1.6,
      currentEdgeId: 'lift-02-inbound-queue-03-service-exit-lift-02-inbound-queue-03-entry-access-clearance'
    });

    const pocketNodeId = internals.agentRefreshForwardYieldPocketNodeId(
      yielder,
      'column-top-b-c20',
      'column-top-b-c21',
      clearance
    );

    expect(pocketNodeId).toBe('module-02-spine-top-b');
    expect(pocketNodeId).not.toBe('storage-r01-c21');
  });

  it('pushes first-row storage traffic away from an active outbound service clearance', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
        legRemainingM: number;
      }>;
      tryYieldStorageAwayFromTopLiftServiceClearance: (
        storageVehicle: { id: string },
        serviceVehicle: { id: string }
      ) => boolean;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-outbound-queue-02-service-exit',
      'lift-01-outbound-queue-03-service-exit',
      'lift-01-outbound-queue-03-entry-access',
      'column-top-b-c08'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r01-c10',
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08'
    ]);

    const serviceClearance = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const storageTraffic = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(serviceClearance, {
      loaded: false,
      taskId: null,
      targetNodeId: 'lift-01-outbound-queue-03-service-exit',
      plannedGoalNodeId: 'column-top-b-c08',
      plannedRouteNodeIds: serviceClearance.routeNodeIds,
      x: 15.55,
      z: 0.6,
      currentEdgeId: null,
      legRemainingM: 0
    });
    Object.assign(storageTraffic, {
      loaded: false,
      taskId: 'outbound-storage-exit',
      targetNodeId: 'column-top-b-c10',
      plannedGoalNodeId: 'storage-r08-c01',
      plannedRouteNodeIds: storageTraffic.routeNodeIds,
      x: 15,
      z: 2,
      currentEdgeId: null,
      legRemainingM: 0
    });

    expect(internals.tryYieldStorageAwayFromTopLiftServiceClearance(storageTraffic, serviceClearance)).toBe(false);
  });

  it('pushes top-b aisle traffic aside for an active inbound service clearance', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
        legRemainingM: number;
      }>;
      tryYieldAisleAwayFromTopLiftServiceClearance: (
        aisleVehicle: { id: string },
        serviceVehicle: { id: string },
        blockedTargetNodeId: string
      ) => boolean;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-inbound-queue-03-service-exit',
      'lift-01-inbound-queue-03-entry-access',
      'column-top-b-c07'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-b-c06',
      'column-top-b-c05',
      'column-top-b-c04'
    ]);

    const serviceClearance = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const aisleTraffic = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(serviceClearance, {
      loaded: true,
      taskId: 'inbound-service-clearance',
      targetNodeId: 'lift-01-inbound-queue-03-entry-access',
      plannedGoalNodeId: 'storage-r03-c03',
      plannedRouteNodeIds: serviceClearance.routeNodeIds,
      x: 7.55,
      z: 1.6,
      currentEdgeId: 'lift-01-inbound-queue-03-service-exit-lift-01-inbound-queue-03-entry-access',
      legRemainingM: 0.8
    });
    Object.assign(aisleTraffic, {
      loaded: false,
      taskId: 'outbound-aisle-cross',
      targetNodeId: 'column-top-b-c05',
      plannedGoalNodeId: 'storage-r03-c02',
      plannedRouteNodeIds: aisleTraffic.routeNodeIds,
      x: 8.75,
      z: 0.8,
      currentEdgeId: null,
      legRemainingM: 0
    });

    expect(internals.tryYieldAisleAwayFromTopLiftServiceClearance(
      aisleTraffic,
      serviceClearance,
      'column-top-b-c05'
    )).toBe(false);
  });

  it('keeps loaded inbound lateral travel on top-b even for lower-bank dropoffs', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    sim.addLoadForTest({ id: 'lower-bank-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'lower-bank-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-02-service-exit',
      dropoffNodeId: 'storage-r12-c16',
      loadId: 'lower-bank-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      agentRefreshLoadedInboundRouteToDropoff: (currentNodeId: string, task: { id: string }) => string[];
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'lower-bank-inbound')!;

    const route = internals.agentRefreshLoadedInboundRouteToDropoff('column-top-b-c21', task);

    expect(route.slice(0, 6)).toEqual([
      'column-top-b-c21',
      'column-top-b-c20',
      'column-top-b-c19',
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16'
    ]);
    expect(route.indexOf('column-top-a-c20')).toBe(-1);
  });

  it('pushes loaded top-b traffic onto top-a instead of letting it retreat through an inbound service clearance', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
        legRemainingM: number;
      }>;
      tryYieldAisleAwayFromTopLiftServiceClearance: (
        aisleVehicle: { id: string },
        serviceVehicle: { id: string },
        blockedTargetNodeId: string
      ) => boolean;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c20',
      'column-top-b-c19',
      'column-top-b-c18'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-02-inbound-queue-01-service-exit',
      'lift-02-inbound-queue-02-service-exit',
      'lift-02-inbound-queue-03-service-exit',
      'lift-02-inbound-queue-03-entry-access',
      'column-top-b-c21'
    ]);

    const loadedTraffic = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const serviceClearance = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(loadedTraffic, {
      loaded: true,
      state: 'waiting-blocked',
      taskId: 'loaded-through-traffic',
      targetNodeId: 'column-top-b-c19',
      plannedGoalNodeId: 'lift-02-outbound-queue-01-service-exit',
      plannedRouteNodeIds: loadedTraffic.routeNodeIds,
      x: 28.75,
      z: 0.8,
      currentEdgeId: null,
      legRemainingM: 0
    });
    Object.assign(serviceClearance, {
      loaded: true,
      taskId: 'inbound-service-clearance',
      targetNodeId: 'lift-02-inbound-queue-03-entry-access',
      plannedGoalNodeId: 'storage-r05-c16',
      plannedRouteNodeIds: serviceClearance.routeNodeIds,
      x: 27.45,
      z: 1.6,
      currentEdgeId: 'lift-02-inbound-queue-03-service-exit-lift-02-inbound-queue-03-entry-access',
      legRemainingM: 0.8
    });

    expect(internals.tryYieldAisleAwayFromTopLiftServiceClearance(
      loadedTraffic,
      serviceClearance,
      'column-top-b-c19'
    )).toBe(false);
    expect(loadedTraffic.state).toBe('waiting-blocked');
  });

  it('flushes an idle storage blocker forward when it is parked in an active column path', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
        legRemainingM: number;
        phaseRemainingSec: number;
      }>;
      tryFlushTasklessStorageBlockerAhead: (
        blocker: { id: string },
        requester: { id: string },
        blockedTargetNodeId: string
      ) => boolean;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'storage-r12-c15',
      'storage-r13-c15',
      'storage-r14-c15',
      'column-bottom-a-c15',
      'column-bottom-a-c16'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r13-c15'
    ]);

    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(requester, {
      loaded: false,
      taskId: 'column-transit',
      targetNodeId: 'storage-r13-c15',
      plannedGoalNodeId: 'column-bottom-a-c16',
      plannedRouteNodeIds: requester.routeNodeIds,
      x: 22.5,
      z: 16.4,
      currentEdgeId: null,
      legRemainingM: 0,
      phaseRemainingSec: 0
    });
    Object.assign(blocker, {
      loaded: false,
      taskId: null,
      targetNodeId: null,
      plannedGoalNodeId: null,
      plannedRouteNodeIds: blocker.routeNodeIds,
      x: 22.5,
      z: 17.6,
      currentEdgeId: null,
      legRemainingM: 0,
      phaseRemainingSec: 0
    });

    expect(internals.tryFlushTasklessStorageBlockerAhead(
      blocker,
      requester,
      'storage-r13-c15'
    )).toBe(true);
    expect(blocker.routeNodeIds).toEqual([
      'storage-r13-c15',
      'storage-r14-c15',
      'column-bottom-a-c15'
    ]);
    expect(blocker.targetNodeId).toBe('storage-r14-c15');
  });

  it('does not let a blocked storage waiter hold a future middle-aisle claim', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'storage-r09-c15',
      'storage-r08-c15',
      'column-middle-c15',
      'module-boundary-01-spine-middle'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r08-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r08-c15',
      'column-middle-c15',
      'column-middle-c16'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[] }>;
      topLiftMiddleAisleOpposingClaimBlocker(
        vehicle: unknown,
        toNodeId: string,
        candidateRouteNodeIds?: string[]
      ): string | null;
    };
    const clearer = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftMiddleAisleOpposingClaimBlocker(
      clearer,
      'column-middle-c15',
      clearer.routeNodeIds
    )).toBeNull();
  });

  it('retreats storage traffic deeper when it is already moving back into a service clearance', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
        legRemainingM: number;
      }>;
      topLiftStorageServiceClearanceRetreatNodeId: (
        vehicle: { id: string },
        serviceVehicle: { id: string }
      ) => string | null;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'storage-r02-c04',
      'storage-r01-c04',
      'column-top-b-c04'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-02-service-exit',
      'lift-01-inbound-queue-03-service-exit',
      'lift-01-inbound-queue-03-entry-access',
      'column-top-b-c07'
    ]);

    const storageTraffic = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const serviceClearance = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(storageTraffic, {
      loaded: false,
      taskId: 'outbound-storage-return',
      targetNodeId: 'storage-r01-c04',
      plannedGoalNodeId: 'storage-r04-c01',
      plannedRouteNodeIds: storageTraffic.routeNodeIds,
      x: 6.25,
      z: 2.632482,
      currentEdgeId: 'storage-r01-c04-storage-r02-c04',
      legRemainingM: 0.6
    });
    Object.assign(serviceClearance, {
      loaded: true,
      taskId: 'inbound-service-clearance',
      targetNodeId: 'lift-01-inbound-queue-03-service-exit',
      plannedGoalNodeId: 'storage-r02-c03',
      plannedRouteNodeIds: serviceClearance.routeNodeIds,
      x: 6.95,
      z: 1.45452,
      currentEdgeId: 'lift-01-inbound-queue-03-service-exit-lift-01-inbound-queue-02-service-exit',
      legRemainingM: 0.5
    });

    expect(internals.topLiftStorageServiceClearanceRetreatNodeId(storageTraffic, serviceClearance)).toBe('storage-r03-c04');
  });

  it('does not displace an empty storage blocker back into a loaded inbound service sweep', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        loaded: boolean;
        taskId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
        plannedRouteNodeIds: string[];
        x: number;
        z: number;
        currentEdgeId: string | null;
        legRemainingM: number;
      }>;
      agentTryDisplaceEmptyBlocker: (
        blockingVehicleId: string,
        requester: { id: string },
        blockedTargetNodeId: string
      ) => boolean;
      agentRefreshYieldPocketCompatibleWithRequester: (
        vehicle: { id: string },
        nodeId: string,
        requester: { id: string }
      ) => boolean;
    };

    sim.setVehicleRouteForTest('SH-01', [
      'storage-r01-c06',
      'column-top-b-c06'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-03-service-exit',
      'lift-01-inbound-queue-03-entry-access',
      'column-top-b-c07'
    ]);

    const emptyBlocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const loadedClearance = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(emptyBlocker, {
      loaded: false,
      taskId: null,
      targetNodeId: 'column-top-b-c06',
      plannedGoalNodeId: 'column-middle-c06',
      plannedRouteNodeIds: emptyBlocker.routeNodeIds,
      x: 8.75,
      z: 2,
      currentEdgeId: null,
      legRemainingM: 0
    });
    Object.assign(loadedClearance, {
      loaded: true,
      taskId: 'inbound-service-clearance',
      targetNodeId: 'lift-01-inbound-queue-03-entry-access',
      plannedGoalNodeId: 'storage-r06-c03',
      plannedRouteNodeIds: loadedClearance.routeNodeIds,
      x: 7.55,
      z: 1.6,
      currentEdgeId: 'lift-01-inbound-queue-03-service-exit-lift-01-inbound-queue-03-entry-access',
      legRemainingM: 0.5
    });

    expect(internals.agentTryDisplaceEmptyBlocker(
      'SH-01',
      loadedClearance,
      'lift-01-inbound-queue-03-entry-access'
    )).toBe(true);
    expect(emptyBlocker.routeNodeIds).not.toEqual(['storage-r01-c06', 'column-top-b-c06']);
    expect(emptyBlocker.routeNodeIds.slice(1).every((nodeId) =>
      internals.agentRefreshYieldPocketCompatibleWithRequester(emptyBlocker, nodeId, loadedClearance)
    )).toBe(true);
  });

  it('does not reroute loaded inbound top-b faceoffs onto the unavailable top-a lane', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    }));
    sim.addLoadForTest({ id: 'inbound-lower-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-lower',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r10-c15',
      loadId: 'inbound-lower-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'outbound-faceoff-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-faceoff',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c18',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'outbound-faceoff-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c19',
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16',
      'column-top-b-c15',
      'module-boundary-01-spine-top-b',
      'module-boundary-01-spine-middle',
      'column-middle-c15',
      'storage-r08-c15',
      'storage-r09-c15',
      'storage-r10-c15'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-b-c18',
      'column-top-b-c19',
      'column-top-b-c20',
      'column-top-b-c21',
      'module-02-spine-top-b',
      'column-top-b-c22',
      'column-top-a-c22',
      'lift-02-outbound-queue-01-entry-access',
      'lift-02-outbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'inbound-lower', true);
    sim.setVehicleTaskForTest('SH-02', 'outbound-faceoff', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[] }>;
      agentRefreshMoveBlocker: (
        vehicle: { id: string },
        toNodeId: string,
        candidateRouteNodeIds: string[]
      ) => { reason: string; blockingVehicleId: string | null } | null;
      agentRefreshHandleMoveBlock: (
        vehicle: { id: string },
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ) => boolean;
    };
    const inbound = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const block = internals.agentRefreshMoveBlocker(inbound, 'column-top-b-c18', inbound.routeNodeIds);

    expect(block).toMatchObject({ blockingVehicleId: 'SH-02' });
    expect(internals.agentRefreshHandleMoveBlock(inbound, 'column-top-b-c18', block!)).toBe(false);
    expect(inbound.routeNodeIds.slice(0, 4)).toEqual([
      'column-top-b-c19',
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16'
    ]);
    expect(inbound.routeNodeIds).toContain('storage-r10-c15');
    expect(inbound.routeNodeIds).not.toContain('column-top-a-c19');
  });

  it('keeps empty top-lift transit out of active inbound storage columns', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 1
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4
      }
    }));
    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      installAgentRefreshPlannedRoute: (
        vehicle: { id: string },
        task: null,
        goalNodeId: string
      ) => string[];
    };
    sim.addTaskForTest({
      id: 'active-inbound-c03',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r09-c03',
      loadId: 'source-load-active-c03',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r07-c04']);
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01');

    const route = internals.installAgentRefreshPlannedRoute(vehicle!, null, 'storage-r10-c02');

    expect(route).not.toContain('column-middle-c03');
    expect(route.some((nodeId) => /^storage-r\d+-c03$/.test(nodeId))).toBe(false);
  });

  it('routes empty top-lift inbound return trips out of the filled column before pickup', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r12-c08']);
    sim.addLoadForTest({ id: 'return-column-load-01', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'return-column-task-01',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r01-c01',
      loadId: 'return-column-load-01',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    sim.start();
    sim.step(0.25);
    const route = sim.getState().vehicles[0]?.routeNodeIds ?? [];
    const exitIndex = route.indexOf('column-bottom-a-c08');

    expect(route.slice(0, 4)).toEqual(['storage-r12-c08', 'storage-r13-c08', 'storage-r14-c08', 'column-bottom-a-c08']);
    expect(exitIndex).toBeGreaterThan(0);
    expect(route.slice(0, exitIndex)).not.toContain('storage-r11-c08');
    expect(route).toContain('column-bottom-a-c08');
    expect(route.at(-1)).not.toMatch(/^column-(?:top-b|middle|bottom-a)-c\d+$/);

    for (let index = 0; index < 40 && !sim.getState().vehicles[0]?.taskId; index += 1) {
      sim.step(0.25);
    }
    const assignedVehicle = sim.getState().vehicles[0];
    expect(assignedVehicle?.taskId).toBe('return-column-task-01');
    expect(assignedVehicle?.routeNodeIds.at(-1)).toBe('lift-02-inbound-queue-01-service-exit');
  });

  it('lets empty top-lift inbound pickup shuttles pass through stored columns after a side yield', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c02']);
    for (let row = 1; row <= 14; row += 1) {
      sim.addLoadForTest({
        id: `stored-c02-r${String(row).padStart(2, '0')}`,
        state: 'stored',
        nodeId: `storage-r${String(row).padStart(2, '0')}-c02`,
        vehicleId: null,
        weightKg: 100
      });
    }
    sim.addLoadForTest({ id: 'pickup-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'pickup-after-yield',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r07-c03',
      loadId: 'pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'pickup-after-yield', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: { id: string }, task: { id: string; pickupNodeId: string }, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'pickup-after-yield')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route[0]).toBe('column-middle-c02');
    expect(route.at(-1)).toBe('lift-01-inbound-queue-01-service-exit');
    expect(route.some((nodeId) => /^storage-r\d+-c02$/.test(nodeId))).toBe(true);
  });

  it('keeps empty top-lift pickup traffic out of an active inbound fill column', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'active-c19-load', state: 'carried', nodeId: null, vehicleId: 'SH-07', weightKg: 100 });
    sim.addTaskForTest({
      id: 'active-c19-fill',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r05-c19',
      loadId: 'active-c19-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', [
      'column-top-b-c21',
      'column-top-b-c20',
      'column-top-b-c19',
      'storage-r01-c19',
      'storage-r02-c19',
      'storage-r03-c19',
      'storage-r04-c19',
      'storage-r05-c19'
    ]);
    sim.setVehicleTaskForTest('SH-07', 'active-c19-fill', true);

    sim.addLoadForTest({ id: 'empty-pickup-c21-load', state: 'waiting', nodeId: 'lift-02-inbound-queue-01-service-exit', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-pickup-c21',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r07-c21',
      loadId: 'empty-pickup-c21-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c19']);
    sim.setVehicleTaskForTest('SH-01', 'empty-pickup-c21', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'empty-pickup-c21')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route[0]).toBe('column-middle-c19');
    expect(route.at(-1)).toBe('lift-02-inbound-queue-01-service-exit');
    expect(route.some((nodeId) => /^storage-r\d+-c19$/.test(nodeId))).toBe(false);
    expect(route).not.toContain('column-top-b-c19');
  });

  it('clears a loaded outbound bottom-a meter blocker into bottom-b when it blocks loaded inbound cross travel', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'blocked-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'blocked-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r08-c15',
      loadId: 'blocked-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-a-c20', 'column-bottom-a-c19', 'column-bottom-a-c18']);
    sim.setVehicleTaskForTest('SH-01', 'blocked-inbound', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-bottom-a-c19',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-04',
      waitingSinceSec: 0
    });

    sim.addLoadForTest({ id: 'earlier-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'earlier-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c16',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'earlier-outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c20']);
    sim.setVehicleTaskForTest('SH-02', 'earlier-outbound', true);

    sim.addLoadForTest({ id: 'meter-blocker-load', state: 'carried', nodeId: null, vehicleId: 'SH-04', weightKg: 100 });
    sim.addTaskForTest({
      id: 'meter-blocker',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r04-c16',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'meter-blocker-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-04', ['column-bottom-a-c19']);
    sim.setVehicleTaskForTest('SH-04', 'meter-blocker', true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        plannedGoalNodeId: string | null;
        yieldHoldNodeId: string | null;
      waitReason: string | null;
      currentEdgeId: string | null;
    }>;
      tryClearTopLiftBottomLaneMeterBlockers(candidateVehicleIds: string[]): boolean;
      outboundDropoffDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      tasks: Array<{ id: string }>;
    };

    expect(internals.tryClearTopLiftBottomLaneMeterBlockers(['SH-01'])).toBe(true);
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-04')!;
    const blockerTask = internals.tasks.find((task) => task.id === 'meter-blocker')!;

    expect(blocker.routeNodeIds).toEqual(['column-bottom-a-c19', 'column-bottom-b-c19']);
    expect(blocker.targetNodeId).toBe('column-bottom-b-c19');
    expect(blocker.plannedGoalNodeId).toBe('column-bottom-b-c19');
    expect(blocker.yieldHoldNodeId).toBe('column-bottom-b-c19');
    expect(blocker.localRouteReason).toBe('bottom-lane-meter-clearance');
    expect(blocker.waitReason).toBeNull();

    sim.step(0.2);
    const movedBlocker = sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-04')!;
    expect(movedBlocker.targetNodeId).toBe('column-bottom-b-c19');
    expect(movedBlocker.currentEdgeId).not.toBeNull();
  });

  it('lets an empty shuttle already inside an active fill column clear through the nearest access', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'active-c15-load', state: 'carried', nodeId: null, vehicleId: 'SH-08', weightKg: 100 });
    sim.addTaskForTest({
      id: 'active-c15-fill',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c21',
      dropoffNodeId: 'storage-r14-c15',
      loadId: 'active-c15-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', [
      'storage-r11-c15',
      'storage-r12-c15',
      'storage-r13-c15',
      'storage-r14-c15'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'active-c15-fill', true);

    sim.addLoadForTest({ id: 'outbound-c18-load', state: 'stored', nodeId: 'storage-r09-c18', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-c18',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r09-c18',
      dropoffNodeId: 'column-top-a-c22',
      loadId: 'outbound-c18-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', ['storage-r12-c15']);
    sim.setVehicleTaskForTest('SH-06', 'outbound-c18', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-06')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-c18')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route.slice(0, 4)).toEqual([
      'storage-r12-c15',
      'storage-r13-c15',
      'storage-r14-c15',
      'column-bottom-a-c15'
    ]);
    expect(route.at(-1)).toBe('storage-r09-c18');
  });

  it('routes empty top-lift outbound pickups directly along the same storage column', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'outbound-c02-r04-load', state: 'stored', nodeId: 'storage-r04-c02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-c02-r04',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r04-c02',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-c02-r04-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r07-c02']);
    sim.setVehicleTaskForTest('SH-01', 'outbound-c02-r04', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-c02-r04')!;

    expect(internals.agentRouteToGoal(vehicle, task, task.pickupNodeId)).toEqual([
      'storage-r07-c02',
      'storage-r06-c02',
      'storage-r05-c02',
      'storage-r04-c02'
    ]);
  });

  it('exits a filled top-lift storage column from the opposite side before returning to inbound work', () => {
    const scenario = createInboundMvpBaselineScenario();
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'exit-column-load-01', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'exit-column-task-01',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r04-c04',
      loadId: 'exit-column-load-01',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const snapshot = sim.createSnapshot();
    const snapshotVehicle = snapshot.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const storageNode = scenario.layout.nodes.find((node) => node.id === 'storage-r04-c04')!;
    snapshotVehicle.state = 'lowering';
    snapshotVehicle.loaded = true;
    snapshotVehicle.taskId = 'exit-column-task-01';
    snapshotVehicle.currentNodeId = 'storage-r04-c04';
    snapshotVehicle.x = storageNode.x;
    snapshotVehicle.z = storageNode.z;
    snapshotVehicle.routeNodeIds = ['storage-r04-c04'];
    snapshotVehicle.plannedRouteNodeIds = ['storage-r04-c04'];
    snapshotVehicle.targetNodeId = null;
    snapshotVehicle.currentEdgeId = null;
    snapshotVehicle.routeIndex = 0;
    snapshotVehicle.phaseRemainingSec = 0;
    snapshot.currentNodeOccupancy = snapshot.currentNodeOccupancy.filter((entry) =>
      entry.vehicleId !== 'SH-01' &&
      !/^storage-r0[4-7]-c13$/.test(entry.nodeId)
    );
    snapshot.currentNodeOccupancy.push({ nodeId: 'storage-r04-c04', vehicleId: 'SH-01' });
    snapshot.stateHash = hashEngineSnapshot(snapshot);
    sim.restoreSnapshot(snapshot);
    sim.step(0.25);
    const state = sim.getState();
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.routeNodeIds[0]).toBe('storage-r04-c04');
    expect(vehicle?.routeNodeIds[1]).toBe('storage-r05-c04');
    expect(vehicle?.routeNodeIds).toContain('column-middle-c04');
    expect(vehicle?.routeNodeIds.slice(1, 3)).not.toContain('storage-r03-c04');
  });

  it('lets an empty post-dropoff shuttle exit directly through an open side of the same column', () => {
    const scenario = createInboundMvpBaselineScenario();
    const sim = new ShuttleSimCore(scenario);
    for (const row of [4, 5, 6, 7]) {
      sim.addLoadForTest({
        id: `blocked-exit-load-r${row}`,
        state: 'stored',
        nodeId: `storage-r${String(row).padStart(2, '0')}-c13`,
        vehicleId: null,
        weightKg: 100
      });
    }
    sim.addLoadForTest({ id: 'near-full-exit-load-01', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'near-full-exit-task-01',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r03-c13',
      loadId: 'near-full-exit-load-01',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const snapshot = sim.createSnapshot();
    const snapshotVehicle = snapshot.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const storageNode = scenario.layout.nodes.find((node) => node.id === 'storage-r03-c13')!;
    snapshotVehicle.state = 'lowering';
    snapshotVehicle.loaded = true;
    snapshotVehicle.taskId = 'near-full-exit-task-01';
    snapshotVehicle.currentNodeId = 'storage-r03-c13';
    snapshotVehicle.x = storageNode.x;
    snapshotVehicle.z = storageNode.z;
    snapshotVehicle.routeNodeIds = ['storage-r03-c13'];
    snapshotVehicle.plannedRouteNodeIds = ['storage-r03-c13'];
    snapshotVehicle.targetNodeId = null;
    snapshotVehicle.currentEdgeId = null;
    snapshotVehicle.routeIndex = 0;
    snapshotVehicle.phaseRemainingSec = 0;
    snapshot.currentNodeOccupancy = snapshot.currentNodeOccupancy.filter((entry) => entry.vehicleId !== 'SH-01');
    snapshot.currentNodeOccupancy.push({ nodeId: 'storage-r03-c13', vehicleId: 'SH-01' });
    snapshot.stateHash = hashEngineSnapshot(snapshot);
    sim.restoreSnapshot(snapshot);
    sim.step(0.25);
    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.routeNodeIds[0]).toBe('storage-r03-c13');
    expect(vehicle?.routeNodeIds.slice(1, 3).every((nodeId) => /^storage-r\d{2}-c13$/.test(nodeId))).toBe(true);
    const exitIndex = vehicle?.routeNodeIds.findIndex((nodeId) => isTopLiftColumnAccessNodeIdForTest(nodeId)) ?? -1;
    expect(exitIndex).toBeGreaterThan(0);
    expect(vehicle?.routeNodeIds.slice(0, exitIndex + 1).some((nodeId) => nodeId.startsWith('module-'))).toBe(false);
  });

  it('keeps an empty post-dropoff shuttle from clearing through loaded storage cells', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'lower-bank-clearance-carried', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'lower-bank-clearance-stored-ahead', state: 'stored', nodeId: 'storage-r14-c15', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'lower-bank-clearance-follower-load', state: 'waiting', nodeId: 'lift-02-inbound-queue-02-service-exit', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'lower-bank-clearance-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c15',
      loadId: 'lower-bank-clearance-carried',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'lower-bank-clearance-follower-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-02-service-exit',
      dropoffNodeId: 'storage-r12-c19',
      loadId: 'lower-bank-clearance-follower-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    const snapshot = sim.createSnapshot();
    const dropoffNode = scenario.layout.nodes.find((node) => node.id === 'storage-r13-c15')!;
    const blockedUpperExitNode = scenario.layout.nodes.find((node) => node.id === 'storage-r12-c15')!;
    const dropoffVehicle = snapshot.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const followerVehicle = snapshot.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(dropoffVehicle, {
      state: 'lowering',
      loaded: true,
      taskId: 'lower-bank-clearance-task',
      currentNodeId: 'storage-r13-c15',
      x: dropoffNode.x,
      z: dropoffNode.z,
      routeNodeIds: ['storage-r13-c15'],
      plannedRouteNodeIds: ['storage-r13-c15'],
      targetNodeId: null,
      currentEdgeId: null,
      routeIndex: 0,
      phaseRemainingSec: 0
    });
    Object.assign(followerVehicle, {
      state: 'waiting-blocked',
      loaded: false,
      taskId: 'lower-bank-clearance-follower-task',
      currentNodeId: 'storage-r12-c15',
      x: blockedUpperExitNode.x,
      z: blockedUpperExitNode.z,
      routeNodeIds: ['storage-r12-c15', 'storage-r13-c15', 'storage-r14-c15', 'column-bottom-a-c15'],
      plannedRouteNodeIds: ['storage-r12-c15', 'storage-r13-c15', 'storage-r14-c15', 'column-bottom-a-c15'],
      targetNodeId: 'storage-r13-c15',
      currentEdgeId: null,
      routeIndex: 0,
      phaseRemainingSec: 0,
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01'
    });
    snapshot.currentNodeOccupancy = snapshot.currentNodeOccupancy.filter((entry) =>
      entry.vehicleId !== 'SH-01' && entry.vehicleId !== 'SH-02'
    );
    snapshot.currentNodeOccupancy.push(
      { nodeId: 'storage-r13-c15', vehicleId: 'SH-01' },
      { nodeId: 'storage-r12-c15', vehicleId: 'SH-02' }
    );
    snapshot.stateHash = hashEngineSnapshot(snapshot);
    sim.restoreSnapshot(snapshot);

    sim.step(0.25);
    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.loaded).toBe(false);
    expect(vehicle?.taskId).toBeNull();
    expect(vehicle?.routeNodeIds).not.toContain('storage-r14-c15');
    expect(vehicle?.routeNodeIds.at(-1)).not.toMatch(/^column-(?:top-b|middle|bottom-a)-c\d+$/);
  });

  it('keeps top-lift post-dropoff exits in the same storage column', () => {
    const scenario = createInboundMvpBaselineScenario();
    const sim = new ShuttleSimCore(scenario);
    for (const row of [6, 7]) {
      sim.addLoadForTest({
        id: `blocked-far-exit-r${row}`,
        state: 'stored',
        nodeId: `storage-r${String(row).padStart(2, '0')}-c01`,
        vehicleId: null,
        weightKg: 100
      });
    }
    sim.addLoadForTest({ id: 'same-column-exit-load-01', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'same-column-exit-task-01',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r05-c01',
      loadId: 'same-column-exit-load-01',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const snapshot = sim.createSnapshot();
    const snapshotVehicle = snapshot.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const storageNode = scenario.layout.nodes.find((node) => node.id === 'storage-r05-c01')!;
    snapshotVehicle.state = 'lowering';
    snapshotVehicle.loaded = true;
    snapshotVehicle.taskId = 'same-column-exit-task-01';
    snapshotVehicle.currentNodeId = 'storage-r05-c01';
    snapshotVehicle.x = storageNode.x;
    snapshotVehicle.z = storageNode.z;
    snapshotVehicle.routeNodeIds = ['storage-r05-c01'];
    snapshotVehicle.plannedRouteNodeIds = ['storage-r05-c01'];
    snapshotVehicle.targetNodeId = null;
    snapshotVehicle.currentEdgeId = null;
    snapshotVehicle.routeIndex = 0;
    snapshotVehicle.phaseRemainingSec = 0;
    snapshot.currentNodeOccupancy = snapshot.currentNodeOccupancy.filter((entry) => entry.vehicleId !== 'SH-01');
    snapshot.currentNodeOccupancy.push({ nodeId: 'storage-r05-c01', vehicleId: 'SH-01' });
    snapshot.stateHash = hashEngineSnapshot(snapshot);
    sim.restoreSnapshot(snapshot);
    sim.step(0.25);
    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.routeNodeIds[0]).toBe('storage-r05-c01');
    expect(vehicle?.localRouteNodeIds.length).toBeLessThanOrEqual(4);
    const exitIndex = vehicle?.routeNodeIds.findIndex((nodeId) => isTopLiftColumnAccessNodeIdForTest(nodeId)) ?? -1;
    expect(exitIndex).toBeGreaterThan(0);
    expect(vehicle?.routeNodeIds.slice(1, exitIndex).every((nodeId) => /^storage-r\d{2}-c01$/.test(nodeId))).toBe(true);
    expect(vehicle?.routeNodeIds.slice(0, exitIndex + 1).some((nodeId) => nodeId.startsWith('module-'))).toBe(false);
  });

  it('keeps top-lift physical column keys separate from storage bank lane labels', () => {
    const scenario = createInboundMvpBaselineScenario();
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      topLiftColumnKey(nodeId: string): string | null;
      nodeStorageRowLabel(nodeId: string): string | null;
    };

    expect(internals.topLiftColumnKey('storage-r03-c13')).toBe('c13');
    expect(internals.topLiftColumnKey('storage-r10-c13')).toBe('c13');
    expect(internals.topLiftColumnKey('column-middle-c13')).toBe('c13');
    expect(internals.nodeStorageRowLabel('storage-r03-c13')).toBe('b01-c13');
    expect(internals.nodeStorageRowLabel('storage-r10-c13')).toBe('b02-c13');
  });

  it('allows either direction on top-lift double aisles', () => {
    const scenario = createInboundMvpBaselineScenario();
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      agentEdgeDirectionAllowed(fromNodeId: string, toNodeId: string, goalNodeId: string): boolean;
    };

    expect(internals.agentEdgeDirectionAllowed('column-top-a-c01', 'column-top-a-c02', 'storage-r01-c14')).toBe(true);
    expect(internals.agentEdgeDirectionAllowed('column-top-a-c02', 'column-top-a-c01', 'storage-r01-c14')).toBe(true);
    expect(internals.agentEdgeDirectionAllowed('column-top-b-c02', 'column-top-b-c01', 'storage-r01-c14')).toBe(true);
    expect(internals.agentEdgeDirectionAllowed('column-top-b-c01', 'column-top-b-c02', 'storage-r01-c14')).toBe(true);
    expect(internals.agentEdgeDirectionAllowed('column-bottom-a-c01', 'column-bottom-a-c02', 'storage-r14-c14')).toBe(true);
    expect(internals.agentEdgeDirectionAllowed('column-bottom-a-c02', 'column-bottom-a-c01', 'storage-r14-c14')).toBe(true);
    expect(internals.agentEdgeDirectionAllowed('column-bottom-b-c02', 'column-bottom-b-c01', 'storage-r14-c14')).toBe(true);
    expect(internals.agentEdgeDirectionAllowed('column-bottom-b-c01', 'column-bottom-b-c02', 'storage-r14-c14')).toBe(true);
  });

  it('claims the top-lift middle aisle by direction while allowing same-direction followers', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c15', 'module-boundary-01-spine-middle']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r07-c13', 'column-middle-c13', 'column-middle-c14']);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
    };
    const oppositeVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshMoveBlocker(oppositeVehicle, 'column-middle-c13')).toEqual({
      reason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-01'
    });

    const sameDirectionSim = new ShuttleSimCore(scenario);
    sameDirectionSim.setVehicleRouteForTest('SH-01', ['column-middle-c15', 'module-boundary-01-spine-middle']);
    sameDirectionSim.setVehicleRouteForTest('SH-02', ['storage-r07-c17', 'column-middle-c17', 'column-middle-c16']);
    const sameDirectionInternals = sameDirectionSim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
    };
    const sameDirectionVehicle = sameDirectionInternals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    expect(sameDirectionInternals.agentRefreshMoveBlocker(sameDirectionVehicle, 'column-middle-c17')?.reason)
      .not.toBe('middle-aisle-opposing-claim');
  });

  it('lets empty top-lift shuttles yield from the middle aisle into an adjacent empty storage pocket', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'outbound-yield-load', state: 'stored', nodeId: 'storage-r11-c16', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-yield',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r11-c16',
      dropoffNodeId: 'lift-02-outbound-buffer-03',
      loadId: 'outbound-yield-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c13']);
    sim.setVehicleTaskForTest('SH-01', 'outbound-yield', false);
    sim.setVehicleRouteForTest('SH-02', ['column-middle-c14']);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; targetNodeId: string | null }>;
      agentRefreshInstallSideYield(
        vehicle: unknown,
        blockedTargetNodeId: string,
        requester: unknown,
        session?: unknown
      ): boolean;
      mustStopAtNode(vehicle: unknown, task: unknown, nodeId: string): boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshInstallSideYield(yielder, 'column-middle-c14', requester, null)).toBe(true);
    expect(yielder.routeNodeIds[0]).toBe('column-middle-c13');
    expect(yielder.routeNodeIds[1]).toMatch(/^storage-r(?:07|08)-c13$/);
    expect(yielder.routeNodeIds).toHaveLength(2);
    expect(yielder.targetNodeId).toBe(yielder.routeNodeIds[1]);

    const spineSim = new ShuttleSimCore(scenario);
    spineSim.addLoadForTest({ id: 'outbound-spine-yield-load', state: 'stored', nodeId: 'storage-r11-c16', vehicleId: null, weightKg: 100 });
    spineSim.addTaskForTest({
      id: 'outbound-spine-yield',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r11-c16',
      dropoffNodeId: 'lift-02-outbound-buffer-03',
      loadId: 'outbound-spine-yield-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    spineSim.setVehicleRouteForTest('SH-01', ['module-01-spine-middle']);
    spineSim.setVehicleTaskForTest('SH-01', 'outbound-spine-yield', false);
    spineSim.setVehicleRouteForTest('SH-02', ['column-middle-c08']);
    const spineInternals = spineSim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[] }>;
      agentRefreshInstallSideYield(
        vehicle: unknown,
        blockedTargetNodeId: string,
        requester: unknown,
        session?: unknown
      ): boolean;
      mustStopAtNode(vehicle: unknown, task: unknown, nodeId: string): boolean;
    };
    const spineYielder = spineInternals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const spineRequester = spineInternals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(spineInternals.agentRefreshInstallSideYield(spineYielder, 'module-01-spine-middle', spineRequester, null)).toBe(true);
    expect(spineYielder.routeNodeIds).toHaveLength(3);
    expect(spineYielder.routeNodeIds[1]).toMatch(/^column-middle-c\d+$/);
    expect(spineYielder.routeNodeIds[2]).toMatch(/^storage-r(?:07|08)-c\d+$/);

    const inboundSpineSim = new ShuttleSimCore(scenario);
    inboundSpineSim.addLoadForTest({ id: 'inbound-spine-yield-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    inboundSpineSim.addTaskForTest({
      id: 'inbound-spine-yield',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r01-c01',
      loadId: 'inbound-spine-yield-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    inboundSpineSim.setVehicleRouteForTest('SH-01', ['module-01-spine-middle']);
    inboundSpineSim.setVehicleTaskForTest('SH-01', 'inbound-spine-yield', false);
    inboundSpineSim.setVehicleRouteForTest('SH-02', ['column-middle-c08']);
    const inboundSpineInternals = inboundSpineSim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[] }>;
      agentRefreshInstallSideYield(
        vehicle: unknown,
        blockedTargetNodeId: string,
        requester: unknown,
        session?: unknown
      ): boolean;
    };
    const inboundSpineYielder = inboundSpineInternals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const inboundSpineRequester = inboundSpineInternals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(inboundSpineInternals.agentRefreshInstallSideYield(inboundSpineYielder, 'module-01-spine-middle', inboundSpineRequester, null)).toBe(true);
    expect(inboundSpineYielder.routeNodeIds).toHaveLength(3);
    expect(inboundSpineYielder.routeNodeIds[1]).toMatch(/^column-middle-c\d+$/);
    expect(inboundSpineYielder.routeNodeIds[2]).toMatch(/^storage-r(?:07|08)-c\d+$/);
  });

  it('keeps top-lift double-aisle side yield on the nearest opposite lane instead of extending into parking', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'inbound-minimal-yield-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-minimal-yield',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r01-c08',
      loadId: 'inbound-minimal-yield-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-bottom-a-c05',
      'column-bottom-a-c06',
      'column-bottom-a-c07',
      'module-01-spine-bottom-a',
      'module-01-spine-middle',
      'module-01-spine-top-b',
      'column-top-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'inbound-minimal-yield', false);
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r09-c01',
      'storage-r08-c01',
      'column-middle-c01',
      'storage-r07-c01'
    ]);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        yieldHoldNodeId: string | null;
      }>;
      agentRefreshInstallSideYield(
        vehicle: unknown,
        blockedTargetNodeId: string,
        requester: unknown,
        session?: unknown
      ): boolean;
      mustStopAtNode(vehicle: unknown, task: unknown, nodeId: string): boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshInstallSideYield(yielder, 'column-bottom-a-c06', requester, null)).toBe(true);
    expect(yielder.routeNodeIds).toEqual(['column-bottom-a-c05', 'column-bottom-b-c05']);
    expect(yielder.targetNodeId).toBe('column-bottom-b-c05');
    expect(yielder.yieldHoldNodeId).toBe('column-bottom-b-c05');
    expect(yielder.routeNodeIds).not.toContain('parking-02');
    expect(internals.mustStopAtNode(yielder, null, 'column-bottom-b-c05')).toBe(true);
  });

  it('lets an empty bottom-b blocker step aside from a loaded bottom-a ingress', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'empty-blocker-assigned-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-blocker-assigned-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-buffer-03',
      dropoffNodeId: 'storage-r14-c04',
      loadId: 'empty-blocker-assigned-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'loaded-outbound-ingress-load', state: 'carried', nodeId: null, vehicleId: 'SH-08', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-outbound-ingress',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c06',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'loaded-outbound-ingress-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-bottom-b-c06',
      'column-bottom-b-c05',
      'column-bottom-b-c04',
      'column-bottom-a-c04',
      'storage-r14-c04'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'empty-blocker-assigned-task', false);
    sim.setVehicleRouteForTest('SH-03', ['column-bottom-b-c05']);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'column-bottom-b-c05',
      waitReason: 'outbound-lift-fifo-wait',
      blockingVehicleId: null
    });
    sim.setVehicleRouteForTest('SH-08', [
      'column-bottom-a-c06',
      'column-bottom-b-c06',
      'lift-01-outbound-queue-01-entry-access',
      'lift-01-outbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'loaded-outbound-ingress', true);
    sim.setVehicleWaitingForTest('SH-08', {
      targetNodeId: 'column-bottom-b-c06',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01'
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        yieldHoldNodeId: string | null;
      }>;
      agentRefreshInstallSideYield(
        vehicle: unknown,
        blockedTargetNodeId: string,
        requester: unknown,
        session?: unknown
      ): boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;

    expect(internals.agentRefreshInstallSideYield(yielder, 'column-bottom-b-c06', requester, null)).toBe(true);
    expect(yielder.routeNodeIds).toEqual(['column-bottom-b-c06', 'column-bottom-b-c07']);
    expect(yielder.targetNodeId).toBe('column-bottom-b-c07');
    expect(yielder.yieldHoldNodeId).toBe('column-bottom-b-c07');
  });

  it('keeps side-yield pockets out of an outbound requester pickup path', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'inbound-pre-pickup-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-pre-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r12-c02',
      loadId: 'inbound-pre-pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'outbound-c01-load', state: 'stored', nodeId: 'storage-r11-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-c01',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r11-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-c01-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-a-c02']);
    sim.setVehicleTaskForTest('SH-01', 'inbound-pre-pickup', false);
    sim.setVehicleRouteForTest('SH-02', [
      'column-bottom-a-c03',
      'column-bottom-a-c02',
      'column-bottom-a-c01',
      'storage-r14-c01',
      'storage-r13-c01',
      'storage-r12-c01',
      'storage-r11-c01'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'outbound-c01', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[] }>;
      agentRefreshRequesterProtectedRouteNodeIds(requester: unknown): string[];
      agentRefreshYieldPocketCompatibleWithRequester(vehicle: unknown, nodeId: string, requester: unknown): boolean;
      agentRefreshInstallSideYield(
        vehicle: unknown,
        blockedTargetNodeId: string,
        requester: unknown,
        session?: unknown
      ): boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshRequesterProtectedRouteNodeIds(requester)).toEqual([
      'column-bottom-a-c02',
      'column-bottom-a-c01',
      'storage-r14-c01',
      'storage-r13-c01',
      'storage-r12-c01',
      'storage-r11-c01'
    ]);
    expect(internals.agentRefreshYieldPocketCompatibleWithRequester(yielder, 'storage-r13-c01', requester)).toBe(false);
    expect(internals.agentRefreshYieldPocketCompatibleWithRequester(yielder, 'column-bottom-a-c01', requester)).toBe(false);

    internals.agentRefreshInstallSideYield(yielder, 'column-bottom-a-c03', requester, null);
    expect(yielder.routeNodeIds).not.toContain('column-bottom-a-c01');
    expect(yielder.routeNodeIds).not.toContain('storage-r14-c01');
    expect(yielder.routeNodeIds).not.toContain('storage-r13-c01');
  });

  it('allows a storage side pocket to clear a loaded inbound service exit', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r02-c03',
      loadId: 'loaded-inbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'outbound-c01-load', state: 'stored', nodeId: 'storage-r03-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-outbound',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'outbound-c01-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c06',
      'column-top-b-c05',
      'column-top-b-c04',
      'column-top-b-c03',
      'column-top-b-c02',
      'column-top-b-c01',
      'storage-r01-c01',
      'storage-r02-c01',
      'storage-r03-c01'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'empty-outbound', false);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-03-service-exit',
      'lift-01-inbound-queue-03-entry-access',
      'column-top-b-c07',
      'column-top-a-c07',
      'column-top-a-c06',
      'column-top-a-c05',
      'column-top-a-c04',
      'column-top-a-c03',
      'column-top-b-c03',
      'storage-r01-c03',
      'storage-r02-c03'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'loaded-inbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; targetNodeId: string | null }>;
      agentRefreshYieldPocketCompatibleWithRequester(vehicle: unknown, nodeId: string, requester: unknown): boolean;
      agentRefreshInstallSideYield(
        vehicle: unknown,
        blockedTargetNodeId: string,
        requester: unknown,
        session?: unknown
      ): boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshYieldPocketCompatibleWithRequester(yielder, 'storage-r01-c06', requester)).toBe(true);
    expect(internals.agentRefreshInstallSideYield(yielder, 'column-top-b-c05', requester, null)).toBe(true);
    expect(yielder.routeNodeIds).toEqual(['column-top-b-c06', 'storage-r01-c06']);
    expect(yielder.targetNodeId).toBe('storage-r01-c06');
  });

  it('does not let an unpicked outbound task hold the loaded dropoff queue', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'earlier-outbound-load', state: 'stored', nodeId: 'storage-r06-c15', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'earlier-outbound',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r06-c15',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'earlier-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'loaded-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c16',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'loaded-outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c18']);
    sim.setVehicleTaskForTest('SH-01', 'earlier-outbound', false);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c17']);
    sim.setVehicleTaskForTest('SH-02', 'loaded-outbound', true);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      outboundTaskHasEarlierDropoffTask(task: unknown): boolean;
      outboundDropoffDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
    };
    const earlierTask = internals.tasks.find((task) => task.id === 'earlier-outbound')!;
    const loadedTask = internals.tasks.find((task) => task.id === 'loaded-outbound')!;
    const earlierVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const loadedVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.outboundTaskHasEarlierDropoffTask(loadedTask)).toBe(false);
    expect(internals.outboundDropoffDispatchGoalNodeId(loadedTask, loadedVehicle)).toBe('lift-02-outbound-queue-01-service-exit');

    sim.setVehicleTaskForTest('SH-01', 'earlier-outbound', true);
    expect(internals.outboundTaskHasEarlierDropoffTask(loadedTask)).toBe(false);

    sim.setVehicleRouteForTest('SH-01', ['lift-02-outbound-queue-01-service-exit']);
    sim.setVehicleTaskForTest('SH-01', 'earlier-outbound', true);
    expect(internals.outboundTaskHasEarlierDropoffTask(loadedTask)).toBe(true);
    expect(internals.outboundDropoffDispatchGoalNodeId(loadedTask, loadedVehicle)).not.toBe('lift-02-outbound-queue-01-service-exit');
    expect(internals.outboundDropoffDispatchGoalNodeId(earlierTask, earlierVehicle)).toBe('lift-02-outbound-queue-01-service-exit');
  });

  it('moves empty storage blockers past a loaded inbound dropoff point', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-inbound-drop-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-inbound-drop',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-buffer-03',
      dropoffNodeId: 'storage-r05-c18',
      loadId: 'loaded-inbound-drop-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'empty-outbound-c03-load', state: 'stored', nodeId: 'storage-r07-c03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-outbound-c03',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c03',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'empty-outbound-c03-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'storage-r01-c18',
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16',
      'column-top-b-c15',
      'module-boundary-01-spine-top-b',
      'column-top-b-c14',
      'column-top-b-c13',
      'column-top-b-c12',
      'column-top-b-c11',
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07',
      'column-top-b-c06',
      'column-top-b-c05',
      'column-top-b-c04',
      'column-top-b-c03',
      'storage-r01-c03',
      'storage-r02-c03',
      'storage-r03-c03',
      'storage-r04-c03',
      'storage-r05-c03',
      'storage-r06-c03',
      'storage-r07-c03'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'empty-outbound-c03', false);
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-b-c18',
      'storage-r01-c18',
      'storage-r02-c18',
      'storage-r03-c18',
      'storage-r04-c18',
      'storage-r05-c18'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'loaded-inbound-drop', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; targetNodeId: string | null; localRouteReason: string | null }>;
      agentRefreshTemporaryStorageNodeAllowed(vehicle: unknown, nodeId: string): boolean;
      tryYieldStorageAwayFromLoadedInboundDropoffRoute(storageVehicle: unknown, loadedVehicle: unknown): boolean;
    };
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const loaded = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshTemporaryStorageNodeAllowed(blocker, 'storage-r01-c18')).toBe(false);
    expect(internals.agentRefreshTemporaryStorageNodeAllowed(blocker, 'storage-r04-c18')).toBe(false);
    expect(internals.agentRefreshTemporaryStorageNodeAllowed(blocker, 'storage-r06-c18')).toBe(true);
    expect(internals.tryYieldStorageAwayFromLoadedInboundDropoffRoute(blocker, loaded)).toBe(true);
    expect(blocker.routeNodeIds).toEqual([
      'storage-r01-c18',
      'storage-r02-c18',
      'storage-r03-c18',
      'storage-r04-c18',
      'storage-r05-c18',
      'storage-r06-c18'
    ]);
    expect(blocker.targetNodeId).toBe('storage-r02-c18');
    expect(blocker.localRouteReason).toBe('inbound-dropoff-column-clearance');
  });

  it('lets top-lift outbound storage exits step into an adjacent pocket when an opposing middle-aisle claim blocks them', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'outbound-middle-yield-load',
      state: 'carried',
      nodeId: null,
      vehicleId: 'SH-01',
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'outbound-middle-yield',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r06-c03',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-middle-yield-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'storage-r07-c03',
      'column-middle-c03',
      'column-middle-c04',
      'column-middle-c05',
      'column-middle-c06',
      'column-middle-c07',
      'module-01-spine-middle'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'outbound-middle-yield', true);
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c07',
      'column-middle-c06',
      'column-middle-c05',
      'column-middle-c04',
      'storage-r08-c04'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, true);

    const state = sim.step(0.2);
    const yielder = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const yieldEvent = sim.getEventLog().find((event) =>
      event.eventType === 'route-replanned' &&
      event.vehicleId === 'SH-01' &&
      event.reason === 'agent-refresh-middle-aisle-storage-yield'
    );

    expect(yieldEvent).toBeDefined();
    expect(yielder).toMatchObject({
      state: 'loaded-moving',
      currentNodeId: 'storage-r07-c03',
      targetNodeId: 'storage-r06-c03',
      waitReason: null,
      blockingVehicleId: null
    });
    expect(yielder?.routeNodeIds).toEqual(['storage-r07-c03', 'storage-r06-c03']);
  });

  it('does not hold a loaded storage yielder for an unrelated top-lift waiting winner', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r06-c01']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-entry-access',
      'column-top-a-c07'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'lift-01-inbound-queue-01-entry-access',
      waitReason: 'min-separation',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; yieldHoldUntilSec: number | null; yieldHoldNodeId: string | null }>;
      conflictSessions: unknown[];
      agentRefreshYieldHoldBlocker(vehicle: unknown, task: null): { reason: string; blockingVehicleId: string | null } | null;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    yielder.yieldHoldUntilSec = 0;
    yielder.yieldHoldNodeId = 'storage-r06-c01';
    internals.conflictSessions.push({
      id: 'conflict-test-unrelated-winner',
      kind: 'pair',
      resourceKey: 'edge:column-middle-c01-storage-r07-c01:SH-01+SH-02',
      state: 'holding-pocket',
      participantVehicleIds: ['SH-01', 'SH-02'],
      winnerVehicleId: 'SH-02',
      yielderVehicleId: 'SH-01',
      createdAtSec: 0,
      createdAtTick: 0,
      updatedAtSec: 0,
      expiresAtSec: 30,
      timeoutAtSec: 20,
      trigger: 'deadlock-break',
      initialBlockerVehicleId: 'SH-02',
      blockerVehicleId: 'SH-02',
      yielderOriginalNodeId: 'storage-r07-c01',
      yielderPocketNodeId: 'storage-r06-c01',
      yielderLocalRouteNodeIds: ['storage-r07-c01', 'storage-r06-c01'],
      resumeNodeId: 'column-middle-c01',
      clearancePolicy: 'short-horizon',
      closeReason: null
    });

    expect(internals.agentRefreshYieldHoldBlocker(yielder, null)).toBeNull();
  });

  it('releases a temporary yield hold after its conflict session times out', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r07-c21']);
    sim.setVehicleTaskForTest('SH-01', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        yieldHoldUntilSec: number | null;
        yieldHoldNodeId: string | null;
        localRouteReason: string | null;
      }>;
      conflictSessions: unknown[];
      agentRefreshYieldHoldBlocker(vehicle: unknown, task: null): { reason: string; blockingVehicleId: string | null } | null;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    yielder.localRouteReason = 'temporary-yield';
    yielder.yieldHoldUntilSec = 120;
    yielder.yieldHoldNodeId = 'storage-r07-c21';
    internals.conflictSessions.push({
      id: 'conflict-test-timed-out-yield',
      kind: 'pair',
      resourceKey: 'node:column-middle-c21:SH-01+SH-02',
      state: 'timed-out',
      participantVehicleIds: ['SH-01', 'SH-02'],
      winnerVehicleId: 'SH-02',
      yielderVehicleId: 'SH-01',
      createdAtSec: 0,
      createdAtTick: 0,
      updatedAtSec: 20,
      expiresAtSec: 30,
      timeoutAtSec: 20,
      trigger: 'deadlock-break',
      initialBlockerVehicleId: 'SH-02',
      blockerVehicleId: 'SH-02',
      yielderOriginalNodeId: 'column-middle-c21',
      yielderPocketNodeId: 'storage-r07-c21',
      yielderLocalRouteNodeIds: ['column-middle-c21', 'storage-r07-c21'],
      resumeNodeId: 'column-middle-c21',
      clearancePolicy: 'immediate-next-move',
      closeReason: 'timeout'
    });

    expect(internals.agentRefreshYieldHoldBlocker(yielder, null)).toBeNull();
  });

  it('holds against the active yielder session instead of an unrelated winner session', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c21']);
    sim.setVehicleTaskForTest('SH-01', null, true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; yieldHoldUntilSec: number | null; yieldHoldNodeId: string | null }>;
      conflictSessions: unknown[];
      agentRefreshYieldHoldBlocker(vehicle: unknown, task: null): { reason: string; blockingVehicleId: string | null } | null;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    yielder.yieldHoldUntilSec = 10;
    yielder.yieldHoldNodeId = 'column-top-a-c21';
    internals.conflictSessions.push({
      id: 'conflict-test-vehicle-is-winner',
      kind: 'pair',
      resourceKey: 'node:module-02-spine-top-b:SH-01+SH-02',
      state: 'yielding',
      participantVehicleIds: ['SH-01', 'SH-02'],
      winnerVehicleId: 'SH-01',
      yielderVehicleId: 'SH-02',
      createdAtSec: 0,
      createdAtTick: 0,
      updatedAtSec: 0,
      expiresAtSec: 30,
      timeoutAtSec: 20,
      trigger: 'same-node',
      initialBlockerVehicleId: 'SH-02',
      blockerVehicleId: 'SH-01',
      yielderOriginalNodeId: 'module-02-spine-top-b',
      yielderPocketNodeId: 'storage-r01-c22',
      yielderLocalRouteNodeIds: ['module-02-spine-top-b', 'column-top-b-c22', 'storage-r01-c22'],
      resumeNodeId: 'column-top-b-c21',
      clearancePolicy: 'immediate-next-move',
      closeReason: null
    });
    internals.conflictSessions.push({
      id: 'conflict-test-vehicle-is-yielder',
      kind: 'pair',
      resourceKey: 'edge:lift-02-inbound-service:SH-01+SH-03',
      state: 'yielding',
      participantVehicleIds: ['SH-01', 'SH-03'],
      winnerVehicleId: 'SH-03',
      yielderVehicleId: 'SH-01',
      createdAtSec: 1,
      createdAtTick: 1,
      updatedAtSec: 1,
      expiresAtSec: 31,
      timeoutAtSec: 21,
      trigger: 'swept-footprint',
      initialBlockerVehicleId: 'SH-01',
      blockerVehicleId: 'SH-03',
      yielderOriginalNodeId: 'column-top-a-c21',
      yielderPocketNodeId: 'module-02-spine-top-a',
      yielderLocalRouteNodeIds: ['column-top-a-c21', 'module-02-spine-top-a'],
      resumeNodeId: 'column-top-b-c21',
      clearancePolicy: 'immediate-next-move',
      closeReason: null
    });

    expect(internals.agentRefreshYieldHoldBlocker(yielder, null)).toEqual({
      reason: 'local-yield-hold',
      blockingVehicleId: 'SH-03'
    });
  });

  it('breaks empty top-lift middle-aisle node swaps by yielding into storage', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c13', 'column-middle-c14']);
    sim.setVehicleRouteForTest('SH-02', ['column-middle-c14', 'column-middle-c13']);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-middle-c14',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c13',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null }>;
      tryBreakAgentRefreshTopLiftAdjacentNodeSwap(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshTopLiftAdjacentNodeSwap(['SH-01', 'SH-02'])).toBe(true);
    const yielder = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'temporary-yield');

    expect(yielder?.routeNodeIds).toHaveLength(2);
    expect(yielder?.routeNodeIds[0]).toMatch(/^column-middle-c(?:13|14)$/);
    expect(yielder?.routeNodeIds[1]).toMatch(/^storage-r(?:07|08)-c(?:13|14)$/);
  });

  it('breaks module-boundary middle swaps by yielding the empty column shuttle into storage', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c15', 'module-boundary-01-spine-middle']);
    sim.setVehicleRouteForTest('SH-02', ['module-boundary-01-spine-middle', 'column-middle-c15']);
    sim.setVehicleTaskForTest('SH-02', null, true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-boundary-01-spine-middle',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null }>;
      tryBreakAgentRefreshTopLiftAdjacentNodeSwap(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshTopLiftAdjacentNodeSwap(['SH-01', 'SH-02'])).toBe(true);

    const yielder = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'temporary-yield');
    expect(yielder?.id).toBe('SH-01');
    expect(yielder?.routeNodeIds).toHaveLength(2);
    expect(yielder?.routeNodeIds[0]).toBe('column-middle-c15');
    expect(yielder?.routeNodeIds[1]).toMatch(/^storage-r(?:07|08)-c15$/);
  });

  it('lets empty top-lift lane shuttles duck into adjacent storage to break lift-lane swaps', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c07', 'column-top-b-c06']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c06', 'column-top-b-c07']);
    sim.setVehicleTaskForTest('SH-02', null, true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-b-c06',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-top-b-c07',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null }>;
      tryBreakAgentRefreshTopLiftAdjacentNodeSwap(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshTopLiftAdjacentNodeSwap(['SH-01', 'SH-02'])).toBe(true);

    const yielder = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'temporary-yield');
    expect(yielder?.id).toBe('SH-01');
    expect(yielder?.routeNodeIds).toEqual(['column-top-b-c07', 'storage-r01-c07']);
  });

  it('preempts loaded top-lift adjacent swaps before the smoke detector counts a deadlock', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c15', 'column-top-b-c16']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c16', 'column-top-b-c15']);
    sim.setVehicleTaskForTest('SH-02', null, true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-b-c16',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-top-b-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    internals.agentRefreshHandleMoveBlock(requester, 'column-top-b-c15', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-01'
    });
    const yielder = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'temporary-yield');

    expect(yielder).toBeDefined();
    expect(yielder?.routeNodeIds[0]).toBe(yielder?.id === 'SH-01' ? 'column-top-b-c15' : 'column-top-b-c16');
    expect(sim.getState().kpis.deadlockCount).toBe(0);
    expect(sim.getEventLog().some((entry) => entry.eventType === 'deadlock-detected')).toBe(false);
  });

  it('keeps a top-lift adjacent-swap yield route when it still terminates at the pickup goal', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'same-goal-pickup-load', state: 'waiting', nodeId: 'column-top-b-c07', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'same-goal-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r01-c07',
      loadId: 'same-goal-pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const yieldRoute = [
      'column-top-b-c05',
      'column-top-b-c04',
      'column-top-a-c04',
      'column-top-a-c05',
      'column-top-a-c06',
      'column-top-a-c07',
      'column-top-b-c07'
    ];
    sim.setVehicleRouteForTest('SH-01', yieldRoute);
    sim.setVehicleTaskForTest('SH-01', 'same-goal-pickup', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        routeIndex: number;
        localRouteNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    yielder.state = 'moving-to-pickup';
    yielder.targetNodeId = 'column-top-b-c04';
    yielder.routeIndex = 0;
    yielder.routeNodeIds = yieldRoute;
    yielder.localRouteNodeIds = yieldRoute;
    yielder.localRouteReason = 'temporary-yield';

    expect(yielder?.localRouteReason).toBe('temporary-yield');
    expect(yielder?.routeNodeIds.at(-1)).toBe('column-top-b-c07');
    expect(yielder?.targetNodeId).toBe('column-top-b-c04');

    sim.step(0.2);
    expect(yielder?.targetNodeId).not.toBe('column-top-b-c06');
    expect(yielder?.routeNodeIds).toEqual(yieldRoute);
    expect(yielder?.localRouteNodeIds).toEqual(yielder?.routeNodeIds);
    expect(yielder?.waitReason).toBeNull();
  });

  it('treats top-a inbound pickup clearance to queue access as active', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'queued-pickup-load', state: 'waiting', nodeId: 'column-top-a-c22', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queued-pickup-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r04-c22',
      loadId: 'queued-pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const route = [
      'column-top-a-c22',
      'module-02-spine-top-a',
      'column-top-a-c23',
      'column-top-b-c23',
      'lift-02-inbound-queue-02-entry-access',
      'lift-02-inbound-queue-02-access'
    ];
    sim.setVehicleRouteForTest('SH-01', route);
    sim.setVehicleTaskForTest('SH-01', 'queued-pickup-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      inboundPickupClearanceRouteActive(vehicle: unknown, task: unknown): boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'queued-pickup-task')!;

    expect(internals.inboundPickupClearanceRouteActive(vehicle, task)).toBe(true);
  });

  it('does not let a storage-local-yield shuttle claim the middle aisle before it can move', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'module-01-spine-top-b',
      'module-01-spine-middle',
      'column-middle-c07',
      'storage-r07-c07'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r06-c07',
      'storage-r07-c07',
      'column-middle-c07',
      'module-01-spine-middle',
      'column-middle-c08',
      'storage-r07-c08'
    ]);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'storage-r07-c07',
      waitReason: 'node-local-yield',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[] }>;
      agentRefreshMoveBlocker(
        vehicle: unknown,
        toNodeId: string,
        candidateRouteNodeIds: string[] | null
      ): { reason: string; blockingVehicleId: string | null } | null;
    };
    const topBEntrant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentRefreshMoveBlocker(
      topBEntrant,
      'module-01-spine-middle',
      topBEntrant.routeNodeIds
    )).toBeNull();
  });

  it('lets an adjacent middle-aisle swap yielder clear through a claimed node into a storage pocket', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'block-c04-r07', state: 'stored', nodeId: 'storage-r07-c04', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'block-c04-r08', state: 'stored', nodeId: 'storage-r08-c04', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'block-c05-r07', state: 'stored', nodeId: 'storage-r07-c05', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'block-c05-r08', state: 'stored', nodeId: 'storage-r08-c05', vehicleId: null, weightKg: 100 });
    sim.setVehicleRouteForTest('SH-07', ['column-middle-c04', 'column-middle-c03']);
    sim.setVehicleWaitingForTest('SH-07', {
      targetNodeId: 'column-middle-c03',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-08',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-08', ['column-middle-c03', 'column-middle-c04', 'column-middle-c05', 'column-middle-c06']);
    sim.setVehicleTaskForTest('SH-08', null, true);
    sim.setVehicleWaitingForTest('SH-08', {
      targetNodeId: 'column-middle-c04',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-07',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null; targetNodeId: string | null }>;
      tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(first: unknown, second: unknown): { id: string } | null;
    };
    const yielder = internals.vehicles.find((candidate) => candidate.id === 'SH-07')!;
    const winner = internals.vehicles.find((candidate) => candidate.id === 'SH-08')!;
    const installedYielder = internals.tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(yielder, winner);

    expect(installedYielder?.id).toBe('SH-07');
    expect(yielder.routeNodeIds).toEqual(['column-middle-c04', 'column-middle-c05', 'column-middle-c06', 'storage-r07-c06']);
    expect(yielder.targetNodeId).toBe('column-middle-c05');
    expect(yielder.localRouteReason).toBe('temporary-yield');
  });

  it('lets an empty outbound middle-aisle swap yielder clear the loaded outbound shuttle path', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    addStoredLoads(sim, ['storage-r07-c06', 'storage-r08-c06']);
    sim.addLoadForTest({ id: 'middle-swap-loaded-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addLoadForTest({ id: 'middle-swap-empty-outbound-load', state: 'stored', nodeId: 'storage-r04-c04', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'middle-swap-loaded-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c02',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'middle-swap-loaded-outbound-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'middle-swap-empty-outbound',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r04-c04',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'middle-swap-empty-outbound-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'column-middle-c05',
      'column-middle-c06',
      'column-middle-c07',
      'module-01-spine-middle',
      'module-01-spine-bottom-a',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'middle-swap-loaded-outbound', true);
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: 'column-middle-c06',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-04',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-04', [
      'column-middle-c06',
      'column-middle-c05',
      'column-middle-c04',
      'storage-r07-c04',
      'storage-r06-c04',
      'storage-r05-c04',
      'storage-r04-c04'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'middle-swap-empty-outbound', false);
    sim.setVehicleWaitingForTest('SH-04', {
      targetNodeId: 'column-middle-c05',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-06',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        localRouteReason: string | null;
        targetNodeId: string | null;
        waitReason: string | null;
      }>;
      tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(first: unknown, second: unknown): { id: string } | null;
    };
    const emptyOutbound = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;
    const loadedOutbound = internals.vehicles.find((candidate) => candidate.id === 'SH-06')!;
    const installedYielder = internals.tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(emptyOutbound, loadedOutbound);

    expect(installedYielder?.id).toBe('SH-04');
    expect(emptyOutbound.routeNodeIds[0]).toBe('column-middle-c06');
    expect(emptyOutbound.routeNodeIds[1]).toBe('column-middle-c07');
    expect(emptyOutbound.routeNodeIds).not.toContain('column-middle-c05');
    expect(emptyOutbound.routeNodeIds.at(-1)).toMatch(/^storage-r(?:07|08)-c07$/);
    expect(emptyOutbound.targetNodeId).toBe('column-middle-c07');
    expect(emptyOutbound.waitReason).toBeNull();
    expect(emptyOutbound.localRouteReason).toBe('temporary-yield');
    expectRouteHopsAdjacent(scenario, emptyOutbound.routeNodeIds);
  });

  it('lets an empty outbound middle-aisle swap yielder continue to the nearest open pocket when local pockets are full', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    addStoredLoads(sim, Array.from({ length: 10 }, (_, columnIndex) => columnIndex + 5).flatMap((column) =>
      Array.from({ length: 14 }, (_, rowIndex) => rowIndex + 1).map((row) =>
        `storage-r${String(row).padStart(2, '0')}-c${String(column).padStart(2, '0')}`
      )
    ));
    sim.addLoadForTest({ id: 'long-swap-loaded-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addLoadForTest({ id: 'long-swap-empty-outbound-load', state: 'stored', nodeId: 'storage-r04-c04', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'long-swap-upstream-outbound-load', state: 'stored', nodeId: 'storage-r04-c03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'long-swap-loaded-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c02',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'long-swap-loaded-outbound-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'long-swap-empty-outbound',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r04-c04',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'long-swap-empty-outbound-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'long-swap-upstream-outbound',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r04-c03',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'long-swap-upstream-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-boundary-01-spine-bottom-a',
      'module-boundary-01-spine-middle',
      'column-middle-c14',
      'column-middle-c13',
      'column-middle-c12',
      'column-middle-c11',
      'column-middle-c10',
      'column-middle-c09',
      'column-middle-c08',
      'module-01-spine-middle',
      'column-middle-c07',
      'column-middle-c06',
      'column-middle-c05',
      'column-middle-c04',
      'column-middle-c03',
      'storage-r07-c03',
      'storage-r06-c03',
      'storage-r05-c03',
      'storage-r04-c03'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'long-swap-upstream-outbound', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-boundary-01-spine-middle',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-06',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-06', [
      'column-middle-c05',
      'column-middle-c06',
      'column-middle-c07',
      'module-01-spine-middle',
      'module-01-spine-bottom-a',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'long-swap-loaded-outbound', true);
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: 'column-middle-c06',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-04',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-04', [
      'column-middle-c06',
      'column-middle-c05',
      'column-middle-c04',
      'storage-r07-c04',
      'storage-r06-c04',
      'storage-r05-c04',
      'storage-r04-c04'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'long-swap-empty-outbound', false);
    sim.setVehicleWaitingForTest('SH-04', {
      targetNodeId: 'column-middle-c05',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-06',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        localRouteReason: string | null;
        targetNodeId: string | null;
        waitReason: string | null;
      }>;
      tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(first: unknown, second: unknown): { id: string } | null;
    };
    const emptyOutbound = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;
    const loadedOutbound = internals.vehicles.find((candidate) => candidate.id === 'SH-06')!;
    const installedYielder = internals.tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(emptyOutbound, loadedOutbound);

    expect(installedYielder?.id).toBe('SH-04');
    expect(emptyOutbound.routeNodeIds[0]).toBe('column-middle-c06');
    expect(emptyOutbound.routeNodeIds[1]).toBe('column-middle-c07');
    expect(emptyOutbound.routeNodeIds).not.toContain('column-middle-c05');
    expect(emptyOutbound.routeNodeIds.length).toBeGreaterThan(8);
    expect(emptyOutbound.routeNodeIds.at(-1)).toMatch(/^storage-r(?:07|08)-c15$/);
    expect(emptyOutbound.targetNodeId).toBe('column-middle-c07');
    expect(emptyOutbound.waitReason).toBeNull();
    expect(emptyOutbound.localRouteReason).toBe('temporary-yield');
    expectRouteHopsAdjacent(scenario, emptyOutbound.routeNodeIds);
  });

  it('retreats a loaded inbound middle faceoff along the yellow line when storage pockets are full', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    addStoredLoads(sim, [
      'storage-r05-c05',
      'storage-r06-c05',
      'storage-r07-c05',
      'storage-r08-c05',
      'storage-r05-c06',
      'storage-r06-c06',
      'storage-r07-c06',
      'storage-r08-c06',
      'storage-r05-c07',
      'storage-r06-c07',
      'storage-r07-c07',
      'storage-r08-c07',
      'storage-r07-c08',
      'storage-r08-c08'
    ]);
    sim.addLoadForTest({ id: 'loaded-middle-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'loaded-middle-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-middle-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r07-c04',
      loadId: 'loaded-middle-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'loaded-middle-outbound-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c04',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'loaded-middle-outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c06', 'column-middle-c05', 'column-middle-c04', 'storage-r07-c04']);
    sim.setVehicleTaskForTest('SH-01', 'loaded-middle-inbound-task', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-middle-c05',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c05',
      'column-middle-c06',
      'column-middle-c07',
      'module-01-spine-middle',
      'module-01-spine-bottom-a',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'loaded-middle-outbound-task', true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c06',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null; targetNodeId: string | null }>;
      tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(first: unknown, second: unknown): { id: string } | null;
    };
    const inbound = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const outbound = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;
    const installedYielder = internals.tryInstallAgentRefreshTopLiftAdjacentNodeSwapYieldForPair(inbound, outbound);

    expect(installedYielder?.id).toBe('SH-01');
    expect(inbound.routeNodeIds).toEqual([
      'column-middle-c06',
      'column-middle-c07',
      'module-01-spine-middle',
      'column-middle-c08'
    ]);
    expect(inbound.targetNodeId).toBe('column-middle-c07');
    expect(inbound.localRouteReason).toBe('loaded-middle-faceoff-retreat');
  });

  it('retreats a loaded inbound column-entry blocker to a legal hold instead of a no-stop spine', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'retreat-carried-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'retreat-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r13-c15',
      loadId: 'retreat-carried-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c15', 'storage-r08-c15']);
    sim.setVehicleTaskForTest('SH-01', 'retreat-inbound-task', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r08-c15', 'column-middle-c15']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        yieldHoldNodeId: string | null;
        localRouteReason: string | null;
      }>;
      layoutNode(nodeId: string): { noStop?: boolean; noParking?: boolean; type: string } | null;
      tryRetreatLoadedInboundColumnEntryForTasklessExit(loadedVehicle: unknown, blocker: unknown): boolean;
    };
    const loadedVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.tryRetreatLoadedInboundColumnEntryForTasklessExit(loadedVehicle, blocker)).toBe(true);
    const terminalNode = internals.layoutNode(loadedVehicle.routeNodeIds.at(-1)!);

    expect(loadedVehicle.localRouteReason).toBe('inbound-column-entry-retreat');
    expect(loadedVehicle.targetNodeId).not.toMatch(/spine-middle$/);
    expect(terminalNode?.noStop).not.toBe(true);
    expect(terminalNode?.noParking).not.toBe(true);
    expect(terminalNode?.type).toBe('storage');
    expect(loadedVehicle.yieldHoldNodeId).toBe(loadedVehicle.routeNodeIds.at(-1));
  });

  it('retreats a loaded inbound column entry into the opposite bank when an empty assigned shuttle must exit first', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'entry-retreat-carried-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'later-entry-retreat-load', state: 'carried', nodeId: null, vehicleId: 'SH-03', weightKg: 100 });
    sim.addLoadForTest({ id: 'empty-exit-assigned-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'entry-retreat-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c23',
      dropoffNodeId: 'storage-r10-c15',
      loadId: 'entry-retreat-carried-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-entry-retreat-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c23',
      dropoffNodeId: 'storage-r07-c15',
      loadId: 'later-entry-retreat-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'empty-exit-assigned-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 2,
      assignedAtSec: 2,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c02',
      loadId: 'empty-exit-assigned-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c15', 'storage-r08-c15', 'storage-r09-c15', 'storage-r10-c15']);
    sim.setVehicleTaskForTest('SH-01', 'entry-retreat-inbound-task', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r08-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['storage-r08-c15', 'column-middle-c15', 'column-middle-c16']);
    sim.setVehicleTaskForTest('SH-02', 'empty-exit-assigned-task', false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-03', ['column-top-b-c16', 'column-top-b-c15', 'storage-r01-c15']);
    sim.setVehicleTaskForTest('SH-03', 'later-entry-retreat-inbound-task', true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
      }>;
      tryRetreatLoadedInboundColumnEntryForTasklessExit(loadedVehicle: unknown, blocker: unknown): boolean;
    };
    const loadedVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.tryRetreatLoadedInboundColumnEntryForTasklessExit(loadedVehicle, blocker)).toBe(true);
    expect(loadedVehicle.localRouteReason).toBe('inbound-column-entry-retreat');
    expect(loadedVehicle.routeNodeIds).toEqual(['column-middle-c15', 'storage-r07-c15']);
    expect(loadedVehicle.targetNodeId).toBe('storage-r07-c15');
  });

  it('lets the physically leading inbound queue shuttle proceed when an earlier task is behind it', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'physically-behind-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'physically-leading-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'physically-behind-earlier-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c08',
      loadId: 'physically-behind-earlier-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'physically-leading-later-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r06-c06',
      loadId: 'physically-leading-later-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', ['column-top-a-c11', 'column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-07', 'physically-behind-earlier-task', false);
    sim.setVehicleWaitingForTest('SH-07', {
      targetNodeId: 'column-top-a-c10',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-06', ['column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-06', 'physically-leading-later-task', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      inboundTaskHasEarlierPickupTask(task: unknown): boolean;
      topLiftInboundQueueHoldActive(task: unknown, vehicle: unknown): boolean;
      topLiftInboundQueueHoldReason(task: unknown): string;
      vehicles: Array<{ id: string }>;
    };
    const laterTask = internals.tasks.find((task) => task.id === 'physically-leading-later-task')!;
    const laterVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;

    expect(internals.inboundTaskHasEarlierPickupTask(laterTask)).toBe(false);
    expect(internals.topLiftInboundQueueHoldActive(laterTask, laterVehicle)).toBe(true);
    expect(internals.topLiftInboundQueueHoldReason(laterTask)).not.toBe('inbound-lift-fifo-wait');
  });

  it('lets a top-queued inbound shuttle pass an earlier task still below the lift approach', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        sourceBufferCapacity: 4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'lower-approach-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'top-queued-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'lower-approach-earlier-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c11',
      loadId: 'lower-approach-earlier-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'top-queued-later-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c12',
      loadId: 'top-queued-later-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: 'inbound-lift-fifo-wait'
    });
    sim.setVehicleRouteForTest('SH-01', ['module-01-spine-bottom-a', 'module-01-spine-middle']);
    sim.setVehicleTaskForTest('SH-01', 'lower-approach-earlier-task', false);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-02', 'top-queued-later-task', false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-top-a-c09',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      loads: Array<{ id: string; nodeId: string | null }>;
      inboundTaskHasEarlierPickupTask(task: unknown): boolean;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
    };
    const laterTask = internals.tasks.find((task) => task.id === 'top-queued-later-task')!;
    const laterVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.inboundTaskHasEarlierPickupTask(laterTask)).toBe(false);
    expect(internals.inboundTaskShouldWaitBeforePickup(laterTask)).toBe(false);
    expect(internals.inboundPickupDispatchGoalNodeId(laterTask, laterVehicle)).toBe('column-top-a-c08');
    expect(internals.loads.find((load) => load.id === 'top-queued-later-load')?.nodeId).toBe('lift-01-inbound-buffer-03');
    expect(internals.loads.find((load) => load.id === 'lower-approach-earlier-load')?.nodeId).toBe('lift-01-inbound-buffer-02');
  });

  it('moves a later inbound requester off the pickup no-stop node while it waits for an earlier queue shuttle', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'earlier-queue-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'later-requester-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'earlier-queue-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r06-c06',
      loadId: 'earlier-queue-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-requester-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r05-c06',
      loadId: 'later-requester-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', ['parking-lift-01-inbound-queue-02']);
    sim.setVehicleTaskForTest('SH-06', 'earlier-queue-task', false);
    sim.setVehicleRouteForTest('SH-03', ['column-top-b-c07', 'parking-lift-01-inbound-queue-03']);
    sim.setVehicleTaskForTest('SH-03', 'later-requester-task', false);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'parking-lift-01-inbound-queue-03',
      waitReason: 'avoidance-clearance',
      blockingVehicleId: 'SH-06',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
      }>;
      tryYieldInboundQueueRequesterForEarlierPickup(requester: unknown, blocker: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;

    expect(internals.tryYieldInboundQueueRequesterForEarlierPickup(requester, blocker, 'parking-lift-01-inbound-queue-03')).toBe(true);
    expect(requester.localRouteReason).toBe('temporary-yield');
    expect(requester.routeNodeIds).toEqual(['column-top-b-c07', 'storage-r01-c07']);
    expect(requester.targetNodeId).toBe('storage-r01-c07');
  });

  it('clears an unready projected inbound pickup onto a legal yellow hold node', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'front-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'later-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'storage-pocket-blocker-load', state: 'stored', nodeId: 'storage-r01-c07', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'front-source-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c08',
      loadId: 'front-source-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-source-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r12-c08',
      loadId: 'later-source-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', ['lift-01-inbound-queue-02-access']);
    sim.setVehicleTaskForTest('SH-08', 'front-source-task', false);
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-b-c07',
      'parking-lift-01-inbound-queue-03'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'later-source-task', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      inboundTaskLoadReadyAtPickup(task: unknown): boolean;
    };
    const laterTask = internals.tasks.find((task) => task.id === 'later-source-task')!;
    expect(internals.inboundTaskLoadReadyAtPickup(laterTask)).toBe(false);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-02');

    expect(vehicle?.waitReason).not.toBe('inbound-source-not-at-pickup');
    expect(vehicle?.targetNodeId).toBe('module-01-spine-top-b');
    expect(vehicle?.routeNodeIds.slice(0, 2)).toEqual(['column-top-b-c07', 'module-01-spine-top-b']);
    expect(vehicle?.routeNodeIds).toContain('parking-lift-01-inbound-queue-03');
  });

  it('lets a top-b requester yield while a front inbound queue access clears into parking', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'front-queue-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'outbound-cross-load', state: 'stored', nodeId: 'storage-r03-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'front-queue-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r13-c05',
      loadId: 'front-queue-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'outbound-cross-task',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c01',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'outbound-cross-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'lift-01-inbound-queue-01-access',
      'parking-lift-01-inbound-queue'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'front-queue-task', false);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'parking-lift-01-inbound-queue',
      waitReason: 'projected-display-clearance',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'outbound-cross-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-b-c08',
      waitReason: 'avoidance-clearance',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
      }>;
      tryYieldTopBRequesterAwayFromInboundQueueTail(requester: unknown, queueYielder: unknown, blockedTargetNodeId: string): boolean;
    };
    const queueYielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.tryYieldTopBRequesterAwayFromInboundQueueTail(requester, queueYielder, 'column-top-b-c08')).toBe(true);
    expect(requester.localRouteReason).toBe('temporary-yield');
    expect(requester.routeNodeIds).toEqual(['column-top-b-c09', 'column-top-a-c09']);
    expect(requester.targetNodeId).toBe('column-top-a-c09');
  });

  it('uses a storage pocket when top-a is occupied and a front inbound queue access must clear', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'front-queue-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'requester-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'front-queue-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r09-c01',
      loadId: 'front-queue-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'requester-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r10-c01',
      loadId: 'requester-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', [
      'lift-01-inbound-queue-01-access',
      'parking-lift-01-inbound-queue'
    ]);
    sim.setVehicleTaskForTest('SH-07', 'front-queue-task', false);
    sim.setVehicleWaitingForTest('SH-07', {
      targetNodeId: 'parking-lift-01-inbound-queue',
      waitReason: 'projected-display-clearance',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'requester-task', false);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'column-top-b-c08',
      waitReason: 'avoidance-clearance',
      blockingVehicleId: 'SH-07',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c09']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
      }>;
      tryYieldTopBRequesterAwayFromInboundQueueTail(requester: unknown, queueYielder: unknown, blockedTargetNodeId: string): boolean;
    };
    const queueYielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;

    expect(internals.tryYieldTopBRequesterAwayFromInboundQueueTail(requester, queueYielder, 'column-top-b-c08')).toBe(true);
    expect(requester.localRouteReason).toBe('temporary-yield');
    expect(requester.routeNodeIds).toEqual(['column-top-b-c09', 'storage-r01-c09']);
    expect(requester.targetNodeId).toBe('storage-r01-c09');
  });

  it('moves a top-b entrant aside when a front inbound queue access clears into parking', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'front-queue-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'entry-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'front-queue-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r08-c02',
      loadId: 'front-queue-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'entry-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r07-c02',
      loadId: 'entry-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-inbound-queue-01-access',
      'parking-lift-01-inbound-queue'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'front-queue-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'parking-lift-01-inbound-queue',
      waitReason: 'projected-display-clearance',
      blockingVehicleId: 'SH-04',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-04', [
      'column-top-b-c09',
      'lift-01-inbound-queue-01-entry-access',
      'lift-01-inbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'entry-task', false);
    sim.setVehicleWaitingForTest('SH-04', {
      targetNodeId: 'lift-01-inbound-queue-01-entry-access',
      waitReason: 'projected-display-clearance',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
      }>;
      tryYieldTopBRequesterAwayFromInboundQueueTail(requester: unknown, queueYielder: unknown, blockedTargetNodeId: string): boolean;
    };
    const queueYielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-04')!;

    expect(internals.tryYieldTopBRequesterAwayFromInboundQueueTail(requester, queueYielder, 'lift-01-inbound-queue-01-entry-access')).toBe(true);
    expect(requester.localRouteReason).toBe('temporary-yield');
    expect(requester.routeNodeIds).toEqual(['column-top-b-c09', 'column-top-a-c09']);
    expect(requester.targetNodeId).toBe('column-top-a-c09');
  });

  it('does not make an earlier top-b pickup requester yield to a FIFO-held inbound queue vehicle', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'earlier-pickup-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'queued-fifo-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'earlier-pickup-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r03-c03',
      loadId: 'earlier-pickup-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'queued-fifo-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r02-c03',
      loadId: 'queued-fifo-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'earlier-pickup-task', false);
    sim.setVehicleRouteForTest('SH-03', ['parking-lift-01-inbound-queue']);
    sim.setVehicleTaskForTest('SH-03', 'queued-fifo-task', false);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'parking-lift-01-inbound-queue',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
      }>;
      tryYieldTopBRequesterAwayFromInboundQueueTail(requester: unknown, queueYielder: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;
    const queueYielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;

    expect(internals.tryYieldTopBRequesterAwayFromInboundQueueTail(requester, queueYielder, 'column-top-b-c09')).toBe(false);
    expect(requester.localRouteReason).toBeNull();
    expect(requester.routeNodeIds.slice(0, 2)).toEqual(['column-top-b-c10', 'column-top-b-c09']);
    expect(requester.targetNodeId).toBe('column-top-b-c09');
  });

  it('treats an inbound queue parking vehicle moving deeper as clearing the front slot', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'queued-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queued-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r08-c02',
      loadId: 'queued-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'cross-task',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r11-c01',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'cross-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', [
      'parking-lift-01-inbound-queue',
      'parking-lift-01-inbound-queue-02'
    ]);
    sim.setVehicleTaskForTest('SH-07', 'queued-task', false);
    sim.setVehicleWaitingForTest('SH-07', {
      targetNodeId: 'parking-lift-01-inbound-queue-02',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c10',
      'column-top-a-c10'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'cross-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      topLiftInboundQueueCompressingVehicle(vehicle: unknown): boolean;
      topLiftInboundQueueClearingVehicle(vehicle: unknown): boolean;
      topLiftInboundQueueTailYieldVehicle(vehicle: unknown): boolean;
      createAgentRefreshConflictSession(
        requester: unknown,
        blocker: unknown,
        blockedTargetNodeId: string,
        reason: string
      ): { winnerVehicleId: string; yielderVehicleId: string };
    };
    const queueVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;
    const crossTraffic = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.topLiftInboundQueueCompressingVehicle(queueVehicle)).toBe(true);
    expect(internals.topLiftInboundQueueClearingVehicle(queueVehicle)).toBe(true);
    expect(internals.topLiftInboundQueueTailYieldVehicle(queueVehicle)).toBe(true);

    const session = internals.createAgentRefreshConflictSession(
      queueVehicle,
      crossTraffic,
      'parking-lift-01-inbound-queue-02',
      'projected-display-clearance'
    );
    expect(session.winnerVehicleId).toBe('SH-07');
    expect(session.yielderVehicleId).toBe('SH-01');
  });

  it('advances a top-a pickup hold sideways instead of returning into an occupied pickup', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'front-c23-source-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'middle-c23-source-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'later-c23-source-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'front-c23-source-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c23',
      dropoffNodeId: 'storage-r04-c15',
      loadId: 'middle-c23-source-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-c23-source-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c23',
      dropoffNodeId: 'storage-r03-c15',
      loadId: 'later-c23-source-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', ['column-top-b-c23']);
    sim.setVehicleTaskForTest('SH-07', 'front-c23-source-task', false);
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-a-c23',
      'column-top-b-c23',
      'lift-02-inbound-queue-03-entry-access',
      'lift-02-inbound-queue-03-access'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'later-c23-source-task', false);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-02');

    expect(vehicle?.waitReason).not.toBe('node-occupied');
    expect(vehicle?.targetNodeId).toBe('column-top-a-c22');
    expect(vehicle?.routeNodeIds).toEqual(['column-top-a-c23', 'column-top-a-c22']);
  });

  it('lets a top-a pickup blocker continue forward before the top-b follower enters its node', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'top-a-forward-blocker-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'top-a-forward-blocker-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r06-c16',
      loadId: 'top-a-forward-blocker-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'top-b-follower-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'top-b-follower-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r08-c16',
      loadId: 'top-b-follower-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', ['column-top-a-c22', 'column-top-a-c23', 'column-top-a-c24']);
    sim.setVehicleTaskForTest('SH-07', 'top-a-forward-blocker-task', false);
    sim.setVehicleRouteForTest('SH-08', ['column-top-b-c22', 'column-top-a-c22']);
    sim.setVehicleTaskForTest('SH-08', 'top-b-follower-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const follower = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;

    expect(internals.agentRefreshHandleMoveBlock(follower, 'column-top-a-c22', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-07'
    })).toBe(false);
    expect(blocker.targetNodeId).toBe('column-top-a-c23');
    expect(blocker.routeNodeIds).toEqual(['column-top-a-c22', 'column-top-a-c23', 'column-top-a-c24']);
    expect(blocker.localRouteNodeIds).toEqual([]);
    expect(blocker.localRouteReason).toBeNull();
    expect(sim.getState().traffic.conflictSessions).toEqual([]);
  });

  it('moves an empty middle access blocker into storage before a loaded spine entrant', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-spine-entrant-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-spine-entrant-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r08-c28',
      loadId: 'loaded-spine-entrant-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c22',
      'column-middle-c23',
      'column-middle-c24',
      'column-middle-c25',
      'column-middle-c26',
      'column-middle-c27',
      'column-middle-c28',
      'storage-r08-c28'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'loaded-spine-entrant-task', true);
    sim.setVehicleRouteForTest('SH-08', [
      'column-middle-c22',
      'module-02-spine-middle',
      'module-02-spine-top-b',
      'column-top-b-c22',
      'column-top-b-c23',
      'column-top-b-c24',
      'column-top-b-c25',
      'column-top-a-c25'
    ]);
    sim.setVehicleTaskForTest('SH-08', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      tryYieldEmptyMiddleAccessHorizontallyForLoadedSpine(
        requester: unknown,
        blocker: unknown,
        blockedTargetNodeId: string
      ): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;

    expect(internals.tryYieldEmptyMiddleAccessHorizontallyForLoadedSpine(
      requester,
      blocker,
      'module-02-spine-middle'
    )).toBe(true);
    expect(blocker.targetNodeId).toMatch(/^storage-r0[78]-c22$/);
    expect(blocker.routeNodeIds).toEqual(['column-middle-c22', blocker.targetNodeId]);
    expect(blocker.localRouteReason).toBe('temporary-yield');
    expect(blocker.waitReason).toBeNull();
  });

  it('does not pre-stage a taskless inbound standby through a projected loaded inbound route', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'projected-loaded-corridor-load',
      state: 'waiting',
      nodeId: 'lift-02-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'projected-loaded-corridor-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r09-c27',
      loadId: 'projected-loaded-corridor-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', [
      'column-top-a-c21',
      'module-02-spine-top-a',
      'column-top-a-c22'
    ]);
    sim.setVehicleTaskForTest('SH-05', 'projected-loaded-corridor-task', false);
    sim.setVehicleRouteForTest('SH-08', ['storage-r07-c28']);
    sim.setVehicleTaskForTest('SH-08', null, false);

    const conflictRoute = [
      'storage-r07-c28',
      'column-middle-c28',
      'column-middle-c27',
      'column-middle-c26',
      'column-middle-c25',
      'column-middle-c24',
      'column-middle-c23',
      'column-middle-c22',
      'module-02-spine-middle',
      'module-02-spine-top-b',
      'column-top-b-c22',
      'column-top-b-c23',
      'column-top-b-c24',
      'column-top-b-c25',
      'column-top-a-c25'
    ];
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        routeNodeIds: string[];
      }>;
      tasklessInboundQueueStandbyRouteAllowed(vehicle: unknown, routeNodeIds: string[]): boolean;
      dispatchTasklessInboundQueueStandbys(): void;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-08')!;

    expect(internals.tasklessInboundQueueStandbyRouteAllowed(vehicle, conflictRoute)).toBe(false);

    internals.dispatchTasklessInboundQueueStandbys();
    const dispatchedVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-08')!;

    expect(dispatchedVehicle.plannedGoalNodeId).not.toBe('column-top-a-c25');
    expect(dispatchedVehicle.routeNodeIds).not.toEqual(conflictRoute);
  });

  it('continues an active inbound pickup clearance route instead of pinning the pickup node', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'pickup-clearance-earlier-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-03',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'pickup-clearance-earlier-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r08-c14',
      loadId: 'pickup-clearance-earlier-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({
      id: 'pickup-clearance-later-load',
      state: 'waiting',
      nodeId: 'lift-01-inbound-buffer-02',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'pickup-clearance-later-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r06-c14',
      loadId: 'pickup-clearance-later-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', ['column-top-b-c08']);
    sim.setVehicleTaskForTest('SH-08', 'pickup-clearance-earlier-task', false);
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-a-c08',
      'column-top-a-c09',
      'column-top-a-c10',
      'column-top-a-c11'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'pickup-clearance-later-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentEdgeId: string | null;
        currentNodeId: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      startNextLegAgentSimple(vehicle: unknown, dtSec: number): void;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;
    vehicle.plannedGoalNodeId = 'column-top-a-c11';
    vehicle.plannedRouteNodeIds = [
      'column-top-a-c08',
      'column-top-a-c09',
      'column-top-a-c10',
      'column-top-a-c11'
    ];
    vehicle.localRouteNodeIds = [...vehicle.plannedRouteNodeIds];
    vehicle.localRouteReason = 'temporary-yield';

    internals.startNextLegAgentSimple(vehicle, 0.2);

    expect(vehicle.currentNodeId).toBe('column-top-a-c08');
    expect(vehicle.targetNodeId).toBe('column-top-a-c09');
    expect(vehicle.currentEdgeId).not.toBeNull();
    expect(vehicle.waitReason).toBeNull();
  });

  it('does not force an inbound column successor into internal parking when projected queue is unsafe', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'successor-c05-r06',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r06-c05',
      loadId: 'successor-c05-r06-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'predecessor-c05-r07',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 11,
      assignedAtSec: 11,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r07-c05',
      loadId: 'predecessor-c05-r07-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['parking-lift-01-inbound-queue']);
    sim.setVehicleTaskForTest('SH-01', 'successor-c05-r06', false);
    sim.setVehicleRouteForTest('SH-04', [
      'column-top-a-c09',
      'column-top-b-c09',
      'lift-01-inbound-queue-01-entry-access',
      'lift-01-inbound-queue-01-access',
      'lift-01-inbound-queue-02-access',
      'lift-01-inbound-queue-03-access',
      'parking-lift-01-inbound-queue-03'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'predecessor-c05-r07', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      inboundTaskBlocksLiftPickupOrder(candidate: unknown, task: unknown): boolean;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
    };
    const successorTask = internals.tasks.find((task) => task.id === 'successor-c05-r06')!;
    const successorVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.inboundTaskBlocksLiftPickupOrder(
      internals.tasks.find((task) => task.id === 'predecessor-c05-r07')!,
      successorTask
    )).toBe(false);
    expect(internals.topLiftInboundQueueNodeIdForTask(successorTask, successorVehicle)).toBeNull();

    const state = sim.step(0.2);
    const successor = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(successor?.targetNodeId).toBe('parking-lift-01-inbound-queue');
    expect(successor?.plannedGoalNodeId).not.toBe('parking-lift-01-inbound-queue-02');
    expect(successor?.routeNodeIds.at(-1)).not.toBe('parking-lift-01-inbound-queue-02');
  });

  it('keeps a deep inbound queue vehicle from shifting back into a shallower queue slot', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'front-queue-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r04-c01',
      loadId: 'front-queue-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'deep-queue-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 11,
      assignedAtSec: 11,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r03-c01',
      loadId: 'deep-queue-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['parking-lift-01-inbound-queue']);
    sim.setVehicleTaskForTest('SH-01', 'front-queue-task', false);
    sim.setVehicleRouteForTest('SH-02', ['parking-lift-01-inbound-queue-03']);
    sim.setVehicleTaskForTest('SH-02', 'deep-queue-task', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; plannedRouteNodeIds: string[] }>;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
    };
    const deepTask = internals.tasks.find((task) => task.id === 'deep-queue-task')!;
    const deepVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    deepVehicle.plannedGoalNodeId = 'parking-lift-01-inbound-queue-02';
    deepVehicle.plannedRouteNodeIds = [
      'parking-lift-01-inbound-queue-03',
      'lift-01-inbound-queue-03-access',
      'lift-01-inbound-queue-02-access',
      'parking-lift-01-inbound-queue-02'
    ];

    expect(internals.inboundPickupDispatchGoalNodeId(deepTask, deepVehicle)).toBe('parking-lift-01-inbound-queue-03');
  });

  it('moves a top-a inbound hold sideways when a parked inbound queue vehicle blocks the return to top-b', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'top-a-return-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r03-c03',
      loadId: 'top-a-return-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['parking-lift-01-inbound-queue-03']);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-a-c11',
      'column-top-b-c11',
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'top-a-return-task', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; localRouteReason: string | null; routeNodeIds: string[]; targetNodeId: string | null; waitReason: string | null }>;
      tryAdvanceTopAHoldAwayFromQueueBlocker(
        vehicle: unknown,
        task: unknown,
        blockedTopBNodeId: string,
        blockingVehicleId: string | null
      ): boolean;
    };
    const task = internals.tasks.find((entry) => entry.id === 'top-a-return-task')!;
    const vehicle = internals.vehicles.find((entry) => entry.id === 'SH-03')!;
    const blocker = internals.vehicles.find((entry) => entry.id === 'SH-02')!;
    blocker.waitReason = 'inbound-lift-fifo-wait';

    expect(internals.tryAdvanceTopAHoldAwayFromQueueBlocker(vehicle, task, 'column-top-b-c11', 'SH-02')).toBe(true);
    expect(vehicle.targetNodeId).toBe('column-top-a-c12');
    expect(vehicle.routeNodeIds).toEqual(['column-top-a-c11', 'column-top-a-c12']);
    expect(vehicle.localRouteReason).toBe('temporary-yield');
  });

  it('moves a top-a outbound hold sideways when an inbound queue footprint blocks the return to top-b', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'outbound-top-a-return-task',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c01',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'outbound-top-a-return-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', ['parking-lift-01-inbound-queue-03']);
    sim.setVehicleTaskForTest('SH-07', null, false);
    sim.setVehicleRouteForTest('SH-03', [
      'storage-r01-c11',
      'column-top-b-c11',
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-03', null, false);
    sim.setVehicleRouteForTest('SH-05', ['column-top-a-c12', 'column-top-b-c12']);
    sim.setVehicleTaskForTest('SH-05', null, false);
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-a-c11',
      'column-top-b-c11',
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07',
      'storage-r07-c01'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'outbound-top-a-return-task', false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; localRouteReason: string | null; routeNodeIds: string[]; targetNodeId: string | null; waitReason: string | null }>;
      tryAdvanceTopAHoldAwayFromQueueBlocker(
        vehicle: unknown,
        task: unknown,
        blockedTopBNodeId: string,
        blockingVehicleId: string | null
      ): boolean;
    };
    const task = internals.tasks.find((entry) => entry.id === 'outbound-top-a-return-task')!;
    const vehicle = internals.vehicles.find((entry) => entry.id === 'SH-01')!;
    const blocker = internals.vehicles.find((entry) => entry.id === 'SH-07')!;
    blocker.waitReason = 'inbound-lift-fifo-wait';

    expect(internals.tryAdvanceTopAHoldAwayFromQueueBlocker(vehicle, task, 'column-top-b-c11', 'SH-03')).toBe(true);
    expect(vehicle.targetNodeId).toBe('column-top-a-c10');
    expect(vehicle.routeNodeIds).toEqual(['column-top-a-c11', 'column-top-a-c10']);
    expect(vehicle.localRouteReason).toBe('temporary-yield');
  });

  it('reroutes a storage exit around a parked inbound queue blocker instead of waiting at top-b', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'storage-exit-pickup-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r08-c02',
      loadId: 'storage-exit-pickup-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'parked-queue-fifo-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 9,
      assignedAtSec: 9,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r07-c02',
      loadId: 'parked-queue-fifo-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', ['parking-lift-01-inbound-queue-03']);
    sim.setVehicleTaskForTest('SH-07', 'parked-queue-fifo-task', false);
    sim.setVehicleRouteForTest('SH-05', [
      'storage-r01-c11',
      'column-top-b-c11',
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-05', 'storage-exit-pickup-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; localRouteReason: string | null; routeNodeIds: string[]; targetNodeId: string | null; waitReason: string | null }>;
      tryRerouteStorageExitAroundParkedInboundQueueBlocker(requester: unknown, blocker: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((entry) => entry.id === 'SH-05')!;
    const blocker = internals.vehicles.find((entry) => entry.id === 'SH-07')!;
    blocker.waitReason = 'inbound-lift-fifo-wait';

    expect(internals.tryRerouteStorageExitAroundParkedInboundQueueBlocker(requester, blocker, 'column-top-b-c11')).toBe(true);
    expect(requester.targetNodeId).not.toBe('column-top-b-c11');
    expect(requester.routeNodeIds[0]).toBe('storage-r01-c11');
    expect(requester.routeNodeIds[1]).not.toBe('column-top-b-c11');
    expect(requester.localRouteReason).toBe('temporary-yield');
  });

  it('routes a top-b inbound requester over top-a around a parked inbound queue blocker', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'right-side-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c21',
      dropoffNodeId: 'storage-r14-c16',
      loadId: 'right-side-pickup-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'parked-queue-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 9,
      assignedAtSec: 9,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r12-c03',
      loadId: 'parked-queue-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', ['parking-lift-01-inbound-queue-03']);
    sim.setVehicleTaskForTest('SH-05', 'parked-queue-task', false);
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c10',
      'column-top-b-c11',
      'column-top-b-c12',
      'column-top-b-c13',
      'column-top-b-c14',
      'module-boundary-01-spine-top-b',
      'column-top-b-c15',
      'column-top-b-c16',
      'column-top-b-c17',
      'column-top-b-c18',
      'column-top-b-c19',
      'column-top-b-c20',
      'column-top-b-c21'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'right-side-pickup', false);
    const internals = sim as unknown as {
      vehicles: Array<{ id: string; localRouteReason: string | null; routeNodeIds: string[]; targetNodeId: string | null; waitReason: string | null }>;
      tryBypassParkedInboundQueueBlockerViaTopA(requester: unknown, blocker: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((entry) => entry.id === 'SH-03')!;
    const blocker = internals.vehicles.find((entry) => entry.id === 'SH-05')!;
    blocker.waitReason = 'inbound-lift-fifo-wait';

    expect(internals.tryBypassParkedInboundQueueBlockerViaTopA(requester, blocker, 'column-top-b-c11')).toBe(true);
    expect(requester.targetNodeId).toBe('column-top-a-c10');
    expect(requester.routeNodeIds.slice(0, 4)).toEqual([
      'column-top-b-c10',
      'column-top-a-c10',
      'column-top-a-c11',
      'column-top-a-c12'
    ]);
    expect(requester.routeNodeIds).toContain('column-top-b-c12');
    expect(requester.localRouteReason).toBe('temporary-yield');
  });

  it('does not overwrite an inbound pickup clearance route with service-clearance side yielding', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'pickup-clearance-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'pickup-clearance-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r13-c05',
      loadId: 'pickup-clearance-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c09', 'storage-r01-c09']);
    sim.setVehicleTaskForTest('SH-02', 'pickup-clearance-task', false);

    sim.addLoadForTest({ id: 'service-clearance-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'service-clearance-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r05-c03',
      loadId: 'service-clearance-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c09',
      'storage-r01-c09'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'service-clearance-task', true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
      }>;
      tryYieldAisleAwayFromTopLiftServiceClearance(aisleVehicle: unknown, serviceVehicle: unknown, blockedTargetNodeId: string): boolean;
    };
    const aisleVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const serviceVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.tryYieldAisleAwayFromTopLiftServiceClearance(aisleVehicle, serviceVehicle, 'column-top-b-c09')).toBe(false);
    expect(aisleVehicle.routeNodeIds).toEqual(['column-top-b-c09', 'storage-r01-c09']);
    expect(aisleVehicle.targetNodeId).toBe('storage-r01-c09');
    expect(aisleVehicle.localRouteReason).toBeNull();
  });

  it('yields a loaded middle-column outbound shuttle into storage for a loaded inbound spine swap', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'middle-spine-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addLoadForTest({ id: 'middle-spine-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'middle-spine-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r10-c06',
      loadId: 'middle-spine-inbound-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'middle-spine-outbound-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r02-c03',
      dropoffNodeId: 'column-bottom-b-c06',
      loadId: 'middle-spine-outbound-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', ['module-01-spine-middle', 'column-middle-c07', 'column-middle-c06', 'storage-r08-c06', 'storage-r09-c06', 'storage-r10-c06']);
    sim.setVehicleTaskForTest('SH-06', 'middle-spine-inbound-task', true);
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: 'column-middle-c07',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-05',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-05', ['column-middle-c07', 'module-01-spine-middle', 'module-01-spine-bottom-a', 'module-01-spine-bottom-b', 'column-bottom-b-c07', 'column-bottom-b-c06']);
    sim.setVehicleTaskForTest('SH-05', 'middle-spine-outbound-task', true);
    sim.setVehicleWaitingForTest('SH-05', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-06',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
      }>;
      tryYieldLoadedMiddleColumnBlockerForSpineSwap(spineVehicle: unknown, columnVehicle: unknown, blockedTargetNodeId: string): boolean;
    };
    const spineVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;
    const columnVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;

    expect(internals.tryYieldLoadedMiddleColumnBlockerForSpineSwap(spineVehicle, columnVehicle, 'column-middle-c07')).toBe(true);
    expect(columnVehicle.localRouteReason).toBe('temporary-yield');
    expect(columnVehicle.routeNodeIds).toEqual(['column-middle-c07', 'storage-r07-c07']);
    expect(columnVehicle.targetNodeId).toBe('storage-r07-c07');
  });

  it('breaks a persisted loaded middle-spine swap before the long-run blocked signature trips', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'persisted-spine-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addLoadForTest({ id: 'persisted-spine-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addTaskForTest({
      id: 'persisted-spine-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c09',
      dropoffNodeId: 'storage-r09-c07',
      loadId: 'persisted-spine-inbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'persisted-spine-outbound-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r04-c02',
      dropoffNodeId: 'column-bottom-b-c06',
      loadId: 'persisted-spine-outbound-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-middle', 'column-middle-c07', 'storage-r08-c07', 'storage-r09-c07']);
    sim.setVehicleTaskForTest('SH-02', 'persisted-spine-inbound-task', true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c07',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-06',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-06', ['column-middle-c07', 'module-01-spine-middle', 'module-01-spine-bottom-a', 'module-01-spine-bottom-b', 'column-bottom-b-c07', 'column-bottom-b-c06']);
    sim.setVehicleTaskForTest('SH-06', 'persisted-spine-outbound-task', true);
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });

    const state = sim.step(0.2);
    const yielder = state.vehicles.find((vehicle) => vehicle.id === 'SH-06');
    const requester = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(yielder).toMatchObject({
      state: 'loaded-moving',
      currentNodeId: 'column-middle-c07',
      targetNodeId: 'storage-r07-c07',
      waitReason: null
    });
    expect(yielder?.routeNodeIds).toEqual(['column-middle-c07', 'storage-r07-c07']);
    expect(requester?.waitReason).toBe('node-occupied');
    expect(state.kpis.deadlockCount).toBe(0);
  });

  it('reroutes a loaded middle-spine swap through a productive top-b path when storage yield pockets are unavailable', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'spine-reroute-pocket-a', state: 'stored', nodeId: 'storage-r07-c07', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'spine-reroute-pocket-b', state: 'stored', nodeId: 'storage-r08-c07', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'spine-reroute-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'spine-reroute-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'spine-reroute-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r12-c05',
      loadId: 'spine-reroute-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'spine-reroute-outbound-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'spine-reroute-outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-01-spine-middle',
      'column-middle-c07',
      'column-middle-c06',
      'column-middle-c05',
      'storage-r08-c05',
      'storage-r09-c05',
      'storage-r10-c05',
      'storage-r11-c05',
      'storage-r12-c05'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'spine-reroute-inbound-task', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-middle-c07',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c07',
      'module-01-spine-middle',
      'module-01-spine-bottom-a',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'spine-reroute-outbound-task', true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const spineVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentRefreshHandleMoveBlock(spineVehicle, 'column-middle-c07', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-02'
    })).toBe(true);

    expect(spineVehicle).toMatchObject({
      state: 'loaded-moving',
      targetNodeId: 'module-01-spine-top-b',
      localRouteReason: 'loaded-middle-spine-swap-reroute',
      waitReason: null
    });
    expect(spineVehicle.routeNodeIds.slice(0, 4)).toEqual([
      'module-01-spine-middle',
      'module-01-spine-top-b',
      'column-top-b-c07',
      'column-top-b-c06'
    ]);
    expect(spineVehicle.routeNodeIds.at(-1)).toBe('storage-r12-c05');
  });

  it('retargets a loaded middle-spine swap when the original inbound dropoff is not safely reachable', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    for (const nodeId of [
      'storage-r01-c05',
      'storage-r02-c05',
      'storage-r03-c05',
      'storage-r04-c05',
      'storage-r05-c05',
      'storage-r06-c05',
      'storage-r13-c05',
      'storage-r07-c07',
      'storage-r08-c07'
    ]) {
      sim.addLoadForTest({ id: `retarget-block-${nodeId}`, state: 'stored', nodeId, vehicleId: null, weightKg: 100 });
    }
    sim.addLoadForTest({ id: 'spine-retarget-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'spine-retarget-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'spine-retarget-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r12-c05',
      loadId: 'spine-retarget-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'spine-retarget-outbound-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'spine-retarget-outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-01-spine-middle',
      'column-middle-c07',
      'column-middle-c06',
      'column-middle-c05',
      'storage-r08-c05',
      'storage-r09-c05',
      'storage-r10-c05',
      'storage-r11-c05',
      'storage-r12-c05'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'spine-retarget-inbound-task', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-middle-c07',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c07',
      'module-01-spine-middle',
      'module-01-spine-bottom-a',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'spine-retarget-outbound-task', true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        waitReason: string | null;
      }>;
      tasks: Array<{ id: string; dropoffNodeId: string; replanCount: number }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const spineVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'spine-retarget-inbound-task')!;

    expect(internals.agentRefreshHandleMoveBlock(spineVehicle, 'column-middle-c07', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-02'
    })).toBe(true);

    expect(task.dropoffNodeId).not.toBe('storage-r12-c05');
    expect(task.replanCount).toBe(1);
    expect(spineVehicle).toMatchObject({
      state: 'loaded-moving',
      targetNodeId: 'column-middle-c08',
      waitReason: null
    });
    expect(spineVehicle.routeNodeIds).not.toContain('column-middle-c07');
    expect(spineVehicle.routeNodeIds.at(-1)).toBe(task.dropoffNodeId);
  });

  it('does not forward-yield through a top-lift node that another shuttle is entering', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['module-01-spine-middle', 'module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c07', 'module-01-spine-top-b', 'module-01-spine-top-a']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        currentEdgeId: string | null;
        legRemainingM: number;
        x: number;
        z: number;
      }>;
      traffic: { findEdge(fromNodeId: string, toNodeId: string): { id: string } | null };
      agentRefreshForwardYieldPocketNodeId(
        vehicle: unknown,
        currentNodeId: string,
        forwardNodeId: string,
        blocker: unknown
      ): string | null;
    };
    const entering = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const edge = internals.traffic.findEdge('module-01-spine-middle', 'module-01-spine-top-b')!;
    entering.currentNodeId = 'module-01-spine-middle';
    entering.targetNodeId = 'module-01-spine-top-b';
    entering.currentEdgeId = edge.id;
    entering.legRemainingM = 0.4;
    entering.x = 11.25;
    entering.z = 1.0;

    expect(
      internals.agentRefreshForwardYieldPocketNodeId(
        yielder,
        'column-top-b-c07',
        'module-01-spine-top-b',
        entering
      )
    ).toBeNull();
  });

  it('does not resolve top-lift adjacent swaps by parking on a no-stop spine node', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    addStoredLoads(sim, ['storage-r07-c15', 'storage-r08-c15']);
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c15', 'column-middle-c16']);
    sim.setVehicleRouteForTest('SH-02', ['column-middle-c16', 'column-middle-c15']);
    sim.setVehicleTaskForTest('SH-02', null, true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-middle-c16',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; localRouteReason: string | null }>;
      layoutNode(nodeId: string): { noStop?: boolean; type?: string } | null;
      tryBreakAgentRefreshTopLiftAdjacentNodeSwap(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshTopLiftAdjacentNodeSwap(['SH-01', 'SH-02'])).toBe(true);
    const yielder = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'temporary-yield');
    const holdNodeId = yielder?.routeNodeIds.at(-1);

    expect(yielder?.id).toBe('SH-01');
    expect(yielder?.routeNodeIds).toContain('module-boundary-01-spine-middle');
    expect(yielder?.routeNodeIds).not.toEqual(['column-middle-c15', 'module-boundary-01-spine-middle']);
    expect(holdNodeId).toBeDefined();
    expect(internals.layoutNode(holdNodeId!)?.noStop).toBe(false);
  });

  it('keeps displaced empty top-lift blockers on their temporary yield route', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'displace-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'displace-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-buffer-03',
      dropoffNodeId: 'storage-r13-c15',
      loadId: 'displace-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'displace-outbound-load', state: 'stored', nodeId: 'storage-r13-c16', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'displace-outbound',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r13-c16',
      dropoffNodeId: 'lift-02-outbound-buffer-03',
      loadId: 'displace-outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c15', 'storage-r08-c15']);
    sim.setVehicleTaskForTest('SH-01', 'displace-inbound', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r08-c15', 'column-middle-c15']);
    sim.setVehicleTaskForTest('SH-02', 'displace-outbound', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      agentTryDisplaceEmptyBlocker(blockingVehicleId: string, requester: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentTryDisplaceEmptyBlocker('SH-02', requester, 'storage-r08-c15')).toBe(true);
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(blocker.routeNodeIds.at(-1)).toBe('storage-r14-c15');
    expect(blocker.targetNodeId).toBe('storage-r09-c15');
    expect(blocker.localRouteReason).toBe('inbound-dropoff-column-clearance');
    expect(blocker.localRouteNodeIds).toEqual(blocker.routeNodeIds);
  });

  it('clears top-lift traffic from no-stop spine nodes into a legal hold when continuation is blocked', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c07', 'module-01-spine-middle', 'module-01-spine-top-b']);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-top-b']);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const terminalNode = scenario.layout.nodes.find((node) => node.id === vehicle?.routeNodeIds.at(-1));

    expect(vehicle).toMatchObject({
      state: 'returning',
      currentNodeId: 'column-middle-c07',
      waitReason: null,
      blockingVehicleId: null,
      localRouteReason: 'temporary-yield'
    });
    expect(vehicle?.targetNodeId).not.toBe('module-01-spine-middle');
    expect(terminalNode?.noStop).not.toBe(true);
    expect(terminalNode?.noParking).not.toBe(true);
    expectRouteHopsAdjacent(scenario, vehicle?.routeNodeIds ?? []);
  });

  it('lets an empty outbound middle no-stop cycle use a temporary storage yield', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'middle-cycle-left-load', state: 'stored', nodeId: 'storage-r12-c05', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'middle-cycle-left-task',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r12-c05',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'middle-cycle-left-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'middle-cycle-right-load', state: 'stored', nodeId: 'storage-r07-c15', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'middle-cycle-right-task',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c15',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'middle-cycle-right-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-middle-c08',
      'module-01-spine-middle',
      'column-middle-c07',
      'column-middle-c06',
      'column-middle-c05',
      'storage-r08-c05',
      'storage-r09-c05',
      'storage-r10-c05',
      'storage-r11-c05',
      'storage-r12-c05'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'middle-cycle-left-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'no-stop-continuation-blocked',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c07',
      'module-01-spine-middle',
      'column-middle-c08',
      'column-middle-c09',
      'column-middle-c10',
      'column-middle-c11',
      'column-middle-c12',
      'column-middle-c13',
      'column-middle-c14',
      'module-boundary-01-spine-middle',
      'column-middle-c15',
      'storage-r07-c15'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'middle-cycle-right-task', false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'no-stop-continuation-blocked',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        localRouteReason: string | null;
        taskId: string | null;
      }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      layoutNode(nodeId: string): { type?: string; noStop?: boolean; noParking?: boolean } | null;
      tryBreakAgentRefreshWaitCycle(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshWaitCycle(['SH-01', 'SH-02'])).toBe(true);
    const yielder = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'temporary-yield');
    const yielderTask = internals.tasks.find((task) => task.id === yielder?.taskId);
    const holdNodeId = yielder?.routeNodeIds.at(-1);

    expect(yielder).toBeDefined();
    expect(yielder?.waitReason).toBeNull();
    expect(yielder?.targetNodeId).not.toBe('module-01-spine-middle');
    expect(holdNodeId).toBeDefined();
    expect(internals.layoutNode(holdNodeId!)?.type).toBe('storage');
    expect(internals.layoutNode(holdNodeId!)?.noStop).not.toBe(true);
    expect(internals.layoutNode(holdNodeId!)?.noParking).not.toBe(true);
    expect(holdNodeId).not.toBe(yielderTask?.pickupNodeId);
    expectRouteHopsAdjacent(scenario, yielder?.routeNodeIds ?? []);
  });

  it('lets a top-lift spine occupant clear when it is blocking an opposing middle-aisle claimant continuation', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['module-01-spine-top-b', 'module-01-spine-middle', 'column-middle-c07']);
    sim.setVehicleRouteForTest('SH-02', ['column-middle-c07', 'module-01-spine-middle', 'module-01-spine-top-b']);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; waitReason: string | null; blockingVehicleId: string | null }>;
      agentRefreshMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
    };
    const spineOccupant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const opposingClaimant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    opposingClaimant.waitReason = 'no-stop-continuation-blocked';
    opposingClaimant.blockingVehicleId = 'SH-01';

    expect(internals.agentRefreshMoveBlocker(spineOccupant, 'module-01-spine-middle')?.reason)
      .not.toBe('middle-aisle-opposing-claim');
  });

  it('immediately moves an empty top-lift spine blocker sideways for a loaded shuttle', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'spine-inbound-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'spine-inbound',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r07-c03',
      loadId: 'spine-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'spine-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'spine-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'spine-outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-01', 'spine-inbound', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-01-spine-top-b',
      waitReason: 'local-yield-hold',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-middle', 'module-01-spine-top-b', 'module-01-spine-top-a']);
    sim.setVehicleTaskForTest('SH-02', 'spine-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      agentTryDisplaceEmptyBlocker(blockingVehicleId: string, requester: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentTryDisplaceEmptyBlocker('SH-01', requester, 'module-01-spine-top-b')).toBe(true);
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(blocker.routeNodeIds[0]).toBe('module-01-spine-top-b');
    expect(blocker.routeNodeIds[1]).toMatch(/^(column-top-b-c0[78]|module-01-spine-top-a)$/);
    expect(blocker.routeNodeIds).not.toContain('module-01-spine-middle');
    expect(blocker.targetNodeId).toBe(blocker.routeNodeIds[1]);
    expect(blocker.waitReason).toBeNull();
    expect(blocker.blockingVehicleId).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
  });

  it('moves a loaded top-b aisle entrant onto top-a when it deadlocks with an opposing vertical spine claim', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'spine-cycle-loaded-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-03', weightKg: 100 });
    sim.addTaskForTest({
      id: 'spine-cycle-loaded-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r12-c16',
      loadId: 'spine-cycle-loaded-inbound-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['module-boundary-01-spine-top-b', 'column-top-b-c15']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-b-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['module-boundary-01-spine-middle', 'module-boundary-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'module-boundary-01-spine-top-b',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c15',
      'module-boundary-01-spine-top-b',
      'module-boundary-01-spine-middle',
      'column-middle-c15',
      'storage-r08-c15',
      'storage-r09-c15',
      'storage-r10-c15',
      'storage-r11-c15',
      'storage-r12-c15',
      'storage-r12-c16'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'spine-cycle-loaded-inbound', true);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'module-boundary-01-spine-top-b',
      waitReason: 'top-lift-spine-opposing-claim',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      tryBreakAgentRefreshTopLiftSpineCycle(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshTopLiftSpineCycle(['SH-01', 'SH-02', 'SH-03'])).toBe(true);
    const entrant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;

    expect(entrant.routeNodeIds).toEqual(['column-top-b-c15', 'column-top-a-c15']);
    expect(entrant.targetNodeId).toBe('column-top-a-c15');
    expect(entrant.waitReason).toBeNull();
    expect(entrant.blockingVehicleId).toBeNull();
    expect(entrant.localRouteReason).toBe('temporary-yield');
  });

  it('moves a loaded bottom-a spine entrant onto bottom-b when it deadlocks with opposing middle traffic', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'bottom-spine-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'bottom-spine-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c15',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'bottom-spine-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'bottom-spine-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'bottom-spine-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r10-c16',
      loadId: 'bottom-spine-inbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-middle-c15',
      'module-boundary-01-spine-middle',
      'module-boundary-01-spine-bottom-a',
      'column-bottom-a-c15',
      'column-bottom-a-c16',
      'lift-02-outbound-queue-01-entry-access',
      'lift-02-outbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'bottom-spine-outbound', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-boundary-01-spine-middle',
      waitReason: 'top-lift-spine-opposing-claim',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'module-boundary-01-spine-bottom-a',
      'module-boundary-01-spine-middle',
      'column-middle-c15',
      'column-middle-c16',
      'storage-r08-c16',
      'storage-r09-c16',
      'storage-r10-c16'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'bottom-spine-inbound', true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'module-boundary-01-spine-middle',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      tryBreakAgentRefreshTopLiftSpineCycle(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshTopLiftSpineCycle(['SH-01', 'SH-02'])).toBe(true);
    const entrant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(entrant.routeNodeIds).toEqual(['module-boundary-01-spine-bottom-a', 'module-boundary-01-spine-bottom-b']);
    expect(entrant.targetNodeId).toBe('module-boundary-01-spine-bottom-b');
    expect(entrant.waitReason).toBeNull();
    expect(entrant.blockingVehicleId).toBeNull();
    expect(entrant.localRouteReason).toBe('temporary-yield');
  });

  it('moves an empty top-b spine entrant onto top-a instead of yielding into middle traffic', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c21',
      'storage-r07-c21'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-02-spine-middle',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c20',
      'column-middle-c21',
      'module-02-spine-middle',
      'module-02-spine-bottom-a'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c21',
      waitReason: 'node-local-yield',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: { id: string },
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentRefreshHandleMoveBlock(yielder, 'module-02-spine-middle', {
      reason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-02'
    })).toBe(true);

    expect(yielder.routeNodeIds).toEqual(['module-02-spine-top-b', 'module-02-spine-top-a']);
    expect(yielder.targetNodeId).toBe('module-02-spine-top-a');
    expect(yielder.waitReason).toBeNull();
    expect(yielder.blockingVehicleId).toBeNull();
    expect(yielder.localRouteReason).toBe('temporary-yield');
  });

  it('does not overwrite an empty top-b spine blocker top-a clearance route with a middle yield', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-top-b-spine-clearance-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-top-b-spine-clearance',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c09',
      dropoffNodeId: 'storage-r12-c04',
      loadId: 'loaded-top-b-spine-clearance-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'column-top-b-c07',
      'module-01-spine-top-b',
      'module-01-spine-middle',
      'column-middle-c07',
      'column-middle-c06',
      'column-middle-c05',
      'column-middle-c04',
      'storage-r08-c04',
      'storage-r09-c04',
      'storage-r10-c04',
      'storage-r11-c04',
      'storage-r12-c04'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'loaded-top-b-spine-clearance', true);
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: 'module-01-spine-top-b',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-07',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-07', [
      'module-01-spine-top-b',
      'module-01-spine-top-a',
      'column-top-a-c08',
      'column-top-a-c09',
      'column-top-a-c10',
      'column-top-a-c11',
      'column-top-a-c12',
      'column-top-b-c12',
      'storage-r01-c12',
      'storage-r02-c12'
    ]);
    sim.setVehicleTaskForTest('SH-07', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: { id: string },
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;

    expect(internals.agentRefreshHandleMoveBlock(requester, 'module-01-spine-top-b', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-07'
    })).toBe(false);

    expect(yielder.routeNodeIds).toEqual([
      'module-01-spine-top-b',
      'module-01-spine-top-a',
      'column-top-a-c08',
      'column-top-a-c09',
      'column-top-a-c10',
      'column-top-a-c11',
      'column-top-a-c12',
      'column-top-b-c12',
      'storage-r01-c12',
      'storage-r02-c12'
    ]);
    expect(yielder.targetNodeId).toBe('module-01-spine-top-a');
    expect(yielder.waitReason).toBeNull();
    expect(yielder.blockingVehicleId).toBeNull();
    expectRouteHopsAdjacent(scenario, yielder.routeNodeIds);
  });

  it('moves an empty storage exit blocker to an adjacent pocket before loaded inbound column entry', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-inbound-r08-c17-load', state: 'carried', nodeId: null, vehicleId: 'SH-04', weightKg: 100 });
    sim.addLoadForTest({ id: 'loaded-inbound-r09-c17-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addLoadForTest({ id: 'outbound-c02-r03-load', state: 'stored', nodeId: 'storage-r03-c02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-inbound-r08-c17',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c23',
      dropoffNodeId: 'storage-r08-c17',
      loadId: 'loaded-inbound-r08-c17-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'loaded-inbound-r09-c17',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c23',
      dropoffNodeId: 'storage-r09-c17',
      loadId: 'loaded-inbound-r09-c17-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'outbound-c02-r03',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c02',
      dropoffNodeId: 'column-bottom-b-c06',
      loadId: 'outbound-c02-r03-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'storage-r08-c17',
      'column-middle-c17',
      'column-middle-c16',
      'column-middle-c15',
      'module-boundary-01-spine-middle',
      'column-middle-c14',
      'column-middle-c13',
      'column-middle-c12',
      'column-middle-c11',
      'column-middle-c10',
      'column-middle-c09',
      'column-middle-c08',
      'module-01-spine-middle',
      'column-middle-c07',
      'column-middle-c06',
      'column-middle-c05',
      'column-middle-c04',
      'column-middle-c03',
      'column-middle-c02',
      'storage-r07-c02',
      'storage-r06-c02',
      'storage-r05-c02',
      'storage-r04-c02',
      'storage-r03-c02'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'outbound-c02-r03', false);
    sim.setVehicleRouteForTest('SH-04', [
      'column-middle-c15',
      'column-middle-c16',
      'column-middle-c17',
      'storage-r08-c17'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'loaded-inbound-r08-c17', true);
    sim.setVehicleRouteForTest('SH-06', [
      'column-middle-c16',
      'column-middle-c17',
      'storage-r08-c17',
      'storage-r09-c17'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'loaded-inbound-r09-c17', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-middle-c17',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-04',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-04', {
      targetNodeId: 'column-middle-c16',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-06',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: 'column-middle-c17',
      waitReason: 'inbound-column-entry-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        localRouteReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: { id: string },
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const storageExit = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentRefreshHandleMoveBlock(storageExit, 'column-middle-c17', {
      reason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-04'
    })).toBe(true);

    expect(storageExit.routeNodeIds).toEqual([
      'storage-r08-c17',
      'column-middle-c17',
      'column-middle-c18',
      'storage-r07-c18'
    ]);
    expect(storageExit.targetNodeId).toBe('column-middle-c17');
    expect(storageExit.waitReason).toBeNull();
    expect(storageExit.localRouteReason).toBe('temporary-yield');
    expectRouteHopsAdjacent(scenario, storageExit.routeNodeIds);
  });

  it('rejects loaded outbound storage yield pockets that cannot reach the dropoff', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    for (const nodeId of ['storage-r01-c22', 'storage-r02-c22', 'storage-r04-c22', 'storage-r07-c22']) {
      sim.addLoadForTest({ id: `stored-${nodeId}`, state: 'stored', nodeId, vehicleId: null, weightKg: 100 });
    }
    sim.addLoadForTest({ id: 'carried-outbound-c22', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-outbound-c22',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c22',
      dropoffNodeId: 'column-bottom-b-c20',
      loadId: 'carried-outbound-c22',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', ['storage-r05-c22']);
    sim.setVehicleTaskForTest('SH-05', 'loaded-outbound-c22', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshYieldPocketKeepsGoalReachable(vehicle: { id: string }, pocketNodeId: string): boolean;
    };
    const loadedOutbound = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;

    expect(internals.agentRefreshYieldPocketKeepsGoalReachable(loadedOutbound, 'storage-r06-c22')).toBe(false);
  });

  it('moves an empty inbound successor out of a loaded predecessor vertical spine claim', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-predecessor-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-predecessor',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c09',
      dropoffNodeId: 'storage-r12-c06',
      loadId: 'loaded-predecessor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'empty-successor',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c09',
      dropoffNodeId: 'storage-r11-c06',
      loadId: 'empty-successor-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-01-spine-top-b',
      'module-01-spine-middle',
      'column-middle-c07',
      'column-middle-c06',
      'storage-r08-c06',
      'storage-r09-c06',
      'storage-r10-c06',
      'storage-r11-c06',
      'storage-r12-c06'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-predecessor', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c07',
      'module-01-spine-middle',
      'module-01-spine-top-b',
      'column-top-b-c08',
      'column-top-b-c09'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'empty-successor', false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'top-lift-spine-opposing-claim',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: { id: string },
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const successor = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshHandleMoveBlock(successor, 'module-01-spine-middle', {
      reason: 'top-lift-spine-opposing-claim',
      blockingVehicleId: 'SH-01'
    })).toBe(true);
    expect(successor.routeNodeIds[0]).toBe('column-middle-c07');
    expect(successor.targetNodeId).not.toBe('module-01-spine-middle');
    expect(successor.waitReason).toBeNull();
    expect(successor.blockingVehicleId).toBeNull();
    expect(successor.localRouteReason).toBe('temporary-yield');
  });

  it('moves a taskless middle-access blocker through stored cells to clear a loaded vertical spine claim', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    for (const nodeId of ['storage-r07-c07', 'storage-r08-c07']) {
      sim.addLoadForTest({ id: `stored-${nodeId}`, state: 'stored', nodeId, vehicleId: null, weightKg: 100 });
    }
    sim.addLoadForTest({ id: 'taskless-spine-loaded', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'taskless-spine-loaded',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r07-c06',
      loadId: 'taskless-spine-loaded',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', [
      'module-01-spine-top-b',
      'module-01-spine-middle',
      'column-middle-c07',
      'column-middle-c06',
      'storage-r07-c06'
    ]);
    sim.setVehicleTaskForTest('SH-05', 'taskless-spine-loaded', true);
    sim.setVehicleWaitingForTest('SH-05', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-03', [
      'column-middle-c07',
      'module-01-spine-middle',
      'module-01-spine-top-b',
      'column-top-b-c07',
      'column-top-b-c06',
      'column-top-b-c05',
      'storage-r01-c05'
    ]);
    sim.setVehicleTaskForTest('SH-03', null, false);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'module-01-spine-middle',
      waitReason: 'top-lift-spine-opposing-claim',
      blockingVehicleId: 'SH-05',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: { id: string },
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;

    expect(internals.agentRefreshHandleMoveBlock(blocker, 'module-01-spine-middle', {
      reason: 'top-lift-spine-opposing-claim',
      blockingVehicleId: 'SH-05'
    })).toBe(true);
    expect(blocker.routeNodeIds).toEqual(['column-middle-c07', 'storage-r07-c07', 'storage-r06-c07']);
    expect(blocker.targetNodeId).toBe('storage-r07-c07');
    expect(blocker.waitReason).toBeNull();
    expect(blocker.blockingVehicleId).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
    expectRouteHopsAdjacent(scenario, blocker.routeNodeIds);
  });

  it('moves an empty top-b spine occupant onto top-a when it blocks a loaded inbound entry', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-entry-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-entry',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c23',
      dropoffNodeId: 'storage-r12-c21',
      loadId: 'loaded-entry-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'empty-outbound',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r06-c23',
      dropoffNodeId: 'column-bottom-b-c20',
      loadId: 'empty-outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c22',
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c21',
      'storage-r08-c21',
      'storage-r09-c21',
      'storage-r10-c21',
      'storage-r11-c21',
      'storage-r12-c21'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-entry', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-02-spine-top-b',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c22',
      'storage-r07-c22',
      'storage-r06-c22',
      'storage-r06-c23'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'empty-outbound', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: { id: string },
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const loadedEntry = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshHandleMoveBlock(loadedEntry, 'module-02-spine-top-b', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-02'
    })).toBe(false);
    expect(blocker.routeNodeIds[0]).toBe('module-02-spine-top-b');
    expect(blocker.targetNodeId).not.toBe('module-02-spine-top-b');
    expect(blocker.targetNodeId).not.toBe('column-top-b-c22');
    expect(blocker.waitReason).toBeNull();
    expect(blocker.blockingVehicleId).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
  });

  it('keeps an empty top-b spine yield out of a loaded inbound vertical-spine continuation', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-spine-entry-load', state: 'carried', nodeId: null, vehicleId: 'SH-08', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-spine-entry',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r14-c25',
      loadId: 'loaded-spine-entry-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'empty-spine-blocker-load', state: 'waiting', nodeId: 'column-top-a-c22', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-spine-blocker',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r13-c25',
      loadId: 'empty-spine-blocker-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'loaded-neighbor-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-neighbor',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 2,
      assignedAtSec: 2,
      startedAtSec: 2,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r12-c25',
      loadId: 'loaded-neighbor-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', [
      'column-top-b-c22',
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c22',
      'column-middle-c23',
      'column-middle-c24',
      'column-middle-c25',
      'storage-r08-c25',
      'storage-r09-c25',
      'storage-r10-c25',
      'storage-r11-c25',
      'storage-r12-c25',
      'storage-r13-c25',
      'storage-r14-c25'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'loaded-spine-entry', true);
    sim.setVehicleRouteForTest('SH-02', [
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c21',
      'column-middle-c20',
      'column-middle-c19',
      'column-middle-c18',
      'column-middle-c17',
      'storage-r07-c17'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'empty-spine-blocker', false);
    sim.setVehicleRouteForTest('SH-06', [
      'column-top-a-c22',
      'column-top-b-c22',
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c22',
      'column-middle-c23',
      'column-middle-c24',
      'column-middle-c25',
      'storage-r08-c25',
      'storage-r09-c25',
      'storage-r10-c25',
      'storage-r11-c25',
      'storage-r12-c25'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'loaded-neighbor', true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: { id: string },
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const loadedEntry = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshHandleMoveBlock(loadedEntry, 'module-02-spine-top-b', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-02'
    })).toBe(false);
    expect(blocker.routeNodeIds[0]).toBe('module-02-spine-top-b');
    expect(blocker.routeNodeIds).not.toContain('module-02-spine-middle');
    expect(blocker.targetNodeId).not.toBe('module-02-spine-middle');
    expect(blocker.waitReason).toBeNull();
    expect(blocker.blockingVehicleId).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
  });

  it('prefers a horizontal middle-lane pocket over bottom-a when clearing a vertical spine cycle', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['module-boundary-01-spine-top-b', 'module-boundary-01-spine-middle']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-boundary-01-spine-middle',
      waitReason: 'top-lift-spine-opposing-claim',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['module-boundary-01-spine-middle', 'module-boundary-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'module-boundary-01-spine-top-b',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentEdgeId: string | null;
        legRemainingM: number;
      }>;
      agentRefreshTopLiftSpineEscapeNodeId(
        vehicle: { id: string },
        middleNodeId: string,
        blockedTopBNodeId: string
      ): string | null;
    };
    for (const vehicle of internals.vehicles) {
      if (vehicle.id === 'SH-01' || vehicle.id === 'SH-02') {
        vehicle.currentEdgeId = null;
        vehicle.legRemainingM = 0;
      }
    }
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshTopLiftSpineEscapeNodeId(
      yielder,
      'module-boundary-01-spine-middle',
      'module-boundary-01-spine-top-b'
    )).toBe('column-middle-c14');
  });

  it('keeps an active middle-lane side-yield route instead of replacing it with another temporary yield', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const route = ['module-boundary-01-spine-middle', 'column-middle-c14', 'storage-r07-c14'];
    sim.setVehicleRouteForTest('SH-01', route);
    sim.setVehicleRouteForTest('SH-02', ['column-middle-c15']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        localRouteNodeIds: string[];
        localRouteReason: string | null;
        yieldHoldUntilSec: number | null;
        currentEdgeId: string | null;
        legRemainingM: number;
      }>;
      agentRefreshTemporaryYieldShouldContinue(vehicle: { id: string }, blockedNodeId: string): boolean;
      installAgentRefreshSideYieldRoute(
        vehicle: unknown,
        requester: unknown,
        blockedTargetNodeId: string,
        route: string[],
        session: null
      ): void;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    yielder.localRouteNodeIds = [...route];
    yielder.localRouteReason = 'temporary-yield';
    yielder.yieldHoldUntilSec = 123;
    yielder.currentEdgeId = null;
    yielder.legRemainingM = 0;

    expect(internals.agentRefreshTemporaryYieldShouldContinue(yielder, 'module-boundary-01-spine-top-b')).toBe(true);
    internals.installAgentRefreshSideYieldRoute(
      yielder,
      requester,
      'module-boundary-01-spine-top-b',
      route,
      null
    );

    expect(yielder.routeNodeIds).toEqual(route);
    expect(yielder.localRouteNodeIds).toEqual(route);
    expect(yielder.yieldHoldUntilSec).toBe(123);
  });

  it('does not break a lift service-clearance faceoff while the service leg is still off the top-b lane', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c06', 'column-top-b-c05']);
    sim.setVehicleTaskForTest('SH-01', 'loaded-outbound', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-b-c05',
      waitReason: 'avoidance-clearance',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-03-service-exit',
      'lift-01-inbound-queue-03-entry-access'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'loaded-inbound', true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'lift-01-inbound-queue-03-entry-access',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
      }>;
      tryBreakAgentRefreshTopLiftServiceClearanceCycle(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshTopLiftServiceClearanceCycle(['SH-01', 'SH-02'])).toBe(false);
  });

  it('uses a same-lane pocket when the lift service-clearance route is still off the top-b lane', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c06', 'column-top-b-c05']);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-b-c05',
      waitReason: 'avoidance-clearance',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-03-service-exit',
      'lift-01-inbound-queue-03-entry-access'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'loaded-inbound', true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'lift-01-inbound-queue-03-entry-access',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-03', ['column-top-a-c06', 'column-top-a-c05']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        currentEdgeId: string | null;
        speedMps: number;
        localRouteReason: string | null;
      }>;
      tryBreakAgentRefreshTopLiftServiceClearanceCycle(candidateVehicleIds: string[]): boolean;
    };
    const movingPocketBlocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    movingPocketBlocker.currentEdgeId = 'column-top-a-c05-column-top-a-c06';
    movingPocketBlocker.speedMps = 1;

    expect(internals.tryBreakAgentRefreshTopLiftServiceClearanceCycle(['SH-01', 'SH-02'])).toBe(true);
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(yielder.routeNodeIds).toEqual(['column-top-b-c06', 'column-top-b-c05']);
    expect(yielder.targetNodeId).toBe('column-top-b-c05');
    expect(yielder.localRouteReason).toBeNull();
  });

  it('moves an empty top-lift queue blocker deeper when a loaded shuttle exits the lift service lane', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-entry-access',
      'column-top-a-c07'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c08']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        localRouteReason: string | null;
      }>;
      agentTryDisplaceEmptyBlocker(blockingVehicleId: string, requester: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentTryDisplaceEmptyBlocker('SH-02', requester, 'lift-01-inbound-queue-01-entry-access')).toBe(true);
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(blocker.routeNodeIds).toEqual([
      'parking-lift-01-inbound-queue',
      'parking-lift-01-inbound-queue-02'
    ]);
    expect(blocker.targetNodeId).toBe('parking-lift-01-inbound-queue-02');
    expect(blocker.routeNodeIds).not.toContain('lift-01-inbound-queue-01-access');
    expect(blocker.waitReason).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
  });

  it('redirects an empty top-lift queue access blocker deeper instead of letting it enter the front slot', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-entry-access',
      'column-top-a-c07'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-01-access',
      'parking-lift-01-inbound-queue'
    ]);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        localRouteReason: string | null;
      }>;
      agentTryDisplaceEmptyBlocker(blockingVehicleId: string, requester: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentTryDisplaceEmptyBlocker('SH-02', requester, 'lift-01-inbound-queue-01-entry-access')).toBe(true);
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(blocker.routeNodeIds).toEqual([
      'lift-01-inbound-queue-01-access',
      'lift-01-inbound-queue-02-access',
      'parking-lift-01-inbound-queue-02'
    ]);
    expect(blocker.targetNodeId).toBe('lift-01-inbound-queue-02-access');
    expect(blocker.waitReason).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
  });

  it('moves an inbound queue entrant sideways on top-b when the storage pocket is occupied by a follower', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-exit-load', state: 'carried', nodeId: null, vehicleId: 'SH-08', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-exit',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r03-c03',
      loadId: 'loaded-exit-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', [
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c07',
      'column-top-b-c06'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'loaded-exit', true);

    sim.addLoadForTest({ id: 'queued-entrant-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queued-entrant',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r02-c03',
      loadId: 'queued-entrant-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', [
      'column-top-b-c07',
      'lift-01-inbound-queue-01-entry-access',
      'lift-01-inbound-queue-01-access',
      'lift-01-inbound-queue-02-access',
      'parking-lift-01-inbound-queue-02'
    ]);
    sim.setVehicleTaskForTest('SH-05', 'queued-entrant', false);

    sim.setVehicleRouteForTest('SH-03', ['storage-r01-c07']);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        localRouteReason: string | null;
      }>;
      agentTryDisplaceEmptyBlocker(blockingVehicleId: string, requester: unknown, blockedTargetNodeId: string): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;

    expect(internals.agentTryDisplaceEmptyBlocker('SH-05', requester, 'column-top-b-c07')).toBe(true);
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;

    expect(blocker.routeNodeIds).toEqual([
      'column-top-b-c07',
      'module-01-spine-top-b',
      'column-top-b-c08'
    ]);
    expect(blocker.targetNodeId).toBe('module-01-spine-top-b');
    expect(blocker.routeNodeIds).not.toContain('storage-r01-c07');
    expect(blocker.routeNodeIds).not.toContain('lift-01-inbound-queue-01-entry-access');
    expect(blocker.waitReason).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
  });

  it('reroutes a loaded inbound shuttle around an occupied non-target storage transit column', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-c04-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-c04',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r14-c04',
      loadId: 'loaded-c04-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'column-top-b-c07',
      'storage-r01-c07',
      'storage-r02-c07',
      'storage-r03-c07',
      'storage-r04-c07',
      'storage-r05-c07',
      'storage-r06-c07',
      'storage-r07-c07',
      'column-middle-c07',
      'column-middle-c06',
      'column-middle-c05',
      'column-middle-c04',
      'storage-r08-c04',
      'storage-r09-c04',
      'storage-r10-c04',
      'storage-r11-c04',
      'storage-r12-c04',
      'storage-r13-c04',
      'storage-r14-c04'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'loaded-c04', true);
    sim.setVehicleRouteForTest('SH-08', [
      'storage-r01-c07',
      'column-top-b-c07',
      'module-01-spine-top-b',
      'column-top-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-08', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        localRouteReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const loaded = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;

    expect(internals.agentRefreshHandleMoveBlock(loaded, 'storage-r01-c07', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-08'
    })).toBe(true);

    expect(loaded.routeNodeIds.slice(0, 4)).toEqual([
      'column-top-b-c07',
      'column-top-b-c06',
      'column-top-b-c05',
      'column-top-b-c04'
    ]);
    expect(loaded.routeNodeIds).not.toContain('storage-r01-c07');
    expect(loaded.targetNodeId).toBe('column-top-b-c06');
    expect(loaded.waitReason).toBeNull();
    expect(loaded.localRouteReason).toBe('loaded-inbound-storage-transit-bypass');
  });

  it('allows a lift queue shuttle to pass through the front parking slot when assigned deeper in line', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'outbound-load-a', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'outbound-load-b', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'older-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'outbound-load-a',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'outbound-load-b',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['parking-lift-01-outbound-queue-02']);
    sim.setVehicleTaskForTest('SH-01', 'older-outbound', true);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-outbound-queue-01-access',
      'parking-lift-01-outbound-queue',
      'parking-lift-01-outbound-queue-02',
      'parking-lift-01-outbound-queue-03'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'later-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      topLiftQueueFifoBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const later = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftQueueFifoBlocker(later, 'parking-lift-01-outbound-queue')).toBeNull();
  });

  it('stages outbound lift queues on upstream yellow-grid approach nodes outside the service lane', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'older-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'older-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'later-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      topLiftOutboundQueueNodeIdForTask(task: unknown): string | null;
    };
    const later = internals.tasks.find((task) => task.id === 'later-outbound')!;

    expect(internals.topLiftOutboundQueueNodeIdForTask(later)).toBe('column-bottom-b-c05');
    expect(scenario.layout.edges.find((edge) =>
      edge.from === 'column-bottom-b-c05' &&
      edge.to === 'column-bottom-b-c06'
    )?.directionMode).toBe('twoWay');
    expect(scenario.layout.edges.find((edge) =>
      edge.from === 'lift-01-outbound-queue-01-entry-access' &&
      edge.to === 'lift-01-outbound-queue-01-service-exit'
    )?.directionMode).toBe('oneWay');
  });

  it('routes loaded outbound followers to their meter slot from the same bottom-a column', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const task = {
      id: 'meter-follower',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r12-c19',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'meter-follower-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    };

    const internals = sim as unknown as {
      agentRefreshLoadedOutboundRouteToDropoff(currentNodeId: string, task: unknown, goalNodeId?: string): string[] | null;
    };

    expect(internals.agentRefreshLoadedOutboundRouteToDropoff('column-bottom-a-c19', task, 'column-bottom-b-c17')).toEqual([
      'column-bottom-a-c19',
      'column-bottom-a-c18',
      'column-bottom-a-c17',
      'column-bottom-b-c17'
    ]);
  });

  it('routes outbound lift approach onto top-a before the service entry column', () => {
    const scenario = createInboundOutboundDemoScenario({
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const task = {
      id: 'outbound-route',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r09-c15',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'route-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    };
    const internals = sim as unknown as {
      agentRefreshLoadedOutboundRouteToDropoff(currentNodeId: string, task: unknown): string[] | null;
    };

    expect(internals.agentRefreshLoadedOutboundRouteToDropoff('storage-r09-c15', task)).toEqual([
      'storage-r09-c15',
      'storage-r10-c15',
      'storage-r11-c15',
      'storage-r12-c15',
      'storage-r13-c15',
      'storage-r14-c15',
      'column-bottom-a-c15',
      'column-bottom-a-c16',
      'column-bottom-a-c17',
      'column-bottom-a-c18',
      'column-bottom-a-c19',
      'column-bottom-a-c20',
      'column-bottom-b-c20',
      'lift-02-outbound-queue-01-entry-access',
      'lift-02-outbound-queue-01-service-exit'
    ]);
  });

  it('routes loaded outbound out the clear side of a storage column instead of crossing stored loads', () => {
    const scenario = createInboundOutboundDemoScenario({
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    addStoredLoads(sim, ['storage-r05-c17', 'storage-r06-c17', 'storage-r07-c17']);
    const task = {
      id: 'outbound-clear-side',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c16',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'route-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    };
    const internals = sim as unknown as {
      agentRefreshLoadedOutboundRouteToDropoff(currentNodeId: string, task: unknown): string[] | null;
    };

    const route = internals.agentRefreshLoadedOutboundRouteToDropoff('storage-r04-c17', task);

    expect(route?.slice(0, 5)).toEqual([
      'storage-r04-c17',
      'storage-r03-c17',
      'storage-r02-c17',
      'storage-r01-c17',
      'column-top-b-c17'
    ]);
    expect(route).not.toContain('storage-r05-c17');
    expect(route).not.toContain('storage-r06-c17');
    expect(route).not.toContain('storage-r07-c17');
  });

  it('lets the leading outbound shuttle enter the lift buffer when a later shuttle is behind it', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'outbound-load-a', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'outbound-load-b', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'older-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'outbound-load-a',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'outbound-load-b',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['lift-01-outbound-buffer-access', 'lift-01-outbound-buffer-03']);
    sim.setVehicleTaskForTest('SH-01', 'older-outbound', true);
    sim.setVehicleRouteForTest('SH-02', ['lift-01-outbound-queue-01-service-exit', 'lift-01-outbound-buffer-access']);
    sim.setVehicleTaskForTest('SH-02', 'later-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshColumnEdgeBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
    };
    const leader = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentRefreshColumnEdgeBlocker(leader, 'lift-01-outbound-buffer-03')).toBeNull();
  });

  it('keeps a loaded outbound shuttle in the service lane moving toward dropoff instead of backing into queue', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'older-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'older-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'later-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['lift-01-outbound-queue-01-service-exit']);
    sim.setVehicleTaskForTest('SH-02', 'later-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      outboundDropoffDispatchGoalNodeId(task: unknown, vehicle: unknown): string;
    };
    const laterVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const laterTask = internals.tasks.find((task) => task.id === 'later-outbound')!;

    expect(internals.outboundDropoffDispatchGoalNodeId(laterTask, laterVehicle)).toBe('lift-01-outbound-buffer-03');
  });

  it('redirects a loaded outbound follower from occupied station entries to an approach meter', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'older-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'older-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'later-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['lift-01-outbound-queue-03-entry-access']);
    sim.setVehicleTaskForTest('SH-01', 'older-outbound', true);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-02', 'later-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      outboundDropoffDispatchGoalNodeId(task: unknown, vehicle: unknown): string;
    };
    const laterVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const laterTask = internals.tasks.find((task) => task.id === 'later-outbound')!;

    expect(internals.outboundDropoffDispatchGoalNodeId(laterTask, laterVehicle)).toBe('column-bottom-b-c05');
  });

  it('meters a loaded outbound follower before a yellow-grid dropoff while the earlier dropoff is active', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'earlier-yellow-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'later-yellow-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'earlier-yellow-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r08-c02',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'earlier-yellow-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-yellow-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c01',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'later-yellow-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-bottom-b-c06',
      'column-bottom-b-c07',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'earlier-yellow-outbound', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r07-c01']);
    sim.setVehicleTaskForTest('SH-02', 'later-yellow-outbound', true);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      outboundTaskHasEarlierDropoffTask(task: unknown): boolean;
      outboundDropoffDispatchGoalNodeId(task: unknown, vehicle: unknown): string;
    };
    const laterTask = internals.tasks.find((task) => task.id === 'later-yellow-outbound')!;
    const laterVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.outboundTaskHasEarlierDropoffTask(laterTask)).toBe(true);
    expect(internals.outboundDropoffDispatchGoalNodeId(laterTask, laterVehicle)).toBe('column-bottom-b-c05');
  });

  it('drops an obsolete station-entry sticky goal and re-meters the outbound follower', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'older-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'older-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'later-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['lift-01-outbound-queue-03-entry-access']);
    sim.setVehicleTaskForTest('SH-01', 'older-outbound', true);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-02', 'later-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null }>;
      tasks: Array<{ id: string }>;
      outboundDropoffDispatchGoalNodeId(task: unknown, vehicle: unknown): string;
    };
    const laterVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    laterVehicle.plannedGoalNodeId = 'lift-01-outbound-queue-03-entry-access';
    const laterTask = internals.tasks.find((task) => task.id === 'later-outbound')!;

    expect(internals.outboundDropoffDispatchGoalNodeId(laterTask, laterVehicle)).toBe('column-bottom-b-c05');
  });

  it('lets a loaded outbound shuttle already at the dock entry complete service before bottom corridor traffic', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'dock-dropoff',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-outbound-queue-01-entry-access',
      'lift-01-outbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'dock-dropoff', true);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-bottom-b', 'column-bottom-b-c08']);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      topLiftOutboundDockCorridorBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const serviceEntrant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const bottomCorridorVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftOutboundDockCorridorBlocker(
      serviceEntrant,
      'column-bottom-b-c08'
    )).toBeNull();
    expect(internals.topLiftOutboundDockCorridorBlocker(bottomCorridorVehicle, 'column-bottom-b-c08')).toBe('SH-01');
  });

  it('lets an empty outbound shuttle clear the service exit before bottom corridor traffic enters', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-approach',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'lift-01-outbound-queue-01-service-exit',
      'lift-01-outbound-queue-01-entry-access'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-bottom-b', 'column-bottom-b-c07']);
    sim.setVehicleTaskForTest('SH-02', 'loaded-approach', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      topLiftOutboundDockCorridorBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const serviceExitVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const bottomCorridorVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftOutboundDockCorridorBlocker(
      serviceExitVehicle,
      'lift-01-outbound-queue-01-entry-access'
    )).toBeNull();
    expect(internals.topLiftOutboundDockCorridorBlocker(bottomCorridorVehicle, 'column-bottom-b-c07')).toBe('SH-01');
  });

  it('does not let a route-unavailable storage outbound claim the dock corridor by planned goal', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'storage-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'storage-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c18',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'storage-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'dock-ready-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'dock-ready',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c15',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'dock-ready-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r05-c18']);
    sim.setVehicleTaskForTest('SH-01', 'storage-outbound', true);
    sim.setVehicleRouteForTest('SH-02', ['module-02-spine-bottom-b', 'column-bottom-b-c22']);
    sim.setVehicleTaskForTest('SH-02', 'dock-ready', true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        targetNodeId: string | null;
        plannedGoalNodeId: string | null;
      }>;
      topLiftOutboundDockCorridorBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const storageOutbound = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const dockReady = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    storageOutbound.targetNodeId = 'column-bottom-b-c22';
    storageOutbound.plannedGoalNodeId = 'column-bottom-b-c22';

    expect(internals.topLiftOutboundDockCorridorBlocker(dockReady, 'column-bottom-b-c22')).toBeNull();
  });

  it('lets the front empty outbound queue slot clear before deeper slots targeting the same bottom exit', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'lift-02-outbound-queue-03-entry-access',
      'column-bottom-b-c20'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-02-outbound-queue-01-entry-access',
      'column-bottom-b-c20'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      topLiftOutboundDockCorridorBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const deeperSlot = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const frontSlot = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftOutboundDockCorridorBlocker(frontSlot, 'column-bottom-b-c20')).toBeNull();
    expect(internals.topLiftOutboundDockCorridorBlocker(deeperSlot, 'column-bottom-b-c20')).toBe('SH-02');
  });

  it('lets a loaded outbound final approach ignore deeper empty queue exits but not the front slot', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-approach-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-approach',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r11-c17',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'loaded-approach-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-a-c20', 'column-bottom-b-c20']);
    sim.setVehicleTaskForTest('SH-01', 'loaded-approach', true);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-02-outbound-queue-03-entry-access',
      'column-bottom-b-c20'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleRouteForTest('SH-03', [
      'lift-02-outbound-queue-01-entry-access',
      'column-bottom-b-c20'
    ]);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      topLiftOutboundDockCorridorBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const loadedApproach = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const frontSlot = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;

    expect(internals.topLiftOutboundDockCorridorBlocker(loadedApproach, 'column-bottom-b-c20')).toBe('SH-03');
    expect(internals.topLiftOutboundDockCorridorBlocker(frontSlot, 'column-bottom-b-c20')).toBeNull();
  });

  it('moves an empty bottom-b inbound blocker out of a loaded outbound dock corridor', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-dock-ingress-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'empty-inbound-blocker-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-dock-ingress',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c08',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'loaded-dock-ingress-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'empty-inbound-blocker',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r03-c08',
      loadId: 'empty-inbound-blocker-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-01-spine-bottom-a',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-dock-ingress', true);
    sim.setVehicleRouteForTest('SH-02', [
      'column-bottom-b-c08',
      'module-01-spine-bottom-b'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'empty-inbound-blocker', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(vehicle: unknown, blockedTargetNodeId: string, block: { reason: string; blockingVehicleId: string | null }): boolean;
      topLiftOutboundDockCorridorBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshHandleMoveBlock(requester, 'module-01-spine-bottom-b', {
      reason: 'outbound-lift-dock-corridor',
      blockingVehicleId: 'SH-02'
    })).toBe(false);

    expect(blocker.routeNodeIds).toEqual(['column-bottom-b-c08', 'column-bottom-b-c09']);
    expect(blocker.targetNodeId).toBe('column-bottom-b-c09');
    expect(blocker.localRouteReason).toBe('temporary-yield');
    expect(blocker.waitReason).toBeNull();
    expect(internals.topLiftOutboundDockCorridorBlocker(requester, 'module-01-spine-bottom-b')).toBeNull();
  });

  it('retreats a loaded outbound gate entrant so an empty queue shuttle can exit first', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-gate-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-gate',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r11-c17',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'loaded-gate-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-b-c20', 'lift-02-outbound-queue-01-entry-access']);
    sim.setVehicleTaskForTest('SH-01', 'loaded-gate', true);
    sim.setVehicleRouteForTest('SH-02', ['lift-02-outbound-queue-01-entry-access', 'column-bottom-b-c20']);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; targetNodeId: string | null; routeNodeIds: string[]; localRouteReason: string | null }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
      topLiftOutboundDockCorridorBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const loaded = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentRefreshHandleMoveBlock(loaded, 'lift-02-outbound-queue-01-entry-access', {
      reason: 'outbound-lift-dock-corridor',
      blockingVehicleId: 'SH-02'
    })).toBe(true);
    expect(loaded.localRouteReason).toBe('temporary-yield');
    expect(loaded.routeNodeIds).toEqual(['column-bottom-b-c20', 'column-bottom-b-c19']);
    expect(loaded.targetNodeId).toBe('column-bottom-b-c19');
    expect(internals.topLiftOutboundDockCorridorBlocker(loaded, loaded.targetNodeId!)).toBeNull();
  });

  it('lets a leading top-lift shuttle move away from a trailing shuttle targeting its current node', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c07', 'lift-01-inbound-queue-01-entry-access']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c06', 'column-top-b-c07']);

    const state = sim.step(0.2);
    const leader = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const follower = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(leader?.state).not.toBe('waiting-blocked');
    expect(leader?.targetNodeId).not.toBe('column-top-b-c07');
    expect(follower).toMatchObject({
      state: 'waiting-blocked',
      targetNodeId: 'column-top-b-c07',
      waitReason: 'node-clearing'
    });
  });

  it('does not reserve a top-lift vertical spine before the shuttle reaches the spine entry', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16',
      'module-boundary-01-spine-top-b',
      'module-boundary-01-spine-middle'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', [
      'module-boundary-01-spine-middle',
      'module-boundary-01-spine-top-b'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.targetNodeId).toBe('column-top-b-c17');
    expect(vehicle?.waitReason).not.toBe('top-lift-spine-opposing-claim');
  });

  it('routes loaded outbound dropoff to the lift service drop point without entering the buffer', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'dropoff-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'dropoff-service-lane',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'dropoff-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-bottom-a-c06']);
    sim.setVehicleTaskForTest('SH-01', 'dropoff-service-lane', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      taskDispatchGoalNodeId(task: { id: string }, vehicle: { id: string }): string;
      agentRouteToGoal(vehicle: { id: string }, task: { id: string }, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'dropoff-service-lane')!;
    const goalNodeId = internals.taskDispatchGoalNodeId(task, vehicle);
    const route = internals.agentRouteToGoal(vehicle, task, goalNodeId);

    expect(route.slice(0, 4)).toEqual([
      'column-bottom-a-c06',
      'column-bottom-b-c06',
      'lift-01-outbound-queue-01-entry-access',
      'lift-01-outbound-queue-01-service-exit'
    ]);
    expect(route).not.toContain('lift-01-outbound-buffer-access');
    expect(route).not.toContain('lift-01-outbound-buffer-03');
  });

  it('routes loaded inbound pickup straight down through the lift service lane into the middle grid', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-service-lane',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r13-c03',
      loadId: 'inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['lift-01-inbound-buffer-03']);
    sim.setVehicleTaskForTest('SH-01', 'inbound-service-lane', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; dropoffNodeId: string }>;
      agentRouteToGoal(vehicle: { id: string }, task: { id: string; dropoffNodeId: string }, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'inbound-service-lane')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.dropoffNodeId);

    expect(route.slice(0, 12)).toEqual([
      'lift-01-inbound-buffer-03',
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c09',
      'storage-r01-c09',
      'storage-r02-c09',
      'storage-r03-c09',
      'storage-r04-c09',
      'storage-r05-c09',
      'storage-r06-c09',
      'storage-r07-c09',
      'column-middle-c09'
    ]);
    expect(route).not.toContain('column-bottom-b-c09');
  });

  it('blocks lift workcell moves that would overlap at the projected yellow-grid dock point', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c07']);
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-01-entry-access',
      'lift-01-inbound-queue-01-service-exit'
    ]);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentMinimalMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
      topLiftProjectedDisplayMoveBlocker(vehicle: unknown, toNodeId: string): string | null;
    };
    const shuttle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftProjectedDisplayMoveBlocker(shuttle, 'lift-01-inbound-queue-01-service-exit')).toBe('SH-01');
    expect(internals.agentMinimalMoveBlocker(shuttle, 'lift-01-inbound-queue-01-service-exit')).toEqual({
      reason: 'projected-display-clearance',
      blockingVehicleId: 'SH-01'
    });
  });

  it('enforces top-lift workcell route contracts for service approach and clear-through order', () => {
    expect(topLiftWorkcellRouteContractViolations([
      'lift-01-inbound-buffer-03',
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c07'
    ], {
      flow: 'inbound-loaded-departure',
      expectedLiftNodeId: 'lift-01-inbound',
      expectedKind: 'inbound'
    })).toEqual([]);

    expect(topLiftWorkcellRouteContractViolations([
      'lift-01-outbound-queue-01-service-exit',
      'lift-01-outbound-queue-02-service-exit',
      'lift-01-outbound-queue-03-service-exit',
      'lift-01-outbound-queue-03-entry-access',
      'column-top-b-c08'
    ], {
      flow: 'outbound-clear-through',
      expectedLiftNodeId: 'lift-01-outbound',
      expectedKind: 'outbound'
    })).toEqual([]);

    expect(topLiftWorkcellRouteContractViolations([
      'parking-lift-01-outbound-queue',
      'lift-01-outbound-queue-01-access',
      'lift-01-outbound-queue-01-entry-access',
      'lift-01-outbound-queue-01-service-exit'
    ], {
      flow: 'outbound-loaded-approach',
      expectedLiftNodeId: 'lift-01-outbound',
      expectedKind: 'outbound'
    })).toEqual([]);

    expect(topLiftWorkcellRouteContractViolations([
      'lift-01-inbound-queue-01-service-exit',
      'lift-01-inbound-queue-01-access',
      'parking-lift-01-inbound-queue'
    ], {
      flow: 'inbound-loaded-departure',
      expectedLiftNodeId: 'lift-01-inbound',
      expectedKind: 'inbound'
    }).map((violation) => violation.code)).toContain('inbound-loaded-reentered-queue');

    expect(topLiftWorkcellRouteContractViolations([
      'column-top-a-c08',
      'lift-01-outbound-buffer-access',
      'lift-01-outbound-queue-01-service-exit'
    ], {
      flow: 'outbound-loaded-approach',
      expectedLiftNodeId: 'lift-01-outbound',
      expectedKind: 'outbound'
    }).map((violation) => violation.code)).toEqual(expect.arrayContaining([
      'outbound-loaded-entered-conveyor',
      'outbound-loaded-service-without-entry'
    ]));
  });

  it('routes loaded inbound top-lift cross travel on top-b before entering the target column', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const task = {
      id: 'inbound-top-a-transfer',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r05-c17',
      loadId: 'inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    };
    const internals = sim as unknown as {
      agentRefreshLoadedInboundRouteToDropoff(currentNodeId: string, task: unknown): string[];
    };

    const route = internals.agentRefreshLoadedInboundRouteToDropoff('lift-02-inbound-queue-01-service-exit', task);

    expect(route.slice(2, 9)).toEqual([
      'column-top-b-c23',
      'column-top-b-c22',
      'module-02-spine-top-b',
      'column-top-b-c21',
      'column-top-b-c20',
      'column-top-b-c19',
      'column-top-b-c18'
    ]);
    expect(route).not.toContain('column-top-a-c19');
    expect(route).not.toContain('lift-02-inbound-queue-02-service-exit');
    expect(route.at(-1)).toBe('storage-r05-c17');
  });

  it('does not let a trailing top-lift middle-aisle shuttle hold the leader at a no-stop spine entry', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'column-middle-c21',
      'module-02-spine-middle',
      'module-02-spine-top-b',
      'module-02-spine-top-a'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', [
      'column-middle-c20',
      'column-middle-c21',
      'module-02-spine-middle'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, true);

    const state = sim.step(0.2);
    const leader = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const follower = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(leader?.state).not.toBe('waiting-blocked');
    expect(leader?.targetNodeId).toBe('module-02-spine-middle');
    expect(follower).toMatchObject({
      state: 'waiting-blocked',
      targetNodeId: 'column-middle-c21'
    });
  });

  it('marks a loaded top-lift no-stop continuation as waiting when its escape node is occupied', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'hidden-hold-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'hidden-hold-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r11-c16',
      loadId: 'hidden-hold-inbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'hidden-hold-outbound-load', state: 'stored', nodeId: 'storage-r01-c18', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'hidden-hold-outbound-task',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c18',
      dropoffNodeId: 'column-bottom-b-c22',
      loadId: 'hidden-hold-outbound-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', [
      'module-boundary-01-spine-top-b',
      'module-boundary-01-spine-middle',
      'column-middle-c15',
      'column-middle-c16',
      'storage-r08-c16',
      'storage-r09-c16',
      'storage-r10-c16',
      'storage-r11-c16'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'hidden-hold-inbound-task', true);
    sim.setVehicleRouteForTest('SH-06', [
      'column-middle-c15',
      'module-boundary-01-spine-middle',
      'module-boundary-01-spine-top-b',
      'column-top-b-c15',
      'column-top-b-c16',
      'column-top-b-c17',
      'column-top-b-c18',
      'storage-r01-c18'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'hidden-hold-outbound-task', false);
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: 'module-boundary-01-spine-middle',
      waitReason: 'top-lift-spine-opposing-claim',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: -900
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentEdgeId: string | null;
        legRemainingM: number;
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
      }>;
      startNextLegAgentSimple(vehicle: unknown, dtSec: number): void;
      agentRefreshMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
    };
    const loaded = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshMoveBlocker(loaded, 'module-boundary-01-spine-middle')).toEqual({
      reason: 'no-stop-continuation-blocked',
      blockingVehicleId: 'SH-06'
    });

    internals.startNextLegAgentSimple(loaded, 0.2);

    expect(loaded).toMatchObject({
      state: 'waiting-blocked',
      currentEdgeId: null,
      legRemainingM: 0,
      targetNodeId: 'module-boundary-01-spine-middle',
      waitReason: 'no-stop-continuation-blocked',
      blockingVehicleId: 'SH-06'
    });
  });

  it('does not strand taskless top-lift shuttles at no-stop middle spine clearance nodes', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'active-column-c15',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r03-c15',
      loadId: 'active-column-c15-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c15']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-02', 'active-column-c15', false);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.waitReason).not.toBe('no-stop-continuation-blocked');
    expect(vehicle?.plannedGoalNodeId).not.toMatch(/spine-middle$/);
    expect(vehicle?.routeNodeIds.at(-1)).not.toMatch(/spine-middle$/);
    expect(vehicle?.routeNodeIds.length).toBeGreaterThan(2);
  });

  it('clears a taskless bottom-b spine blocker for a loaded claimant without using the occupied side', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'bottom-b-loaded-claim-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'bottom-b-loaded-claim',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c08',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'bottom-b-loaded-claim-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['module-01-spine-bottom-b']);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-bottom-a', 'module-01-spine-bottom-b', 'column-bottom-b-c08']);
    sim.setVehicleTaskForTest('SH-02', 'bottom-b-loaded-claim', true);
    sim.setVehicleRouteForTest('SH-03', ['column-bottom-b-c07']);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
        plannedGoalNodeId: string | null;
      }>;
      agentRefreshHandleMoveBlock(vehicle: unknown, blockedTargetNodeId: string, block: { reason: string; blockingVehicleId: string | null }): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.agentRefreshHandleMoveBlock(requester, 'module-01-spine-bottom-b', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-01'
    })).toBe(false);

    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const terminalNode = scenario.layout.nodes.find((node) => node.id === blocker.routeNodeIds.at(-1));

    expect(blocker.currentNodeId).toBe('module-01-spine-bottom-b');
    expect(blocker.targetNodeId).toBe('column-bottom-b-c08');
    expect(blocker.routeNodeIds).toEqual([
      'module-01-spine-bottom-b',
      'column-bottom-b-c08',
      'column-bottom-a-c08',
      'storage-r14-c08'
    ]);
    expect(blocker.waitReason).toBeNull();
    expect(blocker.blockingVehicleId).toBeNull();
    expect(blocker.localRouteReason).toBe('temporary-yield');
    expect(blocker.plannedGoalNodeId).toBe('storage-r14-c08');
    expect(terminalNode?.type).toBe('storage');
    expectRouteHopsAdjacent(scenario, blocker.routeNodeIds);
  });

  it('does not reset a taskless bottom-b blocker that is already clearing on an edge', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-boundary-claim-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-boundary-claim',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r14-c08',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'loaded-boundary-claim-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-01-spine-bottom-b',
      'column-bottom-b-c08',
      'column-bottom-a-c08',
      'storage-r14-c08'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-bottom-a', 'module-01-spine-bottom-b', 'column-bottom-b-c08']);
    sim.setVehicleTaskForTest('SH-02', 'loaded-boundary-claim', true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        currentEdgeId: string | null;
        targetNodeId: string | null;
        legElapsedSec: number;
        legRemainingM: number;
        routeNodeIds: string[];
        localRouteReason: string | null;
      }>;
      traffic: { findEdge: (fromNodeId: string, toNodeId: string) => { id: string } | null };
      agentRefreshHandleMoveBlock(vehicle: unknown, blockedTargetNodeId: string, block: { reason: string; blockingVehicleId: string | null }): boolean;
    };
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const clearingEdge = internals.traffic.findEdge('module-01-spine-bottom-b', 'column-bottom-b-c08')!;
    blocker.currentEdgeId = clearingEdge.id;
    blocker.targetNodeId = 'column-bottom-b-c08';
    blocker.legElapsedSec = 0.2;
    blocker.legRemainingM = 1.226;
    blocker.localRouteReason = 'temporary-yield';

    expect(internals.agentRefreshHandleMoveBlock(requester, 'module-01-spine-bottom-b', {
      reason: 'node-clearing',
      blockingVehicleId: 'SH-01'
    })).toBe(false);

    expect(blocker.currentEdgeId).toBe(clearingEdge.id);
    expect(blocker.targetNodeId).toBe('column-bottom-b-c08');
    expect(blocker.legElapsedSec).toBe(0.2);
    expect(blocker.legRemainingM).toBe(1.226);
    expect(blocker.routeNodeIds).toEqual([
      'module-01-spine-bottom-b',
      'column-bottom-b-c08',
      'column-bottom-a-c08',
      'storage-r14-c08'
    ]);
    expect(blocker.localRouteReason).toBe('temporary-yield');
  });

  it('uses dedicated parking instead of storage standby while top-lift inbound columns are active', () => {
    const scenario = createInboundMvpBaselineScenario();
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'active-column-dropoff',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r03-c13',
      loadId: 'active-column-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const internals = sim as unknown as {
      activeTopLiftInboundColumnKeys(): Set<string>;
      inboundStandbyNodeCandidates(vehicleId: string): Array<{ id: string }>;
    };

    const candidateIds = internals.inboundStandbyNodeCandidates('SH-02').map((node) => node.id);

    expect(internals.activeTopLiftInboundColumnKeys().has('c13')).toBe(true);
    expect(candidateIds.some((nodeId) => /^storage-r\d+-c\d+$/.test(nodeId))).toBe(false);
    expect(candidateIds.some((nodeId) => /^parking-\d+$/.test(nodeId))).toBe(true);
  });

  it('keeps taskless top-lift standby vehicles out of storage columns', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'module-01-spine-middle',
      'module-01-spine-top-b',
      'column-top-b-c07',
      'column-top-b-c06',
      'column-top-b-c05',
      'storage-r01-c05'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, false);
    const internals = sim as unknown as {
      inboundStandbyNodeCandidates(vehicleId: string): Array<{ id: string }>;
    };

    const candidateIds = internals.inboundStandbyNodeCandidates('SH-02').map((node) => node.id);

    expect(candidateIds.some((nodeId) => /^storage-r\d+-c\d+$/.test(nodeId))).toBe(false);
    expect(candidateIds.some((nodeId) => /^parking-\d+$/.test(nodeId))).toBe(true);
  });

  it('uses dedicated parking while a top-lift column is still clearing post-dropoff traffic', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r14-c01', 'column-bottom-a-c01', 'column-bottom-a-c02']);
    const snapshot = sim.createSnapshot();
    const snapshotVehicle = snapshot.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    snapshotVehicle.localRouteReason = 'post-dropoff-column-exit';
    snapshotVehicle.localRouteNodeIds = ['storage-r14-c01', 'column-bottom-a-c01', 'column-bottom-a-c02'];
    snapshotVehicle.plannedRouteNodeIds = ['storage-r14-c01', 'column-bottom-a-c01', 'column-bottom-a-c02'];
    snapshotVehicle.plannedGoalNodeId = 'column-bottom-a-c02';
    snapshot.stateHash = hashEngineSnapshot(snapshot);
    sim.restoreSnapshot(snapshot);
    const internals = sim as unknown as {
      activeTopLiftInboundColumnKeys(): Set<string>;
      inboundStandbyNodeCandidates(vehicleId: string): Array<{ id: string }>;
    };

    const candidateIds = internals.inboundStandbyNodeCandidates('SH-02').map((node) => node.id);

    expect(internals.activeTopLiftInboundColumnKeys().has('c01')).toBe(true);
    expect(candidateIds.some((nodeId) => /^storage-r\d+-c\d+$/.test(nodeId))).toBe(false);
    expect(candidateIds.some((nodeId) => /^parking-\d+$/.test(nodeId))).toBe(true);
  });

  it('keeps top-lift standby candidates on dedicated parking instead of storage rows', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      inboundStandbyNodeCandidates(vehicleId: string): Array<{ id: string }>;
    };

    const candidateIds = internals.inboundStandbyNodeCandidates('SH-02').map((node) => node.id);

    expect(candidateIds.some((nodeId) => /^storage-r\d+-c\d+$/.test(nodeId))).toBe(false);
    expect(candidateIds.every((nodeId) => /^parking-\d+$/.test(nodeId))).toBe(true);
  });

  it('does not fall back to parking a post-dropoff shuttle on a top-lift column access node', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    scenario.layout.nodes = scenario.layout.nodes.map((node) =>
      node.type === 'parking' ? { ...node, noParking: true } : node
    );
    const sim = new ShuttleSimCore(scenario);
    addStoredLoadsForAllStorageExcept(sim, scenario, new Set(['storage-r05-c01']), 'full-standby');
    sim.addLoadForTest({ id: 'no-access-terminal-load-01', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'no-access-terminal-task-01',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-buffer-03',
      dropoffNodeId: 'storage-r05-c01',
      loadId: 'no-access-terminal-load-01',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    const snapshot = sim.createSnapshot();
    const snapshotVehicle = snapshot.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const storageNode = scenario.layout.nodes.find((node) => node.id === 'storage-r05-c01')!;
    snapshotVehicle.state = 'lowering';
    snapshotVehicle.loaded = true;
    snapshotVehicle.taskId = 'no-access-terminal-task-01';
    snapshotVehicle.currentNodeId = 'storage-r05-c01';
    snapshotVehicle.x = storageNode.x;
    snapshotVehicle.z = storageNode.z;
    snapshotVehicle.routeNodeIds = ['storage-r05-c01'];
    snapshotVehicle.plannedRouteNodeIds = ['storage-r05-c01'];
    snapshotVehicle.targetNodeId = null;
    snapshotVehicle.currentEdgeId = null;
    snapshotVehicle.routeIndex = 0;
    snapshotVehicle.phaseRemainingSec = 0;
    snapshot.currentNodeOccupancy = snapshot.currentNodeOccupancy.filter((entry) => entry.vehicleId !== 'SH-01');
    snapshot.currentNodeOccupancy.push({ nodeId: 'storage-r05-c01', vehicleId: 'SH-01' });
    snapshot.stateHash = hashEngineSnapshot(snapshot);
    sim.restoreSnapshot(snapshot);

    sim.step(0.25);
    const state = sim.getState();
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const standbyEvent = sim.getEventLog().find((event) => event.eventType === 'vehicle-standby-dispatched');

    expect(state.kpis.completedInbound).toBe(1);
    expect(standbyEvent).toBeUndefined();
    expect(vehicle?.routeNodeIds.at(-1)).toBe('storage-r05-c01');
    expect(vehicle?.plannedGoalNodeId).toBeNull();
  });

  it('holds a taskless top-lift shuttle on a no-parking access node when no storage standby exists', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    scenario.layout.nodes = scenario.layout.nodes.map((node) =>
      node.type === 'parking' ? { ...node, noParking: true } : node
    );
    const sim = new ShuttleSimCore(scenario);
    addStoredLoadsForAllStorageExcept(sim, scenario, new Set(), 'no-standby');
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c01']);

    sim.start();
    sim.step(0.25);
    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.currentNodeId).toBe('column-middle-c01');
    expect(vehicle?.state).toBe('waiting-blocked');
    expect(vehicle?.waitReason).toBe('standby-unavailable');
    expect(vehicle?.routeNodeIds).toEqual([]);
    expect(vehicle?.plannedGoalNodeId).toBeNull();
  });

  it('clears a taskless top-lift shuttle off no-parking access even when queued work is gated', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c08']);
    for (const [index, vehicleId] of ['SH-02', 'SH-03'].entries()) {
      const taskId = `active-outbound-${index + 1}`;
      const loadId = `active-outbound-load-${index + 1}`;
      sim.addLoadForTest({ id: loadId, state: 'stored', nodeId: `storage-r0${index + 1}-c01`, vehicleId: null, weightKg: 100 });
      sim.addTaskForTest({
        id: taskId,
        kind: 'outbound',
        state: 'assigned',
        createdAtSec: 0,
        assignedAtSec: 0,
        startedAtSec: null,
        completedAtSec: null,
        pickupNodeId: `storage-r0${index + 1}-c01`,
        dropoffNodeId: 'column-top-a-c08',
        loadId,
        vehicleId,
        replanCount: 0,
        waitReason: null
      });
      sim.setVehicleTaskForTest(vehicleId, taskId, false);
    }
    sim.addLoadForTest({ id: 'queued-outbound-load', state: 'stored', nodeId: 'storage-r03-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queued-outbound',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c01',
      dropoffNodeId: 'column-top-a-c08',
      loadId: 'queued-outbound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    sim.start();
    sim.step(0.25);
    const vehicle = sim.getState().vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.currentNodeId).toBe('column-top-a-c08');
    expect(vehicle?.waitReason).not.toBe('queued-work-awaiting-release');
    expect(vehicle?.state).toBe('assigned');
    expect(vehicle?.targetNodeId).not.toBeNull();
    expect(vehicle?.routeNodeIds.at(-1)).not.toBe('column-top-a-c08');
  });

  it('keeps top-lift inbound source capacity independent of lift approach capacity', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        liftApproachCapacity: 8,
        sourceBufferCapacity: 4
      }
    });
    const state = new ShuttleSimCore(scenario).getState();
    const inboundPorts = state.traffic.liftPorts.filter((port) => port.kind === 'inbound');

    expect(inboundPorts.map((port) => port.approachCapacity)).toEqual([8, 8]);
    expect(inboundPorts.map((port) => port.sourceBufferCapacity)).toEqual([4, 4]);
    expect(inboundPorts.map((port) => port.sourceBufferOccupancy)).toEqual([4, 4]);
    expect(state.loads.filter((load) => load.state === 'waiting' && load.nodeId?.startsWith('lift-')).length).toBe(8);
  });

  it('uses one canonical inbound MVP baseline for demo and audit runs', () => {
    const scenario = createInboundMvpBaselineScenario();

    expect(scenario.id).toBe('shuttle-all-inbound-8x-7200');
    expect(scenario.layout.calibrationProfile?.id).toBe('top-lift-column-v1');
    expect(scenario.vehicles.count).toBe(8);
    expect(scenario.taskGeneration.inboundRatePerHour).toBe(7200);
    expect(scenario.taskGeneration.outboundRatePerHour).toBe(0);
    expect(scenario.trafficPolicy.controllerMode).toBe('agent-refresh');
    expect(scenario.trafficPolicy.liftApproachCapacity).toBe(4);
    expect(scenario.trafficPolicy.sourceBufferCapacity).toBe(4);
  });

  it('separates top-lift inbound and outbound workcells onto opposite layout sides', () => {
    const scenario = createInboundOutboundDemoScenario();
    const nodesById = new Map(scenario.layout.nodes.map((node) => [node.id, node]));
    const hasDirectedEdge = (from: string, to: string): boolean =>
      scenario.layout.edges.some((edge) =>
        edge.from === from && edge.to === to ||
        edge.directionMode === 'twoWay' && edge.from === to && edge.to === from
      );
    const findColumnAccessAtX = (level: 'top-a' | 'top-b' | 'bottom-a' | 'bottom-b', x: number): string => {
      const node = scenario.layout.nodes.find((candidate) =>
        candidate.id.startsWith(`column-${level}-c`) && Math.abs(candidate.x - x) < 1e-6
      );
      expect(node).toBeTruthy();
      return node!.id;
    };
    const distance = (leftId: string, rightId: string): number => {
      const left = nodesById.get(leftId)!;
      const right = nodesById.get(rightId)!;
      return Math.hypot(left.x - right.x, left.z - right.z);
    };
    const topOuterLane = nodesById.get('column-top-a-c01')!;
    const bottomOuterLane = nodesById.get('column-bottom-b-c01')!;

    for (let liftIndex = 1; liftIndex <= 2; liftIndex += 1) {
      const liftLabel = String(liftIndex).padStart(2, '0');
      const inboundLiftId = `lift-${liftLabel}-inbound`;
      const outboundLiftId = `lift-${liftLabel}-outbound`;
      const inboundLift = nodesById.get(inboundLiftId)!;
      const outboundLift = nodesById.get(outboundLiftId)!;
      const inboundBufferAccess = nodesById.get(`${inboundLiftId}-buffer-access`)!;
      const outboundBufferAccess = nodesById.get(`${outboundLiftId}-buffer-access`)!;

      expect(inboundLift.z).toBeLessThan(topOuterLane.z);
      expect(inboundBufferAccess.z).toBeLessThan(topOuterLane.z);
      expect(outboundLift.z).toBeGreaterThan(bottomOuterLane.z);
      expect(outboundBufferAccess.z).toBeGreaterThan(bottomOuterLane.z);

      for (const kind of ['inbound', 'outbound'] as const) {
        const liftId = `lift-${liftLabel}-${kind}`;
        const queueNodeIds = [
          `parking-${liftId}-queue`,
          `parking-${liftId}-queue-02`,
          `parking-${liftId}-queue-03`
        ];
        for (let left = 0; left < queueNodeIds.length; left += 1) {
          for (let right = left + 1; right < queueNodeIds.length; right += 1) {
            expect(distance(queueNodeIds[left]!, queueNodeIds[right]!)).toBeGreaterThanOrEqual(scenario.vehicles.widthM);
          }
        }
      }

      expect(distance(`parking-${inboundLiftId}-queue`, `parking-${outboundLiftId}-queue`)).toBeGreaterThanOrEqual(scenario.vehicles.widthM);

      const outboundEntryId = `${outboundLiftId}-queue-01-entry-access`;
      const outboundEntry = nodesById.get(outboundEntryId)!;
      const outboundApproachId = findColumnAccessAtX('bottom-a', outboundEntry.x);
      const outboundExitId = findColumnAccessAtX('bottom-b', outboundEntry.x);
      const oldTopApproachId = findColumnAccessAtX('top-a', outboundEntry.x);

      expect(hasDirectedEdge(outboundApproachId, outboundExitId)).toBe(true);
      expect(hasDirectedEdge(outboundExitId, outboundEntryId)).toBe(true);
      expect(hasDirectedEdge(outboundEntryId, outboundExitId)).toBe(true);
      expect(hasDirectedEdge(outboundApproachId, outboundEntryId)).toBe(false);
      expect(hasDirectedEdge(oldTopApproachId, outboundEntryId)).toBe(false);
    }
  });

  it('expands the inbound MVP layout by whole top-lift regions', () => {
    const scenario = createInboundMvpBaselineScenario({
      layoutProfile: {
        liftPairCount: 3
      }
    });
    const contract = summarizeScenarioStaticSceneContract(scenario);

    expect(scenario.layout.calibrationProfile?.id).toBe('top-lift-column-v1');
    expect(contract.storageRows).toBe(14);
    expect(contract.storageColumns).toBe(42);
    expect(contract.storageCellCount).toBe(588);
    expect(contract.storageIslandCount).toBe(12);
    expect(scenario.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'inbound')).toHaveLength(3);
    expect(scenario.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'outbound')).toHaveLength(3);
    expect(scenario.layout.nodes.some((node) => node.id === 'module-03-spine-top-a')).toBe(true);
    expect(scenario.layout.nodes.some((node) => node.id === 'module-boundary-01-spine-middle')).toBe(true);
    expect(scenario.layout.nodes.some((node) => node.id === 'module-boundary-02-spine-middle')).toBe(true);
    expect(scenario.layout.nodes.some((node) => node.id === 'lift-03-inbound-buffer-03')).toBe(true);
  });

  it('sweeps top-lift region counts for stable buffer topology', () => {
    for (let liftPairCount = 1; liftPairCount <= 8; liftPairCount += 1) {
      const scenario = createInboundMvpBaselineScenario({
        layoutProfile: {
          liftPairCount
        }
      });
      const contract = summarizeScenarioStaticSceneContract(scenario);
      const nodeIds = scenario.layout.nodes.map((node) => node.id);
      const storageNodes = scenario.layout.nodes.filter((node) => node.type === 'storage');
      const liftNodes = scenario.layout.nodes.filter((node) => node.type === 'lift-blackbox');

      expect(new Set(nodeIds).size).toBe(nodeIds.length);
      expect(contract.storageRows).toBe(14);
      expect(contract.storageColumns).toBe(14 * liftPairCount);
      expect(contract.storageCellCount).toBe(196 * liftPairCount);
      expect(contract.storageIslandCount).toBe(4 * liftPairCount);
      expect(liftNodes.filter((node) => node.liftKind === 'inbound')).toHaveLength(liftPairCount);
      expect(liftNodes.filter((node) => node.liftKind === 'outbound')).toHaveLength(liftPairCount);
      expect(scenario.layout.nodes.filter((node) => node.id.startsWith('module-boundary-'))).toHaveLength(Math.max(0, liftPairCount - 1) * 5);

      for (const lift of liftNodes) {
        expect(scenario.layout.nodes.some((node) => node.id === `${lift.id}-buffer-01`)).toBe(true);
        expect(scenario.layout.nodes.some((node) => node.id === `${lift.id}-buffer-02`)).toBe(true);
        expect(scenario.layout.nodes.some((node) => node.id === `${lift.id}-buffer-03`)).toBe(true);
      }
      for (let boundaryIndex = 1; boundaryIndex < liftPairCount; boundaryIndex += 1) {
        const boundaryLabel = String(boundaryIndex).padStart(2, '0');
        for (const [fromLevel, toLevel] of [
          ['top-a', 'top-b'],
          ['top-b', 'middle'],
          ['middle', 'bottom-a'],
          ['bottom-a', 'bottom-b']
        ] as const) {
          const from = `module-boundary-${boundaryLabel}-spine-${fromLevel}`;
          const to = `module-boundary-${boundaryLabel}-spine-${toLevel}`;
          expect(scenario.layout.edges.some((edge) =>
            (edge.from === from && edge.to === to) ||
            (edge.from === to && edge.to === from)
          )).toBe(true);
        }
      }
    }
  });

  it('samples top-lift column exit routes across region counts', async () => {
    for (const liftPairCount of [1, 2, 3, 8]) {
      const scenario = createInboundMvpBaselineScenario({
        layoutProfile: {
          liftPairCount
        }
      });
      const storageNodes = scenario.layout.nodes.filter((node) => node.type === 'storage');
      const sim = new ShuttleSimCore(scenario);
      const internals = sim as unknown as {
        topLiftColumnKey(nodeId: string): string | null;
        postDropoffColumnExitRoute(vehicle: { id: string; currentNodeId: string }): string[] | null;
        routeHasOnlyAdjacentEdges(routeNodeIds: string[]): boolean;
      };
      for (const storageNode of storageNodes) {
        expect(internals.topLiftColumnKey(storageNode.id)).toMatch(/^c\d+$/);
      }

      const sampledColumnNumbers = new Set<number>();
      for (let moduleIndex = 0; moduleIndex < liftPairCount; moduleIndex += 1) {
        const moduleColumnStart = moduleIndex * 14 + 1;
        for (const columnNumber of [
          moduleColumnStart,
          moduleColumnStart + 6,
          moduleColumnStart + 7,
          moduleColumnStart + 13
        ]) {
          sampledColumnNumbers.add(columnNumber);
        }
      }
      const sampledStorageNodeIds = [...sampledColumnNumbers].flatMap((columnNumber) =>
        [1, 7, 8, 14].map((rowNumber) =>
          `storage-r${String(rowNumber).padStart(2, '0')}-c${String(columnNumber).padStart(2, '0')}`
        )
      );
      for (const storageNodeId of sampledStorageNodeIds) {
        const storageNode = storageNodes.find((node) => node.id === storageNodeId);
        expect(storageNode).toBeDefined();
        if (!storageNode) {
          throw new Error(`Missing sampled storage node ${storageNodeId}`);
        }
        const route = internals.postDropoffColumnExitRoute({ id: 'test-vehicle', currentNodeId: storageNode.id });
        const exitNodeId = route?.find((nodeId) => /^column-(?:top-b|middle|bottom-a)-c\d+$/.test(nodeId));
        expect(route).not.toBeNull();
        expect(route?.[0]).toBe(storageNode.id);
        expect(exitNodeId).toMatch(/^column-(?:top-b|middle|bottom-a)-c\d+$/);
        expect(internals.topLiftColumnKey(exitNodeId!)).toBe(internals.topLiftColumnKey(storageNode.id));
        expect(internals.routeHasOnlyAdjacentEdges(route!)).toBe(true);
        await new Promise<void>((resolve) => setTimeout(resolve, 0));
      }
    }
  }, 180000);

  it('emits deterministic fixed-step replay manifests', () => {
    const scenario = createInboundMvpBaselineScenario({
      id: 'fixed-step-test',
      durationSec: 10,
      taskGeneration: { maxTasks: 16 }
    });
    const first = runFixedStep({ scenario, durationSec: 10, stepSec: 0.25, commitSha: 'test-commit' });
    const second = runFixedStep({ scenario, durationSec: 10, stepSec: 0.25, commitSha: 'test-commit' });

    expect(first.manifest).toMatchObject({
      schemaVersion: 'shuttle.fixedStepRunManifest.v1',
      scenarioId: 'fixed-step-test',
      seed: scenario.seed,
      commitSha: 'test-commit',
      stepSec: 0.25,
      durationSec: 10,
      finalSimTimeSec: 10
    });
    expect(first.manifest.scenarioHash).toBe(second.manifest.scenarioHash);
    expect(first.manifest.eventLogHash).toBe(second.manifest.eventLogHash);
    expect(first.manifest.stateHash).toBe(second.manifest.stateHash);
  });

  it('limits generated inbound source loads to one waiting pallet per lift', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      physicsParams: {
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      trafficPolicy: {
        liftApproachCapacity: 8
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();
    for (let index = 0; index < 80; index += 1) {
      sim.step(0.2);
    }

    const state = sim.getState();
    const inboundLiftIds = new Set(
      scenario.layout.nodes
        .filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'inbound')
        .map((node) => node.id)
    );
    const waitingSourceLoads = state.loads.filter((load) =>
      load.state === 'waiting' &&
      load.nodeId !== null &&
      inboundLiftIds.has(load.nodeId)
    );
    const waitingLoadsByLift = new Map<string, number>();
    for (const load of waitingSourceLoads) {
      waitingLoadsByLift.set(load.nodeId!, (waitingLoadsByLift.get(load.nodeId!) ?? 0) + 1);
    }

    expect(waitingSourceLoads.length).toBeLessThanOrEqual(inboundLiftIds.size);
    expect(Math.max(0, ...waitingLoadsByLift.values())).toBeLessThanOrEqual(1);
    expect(state.tasks.filter((task) => task.kind === 'inbound' && task.state === 'assigned')).toHaveLength(8);
    expect(state.tasks.length).toBeLessThanOrEqual(16);
  }, 30000);

  it('keeps inbound source buffers replenished independently of task WIP', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      durationSec: 7200,
      vehicles: {
        count: 8,
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        liftApproachCapacity: 8,
        minimumClearanceSec: 0.4,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();

    const inboundLiftIds = scenario.layout.nodes
      .filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'inbound')
      .map((node) => node.id)
      .sort();
    const storageCapacity = scenario.layout.nodes.filter((node) => node.type === 'storage').length;
    let maxEmptyLiftCount = 0;
    let sourceGapTicks = 0;

    for (let index = 0; index <= 600; index += 1) {
      const state = index === 0 ? sim.getState() : sim.step(0.2);
      const waitingLoadsByLift = new Map<string, number>();
      for (const load of state.loads) {
        if (load.state === 'waiting' && load.nodeId && inboundLiftIds.includes(load.nodeId)) {
          waitingLoadsByLift.set(load.nodeId, (waitingLoadsByLift.get(load.nodeId) ?? 0) + 1);
        }
      }
      const activeInboundTasks = state.tasks.filter((task) =>
        task.kind === 'inbound' &&
        task.state !== 'completed' &&
        task.state !== 'failed'
      );
      const activeInboundLoadIds = activeInboundTasks.map((task) => task.loadId);
      const storedCount = state.loads.filter((load) => load.state === 'stored' && load.nodeId?.startsWith('storage-')).length;
      const storageFull = storedCount >= storageCapacity;
      const emptyLiftCount = storageFull
        ? 0
        : inboundLiftIds.filter((liftNodeId) => (waitingLoadsByLift.get(liftNodeId) ?? 0) === 0).length;

      maxEmptyLiftCount = Math.max(maxEmptyLiftCount, emptyLiftCount);
      if (emptyLiftCount > 0) {
        sourceGapTicks += 1;
      }

      for (const liftNodeId of inboundLiftIds) {
        expect(waitingLoadsByLift.get(liftNodeId) ?? 0).toBeLessThanOrEqual(1);
      }
      expect(new Set(activeInboundLoadIds).size).toBe(activeInboundLoadIds.length);
    }

    const finalState = sim.getState();
    expect(maxEmptyLiftCount).toBe(0);
    expect(sourceGapTicks).toBe(0);
    expect(finalState.kpis.deadlockCount).toBe(0);
    expect(finalState.kpis.livelockCount).toBe(0);
    expect(finalState.traffic.physicalViolationCount).toBe(0);
    expect(finalState.kpis.completedInbound).toBeGreaterThan(0);
  }, 90000);

  it('parses legacy state diagnostics without lift-port allocation details', () => {
    const state = new ShuttleSimCore(createDefaultShuttleScenario()).getState();
    const legacyTraffic: Record<string, unknown> = { ...state.traffic };
    delete legacyTraffic.liftPorts;

    const parsed = ShuttleSimStateSchema.parse({ ...state, traffic: legacyTraffic });

    expect(parsed.traffic.liftPorts).toEqual([]);
  });

  it('starts eight-shuttle smoke runs from distinct staged parking positions', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({ vehicles: { count: 8 } }));
    const state = sim.getState();
    const debug = sim.getDebugState();

    expect(state.vehicles.map((vehicle) => vehicle.currentNodeId).sort()).toEqual([
      'parking-a',
      'parking-b',
      'parking-c',
      'parking-d',
      'parking-e',
      'parking-f',
      'parking-g',
      'parking-h'
    ]);
    expect(debug.currentNodeOccupancy.map((occupancy) => occupancy.nodeId).sort()).toEqual([
      'parking-a',
      'parking-b',
      'parking-c',
      'parking-d',
      'parking-e',
      'parking-f',
      'parking-g',
      'parking-h'
    ]);
  });

  it('uses storage cells as under-load temporary parking beyond dedicated pads', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({ vehicles: { count: 12 } }));
    const state = sim.getState();
    const storageParkedVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-09');
    const storageParkedNodeIds = state.vehicles.slice(8).map((vehicle) => vehicle.currentNodeId);

    expect(storageParkedVehicle?.currentNodeId).toMatch(/^storage-r\d+-c\d+$/);
    expect(storageParkedNodeIds).toEqual([
      'storage-r16-c01',
      'storage-r15-c01',
      'storage-r14-c01',
      'storage-r13-c01'
    ]);
    sim.addLoadForTest({
      id: 'load-above-sh-09',
      state: 'stored',
      nodeId: storageParkedVehicle!.currentNodeId,
      vehicleId: null,
      weightKg: 100
    });

    const debug = sim.getDebugState();
    expect(debug.currentNodeOccupancy).toContainEqual({
      nodeId: storageParkedVehicle!.currentNodeId,
      vehicleId: 'SH-09'
    });
    expect(debug.storageNodeOccupancy).toContainEqual({
      nodeId: storageParkedVehicle!.currentNodeId,
      loadId: 'load-above-sh-09'
    });
  });

  it('parks inbound-only idle shuttles inside storage cells before using aisle-side pads', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 8
      }
    }));

    expect(sim.getState().vehicles.map((vehicle) => vehicle.currentNodeId)).toEqual([
      'storage-r16-c12',
      'storage-r16-c24',
      'storage-r01-c06',
      'storage-r01-c18',
      'storage-r16-c01',
      'storage-r15-c01',
      'storage-r14-c01',
      'storage-r13-c01'
    ]);
    expect(sim.getState().vehicles.every((vehicle) => !vehicle.currentNodeId.startsWith('parking-'))).toBe(true);
  });

  it('primes saturated all-inbound sources and dispatches every shuttle without aisle-wide serialization', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: {
        count: 8,
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      trafficPolicy: {
        liftApproachCapacity: 8,
        minimumClearanceSec: 0.4,
        deadlockDetectSec: 20
      }
    }));
    const resetState = sim.getState();
    const waitingLoads = resetState.loads.filter((load) => load.state === 'waiting');

    expect(resetState.tasks).toHaveLength(8);
    expect(waitingLoads).toHaveLength(8);
    expect(new Set(resetState.tasks.map((task) => task.pickupNodeId)).size).toBe(8);
    expect(new Set(waitingLoads.map((load) => load.nodeId)).size).toBe(8);

    sim.start();
    const dispatchedState = sim.step(0.1);

    expect(dispatchedState.tasks.filter((task) => task.state === 'assigned')).toHaveLength(8);
    expect(dispatchedState.vehicles.filter((vehicle) => vehicle.currentEdgeId !== null)).toHaveLength(8);
    expect(dispatchedState.traffic.waitingVehicles).toEqual([]);
    expect(dispatchedState.vehicles.every((vehicle) => vehicle.waitReason === null)).toBe(true);
    expect(dispatchedState.traffic).toMatchObject({
      trafficMode: 'flow-debug',
      safetyValidated: false,
      longHorizonReservationEnabled: false,
      clearThroughLookaheadEnabled: true,
      clearThroughMaxLookaheadLegs: 8,
      legacyZoneHoldEnabled: false
    });
    expect(dispatchedState.reservations.some((reservation) => reservation.reasonCode === 'zone-hold')).toBe(false);
    expectNoTrafficSafetyFailures(dispatchedState);
  });

  it('keeps the 8-shuttle all-inbound stress flow alive without a legacy reservation wall', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      durationSec: 7200,
      vehicles: {
        count: 8,
        emptySpeedMps: 3,
        loadedSpeedMps: 2.25,
        accelerationMps2: 3,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 3,
        loadedSpeedMps: 2.25,
        accelerationMps2: 3,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      trafficPolicy: {
        liftApproachCapacity: 8,
        minimumClearanceSec: 0.4,
        deadlockDetectSec: 20
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();

    for (let index = 0; index < 5; index += 1) {
      sim.step(0.2);
    }
    const oneSecondState = sim.getState();

    expect(oneSecondState.tasks.filter((task) => task.state === 'assigned')).toHaveLength(8);
    expect(oneSecondState.vehicles.filter((vehicle) => vehicle.currentEdgeId !== null)).toHaveLength(8);
    expect(oneSecondState.traffic.waitingVehicles).toEqual([]);
    expect(oneSecondState.reservations.some((reservation) => reservation.reasonCode === 'zone-hold')).toBe(false);

    for (let index = 0; index < 595; index += 1) {
      sim.step(0.2);
    }
    const state = sim.getState();

    expect(state.kpis.completedInbound).toBeGreaterThan(0);
    expect(state.kpis.totalPph).toBeGreaterThan(0);
    expect(state.traffic.waitingVehicles.length).toBeLessThan(8);
    expect(state.traffic.deadlockCandidateVehicleIds).toEqual([]);
    expect(state.reservations.some((reservation) => reservation.reasonCode === 'zone-hold')).toBe(false);
    expect(state.kpis.blockedTimeByReasonSec['zone-reserved'] ?? 0).toBeGreaterThan(0);
    expect(state.kpis.blockedTimeByReasonSec['zone-reserved'] ?? 0).toBeLessThan(state.kpis.blockedTimeByReasonSec['vehicle-unavailable'] ?? Number.POSITIVE_INFINITY);
    expectNoTrafficSafetyFailures(state);
  }, 60000);

  it('does not place inbound approach queues on lift connector footprints', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2, lengthM: 1.3, widthM: 1.3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        liftApproachCapacity: 8,
        minimumClearanceSec: 0.4
      }
    });
    const stageNodes = scenario.layout.nodes.filter((node) => /-stage-\d\d$/.test(node.id));
    const stagingEdges = scenario.layout.edges.filter((edge) => edge.from.includes('-stage-') || edge.to.includes('-stage-'));

    expect(stageNodes).toHaveLength(0);
    expect(stagingEdges).toHaveLength(0);
  });

  it('brakes a moving side-aisle shuttle before its footprint reaches a newly occupied node', () => {
    const scenario = createDefaultShuttleScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        liftApproachCapacity: 8,
        minimumClearanceSec: 0.4
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 2,
        accelerationMps2: 4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['left-row-06', 'left-row-07']);
    sim.setVehicleRouteForTest('SH-02', ['parking-a']);
    sim.step(0.2);
    sim.setVehicleRouteForTest('SH-02', ['left-row-07']);

    const state = sim.step(0.2);
    const movingVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const stoppedVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(movingVehicle).toMatchObject({
      state: 'waiting-blocked',
      currentEdgeId: 'left-row-06-left-row-07',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-02'
    });
    expect(Math.abs((movingVehicle?.x ?? 0) - (stoppedVehicle?.x ?? 0))).toBeLessThan(scenario.vehicles.widthM);
    expect(Math.abs((movingVehicle?.z ?? 0) - (stoppedVehicle?.z ?? 0))).toBeGreaterThanOrEqual(scenario.vehicles.lengthM);
  });

  it('prefers an empty storage refuge over backing down the side aisle for head-on yield', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2, lengthM: 1.3, widthM: 1.3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['left-row-06', 'left-row-07']);
    sim.setVehicleRouteForTest('SH-02', ['left-row-07', 'left-row-06']);

    const state = sim.step(0.1);
    const lowerVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const upperVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(lowerVehicle?.targetNodeId).toBe('storage-r06-c01');
    expect(lowerVehicle?.routeNodeIds.slice(0, 4)).toEqual([
      'left-row-06',
      'storage-r06-c01',
      'left-row-06',
      'left-row-07'
    ]);
    expect(upperVehicle).toMatchObject({
      state: 'waiting-blocked',
      targetNodeId: 'storage-r07-c01',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-01'
    });
    expect(lowerVehicle?.targetNodeId).not.toBe('left-row-05');
    expect(upperVehicle?.targetNodeId).not.toBe('left-row-08');
  });

  it('agent-simple lets an empty shuttle bypass a blocked side aisle by crossing the storage row', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['left-row-06', 'left-row-08']);
    sim.setVehicleRouteForTest('SH-02', ['left-row-07']);

    const state = sim.step(0.1);
    const bypassVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(state.traffic.trafficMode).toBe('agent-simple');
    expect(bypassVehicle?.targetNodeId).toBe('storage-r06-c01');
    expect(bypassVehicle?.routeNodeIds.slice(0, 2)).toEqual(['left-row-06', 'storage-r06-c01']);
    expect(bypassVehicle?.routeNodeIds).toContain('right-row-06');
    expect(bypassVehicle?.routeNodeIds).toContain('right-row-08');
    expect(bypassVehicle?.routeNodeIds.slice(0, 3)).not.toEqual(['left-row-06', 'storage-r06-c01', 'left-row-06']);
  });

  it('agent-simple commits to a chosen storage bypass instead of bouncing back to the main lane', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'main-north-05',
      'right-row-08',
      'storage-r08-c24',
      'outbound-lift-top-02'
    ]);

    sim.start();
    let state = sim.getState();
    for (let step = 0; step < 30; step += 1) {
      state = sim.step(0.1);
      const vehicle = state.vehicles[0]!;
      if (vehicle.currentNodeId === 'right-row-08' && vehicle.currentEdgeId === null) {
        break;
      }
    }
    state = sim.step(0.1);
    const vehicle = state.vehicles[0]!;

    expect(vehicle.currentNodeId).toBe('right-row-08');
    expect(vehicle.targetNodeId).toBe('storage-r08-c24');
    expect(vehicle.currentEdgeId).toBe('right-row-08-storage-r08-c24');
  });

  it('agent-simple assigns a pickup goal without installing a dispatcher route', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.start();
    const state = sim.step(0.1);
    const assigned = sim.getEventLog().find((event) => event.eventType === 'task-assigned');
    const task = state.tasks[0]!;

    expect(assigned?.reason).toBe('nearest-available-agent-goal');
    expect(assigned?.details.dispatcherRouteInstalled).toBe(false);
    expect(assigned?.details.route).toBeUndefined();
    expect(assigned?.details.pickupNodeId).toBe(task.pickupNodeId);
    expect(assigned?.details.dropoffNodeId).toBe(task.dropoffNodeId);
    expect(state.vehicles[0]?.plannedGoalNodeId).toBe(task.pickupNodeId);
    expect(state.vehicles[0]?.plannedRouteNodeIds.at(-1)).toBe(task.pickupNodeId);
  });

  it('agent-simple uses the direct adjacent main-lane hop to reach a nearby pickup', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['main-north-05']);
    sim.addLoadForTest({ id: 'nearby-pickup-load', state: 'waiting', nodeId: 'outbound-lift-top-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'nearby-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'outbound-lift-top-02',
      dropoffNodeId: 'storage-r08-c20',
      loadId: 'nearby-pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'nearby-pickup', false);

    const state = sim.step(0.2);
    const route = state.vehicles[0]?.plannedRouteNodeIds ?? [];

    expect(route.slice(0, 3)).toEqual(['main-north-05', 'main-north-04', 'outbound-lift-top-02']);
    expect(route).not.toEqual(expect.arrayContaining(['main-south-05', 'main-south-04']));
  });

  it('agent-minimal ignores distant dynamic blockers and only stops at the next occupied node', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-minimal'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['main-north-01', 'main-north-02', 'main-north-03']);
    sim.setVehicleRouteForTest('SH-02', ['main-north-03']);

    let state = sim.step(0.2);
    let vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(state.traffic.trafficMode).toBe('agent-minimal');
    expect(vehicle?.state).not.toBe('waiting-blocked');
    expect(vehicle?.currentEdgeId).toBe('main-north-01-main-north-02');
    expect(vehicle?.targetNodeId).toBe('main-north-02');

    sim.setVehicleRouteForTest('SH-01', ['main-north-01', 'main-north-02', 'main-north-03']);
    sim.setVehicleRouteForTest('SH-02', ['main-north-02']);
    state = sim.step(0.2);
    vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.state).toBe('waiting-blocked');
    expect(vehicle?.targetNodeId).toBe('main-north-02');
    expect(vehicle?.waitReason).toBe('node-occupied');
    expect(vehicle?.blockingVehicleId).toBe('SH-02');
  });

  it('agent-minimal installs a shortest pickup route when the task is assigned', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-minimal'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['main-north-05']);
    sim.addLoadForTest({ id: 'pickup-load', state: 'waiting', nodeId: 'outbound-lift-top-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'pickup-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'outbound-lift-top-02',
      dropoffNodeId: 'storage-r08-c20',
      loadId: 'pickup-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    const state = sim.step(0.2);
    const vehicle = state.vehicles[0]!;
    const assigned = sim.getEventLog().find((event) => event.eventType === 'task-assigned' && event.taskId === 'pickup-task');

    expect(assigned?.details.dispatcherRouteInstalled).toBe(false);
    expect(assigned?.details.route).toBe('main-north-05>main-north-04>outbound-lift-top-02');
    expect(vehicle.routeNodeIds).toEqual(['main-north-05', 'main-north-04', 'outbound-lift-top-02']);
    expect(vehicle.plannedRouteNodeIds).toEqual(['main-north-05', 'main-north-04', 'outbound-lift-top-02']);
    expect(vehicle.localRouteNodeIds).toEqual([]);
  });

  it('agent-minimal uses lift-column storage transfers instead of side-aisle loops for nearby pickups', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: {
        count: 8,
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      trafficPolicy: {
        controllerMode: 'agent-minimal',
        liftApproachCapacity: 8,
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.start();
    const state = sim.step(0.2);
    const shuttle3 = state.vehicles.find((vehicle) => vehicle.id === 'SH-03');
    const shuttle4 = state.vehicles.find((vehicle) => vehicle.id === 'SH-04');
    const shuttle5 = state.vehicles.find((vehicle) => vehicle.id === 'SH-05');
    const shuttle7 = state.vehicles.find((vehicle) => vehicle.id === 'SH-07');

    expect(shuttle3?.routeNodeIds).toEqual([
      'storage-r01-c06',
      'inbound-lift-top-01-row-01-transfer',
      'inbound-lift-top-01'
    ]);
    expect(shuttle4?.routeNodeIds).toEqual([
      'storage-r01-c18',
      'inbound-lift-top-02-row-01-transfer',
      'inbound-lift-top-02'
    ]);
    expect(shuttle5?.routeNodeIds).toEqual([
      'storage-r16-c06',
      'outbound-lift-bottom-01-row-16-transfer',
      'outbound-lift-bottom-01'
    ]);
    expect(shuttle7?.routeNodeIds).toEqual([
      'storage-r01-c12',
      'outbound-lift-top-01-row-01-transfer',
      'outbound-lift-top-01'
    ]);
  });

  it('agent-refresh installs direct one-shot pickup routes and row-ordered inbound dropoffs', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: {
        count: 8,
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        liftApproachCapacity: 8,
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.start();
    const state = sim.step(0.2);
    const inboundRows = state.tasks
      .filter((task) => task.kind === 'inbound')
      .map((task) => /^storage-r(\d+)-c\d+$/.exec(task.dropoffNodeId)?.[1] ?? '');
    const shuttle2 = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');
    const shuttle8 = state.vehicles.find((vehicle) => vehicle.id === 'SH-08');

    expect(state.traffic.trafficMode).toBe('agent-refresh');
    expect(inboundRows).toEqual(['01', '02', '03', '04', '05', '06', '07', '08']);
    expect(shuttle2?.routeNodeIds).toEqual([
      'storage-r16-c24',
      'inbound-lift-bottom-02-row-16-transfer',
      'inbound-lift-bottom-02'
    ]);
    expect(shuttle8?.routeNodeIds).toEqual([
      'storage-r01-c24',
      'outbound-lift-top-02-row-01-transfer',
      'outbound-lift-top-02'
    ]);
    expect(shuttle2?.plannedRouteNodeIds).toEqual(shuttle2?.routeNodeIds);
    expect(shuttle8?.plannedRouteNodeIds).toEqual(shuttle8?.routeNodeIds);
  });

  it('restores agent-refresh snapshots deterministically for replay debugging', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      durationSec: 120,
      vehicles: {
        count: 8,
        emptySpeedMps: 3,
        loadedSpeedMps: 2,
        accelerationMps2: 2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 3,
        loadedSpeedMps: 2,
        accelerationMps2: 2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01,
        switchDirectionSec: 0
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 32
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        liftApproachCapacity: 8,
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();
    for (let index = 0; index < 150; index += 1) {
      sim.step(0.2);
    }
    const snapshot = sim.createSnapshot();
    expect(hashEngineSnapshot(snapshot)).toBe(snapshot.stateHash);

    for (let index = 0; index < 50; index += 1) {
      sim.step(0.2);
    }
    const expected = sim.createSnapshot();

    const replay = new ShuttleSimCore(scenario);
    replay.restoreSnapshot(snapshot);
    for (let index = 0; index < 50; index += 1) {
      replay.step(0.2);
    }
    const actual = replay.createSnapshot();

    expect(actual.stateHash).toBe(expected.stateHash);
    expect(actual.eventLogHash).toBe(expected.eventLogHash);
  }, 30000);

  it('agent-refresh assigns the first sixteen inbound jobs to rows 1 through 16', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      durationSec: 300,
      vehicles: {
        count: 8,
        emptySpeedMps: 6,
        loadedSpeedMps: 5,
        accelerationMps2: 6,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 6,
        loadedSpeedMps: 5,
        accelerationMps2: 6,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01,
        switchDirectionSec: 0
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        liftApproachCapacity: 8,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 20
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();
    let state = sim.getState();

    for (let index = 0; index < 900 && state.tasks.length < 16; index += 1) {
      state = sim.step(0.2);
    }

    const rows = state.tasks
      .slice(0, 16)
      .map((task) => /^storage-r(\d+)-c\d+$/.exec(task.dropoffNodeId)?.[1] ?? '');
    expect(state.tasks.length).toBeGreaterThanOrEqual(16);
    expect(rows).toEqual(Array.from({ length: 16 }, (_, index) => String(index + 1).padStart(2, '0')));
  }, 30000);

  it('agent-refresh plans loaded dropoff through the lift column without a side-aisle loop or stored-load crossing', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh'
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['inbound-lift-bottom-02']);
    sim.addLoadForTest({ id: 'refresh-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'refresh-drop-r08',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-bottom-02',
      dropoffNodeId: 'storage-r08-c01',
      loadId: 'refresh-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'refresh-drop-r08', true);

    const state = sim.step(0.2);
    const vehicle = state.vehicles[0]!;
    const route = vehicle.plannedRouteNodeIds;
    const storageColumns = route.flatMap((nodeId) => storageColumn(nodeId) ?? []);

    expect(route[0]).toBe('inbound-lift-bottom-02');
    expect(route[1]).toBe('inbound-lift-bottom-02-row-08-transfer');
    expect(route.at(-1)).toBe('storage-r08-c01');
    expect(route).not.toContain('right-row-08');
    expect(route).not.toContain('left-row-08');
    for (let index = 1; index < storageColumns.length; index += 1) {
      expect(storageColumns[index]).toBeLessThan(storageColumns[index - 1]!);
    }
    expectNoLoadedStorageTransitThroughStoredLoads(state);
  });

  it('agent-refresh makes the lower-priority empty shuttle side-yield instead of reversing', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['right-row-08', 'storage-r08-c24', 'storage-r08-c23']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r08-c24', 'right-row-08']);

    const state = sim.step(0.2);
    const empty = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');
    const session = state.traffic.conflictSessions.find((candidate) => candidate.yielderVehicleId === 'SH-02');
    const yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.vehicleId === 'SH-02');

    expect(yieldEvent?.details.route).toBe('storage-r08-c24>inbound-lift-bottom-02-row-08-transfer');
    expect(session).toMatchObject({
      state: 'yielding',
      winnerVehicleId: 'SH-01',
      yielderVehicleId: 'SH-02',
      blockerVehicleId: 'SH-01',
      clearancePolicy: 'immediate-next-move'
    });
    expect(session?.yielderLocalRouteNodeIds[0]).toBe('storage-r08-c24');
    expect(session?.yielderLocalRouteNodeIds).not.toContain('right-row-08');
    expect(empty?.routeNodeIds[0]).toBe('storage-r08-c24');
    expect(empty?.targetNodeId).not.toBe('right-row-08');
    expect(empty?.routeNodeIds.slice(0, 2)).not.toEqual(['storage-r08-c24', 'right-row-08']);
  }, 10000);

  it('agent-refresh makes a lower-priority loaded shuttle side-yield after an adjacent main-aisle faceoff', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['main-north-05', 'main-south-05', 'right-row-09', 'storage-r09-c24']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', ['main-south-05', 'main-north-05', 'right-row-08', 'storage-r08-c24']);
    sim.setVehicleTaskForTest('SH-02', null, true);

    const state = sim.step(0.2);
    const yielder = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');
    const session = state.traffic.conflictSessions.find((candidate) => candidate.yielderVehicleId === 'SH-02');

    expect(session).toMatchObject({
      winnerVehicleId: 'SH-01',
      yielderVehicleId: 'SH-02',
      blockerVehicleId: 'SH-01',
      clearancePolicy: 'immediate-next-move'
    });
    expect(yielder?.routeNodeIds[0]).toBe('main-south-05');
    expect(yielder?.routeNodeIds[1]).not.toBe('main-north-05');
    expect(session?.yielderLocalRouteNodeIds.length).toBeGreaterThanOrEqual(2);
  });

  it('agent-refresh lets one empty transfer-faceoff shuttle side-yield and clears the lift column', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['inbound-lift-bottom-01-row-06-transfer', 'inbound-lift-bottom-01']);
    sim.setVehicleRouteForTest('SH-02', ['outbound-lift-top-01-row-12-transfer', 'outbound-lift-top-01']);

    for (let index = 0; index < 30; index += 1) {
      sim.step(0.2);
    }

    const state = sim.getState();
    const yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.reason === 'agent-refresh-near-faceoff-yield');

    expect(yieldEvent?.vehicleId).toBe('SH-02');
    expect(yieldEvent?.details.route).toBe('outbound-lift-top-01>outbound-lift-top-01-row-12-transfer>storage-r12-c13');
    expect(state.kpis.deadlockCount).toBe(0);
    expect(state.traffic.physicalViolationCount).toBe(0);
    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-01')?.currentEdgeId).toBe(
      'inbound-lift-bottom-01-row-06-transfer-inbound-lift-bottom-01'
    );
  }, 10000);

  it('agent-refresh uses intermediate lift-column row pockets instead of long-retreating to a lift', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic'
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);

    runFor(sim, 55);

    const events = sim.getEventLog().filter((event) => event.vehicleId === 'SH-05' && event.eventType === 'route-replanned');
    const longLiftRetreat = events.find((event) =>
      event.reason === 'agent-refresh-near-faceoff-yield' &&
      String(event.details.route ?? '').endsWith('>inbound-lift-top-01')
    );
    const localYield = events.find((event) =>
      (event.reason === 'agent-refresh-side-yield' || event.reason === 'agent-refresh-near-faceoff-yield') &&
      String(event.details.route ?? '').endsWith('>storage-r09-c07')
    );

    expect(longLiftRetreat).toBeUndefined();
    expect(String(localYield?.details.route ?? '')).not.toContain('inbound-lift-top-01>');
    expect(String(localYield?.details.route ?? '').split('>').length).toBeLessThanOrEqual(3);
  }, 20000);

  it('agent-refresh blocks an empty shuttle from using stored load cells as temporary pockets', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['outbound-lift-top-01-row-03-transfer', 'outbound-lift-top-01']);
    sim.setVehicleRouteForTest('SH-02', ['outbound-lift-top-01']);
    sim.addLoadForTest({ id: 'stored-c12', state: 'stored', nodeId: 'storage-r03-c12', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-c13', state: 'stored', nodeId: 'storage-r03-c13', vehicleId: null, weightKg: 100 });

    const state = sim.step(0.2);
    const shuttle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.vehicleId === 'SH-01');

    expect(yieldEvent?.reason).toBe('agent-refresh-side-yield');
    expect(String(yieldEvent?.details.route ?? '')).not.toContain('storage-r03-c12');
    expect(String(yieldEvent?.details.route ?? '')).not.toContain('storage-r03-c13');
    expect(shuttle?.routeNodeIds[0]).toBe('outbound-lift-top-01-row-03-transfer');
    expect(shuttle?.routeNodeIds.slice(0, 2)).not.toEqual(['outbound-lift-top-01-row-03-transfer', 'storage-r03-c12']);
    expect(shuttle?.routeNodeIds.slice(0, 2)).not.toEqual(['outbound-lift-top-01-row-03-transfer', 'storage-r03-c13']);
  });

  it('agent-refresh blocks an empty shuttle from using another active inbound dropoff as a temporary pocket', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['outbound-lift-top-01-row-03-transfer', 'outbound-lift-top-01']);
    sim.setVehicleRouteForTest('SH-02', ['outbound-lift-top-01']);
    sim.addTaskForTest({
      id: 'other-drop-c12',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-01',
      dropoffNodeId: 'storage-r03-c12',
      loadId: 'other-load-c12',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'other-drop-c13',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-01',
      dropoffNodeId: 'storage-r03-c13',
      loadId: 'other-load-c13',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    const state = sim.step(0.2);
    const shuttle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.vehicleId === 'SH-01');

    expect(yieldEvent?.reason).toBe('agent-refresh-side-yield');
    expect(String(yieldEvent?.details.route ?? '')).not.toContain('storage-r03-c12');
    expect(String(yieldEvent?.details.route ?? '')).not.toContain('storage-r03-c13');
    expect(shuttle?.routeNodeIds.slice(0, 2)).not.toEqual(['outbound-lift-top-01-row-03-transfer', 'storage-r03-c12']);
    expect(shuttle?.routeNodeIds.slice(0, 2)).not.toEqual(['outbound-lift-top-01-row-03-transfer', 'storage-r03-c13']);
  });

  it('agent-refresh only lets a loaded shuttle use its own active inbound dropoff as storage', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addTaskForTest({
      id: 'own-drop-c12',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-01',
      dropoffNodeId: 'storage-r03-c12',
      loadId: 'own-load-c12',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshTemporaryStorageNodeAllowed: (vehicle: unknown, nodeId: string) => boolean;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01');

    sim.setVehicleTaskForTest('SH-01', 'own-drop-c12', false);
    expect(internals.agentRefreshTemporaryStorageNodeAllowed(vehicle, 'storage-r03-c12')).toBe(false);

    sim.setVehicleTaskForTest('SH-01', 'own-drop-c12', true);
    expect(internals.agentRefreshTemporaryStorageNodeAllowed(vehicle, 'storage-r03-c12')).toBe(true);
  });

  it('agent-refresh treats active local-yield nodes as short local claims', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c25', 'column-top-b-c25']);
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c24', 'column-top-b-c25', 'column-top-b-c26']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        localRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      localRouteNodeClaimBlocker: (nodeId: string, vehicleId: string) => string | null;
      agentMinimalMoveBlocker: (vehicle: unknown, toNodeId: string) => { reason: string; blockingVehicleId: string | null } | null;
      agentRefreshYieldPocketAllowed: (vehicle: unknown, nodeId: string) => boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const other = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    yielder.localRouteNodeIds = ['column-top-a-c25', 'column-top-b-c25'];
    yielder.localRouteReason = 'temporary-yield';

    expect(internals.localRouteNodeClaimBlocker('column-top-b-c25', 'SH-02')).toBe('SH-01');
    expect(internals.agentMinimalMoveBlocker(other, 'column-top-b-c25')).toEqual({
      reason: 'node-local-yield',
      blockingVehicleId: 'SH-01'
    });
    expect(internals.agentRefreshYieldPocketAllowed(yielder, 'column-top-b-c25')).toBe(false);
  });

  it('agent-refresh makes an empty lift-column faceoff use the current row pocket before crossing the blocker', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'inbound-lift-bottom-02-row-07-transfer',
      'inbound-lift-bottom-02-row-08-transfer',
      'inbound-lift-bottom-02-row-09-transfer',
      'storage-r09-c24'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'outbound-lift-top-02-row-10-transfer',
      'outbound-lift-top-02-row-09-transfer',
      'outbound-lift-top-02-row-08-transfer',
      'outbound-lift-top-02'
    ]);

    for (let index = 0; index < 80; index += 1) {
      sim.step(0.1);
    }

    const events = sim.getEventLog().filter((event) => event.vehicleId === 'SH-01' && event.eventType === 'route-replanned');
    const localYield = events.find((event) =>
      event.reason === 'agent-refresh-side-yield' &&
      event.details.route === 'inbound-lift-bottom-02-row-08-transfer>storage-r08-c24'
    );
    const crossingForwardYield = events.find((event) =>
      event.reason === 'agent-refresh-forward-pocket-yield' &&
      event.details.route === 'inbound-lift-bottom-02-row-08-transfer>inbound-lift-bottom-02-row-09-transfer>storage-r09-c24'
    );

    expect(localYield).toBeDefined();
    expect(crossingForwardYield).toBeUndefined();
    expect(sim.getState().kpis.deadlockCount).toBe(0);
    expect(sim.getState().traffic.physicalViolationCount).toBe(0);
  });

  it('agent-refresh still blocks a loaded shuttle from using a stored load cell as a yield pocket', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['outbound-lift-top-01-row-03-transfer', 'outbound-lift-top-01']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', ['outbound-lift-top-01']);
    sim.addLoadForTest({ id: 'stored-c12', state: 'stored', nodeId: 'storage-r03-c12', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-c13', state: 'stored', nodeId: 'storage-r03-c13', vehicleId: null, weightKg: 100 });

    const state = sim.step(0.2);
    const shuttle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.vehicleId === 'SH-01');

    expect(yieldEvent).toBeUndefined();
    expect(shuttle?.state).toBe('waiting-blocked');
    expect(shuttle?.targetNodeId).toBe('outbound-lift-top-01');
    expect(shuttle?.routeNodeIds.slice(0, 2)).not.toEqual(['outbound-lift-top-01-row-03-transfer', 'storage-r03-c12']);
    expect(shuttle?.routeNodeIds.slice(0, 2)).not.toEqual(['outbound-lift-top-01-row-03-transfer', 'storage-r03-c13']);
  });

  it('agent-refresh lets a loaded shuttle leave a temporary pocket and enter its target row without crossing stored loads', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r09-c24']);
    sim.addLoadForTest({ id: 'carried-r01-c15', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    for (let column = 1; column <= 14; column += 1) {
      sim.addLoadForTest({
        id: `stored-r01-c${String(column).padStart(2, '0')}`,
        state: 'stored',
        nodeId: `storage-r01-c${String(column).padStart(2, '0')}`,
        vehicleId: null,
        weightKg: 100
      });
      sim.addLoadForTest({
        id: `stored-r09-c${String(column).padStart(2, '0')}`,
        state: 'stored',
        nodeId: `storage-r09-c${String(column).padStart(2, '0')}`,
        vehicleId: null,
        weightKg: 100
      });
    }
    sim.addTaskForTest({
      id: 'loaded-pocket-exit',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-bottom-01',
      dropoffNodeId: 'storage-r01-c15',
      loadId: 'carried-r01-c15',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'loaded-pocket-exit', true);

    const state = sim.step(0.2);
    const shuttle = state.vehicles[0]!;
    const route = shuttle.plannedRouteNodeIds;

    expect(shuttle.waitReason).not.toBe('route-unavailable');
    expect(route.slice(0, 2)).toEqual(['storage-r09-c24', 'right-row-09']);
    expect(route).toContain('right-row-01');
    expect(route).toContain('storage-r01-c24');
    expect(route.at(-1)).toBe('storage-r01-c15');
    for (let column = 1; column <= 14; column += 1) {
      expect(route).not.toContain(`storage-r01-c${String(column).padStart(2, '0')}`);
      expect(route).not.toContain(`storage-r09-c${String(column).padStart(2, '0')}`);
    }
  });

  it('agent-refresh clears loaded storage-row swaps through the nearest side aisle', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'swap-load-01', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'swap-load-02', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'swap-drop-01',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-01',
      dropoffNodeId: 'storage-r09-c16',
      loadId: 'swap-load-01',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'swap-drop-02',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-02',
      dropoffNodeId: 'storage-r13-c16',
      loadId: 'swap-load-02',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r09-c18', 'storage-r09-c17', 'storage-r09-c16']);
    sim.setVehicleTaskForTest('SH-01', 'swap-drop-01', true);
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r09-c17',
      'storage-r09-c18',
      'storage-r09-c19',
      'storage-r09-c20',
      'storage-r09-c21',
      'storage-r09-c22',
      'storage-r09-c23',
      'storage-r09-c24',
      'right-row-09'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'swap-drop-02', true);

    for (let index = 0; index < 120; index += 1) {
      sim.step(0.2);
    }

    const event = sim.getEventLog().find((entry) =>
      entry.eventType === 'route-replanned' &&
      entry.reason === 'agent-refresh-loaded-storage-swap-clearance'
    );
    const state = sim.getState();
    const stillSwapped = state.traffic.waitingVehicles.some((vehicle) =>
      vehicle.waitReason === 'node-occupied' &&
      (vehicle.vehicleId === 'SH-01' || vehicle.vehicleId === 'SH-02')
    );

    if (event) {
      expect(event.vehicleId).toBe('SH-01');
      expect(String(event.details.route ?? '')).toContain('storage-r09-c24>right-row-09');
      expect(String(event.details.route ?? '')).not.toContain('storage-r09-c18>storage-r09-c17');
    } else {
      expect(state.tasks.find((task) => task.id === 'swap-drop-01')?.state).toBe('completed');
      expect(state.tasks.find((task) => task.id === 'swap-drop-02')?.state).toBe('completed');
    }
    expect(state.kpis.deadlockCount).toBe(0);
    expect(stillSwapped).toBe(false);
  });

  it('agent-refresh clears top-lift same-column bank-boundary swaps through a legal column access node', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'column-swap-side-block-01', state: 'stored', nodeId: 'storage-r07-c04', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'column-swap-side-block-02', state: 'stored', nodeId: 'storage-r07-c06', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'column-swap-load-01', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'column-swap-load-02', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'column-swap-drop-01',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound',
      dropoffNodeId: 'storage-r08-c05',
      loadId: 'column-swap-load-01',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'column-swap-drop-02',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound',
      dropoffNodeId: 'storage-r07-c05',
      loadId: 'column-swap-load-02',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r07-c05', 'storage-r08-c05']);
    sim.setVehicleTaskForTest('SH-01', 'column-swap-drop-01', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r08-c05', 'storage-r07-c05']);
    sim.setVehicleTaskForTest('SH-02', 'column-swap-drop-02', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshLoadedStorageSwapClearancePlan: (
        vehicle: unknown,
        blocker: unknown
      ) => { routeNodeIds: string[] } | null;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01');
    const blocker = internals.vehicles.find((candidate) => candidate.id === 'SH-02');
    const plan = internals.agentRefreshLoadedStorageSwapClearancePlan(vehicle, blocker);
    const route = plan?.routeNodeIds ?? [];

    expect(plan).not.toBeNull();
    expect(route).toContain('column-middle-c05');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('agent-refresh clears loaded same-column storage swaps to the nearest column access instead of one-step oscillating', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'same-column-side-block-01', state: 'stored', nodeId: 'storage-r12-c12', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'same-column-side-block-02', state: 'stored', nodeId: 'storage-r12-c14', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'same-column-load-01', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'same-column-load-03', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'same-column-drop-01',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound',
      dropoffNodeId: 'storage-r14-c13',
      loadId: 'same-column-load-01',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'same-column-drop-03',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound',
      dropoffNodeId: 'storage-r14-c17',
      loadId: 'same-column-load-03',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r12-c13', 'storage-r13-c13', 'storage-r14-c13']);
    sim.setVehicleTaskForTest('SH-01', 'same-column-drop-01', true);
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r13-c13',
      'storage-r12-c13',
      'storage-r11-c13',
      'storage-r10-c13',
      'storage-r09-c13',
      'storage-r08-c13',
      'column-middle-c13',
      'column-middle-c14',
      'column-middle-c15',
      'column-middle-c16',
      'column-middle-c17',
      'storage-r08-c17',
      'storage-r09-c17',
      'storage-r10-c17',
      'storage-r11-c17',
      'storage-r12-c17',
      'storage-r13-c17',
      'storage-r14-c17'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'same-column-drop-03', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshLoadedStorageSwapClearancePlan: (
        vehicle: unknown,
        blocker: unknown
      ) => { routeNodeIds: string[] } | null;
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01');
    const blocker = internals.vehicles.find((candidate) => candidate.id === 'SH-02');
    const plan = internals.agentRefreshLoadedStorageSwapClearancePlan(vehicle, blocker);
    const route = plan?.routeNodeIds ?? [];

    expect(route).toEqual([
      'storage-r12-c13',
      'storage-r11-c13',
      'storage-r10-c13',
      'storage-r09-c13',
      'storage-r08-c13',
      'column-middle-c13'
    ]);
    expectRouteHopsAdjacent(scenario, route);
  });

  it('agent-refresh clears mixed loaded-empty same-column storage swaps', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'mixed-swap-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'mixed-swap-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c02',
      dropoffNodeId: 'column-top-a-c08',
      loadId: 'mixed-swap-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r05-c02', 'storage-r06-c02', 'storage-r07-c02', 'column-middle-c02']);
    sim.setVehicleTaskForTest('SH-01', 'mixed-swap-outbound', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r06-c02', 'storage-r05-c02', 'storage-r04-c02']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        waitingSinceSec: number | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
      }>;
      layoutNode(nodeId: string): { noStop?: boolean; noParking?: boolean } | null;
      tryBreakAgentRefreshLoadedStorageSwap(candidateVehicleIds: string[]): boolean;
    };
    for (const vehicle of internals.vehicles) {
      if (vehicle.id === 'SH-01') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          targetNodeId: 'storage-r06-c02',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-02',
          waitingSinceSec: 0
        });
      }
      if (vehicle.id === 'SH-02') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          targetNodeId: 'storage-r05-c02',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-01',
          waitingSinceSec: 0
        });
      }
    }

    expect(internals.tryBreakAgentRefreshLoadedStorageSwap(['SH-01', 'SH-02'])).toBe(true);
    const cleared = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'loaded-storage-swap-clearance');
    const terminalNode = internals.layoutNode(cleared?.routeNodeIds.at(-1) ?? '');

    expect(cleared).toBeDefined();
    expect(cleared?.routeNodeIds).not.toEqual(['storage-r05-c02', 'storage-r06-c02']);
    expect(terminalNode?.noStop).not.toBe(true);
    expect(terminalNode?.noParking).not.toBe(true);
    expectRouteHopsAdjacent(scenario, cleared?.routeNodeIds ?? []);
  });

  it('proactively clears reciprocal storage swaps before moving same-column queue followers', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        sourceBufferCapacity: 4,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'swap-priority-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'swap-priority-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r02-c02',
      dropoffNodeId: 'column-top-a-c08',
      loadId: 'swap-priority-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r02-c02', 'storage-r03-c02']);
    sim.setVehicleTaskForTest('SH-01', 'swap-priority-outbound', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r03-c02', 'storage-r02-c02']);
    sim.setVehicleRouteForTest('SH-03', ['storage-r04-c02', 'storage-r03-c02']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        waitingSinceSec: number | null;
        localRouteReason: string | null;
      }>;
    };
    for (const vehicle of internals.vehicles) {
      if (vehicle.id === 'SH-01') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          targetNodeId: 'storage-r03-c02',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-02',
          waitingSinceSec: 0
        });
      }
      if (vehicle.id === 'SH-02') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          targetNodeId: 'storage-r02-c02',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-01',
          waitingSinceSec: 0
        });
      }
      if (vehicle.id === 'SH-03') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          targetNodeId: 'storage-r03-c02',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-02',
          waitingSinceSec: 0
        });
      }
    }

    sim.start();
    sim.step(0.2);
    const swapClearance = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'loaded-storage-swap-clearance');
    const routeReplans = sim.getEventLog().filter((event) =>
      event.eventType === 'route-replanned' &&
      (
        event.reason === 'agent-refresh-loaded-storage-swap-clearance' ||
        event.reason === 'agent-refresh-storage-column-queue-yield'
      )
    );

    expect(swapClearance?.id).not.toBe('SH-03');
    expect(routeReplans[0]?.reason).toBe('agent-refresh-loaded-storage-swap-clearance');
  });

  it('clears empty same-column reciprocal storage swaps as the root conflict', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        sourceBufferCapacity: 4,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r04-c05', 'storage-r05-c05']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r05-c05', 'storage-r04-c05']);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r05-c05',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'storage-r04-c05',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        localRouteReason: string | null;
        routeNodeIds: string[];
        waitReason: string | null;
      }>;
      tryBreakAgentRefreshLoadedStorageSwap(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshLoadedStorageSwap(['SH-01', 'SH-02'])).toBe(true);
    const cleared = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'loaded-storage-swap-clearance');

    expect([
      'storage-r04-c05>storage-r03-c05',
      'storage-r05-c05>storage-r06-c05'
    ]).toContain(cleared?.routeNodeIds.join('>'));
    expect(cleared?.waitReason).toBeNull();
    expectRouteHopsAdjacent(scenario, cleared?.routeNodeIds ?? []);
  });

  it('clears an empty storage blocker from a loaded top-lift outbound exit path', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-outbound-clearance-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'block-left-pocket', state: 'stored', nodeId: 'storage-r07-c16', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'block-deeper-pocket', state: 'stored', nodeId: 'storage-r08-c17', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'source-load-blocker', state: 'waiting', nodeId: 'lift-01-inbound-source-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-outbound-clearance',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r06-c17',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'loaded-outbound-clearance-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'empty-inbound-blocker',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r14-c02',
      loadId: 'source-load-blocker',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r06-c17', 'storage-r07-c17', 'column-middle-c17']);
    sim.setVehicleTaskForTest('SH-01', 'loaded-outbound-clearance', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r07-c17', 'column-middle-c17']);
    sim.setVehicleTaskForTest('SH-02', 'empty-inbound-blocker', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r07-c17',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c17',
      waitReason: 'middle-aisle-opposing-claim',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{ id: string; routeNodeIds: string[]; targetNodeId: string | null; localRouteReason: string | null; waitReason: string | null }>;
      tryClearTopLiftLoadedOutboundStorageExitBlockers(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryClearTopLiftLoadedOutboundStorageExitBlockers(['SH-01', 'SH-02'])).toBe(true);
    const loaded = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(loaded?.localRouteReason).toBe('loaded-outbound-storage-exit-retreat');
    expect(loaded?.routeNodeIds).toEqual(['storage-r06-c17', 'storage-r05-c17']);
    expect(loaded?.targetNodeId).toBe('storage-r05-c17');
    expect(loaded?.waitReason).toBeNull();
    expectRouteHopsAdjacent(scenario, loaded?.routeNodeIds ?? []);
  });

  it('lets later inbound column tasks stage in the yellow queue while deeper predecessors pick', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'column-predecessor-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'column-successor-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'column-predecessor-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r11-c03',
      loadId: 'column-predecessor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'column-successor-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 30,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r10-c03',
      loadId: 'column-successor-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c10', 'column-top-b-c09', 'lift-01-inbound-queue-01-entry-access', 'lift-01-inbound-queue-01-service-exit']);
    sim.setVehicleTaskForTest('SH-01', 'column-predecessor-task', false);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string; state: string; vehicleId: string | null }>;
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; routeNodeIds: string[]; plannedRouteNodeIds: string[] }>;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      queuedTaskAssignmentPriority(task: unknown): number;
    };
    const successor = internals.tasks.find((task) => task.id === 'column-successor-task')!;
    const availableVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const queueNodeId = internals.topLiftInboundQueueNodeIdForTask(successor, availableVehicle);

    expect(internals.queuedTaskAssignmentPriority(successor)).toBe(-2);
    expect(internals.inboundTaskShouldWaitBeforePickup(successor)).toBe(true);
    expect(internals.inboundPickupDispatchGoalNodeId(successor, availableVehicle)).toBe(queueNodeId);
    expect(internals.inboundPickupDispatchGoalNodeId(successor, availableVehicle)).not.toBe('lift-01-inbound-queue-01-service-exit');

    const state = sim.step(0.2);
    const assignedTask = state.tasks.find((task) => task.id === 'column-successor-task');
    const assignedVehicle = state.vehicles.find((vehicle) => vehicle.id === assignedTask?.vehicleId);

    expect(assignedTask?.state).toBe('assigned');
    expect(assignedVehicle?.plannedGoalNodeId).toBe(queueNodeId);
    expect(assignedVehicle?.routeNodeIds).not.toContain('lift-01-inbound-queue-01-service-exit');
    expect(assignedVehicle?.plannedRouteNodeIds.at(-1)).toBe(queueNodeId);
  });

  it('does not stage an inbound successor where its queue display blocks the earlier loaded exit corridor', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'exit-corridor-predecessor-load', state: 'waiting', nodeId: 'lift-01-inbound-source-01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'exit-corridor-successor-load', state: 'waiting', nodeId: 'lift-01-inbound-source-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'exit-corridor-predecessor-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r10-c11',
      loadId: 'exit-corridor-predecessor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'exit-corridor-successor-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 20,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r09-c11',
      loadId: 'exit-corridor-successor-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c07']);
    sim.setVehicleTaskForTest('SH-01', 'exit-corridor-predecessor-task', false);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string; state: string; vehicleId: string | null; waitReason: string | null }>;
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; routeNodeIds: string[] }>;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      topLiftInboundQueueSlotAvailableForTask(task: unknown): boolean;
      queuedTaskAssignmentPriority(task: unknown): number;
    };
    const successor = internals.tasks.find((task) => task.id === 'exit-corridor-successor-task')!;
    const availableVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftInboundQueueNodeIdForTask(successor, availableVehicle)).toBeNull();
    expect(internals.topLiftInboundQueueSlotAvailableForTask(successor)).toBe(false);
    expect(internals.queuedTaskAssignmentPriority(successor)).toBe(2);

    const state = sim.step(0.2);
    const queuedTask = state.tasks.find((task) => task.id === 'exit-corridor-successor-task');
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-02');

    expect(queuedTask?.state).toBe('queued');
    expect(queuedTask?.vehicleId).toBeNull();
    expect(queuedTask?.waitReason).toBe('inbound-lift-fifo-wait');
    expect(vehicle?.plannedGoalNodeId).not.toMatch(/^lift-01-inbound-queue-/);
    expect(vehicle?.routeNodeIds).not.toContain('lift-01-inbound-queue-03-access');
  });

  it('does not stage a deeper inbound queue requester through an occupied earlier queue projection', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'queue-projection-predecessor-load', state: 'waiting', nodeId: 'lift-01-inbound-source-01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'queue-projection-successor-load', state: 'waiting', nodeId: 'lift-01-inbound-source-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queue-projection-predecessor-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r11-c03',
      loadId: 'queue-projection-predecessor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: 'inbound-lift-fifo-wait'
    });
    sim.addTaskForTest({
      id: 'queue-projection-successor-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 20,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r10-c03',
      loadId: 'queue-projection-successor-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['lift-01-inbound-queue-02-access']);
    sim.setVehicleTaskForTest('SH-01', 'queue-projection-predecessor-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'lift-01-inbound-queue-02-access',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['column-top-b-c08']);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string; state: string; vehicleId: string | null; waitReason: string | null }>;
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; routeNodeIds: string[] }>;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
    };
    const successor = internals.tasks.find((task) => task.id === 'queue-projection-successor-task')!;
    const availableVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftInboundQueueNodeIdForTask(successor, availableVehicle)).toBeNull();

    const state = sim.step(0.2);
    const queuedTask = state.tasks.find((task) => task.id === 'queue-projection-successor-task');
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-02');

    expect(queuedTask?.state).toBe('queued');
    expect(queuedTask?.vehicleId).toBeNull();
    expect(vehicle?.plannedGoalNodeId).not.toBe('lift-01-inbound-queue-03-access');
    expect(vehicle?.routeNodeIds).not.toContain('column-top-b-c09');
  });

  it('does not let a storage-full queued inbound predecessor starve later column slots', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'occupying-load', state: 'stored', nodeId: 'storage-r11-c03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'blocked-deeper-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'later-open-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'blocked-deeper-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 10,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r11-c03',
      loadId: 'blocked-deeper-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: 'storage-full'
    });
    sim.addTaskForTest({
      id: 'later-open-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 30,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r10-c03',
      loadId: 'later-open-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-01', null, false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string; waitReason: string | null }>;
      queuedTaskAssignmentPriority(task: unknown): number;
      taskAssignmentBlockReason(task: unknown): string | null;
      topLiftInboundColumnPredecessorPending(task: unknown): boolean;
    };
    const laterTask = internals.tasks.find((task) => task.id === 'later-open-task')!;

    expect(internals.topLiftInboundColumnPredecessorPending(laterTask)).toBe(false);
    expect(internals.queuedTaskAssignmentPriority(laterTask)).toBe(-2);
    expect(internals.taskAssignmentBlockReason(laterTask)).toBeNull();
  });

  it('lets a staged inbound lift follower pass a storage-full queued predecessor', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'occupied-c25-r02-load', state: 'stored', nodeId: 'storage-r02-c25', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'blocked-c25-r02-source-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'staged-c25-r01-source-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'blocked-storage-full-c25-r02-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 10,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r02-c25',
      loadId: 'blocked-c25-r02-source-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: 'storage-full'
    });
    sim.addTaskForTest({
      id: 'staged-c25-r01-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 20,
      assignedAtSec: 20,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r01-c25',
      loadId: 'staged-c25-r01-source-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c23']);
    sim.setVehicleTaskForTest('SH-01', 'staged-c25-r01-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-a-c23',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['module-02-spine-top-b']);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      inboundTaskHasEarlierPickupTask(task: unknown): boolean;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      topLiftInboundColumnPredecessorPending(task: unknown): boolean;
    };
    const stagedTask = internals.tasks.find((candidate) => candidate.id === 'staged-c25-r01-task')!;
    const stagedVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.inboundTaskHasEarlierPickupTask(stagedTask)).toBe(false);
    expect(internals.topLiftInboundColumnPredecessorPending(stagedTask)).toBe(false);
    expect(internals.inboundTaskShouldWaitBeforePickup(stagedTask)).toBe(false);
    expect(internals.inboundPickupDispatchGoalNodeId(stagedTask, stagedVehicle)).toBe('column-top-a-c22');

    const state = sim.step(0.2);
    const releasedVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(releasedVehicle?.waitReason).not.toBe('inbound-lift-fifo-wait');
    expect(releasedVehicle?.plannedGoalNodeId).toBe('column-top-a-c22');
    expect(releasedVehicle?.targetNodeId).toBe('column-top-a-c22');
    expectRouteHopsAdjacent(scenario, releasedVehicle?.routeNodeIds ?? []);
  });

  it('does not FIFO-hold a ready inbound follower behind an unready earlier lift task', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const mutableSim = sim as unknown as {
      loads: unknown[];
      loadsById: Map<string, unknown>;
    };
    mutableSim.loads = [];
    mutableSim.loadsById = new Map();
    sim.addLoadForTest({ id: 'unready-earlier-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'ready-follower-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'unready-earlier-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r10-c09',
      loadId: 'unready-earlier-source-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'ready-follower-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 20,
      assignedAtSec: 20,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r11-c09',
      loadId: 'ready-follower-source-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c11']);
    sim.setVehicleTaskForTest('SH-01', 'ready-follower-task', false);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-02', 'unready-earlier-task', false);
    (mutableSim.loadsById.get('unready-earlier-source-load') as { nodeId: string | null }).nodeId = 'lift-01-inbound-buffer-02';
    (mutableSim.loadsById.get('ready-follower-source-load') as { nodeId: string | null }).nodeId = 'lift-01-inbound-buffer-03';

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      inboundTaskLoadReadyAtPickup(task: unknown): boolean;
      inboundTaskHasEarlierPickupTask(task: unknown): boolean;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      vehicles: Array<{ id: string }>;
    };
    const earlierTask = internals.tasks.find((candidate) => candidate.id === 'unready-earlier-task')!;
    const followerTask = internals.tasks.find((candidate) => candidate.id === 'ready-follower-task')!;
    const followerVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.inboundTaskLoadReadyAtPickup(earlierTask)).toBe(false);
    expect(internals.inboundTaskLoadReadyAtPickup(followerTask)).toBe(true);
    expect(internals.inboundTaskHasEarlierPickupTask(followerTask)).toBe(false);
    expect(internals.inboundTaskShouldWaitBeforePickup(followerTask)).toBe(false);
    expect(internals.inboundPickupDispatchGoalNodeId(followerTask, followerVehicle)).toBe('column-top-a-c08');
  });

  it('promotes a staged projected inbound queue leader load instead of yielding sideways', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const mutableSim = sim as unknown as {
      loads: unknown[];
      loadsById: Map<string, unknown>;
    };
    mutableSim.loads = [];
    mutableSim.loadsById = new Map();
    sim.addLoadForTest({ id: 'leader-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'follower-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'leader-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 30,
      assignedAtSec: 30,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r10-c09',
      loadId: 'leader-source-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'follower-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 40,
      assignedAtSec: 40,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r11-c09',
      loadId: 'follower-source-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c11']);
    sim.setVehicleTaskForTest('SH-01', 'follower-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-a-c11',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-02', 'leader-task', false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-top-a-c10',
      waitReason: 'inbound-lift-source-assigned',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    (mutableSim.loadsById.get('leader-source-load') as { nodeId: string | null }).nodeId = 'lift-01-inbound-buffer-02';
    (mutableSim.loadsById.get('follower-source-load') as { nodeId: string | null }).nodeId = 'lift-01-inbound-buffer-03';

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      inboundTaskLoadReadyAtPickup(task: unknown): boolean;
    };
    const leaderTask = internals.tasks.find((candidate) => candidate.id === 'leader-task')!;
    const followerTask = internals.tasks.find((candidate) => candidate.id === 'follower-task')!;

    expect(internals.inboundTaskLoadReadyAtPickup(leaderTask)).toBe(false);
    expect(internals.inboundTaskLoadReadyAtPickup(followerTask)).toBe(true);

    const state = sim.step(0.2);
    const releasedVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');
    const yieldEvent = sim.getEventLog().find((event) =>
      event.vehicleId === 'SH-02' &&
      event.eventType === 'queue-yield' &&
      event.reason === 'inbound-unready-queue-leader-yield'
    );

    expect(internals.inboundTaskLoadReadyAtPickup(leaderTask)).toBe(true);
    expect(releasedVehicle?.waitReason).toBeNull();
    expect(releasedVehicle?.plannedGoalNodeId).toBe('column-top-a-c08');
    expect(releasedVehicle?.targetNodeId).toBe('column-top-a-c09');
    expect(releasedVehicle?.routeNodeIds).toEqual(['column-top-a-c10', 'column-top-a-c09', 'column-top-a-c08']);
    expect(yieldEvent).toBeUndefined();
    expectRouteHopsAdjacent(scenario, releasedVehicle?.routeNodeIds ?? []);
  });

  it('uses the next yellow queue slot when the front same-column inbound queue slot is already claimed', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'claimed-queue-predecessor-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'claimed-queue-successor-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'claimed-queue-predecessor-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r11-c03',
      loadId: 'claimed-queue-predecessor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'claimed-queue-successor-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 30,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r10-c03',
      loadId: 'claimed-queue-successor-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c10', 'column-top-b-c09', 'lift-01-inbound-queue-01-entry-access', 'lift-01-inbound-queue-01-service-exit']);
    sim.setVehicleTaskForTest('SH-01', 'claimed-queue-predecessor-task', false);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleRouteForTest('SH-03', ['module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-03', null, false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; routeNodeIds: string[]; plannedRouteNodeIds: string[] }>;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      queuedTaskAssignmentPriority(task: unknown): number;
    };
    const successor = internals.tasks.find((task) => task.id === 'claimed-queue-successor-task')!;
    const availableVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    const queueNodeId = internals.topLiftInboundQueueNodeIdForTask(successor, availableVehicle);

    expect(queueNodeId).toBe('column-top-a-c10');
    expect(queueNodeId).not.toBe('column-top-a-c09');
    expect(internals.queuedTaskAssignmentPriority(successor)).toBe(-2);
  });

  it('uses the tail projected queue slot when earlier inbound work exceeds visible queue capacity', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 5 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    for (const index of [1, 2, 3, 4]) {
      sim.addLoadForTest({
        id: `tail-capacity-earlier-load-${index}`,
        state: 'waiting',
        nodeId: `lift-01-inbound-buffer-0${Math.min(3, index)}`,
        vehicleId: null,
        weightKg: 100
      });
      sim.addTaskForTest({
        id: `tail-capacity-earlier-task-${index}`,
        kind: 'inbound',
        state: 'assigned',
        createdAtSec: index,
        assignedAtSec: index,
        startedAtSec: null,
        completedAtSec: null,
        pickupNodeId: 'column-top-a-c08',
        dropoffNodeId: `storage-r${String(14 - index).padStart(2, '0')}-c08`,
        loadId: `tail-capacity-earlier-load-${index}`,
        vehicleId: `SH-0${index}`,
        replanCount: 0,
        waitReason: null
      });
    }
    sim.addLoadForTest({ id: 'tail-capacity-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'tail-capacity-later-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 10,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r09-c08',
      loadId: 'tail-capacity-later-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-01', 'tail-capacity-earlier-task-1', false);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-02', 'tail-capacity-earlier-task-2', false);
    sim.setVehicleRouteForTest('SH-03', ['column-bottom-b-c11']);
    sim.setVehicleTaskForTest('SH-03', 'tail-capacity-earlier-task-3', false);
    sim.setVehicleRouteForTest('SH-04', ['column-bottom-b-c12']);
    sim.setVehicleTaskForTest('SH-04', 'tail-capacity-earlier-task-4', false);
    sim.setVehicleRouteForTest('SH-05', ['module-01-spine-top-b']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
    };
    const laterTask = internals.tasks.find((task) => task.id === 'tail-capacity-later-task')!;
    const availableVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;

    expect(internals.topLiftInboundQueueNodeIdForTask(laterTask, availableVehicle)).toBe('column-top-a-c11');
  });

  it('does not let a detached storage detour reserve every projected inbound queue slot', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'detour-earlier-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'detour-later-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'detour-earlier-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r10-c04',
      loadId: 'detour-earlier-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'detour-later-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 2,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r09-c04',
      loadId: 'detour-later-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-bottom-a-c17',
      'storage-r14-c17',
      'column-bottom-a-c17',
      'column-top-a-c11',
      'column-top-a-c10'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'detour-earlier-task', false);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-top-b']);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{
        id: string;
        plannedGoalNodeId: string | null;
        plannedRouteNodeIds: string[];
        localRouteReason: string | null;
      }>;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
    };
    const detouredVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    detouredVehicle.plannedGoalNodeId = 'column-top-a-c10';
    detouredVehicle.plannedRouteNodeIds = [
      'column-bottom-a-c17',
      'storage-r14-c17',
      'column-bottom-a-c17',
      'column-top-a-c11',
      'column-top-a-c10'
    ];
    detouredVehicle.localRouteReason = null;
    const laterTask = internals.tasks.find((task) => task.id === 'detour-later-task')!;
    const availableVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.topLiftInboundQueueNodeIdForTask(laterTask, availableVehicle)).toBe('column-top-a-c11');
  });

  it('starts lifting when a source-waiting projected pickup load becomes front ready', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'waiting-front-blocker-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'waiting-pickup-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'waiting-pickup-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r10-c04',
      loadId: 'waiting-pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-01', 'waiting-pickup-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: null,
      waitReason: 'inbound-source-not-at-pickup',
      waitingSinceSec: 0
    });

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const task = state.tasks.find((candidate) => candidate.id === 'waiting-pickup-task');

    expect(vehicle?.state).toBe('lifting');
    expect(vehicle?.waitReason).toBeNull();
    expect(task?.state).toBe('in-progress');
    expect(task?.startedAtSec).not.toBeNull();
  });

  it('lets a same-column inbound successor reserve the yellow queue when its predecessor is loaded away', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-away-predecessor-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'loaded-away-successor-load', state: 'waiting', nodeId: 'lift-01-inbound', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-away-predecessor-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: 20,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r11-c03',
      loadId: 'loaded-away-predecessor-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'loaded-away-successor-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 30,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r10-c03',
      loadId: 'loaded-away-successor-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c05', 'column-top-b-c04']);
    sim.setVehicleTaskForTest('SH-01', 'loaded-away-predecessor-task', true);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; plannedGoalNodeId: string | null; routeNodeIds: string[]; plannedRouteNodeIds: string[] }>;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      queuedTaskAssignmentPriority(task: unknown): number;
    };
    const successor = internals.tasks.find((task) => task.id === 'loaded-away-successor-task')!;
    const availableVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const queueNodeId = internals.topLiftInboundQueueNodeIdForTask(successor, availableVehicle);

    expect(queueNodeId).not.toBeNull();
    expect(internals.queuedTaskAssignmentPriority(successor)).toBe(-2);
    expect(internals.inboundTaskShouldWaitBeforePickup(successor)).toBe(true);
    expect(internals.inboundPickupDispatchGoalNodeId(successor, availableVehicle)).toBe(queueNodeId);

    const state = sim.step(0.2);
    const assignedTask = state.tasks.find((task) => task.id === 'loaded-away-successor-task');
    const assignedVehicle = state.vehicles.find((vehicle) => vehicle.id === assignedTask?.vehicleId);

    expect(assignedTask?.state).toBe('assigned');
    expect(assignedVehicle?.plannedGoalNodeId).toBe(queueNodeId);
    expect(assignedVehicle?.routeNodeIds).not.toContain('lift-01-inbound-queue-01-service-exit');
    expect(assignedVehicle?.routeNodeIds).not.toContain('column-top-a-c08');
    expect(assignedVehicle?.plannedRouteNodeIds.at(-1)).toBe(queueNodeId);
  });

  it('promotes a staged inbound task source load before dispatching from queue access to pickup', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'front-unassigned-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'staged-task-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'staged-source-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r12-c06',
      loadId: 'staged-task-source-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-01', 'staged-source-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      loads: Array<{ id: string; nodeId: string | null }>;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      inboundTaskLoadReadyAtPickup(task: unknown): boolean;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'staged-source-task')!;
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.inboundPickupDispatchGoalNodeId(task, vehicle)).toBe('column-top-a-c08');
    expect(internals.inboundTaskLoadReadyAtPickup(task)).toBe(true);
    expect(internals.loads.find((load) => load.id === 'staged-task-source-load')?.nodeId).toBe('lift-01-inbound-buffer-03');
    expect(internals.loads.find((load) => load.id === 'front-unassigned-source-load')?.nodeId).toBe('lift-01-inbound-buffer-02');
  });

  it('does not let a queued pickup follower displace the source load for the shuttle already at pickup', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120,
        sourceBufferCapacity: 4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'behind-source-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'pickup-source-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'behind-source-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r14-c15',
      loadId: 'behind-source-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'pickup-source-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r13-c15',
      loadId: 'pickup-source-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c22']);
    sim.setVehicleTaskForTest('SH-01', 'behind-source-task', false);
    sim.setVehicleRouteForTest('SH-02', ['column-top-a-c22']);
    sim.setVehicleTaskForTest('SH-02', 'pickup-source-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      loads: Array<{ id: string; nodeId: string | null }>;
      prepareInboundSourceLoadForStagedPickup(task: unknown, vehicle?: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      topLiftInboundPickupOccupiedByOtherTask(task: unknown, vehicle?: unknown): boolean;
    };
    const behindTask = internals.tasks.find((candidate) => candidate.id === 'behind-source-task')!;
    const pickupTask = internals.tasks.find((candidate) => candidate.id === 'pickup-source-task')!;
    const behindVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const pickupVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;

    expect(internals.prepareInboundSourceLoadForStagedPickup(pickupTask, pickupVehicle)).toBe(true);
    expect(internals.loads.find((load) => load.id === 'pickup-source-load')?.nodeId).toBe('lift-02-inbound-buffer-03');
    expect(internals.loads.find((load) => load.id === 'behind-source-load')?.nodeId).toBe('lift-02-inbound-buffer-02');

    expect(internals.topLiftInboundPickupOccupiedByOtherTask(behindTask, behindVehicle)).toBe(true);
    expect(internals.prepareInboundSourceLoadForStagedPickup(behindTask, behindVehicle)).toBe(false);
    internals.inboundPickupDispatchGoalNodeId(behindTask, behindVehicle);
    expect(internals.loads.find((load) => load.id === 'pickup-source-load')?.nodeId).toBe('lift-02-inbound-buffer-03');
    expect(internals.loads.find((load) => load.id === 'behind-source-load')?.nodeId).toBe('lift-02-inbound-buffer-02');
  });

  it('promotes a staged inbound task source load before holding an already parked queue vehicle', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'parked-front-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'parked-staged-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'parked-staged-source-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r12-c06',
      loadId: 'parked-staged-source-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c10']);
    sim.setVehicleTaskForTest('SH-01', 'parked-staged-source-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      loads: Array<{ id: string; nodeId: string | null }>;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      inboundTaskLoadReadyAtPickup(task: unknown): boolean;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'parked-staged-source-task')!;
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.inboundPickupDispatchGoalNodeId(task, vehicle)).toBe('column-top-a-c08');
    expect(internals.inboundTaskLoadReadyAtPickup(task)).toBe(true);
    expect(internals.loads.find((load) => load.id === 'parked-staged-source-load')?.nodeId).toBe('lift-01-inbound-buffer-03');
    expect(internals.loads.find((load) => load.id === 'parked-front-source-load')?.nodeId).toBe('lift-01-inbound-buffer-02');
  });

  it('releases an already staged inbound head task when a later deeper column task appears', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'head-task-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'later-deeper-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'head-staged-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r13-c01',
      loadId: 'head-task-source-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'later-deeper-task',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 20,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r14-c01',
      loadId: 'later-deeper-source-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: 'inbound-lift-fifo-wait'
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c11']);
    sim.setVehicleTaskForTest('SH-01', 'head-staged-task', false);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-a-c11',
      waitReason: 'inbound-lift-fifo-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-top-b']);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string }>;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      topLiftInboundColumnPredecessorPending(task: unknown): boolean;
    };
    const headTask = internals.tasks.find((candidate) => candidate.id === 'head-staged-task')!;
    const headVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;

    expect(internals.topLiftInboundColumnPredecessorPending(headTask)).toBe(false);
    expect(internals.inboundTaskShouldWaitBeforePickup(headTask)).toBe(false);
    expect(internals.inboundPickupDispatchGoalNodeId(headTask, headVehicle)).toBe('column-top-a-c08');

    const state = sim.step(0.2);
    const releasedVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(releasedVehicle?.waitReason).toBeNull();
    expect(releasedVehicle?.plannedGoalNodeId).toBe('column-top-a-c08');
    expect(releasedVehicle?.routeNodeIds.at(-1)).toBe('column-top-a-c08');
    expect(releasedVehicle?.targetNodeId).not.toBe('column-top-a-c11');
    expectRouteHopsAdjacent(scenario, releasedVehicle?.routeNodeIds ?? []);
  });

  it('holds the inbound lift pickup while loaded same-column predecessors remain active', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120,
        sourceBufferCapacity: 4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'pipeline-pred-01-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addLoadForTest({ id: 'pipeline-pred-02-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addLoadForTest({ id: 'pipeline-pred-03-load', state: 'carried', nodeId: null, vehicleId: 'SH-03', weightKg: 100 });
    sim.addLoadForTest({ id: 'pipeline-front-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'pipeline-follower-source-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-02', vehicleId: null, weightKg: 100 });
    for (const [index, row] of [14, 13, 12].entries()) {
      const vehicleId = `SH-0${index + 1}`;
      const taskId = `pipeline-pred-0${index + 1}-task`;
      const loadId = `pipeline-pred-0${index + 1}-load`;
      const dropoffNodeId = `storage-r${String(row).padStart(2, '0')}-c01`;
      sim.addTaskForTest({
        id: taskId,
        kind: 'inbound',
        state: 'in-progress',
        createdAtSec: index,
        assignedAtSec: index,
        startedAtSec: index,
        completedAtSec: null,
        pickupNodeId: 'column-top-a-c08',
        dropoffNodeId,
        loadId,
        vehicleId,
        replanCount: 0,
        waitReason: null
      });
      sim.setVehicleRouteForTest(vehicleId, [`storage-r0${index + 4}-c01`, dropoffNodeId]);
      sim.setVehicleTaskForTest(vehicleId, taskId, true);
    }
    sim.addTaskForTest({
      id: 'pipeline-follower-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 10,
      assignedAtSec: 10,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r11-c01',
      loadId: 'pipeline-follower-source-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: 'inbound-column-predecessor-wait'
    });
    sim.setVehicleRouteForTest('SH-04', ['column-top-a-c09']);
    sim.setVehicleTaskForTest('SH-04', 'pipeline-follower-task', false);
    sim.setVehicleWaitingForTest('SH-04', {
      targetNodeId: 'column-top-a-c09',
      waitReason: 'inbound-column-predecessor-wait',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      vehicles: Array<{ id: string; waitReason: string | null; plannedGoalNodeId: string | null }>;
      topLiftInboundColumnPredecessorPending(task: unknown): boolean;
      inboundTaskShouldWaitBeforePickup(task: unknown): boolean;
      inboundPickupDispatchGoalNodeId(task: unknown, vehicle?: unknown): string;
      topLiftInboundQueueNodeIdForTask(task: unknown, vehicle?: unknown): string | null;
      topLiftLoadedInboundWipCountForLift(liftNodeId: string): number;
      topLiftInboundPickupPipelineWipLimit(liftNodeId: string): number;
    };
    const followerTask = internals.tasks.find((candidate) => candidate.id === 'pipeline-follower-task')!;
    const followerVehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;
    const queueNodeId = internals.topLiftInboundQueueNodeIdForTask(followerTask, followerVehicle);

    expect(internals.topLiftLoadedInboundWipCountForLift('lift-01-inbound')).toBe(3);
    expect(internals.topLiftInboundPickupPipelineWipLimit('lift-01-inbound')).toBe(4);
    expect(internals.topLiftInboundColumnPredecessorPending(followerTask)).toBe(true);
    expect(internals.inboundTaskShouldWaitBeforePickup(followerTask)).toBe(true);
    expect(internals.inboundPickupDispatchGoalNodeId(followerTask, followerVehicle)).toBe(queueNodeId);
    expect(internals.inboundPickupDispatchGoalNodeId(followerTask, followerVehicle)).not.toBe('column-top-a-c08');
    expect(followerVehicle.waitReason).toBe('inbound-column-predecessor-wait');
  });

  it('proactively clears column-access storage swaps through a legal hold node', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'access-swap-c01-block-01', state: 'stored', nodeId: 'storage-r07-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'access-swap-c01-block-02', state: 'stored', nodeId: 'storage-r08-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'access-swap-c03-block-01', state: 'stored', nodeId: 'storage-r07-c03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'access-swap-c03-block-02', state: 'stored', nodeId: 'storage-r08-c03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'access-swap-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'access-swap-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c02',
      dropoffNodeId: 'column-top-a-c08',
      loadId: 'access-swap-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c02', 'storage-r07-c02']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r07-c02', 'column-middle-c02', 'column-middle-c03']);
    sim.setVehicleTaskForTest('SH-02', 'access-swap-outbound', true);
    sim.setVehicleRouteForTest('SH-03', ['storage-r08-c02', 'column-middle-c02']);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r07-c02',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c02',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'column-middle-c02',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        localRouteReason: string | null;
        routeNodeIds: string[];
      }>;
      tryBreakAgentRefreshColumnAccessStorageSwap(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshColumnAccessStorageSwap(['SH-01', 'SH-02', 'SH-03'])).toBe(true);
    const cleared = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'column-access-storage-swap-clearance');
    const terminalNode = scenario.layout.nodes.find((node) => node.id === cleared?.routeNodeIds.at(-1));
    const routeReplans = sim.getEventLog().filter((event) => event.eventType === 'route-replanned');

    expect(cleared?.id).toBe('SH-01');
    expect(cleared?.routeNodeIds).toEqual([
      'column-middle-c02',
      'column-middle-c03',
      'column-middle-c04',
      'storage-r07-c04'
    ]);
    expect(terminalNode?.noStop).not.toBe(true);
    expect(terminalNode?.noParking).not.toBe(true);
    expect(routeReplans[0]?.reason).toBe('agent-refresh-column-access-storage-swap-clearance');
    expectRouteHopsAdjacent(scenario, cleared?.routeNodeIds ?? []);
  });

  it('clears loaded top-row storage/access swaps during immediate move-block handling', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-storage-access-swap-a', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-storage-access-swap-a-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r08-c18',
      dropoffNodeId: 'column-bottom-b-c20',
      loadId: 'loaded-storage-access-swap-a',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r01-c15',
      'column-top-b-c15',
      'module-boundary-01-spine-top-b',
      'module-boundary-01-spine-middle',
      'column-middle-c15',
      'storage-r08-c15'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'loaded-storage-access-swap-a-task', true);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-top-b-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });

    sim.addLoadForTest({ id: 'loaded-storage-access-swap-b', state: 'carried', nodeId: null, vehicleId: 'SH-03', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-storage-access-swap-b-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r13-c05',
      dropoffNodeId: 'column-bottom-b-c06',
      loadId: 'loaded-storage-access-swap-b',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c15',
      'storage-r01-c15',
      'storage-r02-c15',
      'storage-r03-c15',
      'storage-r04-c15',
      'storage-r05-c15',
      'storage-r06-c15',
      'storage-r07-c15'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'loaded-storage-access-swap-b-task', true);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'storage-r01-c15',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const storageVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const accessVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;

    expect(internals.agentRefreshHandleMoveBlock(storageVehicle, 'column-top-b-c15', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-03'
    })).toBe(false);
    expect(accessVehicle.waitReason).toBeNull();
    expect(accessVehicle.localRouteReason).toBe('column-access-storage-swap-clearance');
    expect(accessVehicle.routeNodeIds).toEqual([
      'column-top-b-c15',
      'column-top-b-c16',
      'storage-r01-c16'
    ]);
    expect(accessVehicle.targetNodeId).toBe('column-top-b-c16');
    expectRouteHopsAdjacent(scenario, accessVehicle.routeNodeIds);
  });

  it('breaks a persisted loaded storage/access swap before the long-run blocked signature trips', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'persisted-access-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-03', weightKg: 100 });
    sim.addTaskForTest({
      id: 'persisted-access-outbound-task',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c22',
      dropoffNodeId: 'column-bottom-b-c20',
      loadId: 'persisted-access-outbound-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'storage-r07-c22',
      'column-middle-c22',
      'module-02-spine-middle',
      'module-02-spine-bottom-a',
      'module-02-spine-bottom-b',
      'column-bottom-b-c21',
      'column-bottom-b-c20'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'persisted-access-outbound-task', true);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'column-middle-c22',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-04',
      waitingSinceSec: 0
    });

    sim.addLoadForTest({ id: 'persisted-access-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-04', weightKg: 100 });
    sim.addTaskForTest({
      id: 'persisted-access-inbound-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c23',
      dropoffNodeId: 'storage-r07-c22',
      loadId: 'persisted-access-inbound-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-04', ['column-middle-c22', 'storage-r07-c22']);
    sim.setVehicleTaskForTest('SH-04', 'persisted-access-inbound-task', true);
    sim.setVehicleWaitingForTest('SH-04', {
      targetNodeId: 'storage-r07-c22',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });

    const state = sim.step(0.2);
    const cleared = state.vehicles.find((vehicle) => vehicle.localRouteReason === 'column-access-storage-swap-clearance');
    const storageVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-03');
    const accessVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-04');

    expect(cleared).toBeDefined();
    expect(cleared?.waitReason).toBeNull();
    expectRouteHopsAdjacent(scenario, cleared?.routeNodeIds ?? []);
    expect(
      storageVehicle?.waitReason === 'node-occupied' &&
      storageVehicle.blockingVehicleId === 'SH-04' &&
      accessVehicle?.waitReason === 'node-occupied' &&
      accessVehicle.blockingVehicleId === 'SH-03'
    ).toBe(false);
    expect(state.kpis.deadlockCount).toBe(0);
  });

  it('moves an empty access blocker into storage to open a loaded inbound column entry chain', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-c01-chain-load', state: 'carried', nodeId: null, vehicleId: 'SH-07', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-c01-chain',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c09',
      dropoffNodeId: 'storage-r11-c01',
      loadId: 'loaded-c01-chain-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', [
      'storage-r07-c01',
      'column-middle-c01',
      'storage-r08-c01',
      'storage-r09-c01',
      'storage-r10-c01',
      'storage-r11-c01'
    ]);
    sim.setVehicleTaskForTest('SH-07', 'loaded-c01-chain', true);
    sim.setVehicleWaitingForTest('SH-07', {
      targetNodeId: 'column-middle-c01',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-06',
      waitingSinceSec: 0
    });

    sim.setVehicleRouteForTest('SH-06', [
      'column-middle-c01',
      'column-middle-c02',
      'storage-r07-c02'
    ]);
    sim.setVehicleTaskForTest('SH-06', null, false);
    sim.setVehicleWaitingForTest('SH-06', {
      targetNodeId: 'column-middle-c02',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });

    sim.setVehicleRouteForTest('SH-03', ['column-middle-c02']);
    sim.setVehicleTaskForTest('SH-03', null, false);
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'column-middle-c02',
      waitReason: 'route-unavailable',
      blockingVehicleId: null,
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        targetNodeId: string | null;
        routeNodeIds: string[];
        localRouteReason: string | null;
        waitReason: string | null;
      }>;
      agentRefreshHandleMoveBlock(
        vehicle: unknown,
        blockedTargetNodeId: string,
        block: { reason: string; blockingVehicleId: string | null }
      ): boolean;
    };
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;
    const accessBlocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;

    expect(internals.agentRefreshHandleMoveBlock(requester, 'column-middle-c02', {
      reason: 'node-occupied',
      blockingVehicleId: 'SH-03'
    })).toBe(false);
    expect(accessBlocker.routeNodeIds).toEqual(['column-middle-c02', 'storage-r08-c02']);
    expect(accessBlocker.targetNodeId).toBe('storage-r08-c02');
    expect(accessBlocker.waitReason).toBeNull();
    expect(accessBlocker.localRouteReason).toBe('access-blocker-clearance');
    expectRouteHopsAdjacent(scenario, accessBlocker.routeNodeIds);
  });

  it('clears top-row storage/access swaps by yielding the access vehicle into a legal pocket', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'top-row-swap-load', state: 'carried', nodeId: null, vehicleId: 'SH-03', weightKg: 100 });
    sim.addTaskForTest({
      id: 'top-row-swap-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c09',
      dropoffNodeId: 'storage-r05-c04',
      loadId: 'top-row-swap-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', [
      'storage-r01-c04',
      'column-top-b-c04',
      'column-top-b-c05',
      'storage-r01-c05'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleRouteForTest('SH-03', [
      'column-top-b-c04',
      'storage-r01-c04',
      'storage-r02-c04',
      'storage-r03-c04',
      'storage-r04-c04',
      'storage-r05-c04'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'top-row-swap-inbound', true);
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c05', 'column-top-b-c04']);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'column-top-b-c04',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-top-b-c04',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-03',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-03', {
      targetNodeId: 'storage-r01-c04',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        localRouteReason: string | null;
        routeNodeIds: string[];
        targetNodeId: string | null;
      }>;
      tryBreakAgentRefreshColumnAccessStorageSwap(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshColumnAccessStorageSwap(['SH-01', 'SH-02', 'SH-03'])).toBe(true);
    const yielder = internals.vehicles.find((vehicle) => vehicle.localRouteReason === 'column-access-storage-swap-clearance');
    const terminalNode = scenario.layout.nodes.find((node) => node.id === yielder?.routeNodeIds.at(-1));

    expect(yielder?.id).toBe('SH-03');
    expect(yielder?.routeNodeIds[0]).toBe('column-top-b-c04');
    expect(yielder?.routeNodeIds[1]).toMatch(/^column-top-b-c(?:03|05)$/);
    expect(yielder?.routeNodeIds.at(-1)).toMatch(/^storage-r01-c(?:03|05)$/);
    expect(yielder?.targetNodeId).toBe(yielder?.routeNodeIds[1]);
    expect(yielder?.routeNodeIds).not.toContain('storage-r01-c04');
    expect(terminalNode?.noStop).not.toBe(true);
    expect(terminalNode?.noParking).not.toBe(true);
    expectRouteHopsAdjacent(scenario, yielder?.routeNodeIds ?? []);
  });

  it('marks loaded outbound storage vehicles waiting when no exit route is available', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    for (const row of [1, 3, 4, 5, 6, 7]) {
      sim.addLoadForTest({
        id: `blocked-outbound-exit-${row}`,
        state: 'stored',
        nodeId: `storage-r${String(row).padStart(2, '0')}-c02`,
        vehicleId: null,
        weightKg: 100
      });
    }
    sim.addLoadForTest({ id: 'blocked-outbound-carried', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'blocked-outbound-route',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r02-c02',
      dropoffNodeId: 'column-bottom-b-c06',
      loadId: 'blocked-outbound-carried',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r02-c02']);
    sim.setVehicleTaskForTest('SH-01', 'blocked-outbound-route', true);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.state).toBe('waiting-blocked');
    expect(vehicle?.waitReason).toBe('route-unavailable');
    expect(vehicle?.targetNodeId).toBe('column-bottom-b-c06');
    expect(vehicle?.plannedGoalNodeId).toBe('column-bottom-b-c06');
  });

  it('routes loaded outbound storage exits through vertical spine transfers to bottom-b', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'spine-transfer-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'spine-transfer-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r06-c15',
      dropoffNodeId: 'column-bottom-b-c20',
      loadId: 'spine-transfer-outbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r06-c15']);
    sim.setVehicleTaskForTest('SH-01', 'spine-transfer-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      agentRefreshLoadedOutboundRouteToDropoff(currentNodeId: string, task: unknown, goalNodeId?: string): string[] | null;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'spine-transfer-outbound')!;
    const route = internals.agentRefreshLoadedOutboundRouteToDropoff('storage-r06-c15', task, 'column-bottom-b-c20');

    expect(route).not.toBeNull();
    expect(route).toContain('module-boundary-01-spine-bottom-a');
    expect(route?.at(-1)).toBe('column-bottom-b-c20');
    expectRouteHopsAdjacent(scenario, route ?? []);
  });

  it('displaces an idle storage shuttle blocking an empty task route', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 5 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'idle-blocked-pick-load', state: 'stored', nodeId: 'storage-r04-c05', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'idle-blocked-pick',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r04-c05',
      dropoffNodeId: 'column-bottom-b-c06',
      loadId: 'idle-blocked-pick-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r05-c05']);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleRouteForTest('SH-05', ['storage-r06-c05', 'storage-r05-c05', 'storage-r04-c05']);
    sim.setVehicleTaskForTest('SH-05', 'idle-blocked-pick', false);

    sim.step(0.2);
    const state = sim.getState();
    const blocker = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const requester = state.vehicles.find((vehicle) => vehicle.id === 'SH-05');

    expect(requester?.waitReason).toBe('node-occupied');
    expect(requester?.blockingVehicleId).toBe('SH-01');
    expect(blocker?.routeNodeIds.length).toBeGreaterThan(1);
    expect(blocker?.localRouteReason).toBe('temporary-yield');
    expect(blocker?.routeNodeIds).not.toEqual(['storage-r05-c05']);
    expectRouteHopsAdjacent(scenario, blocker?.routeNodeIds ?? []);
  });

  it('holds queued inbound work while idle storage shuttles clear target columns', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'queued-inbound-source-load', state: 'waiting', nodeId: 'lift-01-inbound', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'parked-under-load-c03', state: 'stored', nodeId: 'storage-r11-c03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'parked-under-load-c15', state: 'stored', nodeId: 'storage-r11-c15', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queued-inbound-same-column',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c09',
      dropoffNodeId: 'storage-r08-c03',
      loadId: 'queued-inbound-source-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: 'vehicle-unavailable'
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r11-c03']);
    sim.setVehicleTaskForTest('SH-01', null, false);
    sim.setVehicleRouteForTest('SH-02', ['storage-r11-c15']);
    sim.setVehicleTaskForTest('SH-02', null, false);
    sim.setVehicleRouteForTest('SH-03', ['column-top-b-c08']);
    sim.setVehicleTaskForTest('SH-03', null, false);

    sim.step(0.2);
    const state = sim.getState();
    const task = state.tasks.find((candidate) => candidate.id === 'queued-inbound-same-column');
    const clearingVehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(task?.state).toBe('queued');
    expect(task?.vehicleId).toBeNull();
    expect(task?.waitReason).toBe('inbound-column-clearing:c03');
    expect(clearingVehicle?.taskId).toBeNull();
    expect(clearingVehicle?.localRouteReason).toBe('post-dropoff-column-exit');
    expect(clearingVehicle?.routeNodeIds.length).toBeGreaterThan(1);
    expect(clearingVehicle?.routeNodeIds[0]).toBe('storage-r11-c03');
    expectRouteHopsAdjacent(scenario, clearingVehicle?.routeNodeIds ?? []);
  });

  it('routes a loaded same-column inbound follower around an active shallow predecessor dropoff', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'shallow-predecessor-load', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'shallow-predecessor',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r01-c06',
      loadId: 'shallow-predecessor-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'deeper-follower-load', state: 'carried', nodeId: null, vehicleId: 'SH-08', weightKg: 100 });
    sim.addTaskForTest({
      id: 'deeper-follower',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: 1,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c06',
      loadId: 'deeper-follower-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', ['storage-r01-c06']);
    sim.setVehicleTaskForTest('SH-05', 'shallow-predecessor', true);
    sim.setVehicleRouteForTest('SH-08', ['column-top-b-c07']);
    sim.setVehicleTaskForTest('SH-08', 'deeper-follower', true);

    const state = sim.step(0.2);
    const follower = state.vehicles.find((vehicle) => vehicle.id === 'SH-08');
    const route = follower?.routeNodeIds ?? [];

    expect(follower?.waitReason).toBeNull();
    expect(route).not.toContain('storage-r01-c06');
    expect(route).toContain('column-middle-c06');
    expect(route.at(-1)).toBe('storage-r02-c06');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('does not let later same-column inbound successors block a loaded predecessor route', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    for (const row of [1, 7, 11, 12, 14]) {
      sim.addLoadForTest({
        id: `stored-c13-r${row}`,
        state: 'stored',
        nodeId: `storage-r${String(row).padStart(2, '0')}-c13`,
        vehicleId: null,
        weightKg: 100
      });
    }
    sim.addLoadForTest({ id: 'loaded-predecessor-c13', state: 'carried', nodeId: null, vehicleId: 'SH-04', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-predecessor-c13',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 105,
      assignedAtSec: 105,
      startedAtSec: 105,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r10-c13',
      loadId: 'loaded-predecessor-c13',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    for (const [taskId, row, createdAtSec, vehicleId] of [
      ['successor-c13-r09', 9, 108, 'SH-03'],
      ['successor-c13-r08', 8, 109, 'SH-07']
    ] as const) {
      sim.addLoadForTest({ id: `${taskId}-load`, state: 'waiting', nodeId: 'column-top-a-c08', vehicleId: null, weightKg: 100 });
      sim.addTaskForTest({
        id: taskId,
        kind: 'inbound',
        state: 'assigned',
        createdAtSec,
        assignedAtSec: createdAtSec,
        startedAtSec: null,
        completedAtSec: null,
        pickupNodeId: 'column-top-a-c08',
        dropoffNodeId: `storage-r${String(row).padStart(2, '0')}-c13`,
        loadId: `${taskId}-load`,
        vehicleId,
        replanCount: 0,
        waitReason: null
      });
    }
    sim.setVehicleRouteForTest('SH-04', ['column-top-a-c08']);
    sim.setVehicleTaskForTest('SH-04', 'loaded-predecessor-c13', true);

    const state = sim.step(0.2);
    const predecessor = state.vehicles.find((vehicle) => vehicle.id === 'SH-04');
    const route = predecessor?.routeNodeIds ?? [];

    expect(predecessor?.waitReason).toBeNull();
    expect(route).toContain('column-middle-c13');
    expect(route).toContain('storage-r09-c13');
    expect(route).toContain('storage-r08-c13');
    expect(route.at(-1)).toBe('storage-r10-c13');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('does not start a column-access storage swap clearance when the legal hold is already reserved', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'reserved-access-swap-c01-block-01', state: 'stored', nodeId: 'storage-r07-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'reserved-access-swap-c01-block-02', state: 'stored', nodeId: 'storage-r08-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'reserved-access-swap-c03-block-01', state: 'stored', nodeId: 'storage-r07-c03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'reserved-access-swap-c03-block-02', state: 'stored', nodeId: 'storage-r08-c03', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'reserved-access-swap-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'reserved-access-swap-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c02',
      dropoffNodeId: 'column-top-a-c08',
      loadId: 'reserved-access-swap-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c02', 'storage-r07-c02']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r07-c02', 'column-middle-c02', 'column-middle-c03']);
    sim.setVehicleTaskForTest('SH-02', 'reserved-access-swap-outbound', true);
    sim.setVehicleRouteForTest('SH-03', ['storage-r08-c02']);
    sim.addReservationForTest({
      id: 'reserved-access-swap-hold',
      resourceType: 'node',
      resourceId: 'storage-r07-c04',
      vehicleId: 'SH-03',
      taskId: null,
      startTimeSec: 0,
      endTimeSec: 60,
      priority: 999,
      conflictGroup: null,
      reasonCode: 'test-reserved-legal-hold'
    });
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r07-c02',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'column-middle-c02',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        localRouteReason: string | null;
        routeNodeIds: string[];
        waitReason: string | null;
      }>;
      tryBreakAgentRefreshColumnAccessStorageSwap(candidateVehicleIds: string[]): boolean;
      agentRefreshLocalRouteNodesClear(vehicle: unknown, routeNodeIds: string[]): boolean;
    };
    const accessVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(internals.agentRefreshLocalRouteNodesClear(accessVehicle, [
      'column-middle-c02',
      'column-middle-c03',
      'column-middle-c04',
      'storage-r07-c04'
    ])).toBe(false);
    expect(internals.tryBreakAgentRefreshColumnAccessStorageSwap(['SH-01', 'SH-02'])).toBe(false);
    expect(accessVehicle.localRouteReason).toBeNull();
    expect(accessVehicle.routeNodeIds).toEqual(['column-middle-c02', 'storage-r07-c02']);
    expect(accessVehicle.waitReason).toBe('node-occupied');
  });

  it('agent-refresh opens a same-column queue by moving the legal tail pocket first', () => {
    const scenario = createInboundMvpBaselineScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 3
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r12-c07', 'storage-r11-c07']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r10-c07', 'storage-r11-c07']);
    sim.setVehicleRouteForTest('SH-03', ['storage-r11-c07', 'storage-r10-c07']);
    sim.setVehicleTaskForTest('SH-02', null, true);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        loaded: boolean;
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        waitingSinceSec: number | null;
        routeNodeIds: string[];
        localRouteNodeIds: string[];
      }>;
      tryBreakAgentRefreshStorageColumnQueue: (candidateVehicleIds: string[]) => boolean;
    };
    for (const vehicle of internals.vehicles) {
      if (vehicle.id === 'SH-01') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          loaded: false,
          targetNodeId: 'storage-r11-c07',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-03',
          waitingSinceSec: 0
        });
      }
      if (vehicle.id === 'SH-02') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          loaded: true,
          targetNodeId: 'storage-r11-c07',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-03',
          waitingSinceSec: 0
        });
      }
      if (vehicle.id === 'SH-03') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          loaded: false,
          targetNodeId: 'storage-r10-c07',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-02',
          waitingSinceSec: 0
        });
      }
    }

    expect(internals.tryBreakAgentRefreshStorageColumnQueue(['SH-02', 'SH-03'])).toBe(true);
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    expect(yielder?.routeNodeIds).toEqual(['storage-r12-c07', 'storage-r13-c07']);
    expect(yielder?.localRouteNodeIds).toEqual(['storage-r12-c07', 'storage-r13-c07']);
    expectRouteHopsAdjacent(scenario, yielder?.routeNodeIds ?? []);
  });

  it('agent-refresh extends same-column queue yields through no-stop access to a legal hold', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r07-c02', 'storage-r06-c02']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r06-c02', 'storage-r05-c02']);
    sim.setVehicleRouteForTest('SH-03', ['storage-r08-c02']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        waitingSinceSec: number | null;
        routeNodeIds: string[];
        localRouteNodeIds: string[];
      }>;
      layoutNode(nodeId: string): { noStop?: boolean; noParking?: boolean } | null;
      tryBreakAgentRefreshStorageColumnQueue(candidateVehicleIds: string[]): boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    Object.assign(yielder, {
      state: 'waiting-blocked',
      targetNodeId: 'storage-r06-c02',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });

    expect(internals.tryBreakAgentRefreshStorageColumnQueue(['SH-01', 'SH-02'])).toBe(true);
    const terminalNode = internals.layoutNode(yielder.routeNodeIds.at(-1) ?? '');

    expect(yielder.routeNodeIds[0]).toBe('storage-r07-c02');
    expect(yielder.routeNodeIds).toContain('column-middle-c02');
    expect(terminalNode?.noStop).not.toBe(true);
    expect(terminalNode?.noParking).not.toBe(true);
    expectRouteHopsAdjacent(scenario, yielder.routeNodeIds);
  });

  it('lets a loaded same-column follower retreat one cell to clear a storage exit', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-follower-retreat-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-follower-retreat-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r06-c04',
      dropoffNodeId: 'column-top-a-c08',
      loadId: 'loaded-follower-retreat-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r06-c04', 'storage-r07-c04', 'column-middle-c04']);
    sim.setVehicleTaskForTest('SH-01', 'loaded-follower-retreat-outbound', true);
    sim.setVehicleRouteForTest('SH-02', ['storage-r07-c04', 'column-middle-c04']);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'storage-r07-c04',
      waitReason: 'node-occupied',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
      id: string;
      loaded: boolean;
      localRouteReason: string | null;
      routeNodeIds: string[];
      waitReason: string | null;
      yieldHoldUntilSec: number | null;
      yieldHoldNodeId: string | null;
      }>;
      tryBreakAgentRefreshStorageColumnQueue(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshStorageColumnQueue(['SH-01', 'SH-02'])).toBe(true);
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(yielder?.loaded).toBe(true);
    expect(yielder?.routeNodeIds).toEqual(['storage-r06-c04', 'storage-r05-c04']);
    expect(yielder?.localRouteReason).toBe('temporary-yield');
    expect(yielder?.yieldHoldUntilSec).toBeGreaterThan(0);
    expect(yielder?.yieldHoldNodeId).toBe('storage-r05-c04');
    expect(yielder?.waitReason).toBeNull();
    expectRouteHopsAdjacent(scenario, yielder?.routeNodeIds ?? []);
  });

  it('clears taskless no-stop middle access waits into storage instead of holding the spine', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'no-stop-spine-loaded', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'no-stop-spine-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c21',
      dropoffNodeId: 'storage-r08-c20',
      loadId: 'no-stop-spine-loaded',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-middle-c21']);
    sim.setVehicleRouteForTest('SH-02', [
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'column-middle-c21',
      'column-middle-c20',
      'storage-r08-c20'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'no-stop-spine-inbound', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-02-spine-middle',
      waitReason: 'no-stop-continuation-blocked',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        localRouteReason: string | null;
        routeNodeIds: string[];
        waitReason: string | null;
      }>;
      layoutNode(nodeId: string): { noStop?: boolean; noParking?: boolean } | null;
      agentRefreshNoStopContinuationWaitCandidateVehicleIds(): string[];
      tryBreakAgentRefreshWaitCycle(candidateVehicleIds: string[]): boolean;
    };
    const candidateIds = internals.agentRefreshNoStopContinuationWaitCandidateVehicleIds();

    expect(candidateIds).toEqual(['SH-01']);
    expect(internals.tryBreakAgentRefreshWaitCycle(candidateIds)).toBe(true);
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const terminalNode = internals.layoutNode(yielder?.routeNodeIds.at(-1) ?? '');

    expect(yielder?.localRouteReason).toBe('temporary-yield');
    expect(yielder?.routeNodeIds[0]).toBe('column-middle-c21');
    expect(terminalNode?.noStop).not.toBe(true);
    expect(terminalNode?.noParking).not.toBe(true);
    expect(yielder?.waitReason).toBeNull();
    expectRouteHopsAdjacent(scenario, yielder?.routeNodeIds ?? []);
  });

  it('clears an empty bottom-a spine entrant when it faces a loaded top-b no-stop entrant', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 120
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'loaded-spine-faceoff-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-spine-faceoff-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r14-c15',
      loadId: 'loaded-spine-faceoff-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'empty-spine-faceoff-load', state: 'stored', nodeId: 'storage-r01-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-spine-faceoff-outbound',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'empty-spine-faceoff-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'module-02-spine-top-b',
      'module-02-spine-middle',
      'module-02-spine-bottom-a',
      'column-bottom-a-c21',
      'column-bottom-a-c20',
      'column-bottom-a-c19',
      'column-bottom-a-c18',
      'column-bottom-a-c17',
      'column-bottom-a-c16',
      'column-bottom-a-c15',
      'storage-r14-c15'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-spine-faceoff-inbound', true);
    sim.setVehicleWaitingForTest('SH-01', {
      targetNodeId: 'module-02-spine-middle',
      waitReason: 'no-stop-continuation-blocked',
      blockingVehicleId: 'SH-02',
      waitingSinceSec: 0
    });
    sim.setVehicleRouteForTest('SH-02', [
      'module-02-spine-bottom-a',
      'module-02-spine-middle',
      'module-02-spine-top-b',
      'column-top-b-c21',
      'column-top-b-c20',
      'column-top-b-c19',
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16',
      'column-top-b-c15',
      'module-boundary-01-spine-top-b',
      'column-top-b-c14',
      'column-top-b-c13',
      'column-top-b-c12',
      'column-top-b-c11',
      'column-top-b-c10',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07',
      'column-top-b-c06',
      'column-top-b-c05',
      'column-top-b-c04',
      'column-top-b-c03',
      'column-top-b-c02',
      'column-top-b-c01',
      'storage-r01-c01'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'empty-spine-faceoff-outbound', false);
    sim.setVehicleWaitingForTest('SH-02', {
      targetNodeId: 'module-02-spine-middle',
      waitReason: 'no-stop-continuation-blocked',
      blockingVehicleId: 'SH-01',
      waitingSinceSec: 0
    });

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        routeNodeIds: string[];
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        localRouteReason: string | null;
        loaded: boolean;
      }>;
      layoutNode(nodeId: string): { noStop?: boolean; noParking?: boolean } | null;
      tryBreakAgentRefreshWaitCycle(candidateVehicleIds: string[]): boolean;
    };

    expect(internals.tryBreakAgentRefreshWaitCycle(['SH-01', 'SH-02'])).toBe(true);
    const emptyYielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const terminalNode = internals.layoutNode(emptyYielder.routeNodeIds.at(-1) ?? '');

    expect(emptyYielder.loaded).toBe(false);
    expect(emptyYielder.localRouteReason).toBe('temporary-yield');
    expect(emptyYielder.currentNodeId).toBe('module-02-spine-bottom-a');
    expect(emptyYielder.targetNodeId).not.toBe('module-02-spine-middle');
    expect(emptyYielder.routeNodeIds).not.toEqual([
      'module-02-spine-bottom-a',
      'module-02-spine-middle',
      'module-02-spine-top-b'
    ]);
    expect(terminalNode?.noStop).not.toBe(true);
    expect(terminalNode?.noParking).not.toBe(true);
    expect(emptyYielder.waitReason).toBeNull();
    expect(emptyYielder.blockingVehicleId).toBeNull();
    expectRouteHopsAdjacent(scenario, emptyYielder.routeNodeIds);
  });

  it('agent-refresh clears empty same-column storage swaps by using stored-load cells as pass-through pockets', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'empty-column-swap-stored-top', state: 'stored', nodeId: 'storage-r01-c02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'empty-column-swap-stored-bottom', state: 'stored', nodeId: 'storage-r04-c02', vehicleId: null, weightKg: 100 });
    sim.setVehicleRouteForTest('SH-01', ['storage-r03-c02', 'storage-r02-c02']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r02-c02', 'storage-r03-c02']);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        loaded: boolean;
        targetNodeId: string | null;
        waitReason: string | null;
        blockingVehicleId: string | null;
        waitingSinceSec: number | null;
        routeNodeIds: string[];
        localRouteNodeIds: string[];
      }>;
      tryBreakAgentRefreshStorageColumnQueue: (candidateVehicleIds: string[]) => boolean;
    };
    for (const vehicle of internals.vehicles) {
      if (vehicle.id === 'SH-01') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          loaded: false,
          targetNodeId: 'storage-r02-c02',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-02',
          waitingSinceSec: 0
        });
      }
      if (vehicle.id === 'SH-02') {
        Object.assign(vehicle, {
          state: 'waiting-blocked',
          loaded: false,
          targetNodeId: 'storage-r03-c02',
          waitReason: 'node-occupied',
          blockingVehicleId: 'SH-01',
          waitingSinceSec: 0
        });
      }
    }

    expect(internals.tryBreakAgentRefreshStorageColumnQueue(['SH-01', 'SH-02'])).toBe(true);
    const cleared = internals.vehicles.find((vehicle) => vehicle.localRouteNodeIds.length > 0);

    expect([
      'storage-r02-c02>storage-r01-c02',
      'storage-r03-c02>storage-r04-c02'
    ]).toContain(cleared?.localRouteNodeIds.join('>'));
    expect(cleared?.waitReason).toBeNull();
  });

  it('lets an empty shuttle already inside an active inbound column route out through that column exit', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'active-c18-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'active-c18-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-buffer-03',
      dropoffNodeId: 'storage-r08-c18',
      loadId: 'active-c18-inbound-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'outbound-c02-load', state: 'stored', nodeId: 'storage-r02-c02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-from-c02',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r02-c02',
      dropoffNodeId: 'lift-01-outbound-buffer-03',
      loadId: 'outbound-c02-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r07-c18']);
    sim.setVehicleTaskForTest('SH-01', 'outbound-from-c02', false);
    sim.setVehicleRouteForTest('SH-02', ['storage-r08-c18']);
    sim.setVehicleTaskForTest('SH-02', 'active-c18-inbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-from-c02')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route[0]).toBe('storage-r07-c18');
    expect(route).toContain('column-middle-c18');
    expect(route.at(-1)).toBe('storage-r02-c02');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('continues an existing outbound pickup route tail when replanning from a middle column entry fails', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const routeTail = [
      'column-middle-c04',
      'column-middle-c03',
      'storage-r07-c03',
      'storage-r06-c03',
      'storage-r05-c03',
      'storage-r04-c03',
      'storage-r03-c03'
    ];

    sim.addLoadForTest({ id: 'outbound-c03-load', state: 'stored', nodeId: 'storage-r03-c03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-from-c03',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c03',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-c03-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', routeTail);
    sim.setVehicleTaskForTest('SH-08', 'outbound-from-c03', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        currentNodeId: string;
        targetNodeId: string | null;
        routeIndex: number;
        x: number;
        y: number;
        z: number;
      }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      currentNodeOccupancy: Map<string, string>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-08')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-from-c03')!;
    const middleNode = scenario.layout.nodes.find((node) => node.id === 'column-middle-c03')!;
    internals.currentNodeOccupancy.delete('column-middle-c04');
    internals.currentNodeOccupancy.set('column-middle-c03', 'SH-08');
    vehicle.currentNodeId = 'column-middle-c03';
    vehicle.routeIndex = 1;
    vehicle.targetNodeId = 'storage-r03-c03';
    vehicle.x = middleNode.x;
    vehicle.y = middleNode.y;
    vehicle.z = middleNode.z;

    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route).toEqual(routeTail.slice(1));
    expect(route[1]).toBe('storage-r07-c03');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('keeps outbound pickup traffic out of a loaded inbound lift-exit clearance corridor', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-inbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-07', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r07-c16',
      loadId: 'loaded-inbound-load',
      vehicleId: 'SH-07',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-07', [
      'lift-02-inbound-queue-01-service-exit',
      'lift-02-inbound-queue-02-service-exit',
      'lift-02-inbound-queue-03-service-exit',
      'lift-02-inbound-queue-03-entry-access',
      'column-top-b-c21',
      'column-top-a-c21',
      'column-top-a-c20',
      'column-top-a-c19',
      'column-top-a-c18',
      'column-top-a-c17',
      'column-top-a-c16',
      'column-top-b-c16',
      'storage-r01-c16',
      'storage-r02-c16',
      'storage-r03-c16',
      'storage-r04-c16',
      'storage-r05-c16',
      'storage-r06-c16',
      'storage-r07-c16'
    ]);
    sim.setVehicleTaskForTest('SH-07', 'loaded-inbound', true);

    sim.addLoadForTest({ id: 'outbound-pickup-load', state: 'stored', nodeId: 'storage-r01-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-pickup',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-pickup-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', ['column-middle-c21']);
    sim.setVehicleTaskForTest('SH-08', 'outbound-pickup', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-08')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-pickup')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route[0]).toBe('column-middle-c21');
    expect(route).not.toContain('storage-r01-c21');
    expect(route).not.toContain('column-top-b-c21');
    expect(route).not.toContain('column-top-b-c20');
    expect(route).toContain('column-bottom-a-c21');
    expect(route.at(-1)).toBe('storage-r01-c01');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('keeps empty outbound pickup traffic out of an active outbound dock clearance sweep', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'active-dropoff-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'active-dropoff',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r08-c02',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'active-dropoff-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-bottom-b-c06',
      'column-bottom-b-c07',
      'module-01-spine-bottom-b',
      'column-bottom-b-c08'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'active-dropoff', true);

    sim.addLoadForTest({ id: 'outbound-pickup-during-dropoff-load', state: 'stored', nodeId: 'storage-r07-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-pickup-during-dropoff',
      kind: 'outbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r07-c01',
      dropoffNodeId: 'column-bottom-b-c08',
      loadId: 'outbound-pickup-during-dropoff-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c17']);
    sim.setVehicleTaskForTest('SH-02', 'outbound-pickup-during-dropoff', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-pickup-during-dropoff')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route[0]).toBe('column-bottom-b-c17');
    expect(route).not.toContain('column-bottom-b-c08');
    expect(route).not.toContain('column-bottom-b-c09');
    expect(route).not.toContain('column-bottom-b-c10');
    expect(route).not.toContain('column-bottom-b-c11');
    expect(route.at(-1)).toBe('storage-r07-c01');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('keeps empty inbound pickup traffic out of an active outbound dock clearance sweep', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'column-bottom-b-c22',
      'column-bottom-b-c23'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, false);
    const clearance = (sim as unknown as {
      vehicles: Array<{ id: string; localRouteReason: string | null; plannedRouteNodeIds: string[] }>;
    }).vehicles.find((candidate) => candidate.id === 'SH-01')!;
    clearance.localRouteReason = 'outbound-lift-clearance';
    clearance.plannedRouteNodeIds = ['column-bottom-b-c22', 'column-bottom-b-c23'];

    sim.addLoadForTest({ id: 'inbound-pickup-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-pickup-during-outbound-clearance',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c21',
      dropoffNodeId: 'storage-r10-c19',
      loadId: 'inbound-pickup-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', ['column-bottom-b-c25']);
    sim.setVehicleTaskForTest('SH-02', 'inbound-pickup-during-outbound-clearance', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-02')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'inbound-pickup-during-outbound-clearance')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route[0]).toBe('column-bottom-b-c25');
    expect(route).not.toContain('column-bottom-b-c22');
    expect(route).not.toContain('column-bottom-b-c23');
    expect(route).not.toContain('column-bottom-b-c24');
    expect(route.at(-1)).toBe('column-top-a-c21');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('prioritizes a loaded inbound lift departure over loaded outbound cross traffic', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'inbound-service-load', state: 'carried', nodeId: null, vehicleId: 'SH-08', weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-service-departure',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-03-service-exit',
      dropoffNodeId: 'storage-r07-c16',
      loadId: 'inbound-service-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', [
      'lift-02-inbound-queue-03-service-exit',
      'lift-02-inbound-queue-03-entry-access',
      'column-top-b-c21'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'inbound-service-departure', true);

    sim.addLoadForTest({ id: 'outbound-cross-load', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-cross-traffic',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c19',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'outbound-cross-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', [
      'column-top-b-c19',
      'column-top-b-c20',
      'column-top-b-c21'
    ]);
    sim.setVehicleTaskForTest('SH-05', 'outbound-cross-traffic', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshHasHigherPriority(left: unknown, right: unknown): boolean;
      agentTurnPriority(vehicle: unknown): number;
    };
    const inboundDeparture = internals.vehicles.find((candidate) => candidate.id === 'SH-08')!;
    const outboundCrossTraffic = internals.vehicles.find((candidate) => candidate.id === 'SH-05')!;

    expect(internals.agentRefreshHasHigherPriority(inboundDeparture, outboundCrossTraffic)).toBe(true);
    expect(internals.agentTurnPriority(inboundDeparture)).toBeGreaterThan(internals.agentTurnPriority(outboundCrossTraffic));
  });

  it('routes empty inbound pickup traffic on the lower lift approach lane', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'inbound-approach-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-approach',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r03-c04',
      loadId: 'inbound-approach-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', ['column-top-a-c20']);
    sim.setVehicleTaskForTest('SH-05', 'inbound-approach', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; pickupNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-05')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'inbound-approach')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.pickupNodeId);

    expect(route.slice(0, 2)).toEqual(['column-top-a-c20', 'column-top-b-c20']);
    expect(route).toContain('column-top-b-c09');
    expect(route.slice(1, -2).some((nodeId) => /^column-top-a-c\d+$/.test(nodeId))).toBe(false);
    expect(route.at(-1)).toBe('lift-01-inbound-queue-01-service-exit');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('routes loaded outbound dropoff traffic on the lower lift approach lane', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'outbound-dropoff-load', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-dropoff',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r09-c15',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'outbound-dropoff-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', ['column-top-b-c18']);
    sim.setVehicleTaskForTest('SH-05', 'outbound-dropoff', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; dropoffNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-05')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'outbound-dropoff')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.dropoffNodeId);

    expect(route.slice(0, 3)).toEqual(['column-top-b-c18', 'column-top-b-c19', 'column-top-b-c20']);
    expect(route).toContain('column-bottom-a-c20');
    expect(route).not.toContain('column-top-a-c22');
    expect(route.at(-1)).toBe('lift-02-outbound-queue-01-service-exit');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('lets a loaded outbound shuttle clear top-b when an inbound lift departure is waiting for that exact node', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'inbound-entry-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-entry-departure',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r05-c04',
      loadId: 'inbound-entry-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c09',
      'storage-r01-c09'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'inbound-entry-departure', true);

    sim.addLoadForTest({ id: 'outbound-clear-load', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-clear-top-b',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-clear-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', [
      'column-bottom-a-c06',
      'column-bottom-b-c06',
      'lift-01-outbound-queue-01-entry-access',
      'lift-01-outbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-05', 'outbound-clear-top-b', true);

    const state = sim.step(0.2);
    const inbound = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');
    const outbound = state.vehicles.find((vehicle) => vehicle.id === 'SH-05');

    expect(outbound).toMatchObject({
      state: 'loaded-moving',
      currentNodeId: 'column-bottom-a-c06',
      targetNodeId: 'column-bottom-b-c06',
      waitReason: null
    });
    expect(inbound).toMatchObject({
      state: 'loaded-moving',
      currentNodeId: 'lift-01-inbound-queue-01-entry-access',
      targetNodeId: 'column-top-b-c09',
      waitReason: null,
      blockingVehicleId: null
    });
    expect(state.kpis.deadlockCount).toBe(0);
  });

  it('lets a loaded outbound shuttle clear top-b when the waiting inbound departure is stopped on the entry leg', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'inbound-entry-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-entry-departure',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r05-c04',
      loadId: 'inbound-entry-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', [
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c09',
      'storage-r01-c09'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'inbound-entry-departure', true);

    sim.addLoadForTest({ id: 'outbound-clear-load', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-clear-top-b',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r05-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'outbound-clear-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', [
      'column-bottom-a-c06',
      'lift-01-outbound-queue-01-entry-access',
      'lift-01-outbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-05', 'outbound-clear-top-b', true);

    const node = (nodeId: string) => scenario.layout.nodes.find((candidate) => candidate.id === nodeId)!;
    const entryNode = node('lift-01-inbound-queue-01-entry-access');
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentNodeId: string;
        targetNodeId: string | null;
        currentEdgeId: string | null;
        x: number;
        y: number;
        z: number;
        loaded: boolean;
        speedMps: number;
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        phaseRemainingSec: number;
        waitReason: string | null;
        blockingVehicleId: string | null;
        routeNodeIds: string[];
      }>;
      traffic: { findEdge(fromNodeId: string, toNodeId: string): { id: string } | undefined };
      agentMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
      agentMinimalMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
    };
    const inbound = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    inbound.currentNodeId = 'lift-01-inbound-queue-01-entry-access';
    inbound.targetNodeId = 'column-top-b-c09';
    inbound.currentEdgeId = internals.traffic.findEdge('lift-01-inbound-queue-01-entry-access', 'column-top-b-c09')!.id;
    inbound.x = entryNode.x;
    inbound.y = entryNode.y;
    inbound.z = entryNode.z;
    inbound.loaded = true;
    inbound.state = 'waiting-blocked';
    inbound.speedMps = 0;
    inbound.legRemainingM = 2.2;
    inbound.legElapsedSec = 0.1;
    inbound.legTravelSec = 2.3;
    inbound.phaseRemainingSec = 0;
    inbound.waitReason = 'min-separation';
    inbound.blockingVehicleId = 'SH-05';

    const outbound = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;

    expect(internals.agentMoveBlocker(outbound, 'lift-01-outbound-queue-01-entry-access')).toBeNull();
    expect(internals.agentMinimalMoveBlocker(outbound, 'lift-01-outbound-queue-01-entry-access')).toBeNull();
  });

  it('lets an empty top-b shuttle clear top-b when an inbound lift departure is stopped on the entry leg', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'inbound-entry-load', state: 'carried', nodeId: null, vehicleId: 'SH-04', weightKg: 100 });
    sim.addTaskForTest({
      id: 'inbound-entry-departure',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r05-c04',
      loadId: 'inbound-entry-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-04', [
      'lift-01-inbound-queue-01-entry-access',
      'column-top-b-c09',
      'storage-r01-c09'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'inbound-entry-departure', true);
    sim.setVehicleRouteForTest('SH-07', ['column-top-b-c09', 'column-top-a-c09']);

    const node = (nodeId: string) => scenario.layout.nodes.find((candidate) => candidate.id === nodeId)!;
    const entryNode = node('lift-01-inbound-queue-01-entry-access');
    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentNodeId: string;
        targetNodeId: string | null;
        currentEdgeId: string | null;
        x: number;
        y: number;
        z: number;
        loaded: boolean;
        speedMps: number;
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        phaseRemainingSec: number;
        waitReason: string | null;
        blockingVehicleId: string | null;
        routeNodeIds: string[];
      }>;
      traffic: { findEdge(fromNodeId: string, toNodeId: string): { id: string } | undefined };
      agentMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
      agentMinimalMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
    };
    const inbound = internals.vehicles.find((vehicle) => vehicle.id === 'SH-04')!;
    inbound.currentNodeId = 'lift-01-inbound-queue-01-entry-access';
    inbound.targetNodeId = 'column-top-b-c09';
    inbound.currentEdgeId = internals.traffic.findEdge('lift-01-inbound-queue-01-entry-access', 'column-top-b-c09')!.id;
    inbound.x = entryNode.x;
    inbound.y = entryNode.y;
    inbound.z = entryNode.z;
    inbound.loaded = true;
    inbound.state = 'waiting-blocked';
    inbound.speedMps = 0;
    inbound.legRemainingM = 2.2;
    inbound.legElapsedSec = 0.1;
    inbound.legTravelSec = 2.3;
    inbound.phaseRemainingSec = 0;
    inbound.waitReason = 'min-separation';
    inbound.blockingVehicleId = 'SH-07';

    const emptyClearer = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;
    emptyClearer.loaded = false;

    expect(internals.agentMoveBlocker(emptyClearer, 'column-top-a-c09')).toBeNull();
    expect(internals.agentMinimalMoveBlocker(emptyClearer, 'column-top-a-c09')).toBeNull();
  });

  it('does not route a top-b yielder into a protected inbound queue exit target', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'queue-exit-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queue-exit-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r05-c04',
      loadId: 'queue-exit-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'cross-traffic-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'cross-traffic-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c21',
      dropoffNodeId: 'storage-r05-c20',
      loadId: 'cross-traffic-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'lift-01-inbound-queue-03-entry-access',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'queue-exit-task', false);
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c10',
      'column-top-b-c09',
      'storage-r01-c09'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'cross-traffic-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentNodeId: string;
        targetNodeId: string | null;
        currentEdgeId: string | null;
        loaded: boolean;
        speedMps: number;
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        phaseRemainingSec: number;
        waitReason: string | null;
        blockingVehicleId: string | null;
        routeNodeIds: string[];
      }>;
      traffic: { findEdge(fromNodeId: string, toNodeId: string): { id: string; lengthM: number } | undefined };
      tryRetreatAgentRefreshNearFaceoff(candidateVehicleIds: string[]): boolean;
    };
    const yielder = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    const conflictEdge = internals.traffic.findEdge('column-top-b-c10', 'column-top-b-c09')!;
    yielder.currentNodeId = 'column-top-b-c10';
    yielder.targetNodeId = 'column-top-b-c09';
    yielder.currentEdgeId = conflictEdge.id;
    yielder.state = 'waiting-blocked';
    yielder.waitReason = 'min-separation';
    yielder.blockingVehicleId = 'SH-03';
    yielder.speedMps = 0;
    yielder.legRemainingM = conflictEdge.lengthM;
    yielder.legElapsedSec = 0;
    yielder.legTravelSec = conflictEdge.lengthM;
    yielder.phaseRemainingSec = 0;

    requester.currentNodeId = 'lift-01-inbound-queue-03-entry-access';
    requester.targetNodeId = 'column-top-b-c09';
    requester.currentEdgeId = null;
    requester.state = 'waiting-blocked';
    requester.waitReason = 'projected-display-clearance';
    requester.blockingVehicleId = 'SH-01';
    requester.speedMps = 0;
    requester.legRemainingM = 0;
    requester.legElapsedSec = 0;
    requester.legTravelSec = 0;
    requester.phaseRemainingSec = 0;

    expect(internals.tryRetreatAgentRefreshNearFaceoff(['SH-01', 'SH-03'])).toBe(true);
    expect(yielder.routeNodeIds.slice(1)).not.toContain('column-top-b-c09');
    expect(yielder.routeNodeIds).not.toEqual([
      'column-top-b-c10',
      'column-top-b-c09',
      'storage-r01-c09'
    ]);
  });

  it('lets a shuttle occupying an inbound queue exit target clear away on top-b', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'queue-exit-clear-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queue-exit-clear-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r05-c04',
      loadId: 'queue-exit-clear-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'queue-exit-clear-cross-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'queue-exit-clear-cross-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c21',
      dropoffNodeId: 'storage-r05-c20',
      loadId: 'queue-exit-clear-cross-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'lift-01-inbound-queue-03-entry-access',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'queue-exit-clear-task', false);
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c09',
      'column-top-b-c10',
      'column-top-b-c11',
      'column-top-b-c12',
      'column-top-b-c13',
      'column-top-b-c14',
      'module-boundary-01-spine-top-b',
      'column-top-b-c15',
      'column-top-b-c16',
      'column-top-b-c17',
      'column-top-b-c18',
      'column-top-b-c19',
      'column-top-b-c20',
      'column-top-b-c21'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'queue-exit-clear-cross-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        state: string;
        currentNodeId: string;
        targetNodeId: string | null;
        currentEdgeId: string | null;
        loaded: boolean;
        speedMps: number;
        legRemainingM: number;
        legElapsedSec: number;
        legTravelSec: number;
        phaseRemainingSec: number;
        waitReason: string | null;
        blockingVehicleId: string | null;
        routeNodeIds: string[];
      }>;
      traffic: { findEdge(fromNodeId: string, toNodeId: string): { id: string; lengthM: number } | undefined };
      conflictSessions: Array<Record<string, unknown>>;
      agentMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
      agentMinimalMoveBlocker(vehicle: unknown, toNodeId: string): { reason: string; blockingVehicleId: string | null } | null;
      agentRefreshDeadlockBlockerStillApplies(vehicle: unknown, blockingVehicleId: string): boolean;
      agentRefreshClearProtectedExitYielder(yielder: unknown, session: Record<string, unknown>, winner: unknown, resumeNodeId: string): boolean;
    };
    const clearer = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const requester = internals.vehicles.find((vehicle) => vehicle.id === 'SH-03')!;
    clearer.currentNodeId = 'column-top-b-c09';
    clearer.targetNodeId = 'column-top-b-c10';
    clearer.currentEdgeId = null;
    clearer.state = 'waiting-blocked';
    clearer.waitReason = 'min-separation';
    clearer.blockingVehicleId = 'SH-03';
    clearer.speedMps = 0;
    clearer.legRemainingM = 0;
    clearer.legElapsedSec = 0;
    clearer.legTravelSec = 0;
    clearer.phaseRemainingSec = 0;

    requester.currentNodeId = 'lift-01-inbound-queue-03-entry-access';
    requester.targetNodeId = 'column-top-b-c09';
    requester.currentEdgeId = null;
    requester.state = 'waiting-blocked';
    requester.waitReason = 'projected-display-clearance';
    requester.blockingVehicleId = 'SH-01';
    requester.speedMps = 0;
    requester.legRemainingM = 0;
    requester.legElapsedSec = 0;
    requester.legTravelSec = 0;
    requester.phaseRemainingSec = 0;

    expect(internals.agentMoveBlocker(clearer, 'column-top-b-c10')).toBeNull();
    expect(internals.agentMinimalMoveBlocker(clearer, 'column-top-b-c10')).toBeNull();
    expect(internals.agentRefreshDeadlockBlockerStillApplies(clearer, 'SH-03')).toBe(false);

    const edge = internals.traffic.findEdge('column-top-b-c09', 'column-top-b-c10')!;
    clearer.currentEdgeId = edge.id;
    clearer.legRemainingM = edge.lengthM;
    clearer.legElapsedSec = 0;
    clearer.legTravelSec = edge.lengthM;
    const session = {
      id: 'test-conflict',
      kind: 'pair',
      resourceKey: 'edge:lift-01-inbound-queue-03-entry-access-column-top-b-c09:SH-01+SH-03',
      state: 'open',
      participantVehicleIds: ['SH-01', 'SH-03'],
      winnerVehicleId: 'SH-03',
      yielderVehicleId: 'SH-01',
      createdAtSec: 0,
      createdAtTick: 0,
      updatedAtSec: 0,
      expiresAtSec: 20,
      timeoutAtSec: 12,
      trigger: 'deadlock-break',
      initialBlockerVehicleId: 'SH-01',
      blockerVehicleId: 'SH-03',
      yielderOriginalNodeId: 'column-top-b-c09',
      yielderPocketNodeId: null,
      yielderLocalRouteNodeIds: [],
      resumeNodeId: 'column-top-b-c09',
      clearancePolicy: 'immediate-next-move',
      closeReason: null
    };
    internals.conflictSessions.push(session);

    expect(internals.agentRefreshClearProtectedExitYielder(clearer, session, requester, 'column-top-b-c09')).toBe(true);
    expect(clearer.waitReason).toBeNull();
    expect(clearer.blockingVehicleId).toBeNull();
    expect(clearer.currentEdgeId).toBeNull();
    expect(clearer.targetNodeId).not.toBe('column-top-b-c10');
    expect(clearer.routeNodeIds).not.toContain('column-top-b-c10');

    const state = sim.step(0.2);
    const movingClearer = state.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;

    expect(movingClearer.waitReason).not.toBe('min-separation');
    expect(movingClearer.blockingVehicleId).not.toBe('SH-03');
  });

  it('blocks empty transit routes from entering an active inbound queue exit pocket', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true,
        deadlockDetectSec: 0.4
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'protected-queue-load', state: 'waiting', nodeId: 'lift-01-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'protected-queue-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c07',
      dropoffNodeId: 'storage-r05-c02',
      loadId: 'protected-queue-load',
      vehicleId: 'SH-03',
      replanCount: 0,
      waitReason: null
    });
    sim.addLoadForTest({ id: 'protected-transit-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'protected-transit-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-b-c21',
      dropoffNodeId: 'storage-r05-c20',
      loadId: 'protected-transit-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-03', [
      'lift-01-inbound-queue-03-entry-access',
      'column-top-b-c09',
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c07'
    ]);
    sim.setVehicleTaskForTest('SH-03', 'protected-queue-task', false);
    sim.setVehicleRouteForTest('SH-01', ['column-top-b-c08']);
    sim.setVehicleTaskForTest('SH-01', 'protected-transit-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
      }>;
      tasks: Array<{
        id: string;
      }>;
      activeTopLiftInboundTransitBlockedNodeIds(vehicle: unknown, task: unknown, goalNodeId: string): Set<string>;
      agentRefreshNominalRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const transitVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const transitTask = internals.tasks.find((task) => task.id === 'protected-transit-task')!;

    const blockedNodeIds = internals.activeTopLiftInboundTransitBlockedNodeIds(
      transitVehicle,
      transitTask,
      'column-top-b-c21'
    );
    expect(blockedNodeIds.has('column-top-b-c09')).toBe(true);
    expect(blockedNodeIds.has('column-top-b-c10')).toBe(true);

    const route = internals.agentRefreshNominalRouteToGoal(transitVehicle, transitTask, 'column-top-b-c21');
    expect(route).not.toContain('column-top-b-c09');
    expect(route).not.toContain('column-top-b-c10');
  });

  it('keeps top-level empty inbound queue approaches from detouring through lower lanes', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'top-lane-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'top-lane-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r06-c18',
      loadId: 'top-lane-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['column-top-a-c07']);
    sim.setVehicleTaskForTest('SH-01', 'top-lane-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string }>;
      agentRefreshNominalRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-01')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'top-lane-task')!;
    const route = internals.agentRefreshNominalRouteToGoal(vehicle, task, 'column-top-a-c25');

    expect(route[0]).toBe('column-top-a-c07');
    expect(route.at(-1)).toBe('column-top-a-c25');
    expect(route).toContain('column-top-b-c25');
    expect(route.some((nodeId) => /(?:bottom-[ab]|-middle)$/.test(nodeId) || /^column-middle-c\d+$/.test(nodeId))).toBe(false);
    expectRouteHopsAdjacent(scenario, route);
  });

  it('rejects storage-start top-lift inbound approaches that leave the top lane after joining it', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'storage-start-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'storage-start-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c22',
      dropoffNodeId: 'storage-r13-c19',
      loadId: 'storage-start-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });

    const internals = sim as unknown as {
      tasks: Array<{ id: string }>;
      topLiftInboundEmptyApproachRouteAllowed(routeNodeIds: string[] | null, fromNodeId: string, task: unknown, goalNodeId: string): boolean;
    };
    const task = internals.tasks.find((candidate) => candidate.id === 'storage-start-task')!;
    const legalRoute = [
      'storage-r01-c18',
      'column-top-b-c18',
      'column-top-b-c19',
      'column-top-b-c20',
      'column-top-b-c21',
      'module-02-spine-top-b',
      'column-top-b-c22',
      'column-top-b-c23',
      'column-top-a-c23'
    ];
    const lowerDetourRoute = [
      'storage-r01-c18',
      'column-top-b-c18',
      'column-top-b-c17',
      'column-top-b-c16',
      'column-top-b-c15',
      'module-boundary-01-spine-top-b',
      'module-boundary-01-spine-middle',
      'module-boundary-01-spine-bottom-a',
      'column-bottom-a-c15',
      'column-bottom-a-c16',
      'module-02-spine-bottom-a',
      'module-02-spine-middle',
      'module-02-spine-top-b',
      'module-02-spine-top-a',
      'column-top-a-c22',
      'column-top-a-c23'
    ];

    expect(internals.topLiftInboundEmptyApproachRouteAllowed(legalRoute, 'storage-r01-c18', task, 'column-top-a-c23')).toBe(true);
    expect(internals.topLiftInboundEmptyApproachRouteAllowed(lowerDetourRoute, 'storage-r01-c18', task, 'column-top-a-c23')).toBe(false);
  });

  it('keeps same-display top-lift queue access legs physically nonzero', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      traffic: { findEdge(fromNodeId: string, toNodeId: string): { id: string; lengthM: number } | undefined };
      routeLegDistanceM(edge: { id: string; lengthM: number }): number;
    };
    const edge = internals.traffic.findEdge(
      'lift-01-inbound-queue-03-entry-access',
      'lift-01-inbound-queue-03-access'
    )!;

    expect(edge.lengthM).toBeGreaterThan(0);
    expect(internals.routeLegDistanceM(edge)).toBe(edge.lengthM);
  });

  it('treats an active inbound service segment as an exclusion zone for outbound lift approach traffic', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'active-service-load', state: 'waiting', nodeId: 'lift-02-inbound-buffer-03', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'active-service',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r03-c17',
      loadId: 'active-service-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'lift-02-inbound-queue-01-entry-access',
      'lift-02-inbound-queue-01-service-exit'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'active-service', false);

    sim.addLoadForTest({ id: 'protected-outbound-load', state: 'carried', nodeId: null, vehicleId: 'SH-05', weightKg: 100 });
    sim.addTaskForTest({
      id: 'protected-outbound',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r09-c15',
      dropoffNodeId: 'lift-02-outbound-queue-01-service-exit',
      loadId: 'protected-outbound-load',
      vehicleId: 'SH-05',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-05', ['column-top-a-c18']);
    sim.setVehicleTaskForTest('SH-05', 'protected-outbound', true);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      tasks: Array<{ id: string; dropoffNodeId: string }>;
      agentRouteToGoal(vehicle: unknown, task: unknown, goalNodeId: string): string[];
    };
    const vehicle = internals.vehicles.find((candidate) => candidate.id === 'SH-05')!;
    const task = internals.tasks.find((candidate) => candidate.id === 'protected-outbound')!;
    const route = internals.agentRouteToGoal(vehicle, task, task.dropoffNodeId);

    expect(route.slice(0, 2)).toEqual(['column-top-a-c18', 'column-top-a-c19']);
    expect(route).toContain('column-bottom-b-c20');
    expect(route).not.toContain('column-top-a-c22');
    expect(route).not.toContain('column-top-b-c21');
    expect(route.at(-1)).toBe('lift-02-outbound-queue-01-service-exit');
    expectRouteHopsAdjacent(scenario, route);
  });

  it('keeps loaded inbound traffic off a column entrance until the first storage cell can clear', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-column-entry-load', state: 'carried', nodeId: null, vehicleId: 'SH-04', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-column-entry',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r04-c19',
      loadId: 'loaded-column-entry-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-04', [
      'column-top-b-c20',
      'column-top-b-c19',
      'storage-r01-c19',
      'storage-r02-c19',
      'storage-r03-c19',
      'storage-r04-c19'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'loaded-column-entry', true);

    sim.setVehicleRouteForTest('SH-02', [
      'storage-r01-c19',
      'column-top-b-c19'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshMoveBlocker(vehicle: unknown, toNodeId: string, routeNodeIds: string[]): { reason: string; blockingVehicleId: string | null } | null;
    };
    const loadedInbound = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;
    const block = internals.agentRefreshMoveBlocker(loadedInbound, 'column-top-b-c19', [
      'column-top-b-c20',
      'column-top-b-c19',
      'storage-r01-c19',
      'storage-r02-c19',
      'storage-r03-c19',
      'storage-r04-c19'
    ]);

    expect(block).toEqual({ reason: 'inbound-column-entry-occupied', blockingVehicleId: 'SH-02' });
  });

  it('keeps loaded inbound traffic off a column entrance while a deeper storage shuttle is exiting', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-column-deep-entry-load', state: 'carried', nodeId: null, vehicleId: 'SH-04', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-column-deep-entry',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-02-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r05-c19',
      loadId: 'loaded-column-deep-entry-load',
      vehicleId: 'SH-04',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-04', [
      'column-top-b-c20',
      'column-top-b-c19',
      'storage-r01-c19',
      'storage-r02-c19',
      'storage-r03-c19',
      'storage-r04-c19',
      'storage-r05-c19'
    ]);
    sim.setVehicleTaskForTest('SH-04', 'loaded-column-deep-entry', true);

    sim.setVehicleRouteForTest('SH-02', [
      'storage-r03-c19',
      'storage-r02-c19',
      'storage-r01-c19',
      'column-top-b-c19'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, false);

    const internals = sim as unknown as {
      vehicles: Array<{ id: string }>;
      agentRefreshMoveBlocker(vehicle: unknown, toNodeId: string, routeNodeIds: string[]): { reason: string; blockingVehicleId: string | null } | null;
    };
    const loadedInbound = internals.vehicles.find((candidate) => candidate.id === 'SH-04')!;
    const block = internals.agentRefreshMoveBlocker(loadedInbound, 'column-top-b-c19', [
      'column-top-b-c20',
      'column-top-b-c19',
      'storage-r01-c19',
      'storage-r02-c19',
      'storage-r03-c19',
      'storage-r04-c19',
      'storage-r05-c19'
    ]);

    expect(block).toEqual({ reason: 'inbound-column-entry-occupied', blockingVehicleId: 'SH-02' });
  });

  it('ignores stale local-route node claims after a vehicle has switched to a different active route', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
      localRouteNodeClaimBlocker(nodeId: string, vehicleId: string): string | null;
    };
    const claimant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(claimant, {
      state: 'moving-to-pickup',
      currentNodeId: 'storage-r08-c05',
      targetNodeId: 'column-middle-c05',
      routeNodeIds: ['storage-r08-c05', 'column-middle-c05'],
      plannedRouteNodeIds: ['storage-r08-c05', 'column-middle-c05', 'column-middle-c06'],
      localRouteNodeIds: ['storage-r08-c05', 'column-top-a-c10'],
      localRouteReason: 'temporary-yield',
      yieldHoldUntilSec: null,
      yieldHoldNodeId: null
    });

    expect(internals.localRouteNodeClaimBlocker('column-top-a-c10', 'SH-06')).toBeNull();

    Object.assign(claimant, {
      routeNodeIds: ['storage-r08-c05', 'column-top-a-c10'],
      targetNodeId: 'column-top-a-c10'
    });

    expect(internals.localRouteNodeClaimBlocker('column-top-a-c10', 'SH-06')).toBe('SH-02');
  });

  it('reports stale local-route claims in the shadow resource ledger', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
    };
    const claimant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(claimant, {
      state: 'moving-to-pickup',
      currentNodeId: 'storage-r08-c05',
      targetNodeId: 'column-middle-c05',
      routeNodeIds: ['storage-r08-c05', 'column-middle-c05'],
      plannedRouteNodeIds: ['storage-r08-c05', 'column-middle-c05', 'column-middle-c06'],
      localRouteNodeIds: ['storage-r08-c05', 'column-top-a-c10'],
      localRouteReason: 'temporary-yield',
      yieldHoldUntilSec: null,
      yieldHoldNodeId: null
    });

    const parsed = ShuttleSimStateSchema.parse(sim.getState());
    expect(parsed.traffic.shadowLedger.enabled).toBe(true);
    expect(parsed.traffic.shadowLedger.invariantCounts.staleLocalRouteClaim).toBeGreaterThanOrEqual(1);
    expect(parsed.traffic.shadowLedger.violations.some((violation) => violation.code === 'stale-local-route-claim')).toBe(true);
  });

  it('clears inactive local-route claims before the next diagnostic sample', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
      clearInactiveLocalRouteClaims(): void;
    };
    const claimant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    Object.assign(claimant, {
      state: 'moving-to-pickup',
      currentNodeId: 'storage-r08-c05',
      targetNodeId: 'column-middle-c05',
      routeNodeIds: ['storage-r08-c05', 'column-middle-c05'],
      plannedRouteNodeIds: ['storage-r08-c05', 'column-middle-c05', 'column-middle-c06'],
      localRouteNodeIds: ['storage-r08-c05', 'column-top-a-c10'],
      localRouteReason: 'temporary-yield',
      yieldHoldUntilSec: null,
      yieldHoldNodeId: null
    });

    internals.clearInactiveLocalRouteClaims();

    const parsed = ShuttleSimStateSchema.parse(sim.getState());
    expect(claimant.localRouteNodeIds).toEqual([]);
    expect(claimant.localRouteReason).toBeNull();
    expect(parsed.traffic.shadowLedger.invariantCounts.staleLocalRouteClaim).toBe(0);
  });

  it('does not let reservation-blocked waiters target-claim future nodes', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
      nodeClaimedByOtherVehicle(nodeId: string, vehicleId: string): string | null;
    };
    const waiter = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;
    Object.assign(waiter, {
      state: 'waiting-blocked',
      currentNodeId: 'column-middle-c06',
      targetNodeId: 'column-middle-c05',
      plannedGoalNodeId: 'column-middle-c05',
      routeNodeIds: ['column-middle-c06', 'column-middle-c05'],
      plannedRouteNodeIds: ['column-middle-c06', 'column-middle-c05'],
      localRouteNodeIds: [],
      localRouteReason: null,
      waitReason: 'reservation-conflict',
      blockingVehicleId: null,
      blockingReservationId: 'test-reservation'
    });

    expect(internals.nodeClaimedByOtherVehicle('column-middle-c05', 'SH-06')).toBeNull();
  });

  it('does not let blocked waiters reserve clearance pockets by target or planned goal', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
      topLiftAccessClearanceNodeClaimedByOtherVehicle(blocker: unknown, nodeId: string): boolean;
    };
    const blocker = internals.vehicles.find((vehicle) => vehicle.id === 'SH-05')!;
    const waiter = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;
    Object.assign(waiter, {
      state: 'waiting-blocked',
      currentNodeId: 'column-middle-c06',
      targetNodeId: 'column-middle-c05',
      plannedGoalNodeId: 'column-middle-c05',
      routeNodeIds: ['column-middle-c06', 'column-middle-c05'],
      plannedRouteNodeIds: ['column-middle-c06', 'column-middle-c05'],
      localRouteNodeIds: [],
      localRouteReason: null,
      waitReason: 'inbound-column-entry-occupied',
      blockingVehicleId: 'SH-02'
    });

    expect(internals.topLiftAccessClearanceNodeClaimedByOtherVehicle(blocker, 'column-middle-c05')).toBe(false);
    expect(internals.topLiftAccessClearanceNodeClaimedByOtherVehicle(blocker, 'column-middle-c06')).toBe(true);
  });

  it('does not count waiting route fields as active shadow future claims', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
    };
    const waiter = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;
    Object.assign(waiter, {
      state: 'waiting-blocked',
      currentNodeId: 'column-middle-c06',
      targetNodeId: 'column-middle-c05',
      plannedGoalNodeId: 'column-middle-c05',
      routeNodeIds: ['column-middle-c06', 'column-middle-c05'],
      plannedRouteNodeIds: ['column-middle-c06', 'column-middle-c05'],
      localRouteNodeIds: [],
      localRouteReason: null,
      waitReason: 'inbound-column-entry-occupied',
      blockingVehicleId: 'SH-02'
    });

    const parsed = ShuttleSimStateSchema.parse(sim.getState());
    expect(parsed.traffic.shadowLedger.invariantCounts.blockedWaiterFutureClaim).toBe(0);
    expect(parsed.traffic.shadowLedger.violations.some((violation) => violation.code === 'blocked-waiter-future-claim')).toBe(false);
  });

  it('does not let far planned route nodes become active shadow owners', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({
      id: 'far-planned-inbound-load',
      state: 'waiting',
      nodeId: 'column-top-a-c09',
      vehicleId: null,
      weightKg: 100
    });
    sim.addTaskForTest({
      id: 'far-planned-inbound-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c09',
      dropoffNodeId: 'storage-r08-c05',
      loadId: 'far-planned-inbound-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', ['storage-r08-c05']);
    sim.setVehicleTaskForTest('SH-01', 'far-planned-inbound-task', false);
    sim.setVehicleRouteForTest('SH-02', ['module-01-spine-middle']);

    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
    };
    const claimant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    Object.assign(claimant, {
      state: 'moving-to-pickup',
      currentNodeId: 'storage-r08-c05',
      targetNodeId: 'column-middle-c05',
      taskId: 'far-planned-inbound-task',
      routeNodeIds: ['storage-r08-c05', 'column-middle-c05'],
      plannedGoalNodeId: 'column-top-a-c09',
      plannedRouteNodeIds: [
        'storage-r08-c05',
        'column-middle-c05',
        'column-middle-c06',
        'column-middle-c07',
        'column-middle-c08',
        'column-middle-c09',
        'module-01-spine-middle',
        'column-top-a-c09'
      ],
      localRouteNodeIds: [],
      localRouteReason: null
    });

    const parsed = ShuttleSimStateSchema.parse(sim.getState());
    expect(parsed.traffic.shadowLedger.invariantCounts.duplicateResourceOwner).toBe(0);
    expect(parsed.traffic.shadowLedger.violations.some((violation) =>
      violation.code === 'duplicate-resource-owner' &&
      violation.resourceKey === 'node:module-01-spine-middle'
    )).toBe(false);
  });

  it('reports blocked waiters that still own future reservations in the shadow resource ledger', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
    };
    const waiter = internals.vehicles.find((vehicle) => vehicle.id === 'SH-07')!;
    Object.assign(waiter, {
      state: 'waiting-blocked',
      currentNodeId: 'column-middle-c06',
      targetNodeId: 'column-middle-c05',
      plannedGoalNodeId: 'column-middle-c05',
      routeNodeIds: ['column-middle-c06', 'column-middle-c05'],
      plannedRouteNodeIds: ['column-middle-c06', 'column-middle-c05'],
      localRouteNodeIds: [],
      localRouteReason: null,
      waitReason: 'reservation-conflict',
      blockingVehicleId: null,
      blockingReservationId: 'test-reservation'
    });
    sim.addReservationForTest({
      resourceType: 'node',
      resourceId: 'column-middle-c05',
      vehicleId: 'SH-07',
      taskId: null,
      startTimeSec: 0,
      endTimeSec: 10,
      priority: 0,
      conflictGroup: null,
      reasonCode: 'test-waiter-future-lease'
    });

    const parsed = ShuttleSimStateSchema.parse(sim.getState());
    expect(parsed.traffic.shadowLedger.invariantCounts.blockedWaiterFutureClaim).toBeGreaterThanOrEqual(1);
    expect(parsed.traffic.shadowLedger.violations.some((violation) => violation.code === 'blocked-waiter-future-claim')).toBe(true);
  });

  it('does not reapply a turn delay to an already-started lift-corner clearance move', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
      advanceMovement(vehicle: unknown, dtSec: number): void;
    };
    for (const vehicle of internals.vehicles) {
      Object.assign(vehicle, {
        x: 1000,
        z: 1000,
        loaded: false,
        currentNodeId: 'storage-r01-c01',
        targetNodeId: null,
        currentEdgeId: null
      });
    }

    const clearingVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;
    const inboundDeparture = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;
    Object.assign(clearingVehicle, {
      state: 'waiting-blocked',
      currentNodeId: 'column-top-b-c21',
      targetNodeId: 'module-02-spine-top-b',
      currentEdgeId: 'column-top-b-c21-module-02-spine-top-b',
      routeNodeIds: ['column-top-b-c21', 'module-02-spine-top-b', 'column-top-b-c22'],
      routeIndex: 0,
      loaded: false,
      taskId: null,
      x: 30.665816,
      z: 0.8,
      yaw: 0,
      speedMps: 0,
      targetSpeedMps: 2,
      legMotionMode: 'cruise',
      legRemainingM: 0.584184,
      legElapsedSec: 0.332908,
      legTravelSec: 0.625,
      lastMovementAxis: 'z',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-08'
    });
    Object.assign(inboundDeparture, {
      state: 'waiting-blocked',
      currentNodeId: 'lift-02-inbound-queue-02-service-exit',
      targetNodeId: 'lift-02-inbound-queue-02-entry-access',
      currentEdgeId: 'lift-02-inbound-queue-02-service-exit-lift-02-inbound-queue-02-entry-access',
      routeNodeIds: [
        'lift-02-inbound-queue-02-service-exit',
        'lift-02-inbound-queue-02-entry-access',
        'column-top-b-c21'
      ],
      routeIndex: 0,
      loaded: true,
      taskId: 'loaded-inbound',
      x: 29.4585,
      z: 0.6,
      yaw: 0,
      speedMps: 0,
      targetSpeedMps: 1.5,
      legMotionMode: 'profile',
      legRemainingM: 0.5415,
      legElapsedSec: 3.333333,
      legTravelSec: 4.283333,
      lastMovementAxis: 'z',
      waitReason: 'min-separation',
      blockingVehicleId: 'SH-02'
    });

    internals.advanceMovement(clearingVehicle, 0.2);

    expect(clearingVehicle.x as number).toBeGreaterThan(30.665816);
    expect(clearingVehicle.waitReason).toBeNull();
    expect(clearingVehicle.blockingVehicleId).toBeNull();
  });

  it('does not yield loaded cross traffic into the inbound service access lane', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-inbound-service-load', state: 'carried', nodeId: null, vehicleId: 'SH-06', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-inbound-service',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'lift-01-inbound-queue-01-service-exit',
      dropoffNodeId: 'storage-r11-c03',
      loadId: 'loaded-inbound-service-load',
      vehicleId: 'SH-06',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-06', [
      'lift-01-inbound-queue-02-service-exit',
      'lift-01-inbound-queue-02-entry-access',
      'column-top-b-c08',
      'column-top-a-c08',
      'column-top-a-c07'
    ]);
    sim.setVehicleTaskForTest('SH-06', 'loaded-inbound-service', true);

    sim.addLoadForTest({ id: 'loaded-cross-load', state: 'carried', nodeId: null, vehicleId: 'SH-08', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-cross',
      kind: 'outbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'storage-r03-c01',
      dropoffNodeId: 'lift-01-outbound-queue-01-service-exit',
      loadId: 'loaded-cross-load',
      vehicleId: 'SH-08',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-08', [
      'column-top-b-c08',
      'module-01-spine-top-b',
      'column-top-b-c09'
    ]);
    sim.setVehicleTaskForTest('SH-08', 'loaded-cross', true);

    const internals = sim as unknown as {
      vehicles: Array<Record<string, unknown>>;
      tryYieldAisleAwayFromTopLiftServiceClearance(
        aisleVehicle: unknown,
        serviceVehicle: unknown,
        blockedTargetNodeId: string
      ): boolean;
    };
    const serviceVehicle = internals.vehicles.find((vehicle) => vehicle.id === 'SH-06')!;
    const crossTraffic = internals.vehicles.find((vehicle) => vehicle.id === 'SH-08')!;

    expect(internals.tryYieldAisleAwayFromTopLiftServiceClearance(
      crossTraffic,
      serviceVehicle,
      'lift-01-inbound-queue-02-entry-access'
    )).toBe(true);
    expect((crossTraffic.routeNodeIds as string[])[1]).not.toBe('column-top-a-c08');
  });

  it('keeps a projected inbound queue entrant near the lift when a loaded shuttle exits', () => {
    const scenario = createInboundOutboundDemoScenario({
      layoutProfile: {
        layoutKind: 'top-lift-column',
        liftPairCount: 2
      },
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 0
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        collisionAvoidanceEnabled: true
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.addLoadForTest({ id: 'loaded-exit-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'loaded-exit-task',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r04-c08',
      loadId: 'loaded-exit-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-01', [
      'column-top-b-c09',
      'column-top-b-c08',
      'storage-r01-c08',
      'storage-r02-c08',
      'storage-r03-c08',
      'storage-r04-c08'
    ]);
    sim.setVehicleTaskForTest('SH-01', 'loaded-exit-task', true);

    sim.addLoadForTest({ id: 'entrant-load', state: 'waiting', nodeId: 'lift-01-inbound', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'entrant-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 1,
      assignedAtSec: 1,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'column-top-a-c08',
      dropoffNodeId: 'storage-r02-c08',
      loadId: 'entrant-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleRouteForTest('SH-02', [
      'column-top-b-c08',
      'column-top-b-c09',
      'column-top-b-c10',
      'column-top-b-c11',
      'column-top-a-c11'
    ]);
    sim.setVehicleTaskForTest('SH-02', 'entrant-task', false);

    const internals = sim as unknown as {
      vehicles: Array<{
        id: string;
        localRouteReason: string | null;
        routeNodeIds: string[];
      }>;
      tryYieldInboundQueueEntrantAwayFromLoadedExit(entrant: unknown, exiting: unknown): boolean;
    };
    const exiting = internals.vehicles.find((vehicle) => vehicle.id === 'SH-01')!;
    const entrant = internals.vehicles.find((vehicle) => vehicle.id === 'SH-02')!;

    expect(internals.tryYieldInboundQueueEntrantAwayFromLoadedExit(entrant, exiting)).toBe(true);
    expect(entrant.localRouteReason).toBe('temporary-yield');
    expect(entrant.routeNodeIds).toEqual(['column-top-b-c08', 'module-01-spine-top-b', 'column-top-b-c07']);
    expect(entrant.routeNodeIds).not.toContain('storage-r01-c06');
    expect(entrant.routeNodeIds).not.toContain('storage-r01-c08');
  });

  it('agent-refresh does not send a loaded lift departure through a blocker to reach a forward pocket', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'outbound-lift-top-02',
      'outbound-lift-top-02-row-03-transfer',
      'storage-r03-c24'
    ]);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleRouteForTest('SH-02', [
      'inbound-lift-bottom-02-row-02-transfer',
      'inbound-lift-bottom-02-row-01-transfer',
      'storage-r01-c24'
    ]);
    sim.setVehicleTaskForTest('SH-02', null, true);

    for (let index = 0; index < 40; index += 1) {
      sim.step(0.2);
    }

    const badForwardYield = sim.getEventLog().find((entry) =>
      entry.vehicleId === 'SH-01' &&
      entry.eventType === 'route-replanned' &&
      entry.reason === 'agent-refresh-forward-pocket-yield'
    );

    expect(badForwardYield).toBeUndefined();
    expect(sim.getState().kpis.deadlockCount).toBe(0);
  });

  it('agent-refresh does not hold a storage exit for a far lift-column claimant', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', [
      'storage-r12-c13',
      'outbound-lift-top-01-row-12-transfer',
      'outbound-lift-top-01'
    ]);
    sim.setVehicleRouteForTest('SH-02', [
      'outbound-lift-top-01-row-01-transfer',
      'outbound-lift-top-01'
    ]);

    const state = sim.step(0.2);
    const shuttle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(shuttle?.state).not.toBe('waiting-blocked');
    expect(shuttle?.waitReason).toBeNull();
    expect(shuttle?.targetNodeId).toBe('outbound-lift-top-01-row-12-transfer');
    expect(shuttle?.currentEdgeId).toBe('storage-r12-c13-outbound-lift-top-01-row-12-transfer');
  });

  it('agent-refresh does not pre-yield adjacent loaded lift-column moves before near contact', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      durationSec: 30,
      vehicles: {
        count: 8,
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        liftApproachCapacity: 8,
        dynamicAvoidanceClearanceM: 0.5,
        deadlockDetectSec: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();
    for (let index = 0; index < 40; index += 1) {
      sim.advanceByInPlace(0.2);
    }

    const state = sim.getState();
    const shuttle8 = state.vehicles.find((vehicle) => vehicle.id === 'SH-08');
    const earlyColumnYield = sim.getEventLog().find((event) =>
      event.timeSec <= 8 &&
      event.vehicleId === 'SH-08' &&
      event.reason === 'lift-column-near'
    );

    expect(shuttle8?.waitReason).toBeNull();
    expect(shuttle8?.blockingVehicleId).toBeNull();
    expect(shuttle8?.localRouteNodeIds).toEqual([]);
    expect(earlyColumnYield).toBeUndefined();
    expect(state.traffic.conflictSessions.some((session) =>
      session.participantVehicleIds.includes('SH-02') &&
      session.participantVehicleIds.includes('SH-08')
    )).toBe(false);
  });

  it('agent-refresh does not keep a cleared side-yield shuttle waiting on far lift-column traffic', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      durationSec: 120,
      vehicles: {
        count: 8,
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 16
      },
      trafficPolicy: {
        controllerMode: 'agent-refresh',
        liftApproachCapacity: 8,
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();
    let state = sim.getState();
    for (let index = 0; index < 408; index += 1) {
      state = sim.step(0.1);
    }

    const shuttle5 = state.vehicles.find((vehicle) => vehicle.id === 'SH-05');
    expect(shuttle5?.state).not.toBe('waiting-blocked');
    expect(shuttle5?.waitReason).toBeNull();
    expect(shuttle5?.currentEdgeId).toContain('inbound-lift-top-01');
    expect(state.traffic.waitingVehicles.some((vehicle) =>
      vehicle.vehicleId === 'SH-05' &&
      vehicle.waitReason === 'local-yield-hold'
    )).toBe(false);
  }, 30000);

  it('agent-minimal keeps a committed task route instead of reselecting at each node', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-minimal'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['main-north-05', 'main-south-05', 'main-south-04', 'outbound-lift-top-02']);
    sim.addLoadForTest({ id: 'committed-load', state: 'waiting', nodeId: 'outbound-lift-top-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'committed-task',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'outbound-lift-top-02',
      dropoffNodeId: 'storage-r08-c20',
      loadId: 'committed-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'committed-task', false);

    const state = sim.step(0.2);
    const vehicle = state.vehicles[0]!;

    expect(vehicle.targetNodeId).toBe('main-south-05');
    expect(vehicle.routeNodeIds).toEqual(['main-north-05', 'main-south-05', 'main-south-04', 'outbound-lift-top-02']);
    expect(vehicle.plannedRouteNodeIds).toEqual(['main-north-05', 'main-south-05', 'main-south-04', 'outbound-lift-top-02']);
    expect(vehicle.localRouteNodeIds).toEqual([]);
  });

  it('agent-minimal does not stop for a same-target claimant until both vehicles are close to the target', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-minimal',
        dynamicAvoidanceClearanceM: 0.5
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.setVehicleRouteForTest('SH-02', ['main-north-03', 'main-north-02']);
    sim.step(0.2);
    sim.setVehicleRouteForTest('SH-01', ['main-north-01', 'main-north-02']);
    let state = sim.step(0.2);
    let requester = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(requester?.state).not.toBe('waiting-blocked');
    expect(requester?.currentEdgeId).toBe('main-north-01-main-north-02');
    sim.setVehicleRouteForTest('SH-01', ['parking-c']);

    const target = scenario.layout.nodes.find((node) => node.id === 'main-north-02');
    expect(target).toBeDefined();
    for (let index = 0; index < 80; index += 1) {
      const claimant = sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-02');
      if (
        claimant?.currentEdgeId &&
        Math.hypot(claimant.x - target!.x, claimant.z - target!.z) <= scenario.vehicles.lengthM / 2 + scenario.trafficPolicy.dynamicAvoidanceClearanceM
      ) {
        break;
      }
      sim.step(0.2);
    }

    sim.setVehicleRouteForTest('SH-01', ['main-north-01', 'main-north-02']);
    state = sim.step(0.2);
    requester = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(requester?.state).toBe('waiting-blocked');
    expect(['node-target-near', 'avoidance-clearance']).toContain(requester?.waitReason);
    expect(requester?.blockingVehicleId).toBe('SH-02');
  });

  it('agent-minimal lets an empty face-to-face blocker finish a local yield move', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-minimal'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['right-row-08']);
    sim.setVehicleRouteForTest('SH-02', ['main-north-05']);
    sim.addLoadForTest({ id: 'empty-pickup-load', state: 'waiting', nodeId: 'outbound-lift-bottom-01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'loaded-drop-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'outbound-lift-bottom-01',
      dropoffNodeId: 'storage-r12-c01',
      loadId: 'empty-pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'loaded-drop',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-bottom-01',
      dropoffNodeId: 'storage-r08-c02',
      loadId: 'loaded-drop-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'empty-pickup', false);
    sim.setVehicleTaskForTest('SH-02', 'loaded-drop', true);

    let state = sim.getState();
    let yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.vehicleId === 'SH-01' && event.reason === 'empty-yields-to-loaded');
    for (let index = 0; index < 80 && !yieldEvent; index += 1) {
      state = sim.step(0.2);
      yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.vehicleId === 'SH-01' && event.reason === 'empty-yields-to-loaded');
    }
    const emptyVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const emptyTask = state.tasks.find((task) => task.id === 'empty-pickup');

    expect(yieldEvent?.details.route).toBe('right-row-08>storage-r08-c24');
    expect(emptyVehicle?.targetNodeId).toBe('storage-r08-c24');
    expect(emptyVehicle?.currentEdgeId).toBe('right-row-08-storage-r08-c24');
    expect(emptyVehicle?.localRouteReason).toBe('temporary-yield');
    expect(emptyVehicle?.localRouteNodeIds).toEqual(['right-row-08', 'storage-r08-c24']);
    expect(emptyVehicle?.waitReason).toBeNull();
    expect(emptyTask?.replanCount).toBe(0);
  });

  it('agent-minimal backs the lower-priority loaded shuttle out of a lift-port faceoff', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2, lengthM: 1.3, widthM: 1.3 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-minimal',
        deadlockDetectSec: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['inbound-lift-bottom-01', 'main-north-02', 'main-north-03']);
    sim.setVehicleRouteForTest('SH-02', ['outbound-lift-top-01', 'main-north-02', 'main-north-03']);
    sim.setVehicleTaskForTest('SH-01', null, true);
    sim.setVehicleTaskForTest('SH-02', null, true);

    let state = sim.getState();
    let retreatEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.reason === 'loaded-retreats-from-faceoff');
    for (let index = 0; index < 80 && !retreatEvent; index += 1) {
      state = sim.step(0.2);
      retreatEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.reason === 'loaded-retreats-from-faceoff');
    }
    const yieldingVehicle = state.vehicles.find((vehicle) => vehicle.id === retreatEvent?.vehicleId);

    expect(retreatEvent?.vehicleId).toBe('SH-02');
    expect(retreatEvent?.details.route).toBe('main-north-02>outbound-lift-top-01');
    expect(yieldingVehicle?.targetNodeId).toBe('outbound-lift-top-01');
    expect(state.traffic.physicalViolationCount).toBe(0);

    let heldState = state;
    for (let index = 0; index < 80; index += 1) {
      heldState = sim.step(0.2);
      const heldVehicle = heldState.vehicles.find((vehicle) => vehicle.id === retreatEvent?.vehicleId);
      if (
        heldVehicle?.currentNodeId === 'outbound-lift-top-01' &&
        heldVehicle.currentEdgeId === null &&
        heldVehicle.waitReason === 'local-yield-hold'
      ) {
        break;
      }
    }
    const heldVehicle = heldState.vehicles.find((vehicle) => vehicle.id === retreatEvent?.vehicleId);
    expect(heldVehicle?.currentNodeId).toBe('outbound-lift-top-01');
    expect(heldVehicle?.currentEdgeId).toBeNull();
    expect(heldVehicle?.waitReason).toBe('local-yield-hold');
    expect(heldVehicle?.targetNodeId).toBe('outbound-lift-top-01');
  });

  it('agent-simple releases an inbound shuttle to the available pool after dropoff', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      physicsParams: {
        emptySpeedMps: 6,
        loadedSpeedMps: 5,
        accelerationMps2: 6,
        switchDirectionSec: 0,
        liftTimeSec: 0.05,
        lowerTimeSec: 0.05
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.start();
    let state = sim.getState();
    for (let elapsedSec = 0; elapsedSec < 120 && state.kpis.completedInbound < 1; elapsedSec = Number((elapsedSec + 0.2).toFixed(6))) {
      state = sim.step(0.2);
    }
    const vehicle = state.vehicles[0]!;

    expect(state.kpis.completedInbound).toBe(1);
    expect(vehicle.loaded).toBe(false);
    expect(vehicle.taskId).toBeNull();
    expect(vehicle.state).toBe('idle');
    expect(vehicle.routeNodeIds).toEqual([vehicle.currentNodeId]);
    expect(sim.getEventLog().some((event) => event.eventType === 'vehicle-standby-dispatched')).toBe(false);
    expect(sim.getEventLog().some((event) => event.eventType === 'route-replanned' && event.reason === 'loaded-shortest-path')).toBe(false);
  });

  it('agent-simple makes an empty blocker follow a yield pocket before resuming its task goal', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['right-row-08']);
    sim.setVehicleRouteForTest('SH-02', ['main-north-05']);
    sim.addLoadForTest({ id: 'empty-pickup-load', state: 'waiting', nodeId: 'outbound-lift-bottom-01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'loaded-drop-load', state: 'carried', nodeId: null, vehicleId: 'SH-02', weightKg: 100 });
    sim.addTaskForTest({
      id: 'empty-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'outbound-lift-bottom-01',
      dropoffNodeId: 'storage-r12-c01',
      loadId: 'empty-pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'loaded-drop',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-bottom-01',
      dropoffNodeId: 'storage-r08-c02',
      loadId: 'loaded-drop-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'empty-pickup', false);
    sim.setVehicleTaskForTest('SH-02', 'loaded-drop', true);

    let state = sim.getState();
    let yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.vehicleId === 'SH-01' && event.reason === 'empty-yields-to-loaded');
    for (let index = 0; index < 80 && !yieldEvent; index += 1) {
      state = sim.step(0.2);
      yieldEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.vehicleId === 'SH-01' && event.reason === 'empty-yields-to-loaded');
    }
    const emptyVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(yieldEvent?.details.route).toBe('right-row-08>storage-r08-c24');
    expect(emptyVehicle?.targetNodeId).toBe('storage-r08-c24');
    expect(emptyVehicle?.currentEdgeId).toBe('right-row-08-storage-r08-c24');
    expect(emptyVehicle?.plannedGoalNodeId).toBe('outbound-lift-bottom-01');
    expect(emptyVehicle?.localRouteReason).toBe('temporary-yield');
    expect(emptyVehicle?.localRouteNodeIds).toEqual(['right-row-08', 'storage-r08-c24']);
    expect(emptyVehicle?.waitReason).toBeNull();
  });

  it('agent-simple waits inside a storage row instead of reversing into a long obstacle reroute', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r07-c05']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r07-c06']);
    sim.addLoadForTest({ id: 'blocked-pickup-load', state: 'waiting', nodeId: 'outbound-lift-top-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'blocked-pickup',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'outbound-lift-top-02',
      dropoffNodeId: 'storage-r12-c01',
      loadId: 'blocked-pickup-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'blocked-pickup', false);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.state).toBe('waiting-blocked');
    expect(vehicle?.targetNodeId).toBe('storage-r07-c06');
    expect(vehicle?.blockingVehicleId).toBe('SH-02');
    expect(vehicle?.localRouteNodeIds).toEqual([]);
    expect(sim.getEventLog().some((event) => event.eventType === 'route-replanned' && event.reason === 'empty-local-obstacle-reroute')).toBe(false);
  });

  it('agent-simple makes the lower-priority empty shuttle clear a storage-row head-on conflict', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['storage-r08-c17']);
    sim.setVehicleRouteForTest('SH-02', ['storage-r08-c18']);
    sim.addLoadForTest({ id: 'pickup-left-load', state: 'waiting', nodeId: 'inbound-lift-top-01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'pickup-right-load', state: 'waiting', nodeId: 'inbound-lift-top-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'pickup-right',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-02',
      dropoffNodeId: 'storage-r08-c20',
      loadId: 'pickup-right-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'pickup-left',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-01',
      dropoffNodeId: 'storage-r08-c21',
      loadId: 'pickup-left-load',
      vehicleId: 'SH-02',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'pickup-right', false);
    sim.setVehicleTaskForTest('SH-02', 'pickup-left', false);

    const state = sim.step(0.2);
    const yieldingVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(yieldingVehicle?.targetNodeId).toBe('storage-r08-c19');
    expect(yieldingVehicle?.plannedRouteNodeIds).toContain('inbound-lift-top-01');
    expect(yieldingVehicle?.localRouteReason).toBe('temporary-reroute');
    expect(sim.getEventLog().some((event) => event.eventType === 'route-replanned' && event.reason === 'empty-storage-head-on-yield')).toBe(true);
  });

  it('moves a storage refuge occupant deeper so an aisle shuttle can enter the pocket', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r06-c01', 'left-row-06']);
    sim.setVehicleRouteForTest('SH-02', ['left-row-06', 'storage-r06-c01']);

    const state = sim.step(0.1);
    const refugeOccupant = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');
    const aisleVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(refugeOccupant).toMatchObject({
      state: 'returning',
      currentNodeId: 'storage-r06-c01',
      targetNodeId: 'storage-r06-c02',
      waitReason: null
    });
    expect(aisleVehicle?.targetNodeId).toBe('storage-r06-c01');
  });

  it('does not bounce a task shuttle deeper into its own storage cell when its side aisle exit is occupied', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r06-c01', 'left-row-06']);
    sim.setVehicleRouteForTest('SH-02', ['left-row-06']);

    const state = sim.step(0.1);
    const waitingVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(waitingVehicle).toMatchObject({
      state: 'waiting-blocked',
      currentNodeId: 'storage-r06-c01',
      targetNodeId: 'left-row-06'
    });
    expect(waitingVehicle?.routeNodeIds.slice(0, 3)).toEqual(['storage-r06-c01', 'left-row-06']);
    expect(waitingVehicle?.targetNodeId).not.toBe('storage-r06-c02');
  });

  it('does not repeatedly insert the same side-aisle refuge after returning from it', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['left-row-06', 'storage-r06-c01', 'left-row-06', 'left-row-07']);
    sim.setVehicleRouteForTest('SH-02', ['left-row-07']);

    const state = runFor(sim, 4);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    const route = vehicle?.routeNodeIds ?? [];
    const refugeVisits = route.filter((nodeId) => nodeId === 'storage-r06-c01').length;

    expect(refugeVisits).toBe(2);
    expect(route.join('>')).not.toContain('storage-r06-c01>left-row-06>storage-r06-c01');
  });

  it('moves an empty storage-refuge shuttle deeper when the side-aisle continuation is blocked', () => {
    const scenario = createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['left-row-06', 'storage-r06-c01', 'left-row-06', 'left-row-07']);
    sim.setVehicleRouteForTest('SH-02', ['left-row-07']);

    const state = runFor(sim, 3);
    const refugeVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-01');

    expect(refugeVehicle).toMatchObject({
      state: 'waiting-blocked',
      currentNodeId: 'storage-r06-c02',
      targetNodeId: 'storage-r06-c01',
      waitReason: 'refuge-exit-blocked'
    });
  });

  it('does not allocate inbound dropoff cells currently occupied by parked shuttles', () => {
    const scenario = createDefaultShuttleScenario({
      vehicles: { count: 9 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const parkedStorageNodeId = sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-09')?.currentNodeId;
    for (const node of scenario.layout.nodes.filter((candidate) => candidate.type === 'storage' && candidate.id !== parkedStorageNodeId)) {
      sim.addLoadForTest({
        id: `load-${node.id}`,
        state: 'stored',
        nodeId: node.id,
        vehicleId: null,
        weightKg: 100
      });
    }

    sim.start();
    for (let index = 0; index < 5 && !(sim.getState().kpis.blockedTimeByReasonSec['storage-full'] > 0); index += 1) {
      sim.step(0.2);
    }

    expect(parkedStorageNodeId).toMatch(/^storage-r\d+-c\d+$/);
    expect(sim.getState().tasks.some((task) => task.dropoffNodeId === parkedStorageNodeId)).toBe(false);
    expect(sim.getState().kpis.blockedTimeByReasonSec['storage-full']).toBeGreaterThan(0);
  });

  it('keeps the default demo on orthogonal four-way shuttle aisles', () => {
    const scenario = createDefaultShuttleScenario();
    const nodes = new Map(scenario.layout.nodes.map((node) => [node.id, node]));

    for (const edge of scenario.layout.edges) {
      const from = nodes.get(edge.from)!;
      const to = nodes.get(edge.to)!;
      expect(from.x === to.x || from.z === to.z).toBe(true);
      expect(from.x === to.x && from.z === to.z).toBe(false);
    }

    const fifoLaneEdges = scenario.layout.edges.filter((edge) => edge.conflictGroup?.startsWith('fifo-lane'));
    const storageNodes = scenario.layout.nodes.filter((node) => node.type === 'storage');
    const storageXs = storageNodes.map((node) => node.x);
    const storageRows = [...new Set(storageNodes.map((node) => node.z))].sort((left, right) => left - right);
    const storageColumns = [...new Set(storageNodes.map((node) => node.x))].sort((left, right) => left - right);
    const inboundLifts = scenario.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'inbound');
    const outboundLifts = scenario.layout.nodes.filter((node) => node.type === 'lift-blackbox' && node.liftKind === 'outbound');
    const storageColumnGaps = storageColumns.slice(1).map((x, index) => x - storageColumns[index]!);
    const storageRowGaps = storageRows.slice(1).map((z, index) => z - storageRows[index]!);
    const mainNorthNodes = scenario.layout.nodes.filter((node) => node.id.startsWith('main-north-')).sort((left, right) => left.x - right.x);
    const mainSouthNodes = scenario.layout.nodes.filter((node) => node.id.startsWith('main-south-')).sort((left, right) => left.x - right.x);
    const mainLaneEdges = scenario.layout.edges.filter((edge) =>
      edge.conflictGroup?.startsWith('main-lane-north') || edge.conflictGroup?.startsWith('main-lane-south')
    );
    const rightUprightEdges = scenario.layout.edges.filter((edge) => edge.conflictGroup?.startsWith('right-upright'));

    expect(scenario.layout.nodes.every((node) => node.y === 0)).toBe(true);
    expect(storageRows).toHaveLength(16);
    expect(storageColumns).toHaveLength(24);
    expect(storageNodes).toHaveLength(384);
    expect(storageRows.every((z) => storageNodes.filter((node) => node.z === z).length === storageColumns.length)).toBe(true);
    expect(fifoLaneEdges).toHaveLength(storageRows.length * (storageColumns.length + 1) + 224);
    expect(fifoLaneEdges.every((edge) => edge.directionMode === 'twoWay')).toBe(true);
    expect(storageColumnGaps.filter((gap) => gap > 1.3)).toHaveLength(3);
    expect(storageColumnGaps.filter((gap) => gap <= 1.3)).toHaveLength(20);
    expect(storageRowGaps.filter((gap) => gap > 1.25)).toEqual([4.4]);
    expect(mainNorthNodes).toHaveLength(6);
    expect(mainSouthNodes).toHaveLength(6);
    expect(mainNorthNodes.every((node, index) => node.x === mainSouthNodes[index]!.x)).toBe(true);
    expect(mainLaneEdges).toHaveLength(10);
    expect(mainLaneEdges.every((edge) => edge.directionMode === 'twoWay')).toBe(true);
    expect(rightUprightEdges.length).toBeGreaterThan(0);
    expect(rightUprightEdges.every((edge) => edge.directionMode === 'twoWay')).toBe(true);
    expect(mainNorthNodes.every((node) => node.z < 0)).toBe(true);
    expect(mainSouthNodes.every((node) => node.z > 0)).toBe(true);
    expect(Math.max(...storageXs)).toBeLessThan(Math.max(...mainNorthNodes.map((node) => node.x)));
    expect(Math.min(...storageXs)).toBeGreaterThan(Math.min(...mainNorthNodes.map((node) => node.x)));
    expect(scenario.layout.nodes.some((node) => node.id === 'inbound' || node.id === 'outbound')).toBe(false);
    expect(scenario.layout.edges.some((edge) => edge.id === 'inbound-x-main' || edge.id === 'x-outbound-outbound')).toBe(false);
    expect(inboundLifts).toHaveLength(4);
    expect(outboundLifts).toHaveLength(4);
    expect(inboundLifts.every((node) => Math.abs(node.z) > 0)).toBe(true);
    expect(outboundLifts.every((node) => Math.abs(node.z) > 0)).toBe(true);
    expect(scenario.layout.edges.some((edge) => edge.id === 'inbound-lift-top-01-main-north-01')).toBe(true);
    expect(scenario.layout.edges.some((edge) => edge.id === 'outbound-lift-bottom-02-main-south-03')).toBe(true);
  });

  it('rejects diagonal or multi-level custom layout edges', () => {
    const scenario = createDefaultShuttleScenario();

    expect(() =>
      ShuttleScenarioSchema.parse({
        ...scenario,
        layout: {
          ...scenario.layout,
          nodes: scenario.layout.nodes.map((node) =>
            node.id === 'main-south-01' ? { ...node, x: node.x + 0.4 } : node
          )
        }
      })
    ).toThrow(/diagonal/);

    expect(() =>
      ShuttleScenarioSchema.parse({
        ...scenario,
        layout: {
          ...scenario.layout,
          nodes: scenario.layout.nodes.map((node) =>
            node.id === 'main-south-01' ? { ...node, y: 1.2 } : node
          )
        }
      })
    ).toThrow(/single-floor/);
  });

  it('rejects zones that reference unknown graph resources', () => {
    const scenario = createDefaultShuttleScenario();

    expect(() =>
      ShuttleScenarioSchema.parse({
        ...scenario,
        layout: {
          ...scenario.layout,
          zones: [
            ...scenario.layout.zones,
            {
              id: 'bad-zone',
              type: 'intersection',
              nodeIds: ['missing-node'],
              edgeIds: ['missing-edge'],
              noStop: true,
              noParking: true,
              capacity: 1
            }
          ]
        }
      })
    ).toThrow(/references unknown node missing-node/);
  });

  it('summarizes the default layout contract used by the Unreal static scaffold', () => {
    const contract = summarizeScenarioStaticSceneContract(createDefaultShuttleScenario());

    expect(contract).toEqual(goldenStaticSceneContract);
    expect(contract).toMatchObject({
      schemaVersion: 'shuttle.simCoreStaticSceneContract.v1',
      scenarioId: 'shuttle-phase0-balanced',
      units: 'meter',
      storageRows: 16,
      storageColumns: 24,
      storageCellCount: 384,
      blockedCellCount: 0,
      structuralCellCount: 0,
      trackBedCount: 958,
      storageLaneTrackCount: 624,
      sideAisleTrackCount: 162,
      crossAisleTrackCount: 12,
      inboundConnectorTrackCount: 76,
      outboundConnectorTrackCount: 76,
      parkingConnectorTrackCount: 8,
      diagonalTrackCount: 0,
      inboundLiftPadCount: 4,
      outboundLiftPadCount: 4,
      parkingPadCount: 8,
      singleLevel: true,
      storageIslandCount: 8,
      denseStorageIslands: true,
      denseStorageBlock: false,
      orthogonalTrackOnly: true,
      dedicatedLiftPorts: true,
      inboundSide: 'mixed',
      outboundSide: 'mixed',
      layoutCalibrationProfile: {
        id: 'phase0-cad-assumption-v1',
        status: 'assumption'
      },
      calibrationReadiness: {
        status: 'assumption',
        readyForIndustrialThroughputClaims: false
      }
    });
    expect(contract.calibrationReadiness.requiredDimensionKeys).toEqual([...REQUIRED_CALIBRATION_DIMENSION_KEYS]);
    expect(contract.calibrationReadiness.presentDimensionKeys).toEqual([
      'storageCellPitchX',
      'storageCellPitchZ',
      'storageBayGapX',
      'mainLaneCenterSpacingZ',
      'innerStorageBankGapZ',
      'liftStandoffZ',
      'sideClearanceX'
    ]);
    expect(contract.calibrationReadiness.assumedDimensionKeys).toContain('storageCellPitchX');
    expect(contract.calibrationReadiness.lowConfidenceDimensionKeys).toContain('storageCellPitchX');
    expect(contract.calibrationReadiness.missingDimensionKeys).toEqual([
      'palletLength',
      'palletWidth',
      'palletHeight',
      'shuttleLength',
      'shuttleWidth',
      'shuttleHeight',
      'loadedClearance',
      'liftPadLength',
      'liftPadWidth',
      'rollerTransferLength',
      'rollerTransferWidth',
      'parkingPadLength',
      'parkingPadWidth'
    ]);
    expect(contract.calibrationReadiness.message).toContain('throughput remains smoke-test only');
    expect(contract.storagePitchXM).toBeCloseTo(1.25, 6);
    expect(contract.storagePitchZM).toBeCloseTo(1.2, 6);
    expect(contract.storageBlockMinXM).toBeCloseTo(2.5, 6);
    expect(contract.storageBlockMaxXM).toBeCloseTo(38, 6);
    expect(contract.storageBlockMinZM).toBeCloseTo(-10.6, 6);
    expect(contract.storageBlockMaxZM).toBeCloseTo(10.6, 6);
    expect(contract.inboundLiftXM).toBeCloseTo(25, 6);
    expect(contract.outboundLiftXM).toBeCloseTo(25, 6);
    expect(contract.storageCells).toHaveLength(384);
    expect(contract.blockedCells).toEqual([]);
    expect(contract.storageIslandCount).toBe(8);
    expect(contract.denseStorageIslands).toBe(true);
    expect(contract.denseStorageBlock).toBe(false);
    expect(contract.storageCells[0]).toMatchObject({
      id: 'storage-r01-c01',
      row: 1,
      column: 1,
      xM: 2.5,
      yM: 0,
      zM: -10.6
    });
    expect(contract.storageCells.at(-1)).toMatchObject({
      id: 'storage-r16-c24',
      row: 16,
      column: 24,
      xM: 38,
      yM: 0,
      zM: 10.6
    });
    expect(contract.storageCells.every((cell) => cell.lengthXM === 1.25 && cell.lengthZM === 1.2)).toBe(true);
    expect(contract.layoutCalibrationProfile?.dimensions.find((dimension) => dimension.key === 'storageCellPitchX')).toMatchObject({
      valueM: 1.25,
      source: 'assumed',
      confidence: 'low'
    });
    expect(
      Array.from({ length: 16 }, (_, rowIndex) =>
        contract.storageCells.filter((cell) => cell.row === rowIndex + 1).map((cell) => cell.column)
      )
    ).toEqual(Array.from({ length: 16 }, () => Array.from({ length: 24 }, (_, columnIndex) => columnIndex + 1)));
    expect(contract.trackBeds.every((track) => track.orientation === 'x' || track.orientation === 'z')).toBe(true);
    expect(contract.trackBeds.filter((track) => track.category === 'storageLane')).toHaveLength(624);
    expect(contract.trackBeds.find((track) => track.id === 'right-row-01-storage-r01-c24')).toMatchObject({
      category: 'storageLane',
      zM: -10.6,
      lengthXM: 2.5,
      lengthZM: 0.08,
      orientation: 'x',
      row: 1,
      side: 'right'
    });
    expect(contract.trackBeds.find((track) => track.id === 'main-north-01-main-north-02')).toMatchObject({
      category: 'crossAisle',
      zM: -0.8,
      orientation: 'x'
    });
    expect(contract.trackBeds.find((track) => track.id === 'left-row-08-main-north-00')).toMatchObject({
      category: 'sideAisle',
      xM: 0,
      lengthXM: 0.1,
      orientation: 'z',
      side: 'left'
    });
    expect(contract.liftPads.filter((pad) => pad.category === 'inboundLift').map((pad) => pad.id).sort()).toEqual([
      'inbound-lift-bottom-01',
      'inbound-lift-bottom-02',
      'inbound-lift-top-01',
      'inbound-lift-top-02'
    ]);
    expect(contract.liftPads.filter((pad) => pad.category === 'outboundLift')).toHaveLength(4);
    expect(contract.parkingPads.filter((pad) => pad.id.startsWith('parking-')).map((pad) => pad.id).sort()).toEqual([
      'parking-a',
      'parking-b',
      'parking-c',
      'parking-d',
      'parking-e',
      'parking-f',
      'parking-g',
      'parking-h'
    ]);
    expect(contract.parkingPads.filter((pad) => pad.id.includes('-stage-'))).toHaveLength(0);
  });

  it('rejects vertical travel edges that cut through storage-cell footprints', () => {
    const base = createDefaultShuttleScenario();
    const scenarioWithVerticalStorageCut = createDefaultShuttleScenario({
      layout: {
        ...base.layout,
        nodes: [
          ...base.layout.nodes,
          { id: 'bad-portal-top', type: 'intersection', x: 2.5, y: 0, z: -11.3, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'bad-portal-bottom', type: 'intersection', x: 2.5, y: 0, z: -8.7, noStop: false, noParking: true, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          ...base.layout.edges,
          {
            id: 'bad-vertical-storage-edge',
            from: 'bad-portal-top',
            to: 'bad-portal-bottom',
            lengthM: 2.6,
            directionMode: 'twoWay',
            reservationType: 'edge',
            conflictGroup: 'bad-storage-cut',
            noParking: true
          }
        ]
      }
    });

    expect(verticalStorageFootprintEdgeViolations(base)).toEqual([]);
    expect(verticalStorageFootprintEdgeViolations(scenarioWithVerticalStorageCut)).toEqual([
      { edgeId: 'bad-vertical-storage-edge', cellId: 'storage-r01-c01' }
    ]);
    expect(() => new ShuttleSimCore(scenarioWithVerticalStorageCut)).toThrow(/vertical edge bad-vertical-storage-edge crosses storage-cell footprint/);
  });

  it('carries blocked and structural CAD reference cells into the static scene contract without routing them', () => {
    const contract = summarizeScenarioStaticSceneContract(createDefaultShuttleScenario({
      layoutProfile: {
        calibrationProfile: {
          id: 'blocked-cell-contract-test-profile',
          label: 'Blocked cell contract test profile',
          status: 'partial-cad',
          sourceDescription: 'Test profile with explicit non-storage CAD reference cells.',
          blockedCells: [
            {
              id: 'cad-structural-gap-01',
              role: 'structural',
              xM: 17.25,
              yM: 0,
              zM: 0,
              lengthXM: 1.25,
              lengthZM: 1.2,
              source: 'cad',
              confidence: 'medium',
              note: 'Reference cell drawn from CAD as non-routable structure.'
            },
            {
              id: 'cad-blocked-gap-01',
              role: 'blocked',
              xM: 28.5,
              yM: 0,
              zM: 0,
              lengthXM: 1.25,
              lengthZM: 1.2,
              source: 'site',
              confidence: 'high'
            }
          ]
        }
      }
    }));

    expect(contract.blockedCellCount).toBe(2);
    expect(contract.structuralCellCount).toBe(1);
    expect(contract.blockedCells.map((cell) => cell.id)).toEqual(['cad-blocked-gap-01', 'cad-structural-gap-01']);
    expect(contract.blockedCells[0]).toMatchObject({
      role: 'blocked',
      xM: 28.5,
      zM: 0,
      source: 'site',
      confidence: 'high'
    });
    expect(contract.storageCells.some((cell) => cell.id === 'cad-blocked-gap-01')).toBe(false);
    expect(contract.trackBeds.some((track) => track.id === 'cad-blocked-gap-01')).toBe(false);
  });

  it('rejects route edges whose swept square shuttle footprint intersects blocked structural cells', () => {
    const base = createDefaultShuttleScenario();
    const edge = base.layout.edges.find((candidate) => candidate.id === 'main-north-01-main-north-02')!;
    const from = base.layout.nodes.find((node) => node.id === edge.from)!;
    const to = base.layout.nodes.find((node) => node.id === edge.to)!;
    const scenario: ShuttleScenario = {
      ...base,
      layout: {
        ...base.layout,
        calibrationProfile: {
          ...base.layout.calibrationProfile!,
          id: 'blocked-edge-footprint-test-profile',
          label: 'Blocked edge footprint test profile',
          status: 'partial-cad',
          sourceDescription: 'Test profile with a structural keepout placed on a route edge.',
          blockedCells: [
            {
              id: 'cad-structural-main-north-edge',
              role: 'structural',
              xM: (from.x + to.x) / 2,
              yM: 0,
              zM: (from.z + to.z) / 2,
              lengthXM: 0.25,
              lengthZM: 0.25,
              source: 'cad',
              confidence: 'high'
            }
          ]
        }
      }
    };

    expect(staticObstacleFootprintViolations(base)).toEqual([]);
    expect(staticObstacleFootprintViolations(scenario)).toContainEqual({
      resourceType: 'edge',
      resourceId: 'main-north-01-main-north-02',
      blockedCellId: 'cad-structural-main-north-edge'
    });
    expect(() => new ShuttleSimCore(scenario)).toThrow(/blocked cell cad-structural-main-north-edge/);
  });

  it('rejects queue and yield-pocket nodes whose shuttle footprint intersects blocked structural cells', () => {
    const base = createInboundOutboundDemoScenario();
    const queueNode = base.layout.nodes.find((node) => node.id === 'parking-lift-01-inbound-queue')!;
    const scenario: ShuttleScenario = {
      ...base,
      layout: {
        ...base.layout,
        calibrationProfile: {
          ...base.layout.calibrationProfile!,
          label: 'Blocked queue footprint test profile',
          status: 'partial-cad',
          sourceDescription: 'Test profile with a structural keepout placed on a lift queue pocket.',
          blockedCells: [
            {
              id: 'cad-structural-inbound-queue-pocket',
              role: 'structural',
              xM: queueNode.x,
              yM: 0,
              zM: queueNode.z,
              lengthXM: 0.25,
              lengthZM: 0.25,
              source: 'cad',
              confidence: 'high'
            }
          ]
        }
      }
    };

    expect(staticObstacleFootprintViolations(base)).toEqual([]);
    expect(staticObstacleFootprintViolations(scenario)).toContainEqual({
      resourceType: 'node',
      resourceId: 'parking-lift-01-inbound-queue',
      blockedCellId: 'cad-structural-inbound-queue-pocket'
    });
    expect(() => new ShuttleSimCore(scenario)).toThrow(/blocked cell cad-structural-inbound-queue-pocket/);
  });

  it('marks the static scene ready for industrial throughput claims only with a fully verified calibration profile', () => {
    const contract = summarizeScenarioStaticSceneContract(createDefaultShuttleScenario({
      layoutProfile: {
        calibrationProfile: {
          id: 'verified-calibration-test-profile',
          label: 'Verified calibration test profile',
          status: 'verified',
          sourceDescription: 'Test profile with every required dimension sourced from CAD.',
          dimensions: verifiedCalibrationDimensions()
        }
      }
    }));

    expect(contract.calibrationReadiness).toMatchObject({
      status: 'verified',
      readyForIndustrialThroughputClaims: true,
      missingDimensionKeys: [],
      assumedDimensionKeys: [],
      lowConfidenceDimensionKeys: []
    });
    expect(contract.calibrationReadiness.calibratedDimensionKeys).toEqual([...REQUIRED_CALIBRATION_DIMENSION_KEYS]);
    expect(contract.calibrationReadiness.message).toBe('Layout dimensions are verified for industrial throughput claims.');
  });

  it('keeps layout-profile overrides synchronized with calibration metadata and static-scene footprints', () => {
    const scenario = createDefaultShuttleScenario({
      layoutProfile: {
        storageCellPitchXM: 1.3,
        storageCellPitchZM: 1.25,
        storageBayGapXM: 2.5,
        storageInnerRowZM: 2.35,
        sideClearanceXM: 2.7,
        mainLaneNorthZM: -0.9,
        mainLaneSouthZM: 0.9,
        liftStandoffZM: 2,
        calibrationProfile: {
          id: 'partial-cad-test-profile',
          label: 'Partial CAD test profile',
          status: 'partial-cad',
          sourceDescription: 'Test override values from a partial CAD profile.'
        }
      }
    });
    const contract = summarizeScenarioStaticSceneContract(scenario);
    const dimensionsByKey = new Map(scenario.layout.calibrationProfile?.dimensions.map((dimension) => [dimension.key, dimension.valueM]));

    expect(scenario.layout.calibrationProfile).toMatchObject({
      id: 'partial-cad-test-profile',
      label: 'Partial CAD test profile',
      status: 'partial-cad',
      sourceDescription: 'Test override values from a partial CAD profile.'
    });
    expect(dimensionsByKey.get('storageCellPitchX')).toBe(1.3);
    expect(dimensionsByKey.get('storageCellPitchZ')).toBe(1.25);
    expect(dimensionsByKey.get('storageBayGapX')).toBe(2.5);
    expect(dimensionsByKey.get('mainLaneCenterSpacingZ')).toBe(1.8);
    expect(dimensionsByKey.get('innerStorageBankGapZ')).toBe(4.7);
    expect(dimensionsByKey.get('liftStandoffZ')).toBe(2);
    expect(dimensionsByKey.get('sideClearanceX')).toBe(2.7);
    expect(contract.layoutCalibrationProfile?.dimensions.find((dimension) => dimension.key === 'storageCellPitchX')).toMatchObject({
      valueM: 1.3,
      source: 'assumed',
      confidence: 'low'
    });
    expect(contract.storagePitchXM).toBeCloseTo(1.3, 6);
    expect(contract.storagePitchZM).toBeCloseTo(1.25, 6);
    expect(contract.storageCells.every((cell) => cell.lengthXM === 1.3 && cell.lengthZM === 1.25)).toBe(true);
  });

  it('rejects multi-capacity traffic resources for Phase 0', () => {
    expect(() =>
      ShuttleScenarioSchema.parse({
        ...createDefaultShuttleScenario(),
        trafficPolicy: {
          ...createDefaultShuttleScenario().trafficPolicy,
          edgeCapacity: 2
        }
      })
    ).toThrow(/edgeCapacity=1/);
  });

  it('rejects scenarios without one parkable non-aisle node per vehicle for Phase 0', () => {
    const base = createDefaultShuttleScenario();
    expect(() =>
      createDefaultShuttleScenario({
        vehicles: { count: 9 },
        layout: {
          ...base.layout,
          nodes: base.layout.nodes.map((node) => node.type === 'storage' ? { ...node, noParking: true } : node)
        }
      })
    ).toThrow(/parkable non-aisle node per vehicle/);
  });

  it('rejects duplicate node ids before reset occupancy is initialized', () => {
    expect(() =>
      ShuttleScenarioSchema.parse({
        ...createDefaultShuttleScenario(),
        layout: {
          ...createDefaultShuttleScenario().layout,
          nodes: createDefaultShuttleScenario().layout.nodes.map((node) =>
            node.id === 'parking-b' ? { ...node, id: 'parking-a' } : node
          )
        }
      })
    ).toThrow(/Duplicate node id parking-a/);
  });

  it('rejects storage nodes without explicit FIFO row and column ids', () => {
    const scenario = createDefaultShuttleScenario();
    expect(() =>
      ShuttleScenarioSchema.parse({
        ...scenario,
        layout: {
          ...scenario.layout,
          nodes: scenario.layout.nodes.map((node) =>
            node.id === 'storage-r01-c01' ? { ...node, id: 'storage-a' } : node
          )
        }
      })
    ).toThrow(/storage-rNN-cNN/);
  });

  it('rejects lift-blackbox nodes without explicit liftKind metadata', () => {
    const scenario = createDefaultShuttleScenario();
    expect(() =>
      ShuttleScenarioSchema.parse({
        ...scenario,
        layout: {
          ...scenario.layout,
          nodes: scenario.layout.nodes.map((node) =>
            node.id === 'inbound-lift-top-01' ? { ...node, liftKind: undefined } : node
          )
        }
      })
    ).toThrow(/lift-blackbox nodes must declare liftKind/);
  });

  it('rejects storage rows without left and right side access nodes', () => {
    const scenario = createDefaultShuttleScenario();
    expect(() =>
      ShuttleScenarioSchema.parse({
        ...scenario,
        layout: {
          ...scenario.layout,
          nodes: scenario.layout.nodes.filter((node) => node.id !== 'left-row-01')
        }
      })
    ).toThrow(/requires side access node left-row-01/);
  });

  it('rejects storage-to-storage edges that cross FIFO rows', () => {
    const scenario = createDefaultShuttleScenario();
    expect(() =>
      ShuttleScenarioSchema.parse({
        ...scenario,
        layout: {
          ...scenario.layout,
          edges: [
            ...scenario.layout.edges,
            {
              id: 'storage-r01-c01-storage-r02-c01',
              from: 'storage-r01-c01',
              to: 'storage-r02-c01',
              lengthM: 1.2,
              directionMode: 'twoWay',
              reservationType: 'edge',
              conflictGroup: 'invalid-storage-cross-row',
              noParking: true
            }
          ]
        }
      })
    ).toThrow(/storage-area traversal must stay horizontal/);
  });

  it('produces the same event log hash for the same seed', () => {
    const scenario = createDefaultShuttleScenario({ durationSec: 90, taskGeneration: { maxTasks: 8 } });
    const hashes = Array.from({ length: 3 }, () => {
      const sim = new ShuttleSimCore(scenario);
      sim.runToEnd(90);
      return hashEventLog(sim.getEventLog());
    });

    expect(new Set(hashes).size).toBe(1);
    expect(hashes[0]).toMatch(/^[a-f0-9]{64}$/);
  }, 60000);

  it('does not advance while paused and resumes without losing state', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({ durationSec: 120 }));
    sim.start();
    sim.step(5);
    const beforePause = sim.getState();

    sim.pause();
    sim.step(20);
    const paused = sim.getState();
    expect(paused.simTimeSec).toBe(beforePause.simTimeSec);

    sim.resume();
    sim.step(5);
    expect(sim.getState().simTimeSec).toBeGreaterThan(paused.simTimeSec);
  });

  it('keeps dedicated lift ports from creating artificial two-vehicle deadlocks', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 120,
      taskGeneration: {
        inboundRatePerHour: 720,
        outboundRatePerHour: 720,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 12
      },
      physicsParams: {
        emptySpeedMps: 8,
        loadedSpeedMps: 7,
        accelerationMps2: 10,
        switchDirectionSec: 0,
        liftTimeSec: 0.05,
        lowerTimeSec: 0.05,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.runToEnd(120);
    const state = sim.getState();

    expect(state.kpis.completedInbound).toBeGreaterThan(0);
    expect(state.kpis.deadlockCount).toBe(0);
    const liftKindByNodeId = new Map(scenario.layout.nodes.map((node) => [node.id, node.liftKind ?? null]));
    expect(state.tasks.every((task) => task.kind !== 'inbound' || liftKindByNodeId.get(task.pickupNodeId) === 'inbound')).toBe(true);
    expect(state.tasks.every((task) => task.kind !== 'outbound' || liftKindByNodeId.get(task.dropoffNodeId) === 'outbound')).toBe(true);
    expect(state.traffic.liftPorts).toHaveLength(8);
    expect(state.traffic.liftPorts.some((port) => port.kind === 'inbound' && port.utilization > 0)).toBe(true);
  }, 15000);

  it('lets opposite main-lane traffic pass without portal-zone deadlock in the default layout', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 30,
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      physicsParams: {
        emptySpeedMps: 4,
        loadedSpeedMps: 3,
        accelerationMps2: 4,
        switchDirectionSec: 0,
        liftTimeSec: 0.1,
        lowerTimeSec: 0.1,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['main-north-03', 'main-north-02', 'main-north-01', 'main-north-00', 'parking-c']);
    sim.setVehicleRouteForTest('SH-02', ['main-south-02', 'main-south-03', 'main-south-04', 'main-south-05', 'parking-b']);

    for (let index = 0; index < 120; index += 1) {
      sim.step(0.2);
    }

    const state = sim.getState();
    expect(state.kpis.deadlockCount).toBe(0);
    expect(state.vehicles.every((vehicle) => vehicle.waitReason !== 'zone-reserved')).toBe(true);
    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-01')?.currentNodeId).toBe('parking-c');
    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-02')?.currentNodeId).toBe('parking-b');
  });

  it('holds head-on main-lane swaps at parking pads when neither shuttle can clear a no-stop route', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 60,
      vehicles: {
        count: 2
      },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      physicsParams: {
        emptySpeedMps: 4,
        loadedSpeedMps: 3,
        accelerationMps2: 4,
        switchDirectionSec: 0,
        liftTimeSec: 0.1,
        lowerTimeSec: 0.1,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['parking-c', 'main-north-00', 'main-north-01', 'main-north-02', 'main-north-03', 'main-north-04', 'main-north-05', 'parking-a']);
    sim.setVehicleRouteForTest('SH-02', ['parking-a', 'main-north-05', 'main-north-04', 'main-north-03', 'main-north-02', 'main-north-01', 'main-north-00', 'parking-c']);

    for (let index = 0; index < 10; index += 1) {
      sim.step(0.2);
    }

    const state = sim.getState();
    expect(state.kpis.deadlockCount).toBe(0);
    expect(state.traffic.deadlockCandidateVehicleIds).toEqual([]);
    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-01')).toMatchObject({
      currentNodeId: 'parking-c',
      currentEdgeId: null,
      waitReason: 'no-stop-clearance-incomplete'
    });
    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-02')).toMatchObject({
      currentNodeId: 'parking-a',
      currentEdgeId: null,
      waitReason: 'no-stop-clearance-incomplete'
    });
  });

  it('does not serialize parallel main-aisle travel through a broad portal zone', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 20,
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      physicsParams: {
        emptySpeedMps: 2.6,
        loadedSpeedMps: 2.2,
        accelerationMps2: 2,
        switchDirectionSec: 0,
        liftTimeSec: 0.5,
        lowerTimeSec: 0.5,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.1
      }
    });
    expect(scenario.layout.zones.find((zone) => zone.id === 'zone-main-portal-02')).toBeUndefined();

    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['main-south-03', 'main-south-04', 'main-south-05', 'parking-b']);
    sim.setVehicleRouteForTest('SH-02', ['main-north-03', 'main-north-04', 'main-north-05', 'parking-a']);
    sim.step(0.2);

    const state = sim.getState();
    expect(state.reservations.some((reservation) => reservation.resourceId === 'zone-main-portal-03')).toBe(false);
    expect(state.reservations.some((reservation) => reservation.resourceId === 'zone-main-portal-04')).toBe(false);
    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-01')).toMatchObject({
      currentNodeId: 'main-south-03',
      currentEdgeId: 'main-south-03-main-south-04'
    });
    const parallelVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');
    expect(parallelVehicle).toMatchObject({
      currentNodeId: 'main-north-03',
      currentEdgeId: 'main-north-03-main-north-04'
    });
    expect(parallelVehicle?.state).not.toBe('waiting-blocked');
  });

  it('does not let a stopped portal-node occupant hold downstream portal zones', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 20,
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      physicsParams: {
        emptySpeedMps: 2.6,
        loadedSpeedMps: 2.2,
        accelerationMps2: 2,
        switchDirectionSec: 0,
        liftTimeSec: 0.5,
        lowerTimeSec: 0.5,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addReservationForTest({
      resourceType: 'edge',
      resourceId: 'main-north-01-main-north-02',
      vehicleId: 'external-blocker',
      taskId: null,
      startTimeSec: 0,
      endTimeSec: 60,
      priority: 0,
      conflictGroup: null,
      reasonCode: 'test-block'
    });
    sim.setVehicleRouteForTest('SH-01', ['main-north-02', 'main-north-01']);

    sim.step(0.2);

    expect(sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01')).toMatchObject({
      currentNodeId: 'main-north-02',
      currentEdgeId: null,
      waitReason: 'edge-reserved'
    });
    expect(sim.getState().reservations).not.toContainEqual(
      expect.objectContaining({ vehicleId: 'SH-01', resourceType: 'zone', resourceId: 'zone-main-portal-node-02', reasonCode: 'zone-hold' })
    );

    sim.setVehicleRouteForTest('SH-02', ['outbound-lift-top-01', 'main-south-02']);
    sim.step(0.2);

    expect(sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-02')).toMatchObject({
      state: 'waiting-blocked',
      waitReason: 'zone-reserved',
      currentNodeId: 'outbound-lift-top-01'
    });
  });

  it('does not enter a no-stop control chain unless the route can clear to a stoppable node', () => {
    const sim = new ShuttleSimCore(testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'parking', x: 0, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'X', type: 'intersection', x: 4, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'Y', type: 'intersection', x: 8, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 12, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-X', from: 'A', to: 'X', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-X', noParking: true },
          { id: 'X-Y', from: 'X', to: 'Y', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'X-Y', noParking: true },
          { id: 'Y-C', from: 'Y', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'Y-C', noParking: true }
        ],
        zones: []
      }
    }));
    sim.setVehicleRouteForTest('SH-02', ['C']);
    sim.setVehicleRouteForTest('SH-01', ['A', 'X', 'Y', 'C']);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle).toMatchObject({
      state: 'waiting-blocked',
      currentNodeId: 'A',
      currentEdgeId: null,
      targetNodeId: 'X',
      waitReason: 'no-stop-clearance-incomplete'
    });
  });

  it('uses the target node as the exit buffer for an edge-only no-stop zone', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'B-C', noParking: true }
        ],
        zones: [
          { id: 'zone-edge-only', type: 'intersection', nodeIds: [], edgeIds: ['A-B'], noStop: true, noParking: true, capacity: 1, conflictGroup: 'zone-edge-only' }
        ]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-02', ['C']);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B']);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.currentEdgeId).toBe('A-B');
    expect(vehicle?.waitReason).toBeNull();
    expect(state.reservations).toContainEqual(expect.objectContaining({ vehicleId: 'SH-01', resourceType: 'zone', resourceId: 'zone-edge-only' }));
  });

  it('preauthorizes the downstream exit buffer when an edge-only no-stop movement ends at a no-stop node', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'P', type: 'parking', x: 0, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'B-C', noParking: true }
        ],
        zones: [
          { id: 'zone-edge-only', type: 'intersection', nodeIds: [], edgeIds: ['A-B'], noStop: true, noParking: true, capacity: 1, conflictGroup: 'zone-edge-only' }
        ]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-02', ['P']);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C']);

    const state = sim.step(0.2);

    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-01')?.currentEdgeId).toBe('A-B');
    expect(state.reservations).toContainEqual(expect.objectContaining({ vehicleId: 'SH-01', resourceType: 'zone', resourceId: 'zone-edge-only' }));
    expect(state.reservations).toContainEqual(expect.objectContaining({ vehicleId: 'SH-01', resourceType: 'edge', resourceId: 'B-C' }));
    expect(state.reservations).toContainEqual(expect.objectContaining({ vehicleId: 'SH-01', resourceType: 'node', resourceId: 'C' }));
  });

  it('keeps a no-stop clear-through grant valid across one simulation tick of arrival rounding', () => {
    const scenario = testScenario({
      vehicles: {
        count: 2,
        lengthM: 1,
        widthM: 1,
        heightM: 0.2,
        emptySpeedMps: 2,
        loadedSpeedMps: 1.4,
        accelerationMps2: 10,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        maxLoadKg: 1000,
        safetyRadiusM: 0.4,
        batteryEnabled: false,
        initialSoc: 1
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.4,
        accelerationMps2: 10,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.2
      },
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'X', type: 'intersection', x: 1.4, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 18.2, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'P', type: 'parking', x: 0, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-X', from: 'A', to: 'X', lengthM: 1.4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-X', noParking: true },
          { id: 'X-C', from: 'X', to: 'C', lengthM: 16.8, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'X-C', noParking: true }
        ],
        zones: []
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-02', ['P']);
    sim.setVehicleRouteForTest('SH-01', ['A', 'X', 'C']);

    const state = runFor(sim, 0.8);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle?.waitReason).not.toBe('no-stop-continuation-blocked');
    expect(vehicle?.currentEdgeId).toBe('X-C');
    expect(vehicle?.targetNodeId).toBe('C');
  });

  it('does not use a node-zone no-stop target as an exit buffer', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'P', type: 'parking', x: 0, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'B-C', noParking: true }
        ],
        zones: [
          { id: 'zone-node-b', type: 'intersection', nodeIds: ['B'], edgeIds: [], noStop: true, noParking: true, capacity: 1, conflictGroup: 'zone-node-b' }
        ]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-02', ['P']);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C']);

    const state = sim.step(0.2);

    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-01')?.currentEdgeId).toBe('A-B');
    expect(state.reservations).toContainEqual(expect.objectContaining({ vehicleId: 'SH-01', resourceType: 'zone', resourceId: 'zone-node-b' }));
    expect(state.reservations).toContainEqual(expect.objectContaining({ vehicleId: 'SH-01', resourceType: 'edge', resourceId: 'B-C' }));
    expect(state.reservations).toContainEqual(expect.objectContaining({ vehicleId: 'SH-01', resourceType: 'node', resourceId: 'C' }));
  });

  it('waits upstream when a no-stop movement cannot reserve its downstream exit buffer', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'B-C', noParking: true }
        ],
        zones: [
          { id: 'zone-node-b', type: 'intersection', nodeIds: ['B'], edgeIds: [], noStop: true, noParking: true, capacity: 1, conflictGroup: 'zone-node-b' }
        ]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-02', ['C']);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C']);

    const state = sim.step(0.2);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');

    expect(vehicle).toMatchObject({
      state: 'waiting-blocked',
      currentNodeId: 'A',
      currentEdgeId: null,
      targetNodeId: 'B',
      waitReason: 'no-stop-continuation-blocked'
    });
    expect(state.reservations).not.toContainEqual(expect.objectContaining({ vehicleId: 'SH-01', resourceId: 'zone-node-b' }));
  });

  it('defers outbound work instead of creating phantom pallets when contiguous lane-fill storage is empty', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      durationSec: 10,
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 3
      }
    }));

    sim.start();
    sim.step(1);
    const state = sim.getState();

    expect(state.tasks).toHaveLength(0);
    expect(state.loads).toHaveLength(0);
    expect(state.kpis.blockedTimeByReasonSec['storage-empty']).toBeGreaterThan(0);
    expect(sim.getEventLog().some((entry) => entry.eventType === 'task-deferred' && entry.reason === 'storage-empty')).toBe(true);
  });

  it('spreads inbound work across FIFO rows while preserving contiguous fill inside each row', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      liftMode: 'all-inbound',
      durationSec: 120,
      vehicles: { count: 8 },
      taskGeneration: {
        inboundRatePerHour: 720,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 8
      },
      physicsParams: {
        emptySpeedMps: 6,
        loadedSpeedMps: 5,
        accelerationMps2: 6,
        switchDirectionSec: 0,
        liftTimeSec: 0.1,
        lowerTimeSec: 0.1,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.1
      }
    }));

    sim.start();
    for (let index = 0; index < 300 && sim.getState().tasks.filter((task) => task.kind === 'inbound').length < 8; index += 1) {
      sim.step(0.2);
    }
    const state = sim.getState();
    const inboundTasks = state.tasks.filter((task) => task.kind === 'inbound');
    const storageOccupancy = sim.getDebugState().storageNodeOccupancy;

    expect(inboundTasks).toHaveLength(8);
    expect(state.kpis.deadlockCount).toBe(0);
    expect(inboundTasks.slice(0, 8).map((task) => task.dropoffNodeId)).toEqual([
      'storage-r01-c01',
      'storage-r02-c01',
      'storage-r03-c01',
      'storage-r04-c01',
      'storage-r05-c01',
      'storage-r06-c01',
      'storage-r07-c01',
      'storage-r08-c01'
    ]);
    expect(new Set(inboundTasks.slice(0, 8).map((task) => task.loadId)).size).toBe(8);
    const storedLoadByNode = new Map(storageOccupancy.map((occupancy) => [occupancy.nodeId, occupancy.loadId]));
    for (const task of inboundTasks.slice(0, 8)) {
      expect(storedLoadByNode.get(task.dropoffNodeId)).toBe(task.loadId);
    }
  });

  it('routes through stored pallet cells on the same horizontal storage row', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      durationSec: 120,
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['left-row-01']);
    sim.setVehicleRouteForTest('SH-02', ['parking-a', 'main-north-04']);
    sim.addLoadForTest({ id: 'load-pass-through', state: 'stored', nodeId: 'storage-r01-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'load-pick', state: 'stored', nodeId: 'storage-r01-c02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-c02',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'outbound-lift-top-01',
      loadId: 'load-pick',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    sim.step(0.2);
    const assignedVehicle = sim.getState().vehicles.find((vehicle) => vehicle.taskId === 'outbound-c02');
    const route = assignedVehicle?.routeNodeIds ?? [];

    expect(assignedVehicle?.id).toBe('SH-01');
    expect(route.indexOf('storage-r01-c01')).toBeGreaterThan(route.indexOf('left-row-01'));
    expect(route.indexOf('storage-r01-c02')).toBeGreaterThan(route.indexOf('storage-r01-c01'));
    expect(crossRowStorageHops(route)).toEqual([]);
  });

  it('uses the outfeed-side storage exit when an idle shuttle is parked under a load', () => {
    const scenario = createDefaultShuttleScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    scenario.layout.nodes = scenario.layout.nodes.map((node) =>
      node.type === 'parking' ? { ...node, noParking: true } : node
    );
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['storage-r16-c01']);

    expect(sim.getState().vehicles[0]?.currentNodeId).toBe('storage-r16-c01');
    sim.start();
    sim.step(0.2);
    const assigned = sim.getEventLog().find((event) => event.eventType === 'task-assigned');
    const route = String(assigned?.details.route ?? '').split('>');

    expect(route.slice(0, 2)).toEqual(['storage-r16-c01', 'left-row-16']);
  });

  it('routes empty inbound pickup shuttles out through the left side instead of counterflowing on the right infeed aisle', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['storage-r03-c24']);
    sim.addLoadForTest({ id: 'manual-inbound-load', state: 'waiting', nodeId: 'inbound-lift-top-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'manual-inbound',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-02',
      dropoffNodeId: 'storage-r04-c01',
      loadId: 'manual-inbound-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    sim.step(0.2);
    const route = sim.getState().vehicles[0]?.routeNodeIds ?? [];
    const pickupIndex = route.indexOf('inbound-lift-top-02');

    expect(route.slice(0, 2)).toEqual(['storage-r03-c24', 'storage-r03-c23']);
    expect(route.indexOf('left-row-03')).toBeGreaterThan(route.indexOf('storage-r03-c01'));
    expect(route.slice(0, pickupIndex).includes('right-row-03')).toBe(false);
  });

  it('ends inbound work at the target storage cell without a post-dropoff route tail', () => {
    const scenario = createDefaultShuttleScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.start();
    sim.step(0.2);
    const assigned = sim.getEventLog().find((event) => event.eventType === 'task-assigned');
    const route = String(assigned?.details.route ?? '').split('>');
    const dropoffIndex = route.indexOf('storage-r01-c01');

    expect(dropoffIndex).toBeGreaterThan(0);
    expect(dropoffIndex).toBe(route.length - 1);
    expect(route.slice(route.indexOf('right-row-01') + 1).flatMap((nodeId) => storageColumn(nodeId) ?? [])).toEqual(
      expect.arrayContaining([1])
    );
  });

  it('recomputes a direct loaded shortest route after pickup', () => {
    const scenario = createDefaultShuttleScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      physicsParams: {
        liftTimeSec: 0.05,
        lowerTimeSec: 0.05,
        switchDirectionSec: 0
      }
    });
    const sim = new ShuttleSimCore(scenario);

    sim.start();
    let state = sim.getState();
    for (let index = 0; index < 300 && !state.vehicles[0]?.loaded; index += 1) {
      state = sim.step(0.2);
    }

    const vehicle = state.vehicles[0]!;
    const task = state.tasks.find((candidate) => candidate.id === vehicle.taskId)!;
    const loadedRouteEvent = sim.getEventLog().find((event) => event.eventType === 'route-replanned' && event.reason === 'loaded-shortest-path');
    const loadedRoute = String(loadedRouteEvent?.details.route ?? '').split('>');

    expect(vehicle.loaded).toBe(true);
    expect(vehicle.routeIndex).toBe(0);
    expect(vehicle.routeNodeIds[0]).toBe(task.pickupNodeId);
    expect(vehicle.routeNodeIds.at(-1)).toBe(task.dropoffNodeId);
    expect(loadedRoute[0]).toBe(task.pickupNodeId);
    expect(loadedRoute.at(-1)).toBe(task.dropoffNodeId);
    expect(vehicle.routeNodeIds.slice(1, -1)).not.toContain(task.pickupNodeId);
  });

  it('agent-simple routes loaded dropoffs from the infeed side without crossing stored pallets', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['main-north-03']);
    sim.addLoadForTest({ id: 'stored-r05-c01', state: 'stored', nodeId: 'storage-r05-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'stored-r05-c02', state: 'stored', nodeId: 'storage-r05-c02', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'carried-r05-c03', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'drop-r05-c03',
      kind: 'inbound',
      state: 'assigned',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-bottom-02',
      dropoffNodeId: 'storage-r05-c03',
      loadId: 'carried-r05-c03',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'drop-r05-c03', true);

    const state = sim.step(0.2);
    const vehicle = state.vehicles[0]!;
    const route = vehicle.plannedRouteNodeIds;
    const actualRoute = vehicle.routeNodeIds;
    const dropoffIndex = route.indexOf('storage-r05-c03');
    const actualDropoffIndex = actualRoute.indexOf('storage-r05-c03');

    expect(route).toContain('right-row-05');
    expect(route).toContain('storage-r05-c03');
    expect(dropoffIndex).toBeGreaterThan(0);
    expect(actualDropoffIndex).toBeGreaterThan(0);
    expect(route.slice(0, dropoffIndex)).not.toContain('storage-r05-c01');
    expect(route.slice(0, dropoffIndex)).not.toContain('storage-r05-c02');
    expect(actualRoute.slice(0, actualDropoffIndex)).not.toContain('storage-r05-c01');
    expect(actualRoute.slice(0, actualDropoffIndex)).not.toContain('storage-r05-c02');
    expectNoLoadedStorageTransitThroughStoredLoads(state);
  });

  it('dispatches an unloaded inbound-only shuttle from dropoff to lift-near storage standby', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 120,
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      physicsParams: {
        emptySpeedMps: 6,
        loadedSpeedMps: 5,
        accelerationMps2: 6,
        switchDirectionSec: 0,
        liftTimeSec: 0.05,
        lowerTimeSec: 0.05,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();
    let state = sim.getState();
    for (let elapsedSec = 0; elapsedSec < 120 && state.kpis.completedInbound < 1; elapsedSec = Number((elapsedSec + 0.2).toFixed(6))) {
      state = sim.step(0.2);
    }
    const vehicle = state.vehicles[0]!;
    const standbyEvent = sim.getEventLog().find((event) => event.eventType === 'vehicle-standby-dispatched');
    const standbyRoute = String(standbyEvent?.details.route ?? '').split('>');
    const standbyTarget = standbyRoute.at(-1);

    expect(state.kpis.completedInbound).toBe(1);
    expect(vehicle.state).toBe('assigned');
    expect(vehicle.loaded).toBe(false);
    expect(vehicle.taskId).toBeNull();
    expect(standbyEvent).toBeDefined();
    expect(standbyTarget).toMatch(/^storage-r\d+-c\d+$/);
    expect(standbyTarget).not.toBe('storage-r01-c01');
    expect(vehicle.routeNodeIds.at(-1)).toBe(standbyTarget);
    expect(crossRowStorageHops(vehicle.routeNodeIds)).toEqual([]);
    expect(state.loads.find((load) => load.state === 'stored' && load.nodeId === 'storage-r01-c01')).toMatchObject({
      state: 'stored',
      nodeId: 'storage-r01-c01',
      vehicleId: null
    });
    expect(sim.getDebugState().currentNodeOccupancy).toContainEqual({
      nodeId: 'storage-r01-c01',
      vehicleId: 'SH-01'
    });
  });

  it('preempts an empty inbound-standby shuttle at the next control node when a lift calls it', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['storage-r01-c24', 'right-row-01', 'main-north-05']);
    sim.addLoadForTest({ id: 'manual-call-load', state: 'waiting', nodeId: 'inbound-lift-top-02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'manual-call',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-02',
      dropoffNodeId: 'storage-r02-c01',
      loadId: 'manual-call-load',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    sim.step(0.2);
    const vehicle = sim.getState().vehicles[0]!;

    expect(vehicle.taskId).toBe('manual-call');
    expect(vehicle.routeNodeIds).toContain('inbound-lift-top-02');
    expect(vehicle.routeNodeIds.at(-1)).toBe('storage-r02-c01');
  });

  it('routes cross-row storage moves through side aisles instead of vertical storage hops', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      durationSec: 120,
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['storage-r01-c01']);
    sim.setVehicleRouteForTest('SH-02', ['parking-a', 'main-north-04']);
    sim.addLoadForTest({ id: 'load-r02-c01', state: 'stored', nodeId: 'storage-r02-c01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-r02-c01',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r02-c01',
      dropoffNodeId: 'outbound-lift-top-01',
      loadId: 'load-r02-c01',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    sim.step(0.2);
    const assignedVehicle = sim.getState().vehicles.find((vehicle) => vehicle.taskId === 'outbound-r02-c01');

    expect(assignedVehicle?.id).toBe('SH-01');
    expect(assignedVehicle?.routeNodeIds).toEqual(expect.arrayContaining(['left-row-01', 'left-row-02', 'storage-r02-c01']));
    expect(crossRowStorageHops(assignedVehicle?.routeNodeIds ?? [])).toEqual([]);
    expect(storageRowsInRoute(assignedVehicle?.routeNodeIds ?? [])).toEqual(['01', '02']);
  });

  it('assigns executable work to the nearest idle shuttle resource', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'parking-a', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'parking-b', type: 'parking', x: 9, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'inbound-lift-test', type: 'lift-blackbox', liftKind: 'inbound', x: 10, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'right-row-01', type: 'intersection', x: 11, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'storage-r01-c01', type: 'storage', x: 12, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'left-row-01', type: 'intersection', x: 13, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'parking-a-inbound-lift-test', from: 'parking-a', to: 'inbound-lift-test', lengthM: 10, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'far-resource', noParking: true },
          { id: 'parking-b-inbound-lift-test', from: 'parking-b', to: 'inbound-lift-test', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'near-resource', noParking: true },
          { id: 'inbound-lift-test-right-row-01', from: 'inbound-lift-test', to: 'right-row-01', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'lift-row', noParking: true },
          { id: 'right-row-01-storage-r01-c01', from: 'right-row-01', to: 'storage-r01-c01', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'fifo-lane-01', noParking: true },
          { id: 'storage-r01-c01-left-row-01', from: 'storage-r01-c01', to: 'left-row-01', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'fifo-lane-01', noParking: true },
          { id: 'left-row-01-parking-b', from: 'left-row-01', to: 'parking-b', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'left-exit', noParking: true }
        ],
        zones: []
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.addLoadForTest({ id: 'load-near', state: 'waiting', nodeId: 'inbound-lift-test', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'task-near',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-test',
      dropoffNodeId: 'storage-r01-c01',
      loadId: 'load-near',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    sim.step(0.2);

    expect(sim.getState().tasks.find((task) => task.id === 'task-near')).toMatchObject({
      state: 'assigned',
      vehicleId: 'SH-02'
    });
    expect(sim.getEventLog().find((entry) => entry.eventType === 'task-assigned' && entry.taskId === 'task-near')?.reason).toBe('nearest-available');
  });

  it('does not treat future queued inbound slots as physical storage obstacles', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      durationSec: 120,
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 10
      }
    }));
    sim.addLoadForTest({ id: 'load-r06-c03', state: 'waiting', nodeId: 'inbound-lift-top-01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'load-r06-c04', state: 'waiting', nodeId: 'inbound-lift-top-01', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'task-r06-c03',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-01',
      dropoffNodeId: 'storage-r06-c03',
      loadId: 'load-r06-c03',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });
    sim.addTaskForTest({
      id: 'task-r06-c04',
      kind: 'inbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-01',
      dropoffNodeId: 'storage-r06-c04',
      loadId: 'load-r06-c04',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    sim.step(0.2);
    const assignedVehicle = sim.getState().vehicles.find((vehicle) => vehicle.taskId === 'task-r06-c03');

    expect(assignedVehicle?.routeNodeIds).toEqual(expect.arrayContaining(['right-row-06', 'storage-r06-c04', 'storage-r06-c03']));
  });

  it('routes loaded agent-simple inbound shuttles into storage rows from the right side', () => {
    const scenario = createDefaultShuttleScenario({
      id: 'agent-simple-right-side-inbound',
      liftMode: 'all-inbound',
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 2
      },
      trafficPolicy: {
        controllerMode: 'agent-simple'
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['inbound-lift-top-01']);
    sim.addLoadForTest({ id: 'manual-agent-load', state: 'carried', nodeId: null, vehicleId: 'SH-01', weightKg: 100 });
    sim.addTaskForTest({
      id: 'manual-agent-inbound',
      kind: 'inbound',
      state: 'in-progress',
      createdAtSec: 0,
      assignedAtSec: 0,
      startedAtSec: 0,
      completedAtSec: null,
      pickupNodeId: 'inbound-lift-top-01',
      dropoffNodeId: 'storage-r01-c01',
      loadId: 'manual-agent-load',
      vehicleId: 'SH-01',
      replanCount: 0,
      waitReason: null
    });
    sim.setVehicleTaskForTest('SH-01', 'manual-agent-inbound', true);
    sim.start();
    const state = sim.step(0.2);

    expectLoadedInboundRoutesApproachDropoffFromRight(state);
    const vehicle = state.vehicles.find((candidate) => candidate.id === 'SH-01');
    expect(vehicle?.routeNodeIds).toContain('right-row-01');
    expect(vehicle?.routeNodeIds).not.toContain('left-row-01');
    expect(vehicle?.plannedRouteNodeIds).toContain('right-row-01');
    expect(vehicle?.plannedRouteNodeIds).not.toContain('left-row-01');
    expectNoTrafficSafetyFailures(state);
  });

  it('drains multiple pallets from the same storage row without hidden compaction', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      durationSec: 300,
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 10
      },
      physicsParams: {
        emptySpeedMps: 6,
        loadedSpeedMps: 5,
        accelerationMps2: 6,
        switchDirectionSec: 0,
        liftTimeSec: 0.1,
        lowerTimeSec: 0.1,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.1
      }
    }));
    sim.addLoadForTest({ id: 'load-a', state: 'stored', nodeId: 'storage-r01-c01', vehicleId: null, weightKg: 100 });
    sim.addLoadForTest({ id: 'load-b', state: 'stored', nodeId: 'storage-r01-c02', vehicleId: null, weightKg: 100 });
    sim.addTaskForTest({
      id: 'outbound-a',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: 0,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c01',
      dropoffNodeId: 'outbound-lift-top-01',
      loadId: 'load-a',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    for (let index = 0; index < 2000 && sim.getState().tasks.find((task) => task.id === 'outbound-a')?.state !== 'completed'; index += 1) {
      sim.step(0.2);
    }

    expect(sim.getState().tasks.find((task) => task.id === 'outbound-a')?.state).toBe('completed');
    expect(sim.getDebugState().storageNodeOccupancy).toEqual([{ nodeId: 'storage-r01-c02', loadId: 'load-b' }]);

    sim.addTaskForTest({
      id: 'outbound-b',
      kind: 'outbound',
      state: 'queued',
      createdAtSec: sim.getState().simTimeSec,
      assignedAtSec: null,
      startedAtSec: null,
      completedAtSec: null,
      pickupNodeId: 'storage-r01-c02',
      dropoffNodeId: 'outbound-lift-top-01',
      loadId: 'load-b',
      vehicleId: null,
      replanCount: 0,
      waitReason: null
    });

    for (let index = 0; index < 2000 && sim.getState().tasks.find((task) => task.id === 'outbound-b')?.state !== 'completed'; index += 1) {
      sim.step(0.2);
    }

    expect(sim.getState().tasks.find((task) => task.id === 'outbound-b')?.state).toBe('completed');
    expect(sim.getDebugState().storageNodeOccupancy).toEqual([]);
    expect(sim.getState().loads.filter((load) => load.state === 'delivered').map((load) => load.id).sort()).toEqual(['load-a', 'load-b']);
    expect(sim.getState().loads.filter((load) => load.state === 'delivered').every((load) => load.nodeId === null && load.vehicleId === null)).toBe(true);
  });

  it('removes delivered top-lift outbound loads from the yellow-grid dropoff node', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 2 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0,
        arrivalDistribution: 'deterministic',
        maxTasks: 2,
        initialOutboundFullColumns: 1
      },
      physicsParams: {
        emptySpeedMps: 6,
        loadedSpeedMps: 5,
        accelerationMps2: 6,
        switchDirectionSec: 0,
        liftTimeSec: 0.01,
        lowerTimeSec: 0.01,
        loadedClearanceM: 0.1,
        reservationClearanceSec: 0.05
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();

    let state = sim.getState();
    for (let index = 0; index < 2000 && state.kpis.completedOutbound < 1; index += 1) {
      state = sim.step(0.2);
    }

    const deliveredLoads = state.loads.filter((load) => load.state === 'delivered');
    expect(state.kpis.completedOutbound).toBeGreaterThan(0);
    expect(deliveredLoads).toHaveLength(1);
    expect(deliveredLoads[0]).toMatchObject({ nodeId: null, vehicleId: null });
    expect(state.loads.some((load) => load.state === 'delivered' && /^column-bottom-a-/.test(load.nodeId ?? ''))).toBe(false);
  });

  it('uses lift-side yellow edge pickup stops and yellow-grid outbound dropoff stops for top-lift tasks', () => {
    const scenario = createInboundOutboundDemoScenario({
      vehicles: { count: 4 },
      taskGeneration: {
        inboundRatePerHour: 3600,
        outboundRatePerHour: 3600,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 4,
        initialOutboundFullColumns: 1
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.start();

    let state = sim.getState();
    for (let index = 0; index < 20 && !state.tasks.some((task) => task.kind === 'outbound'); index += 1) {
      state = sim.step(0.2);
    }
    const inboundTask = state.tasks.find((task) => task.kind === 'inbound');
    const outboundTask = state.tasks.find((task) => task.kind === 'outbound');

    expect(inboundTask?.pickupNodeId).toBe('column-top-a-c08');
    expect(inboundTask?.pickupNodeId).not.toContain('service-exit');
    expect(outboundTask?.dropoffNodeId).toBe('column-bottom-b-c08');
    expect(outboundTask?.dropoffNodeId).not.toContain('service-exit');
  });

  it('defers inbound work when contiguous lane-fill storage cells are stored or already reserved', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 20,
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 10
      }
    });
    const sim = new ShuttleSimCore(scenario);
    for (const node of scenario.layout.nodes.filter((candidate) => candidate.type === 'storage')) {
      sim.addLoadForTest({
        id: `load-${node.id}`,
        state: 'stored',
        nodeId: node.id,
        vehicleId: null,
        weightKg: 100
      });
    }

    sim.start();
    for (let index = 0; index < 10 && !(sim.getState().kpis.blockedTimeByReasonSec['storage-full'] > 0); index += 1) {
      sim.step(0.2);
    }
    const state = sim.getState();

    expect(state.tasks).toHaveLength(0);
    expect(sim.getDebugState().storageNodeOccupancy).toHaveLength(384);
    expect(state.kpis.blockedTimeByReasonSec['storage-full']).toBeGreaterThan(0);
    expect(sim.getEventLog().some((entry) => entry.eventType === 'task-deferred' && entry.reason === 'storage-full')).toBe(true);
  });

  it('keeps a near-full contiguous lane-fill store bounded without over-allocating slots', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 20,
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 3
      }
    });
    const sim = new ShuttleSimCore(scenario);
    const finalSlotNodeId = 'storage-r15-c24';
    const occupiedStorageNodeIds = scenario.layout.nodes
      .filter((node) => node.type === 'storage' && node.id !== finalSlotNodeId)
      .map((node) => node.id);
    addStoredLoads(sim, occupiedStorageNodeIds);

    sim.start();
    for (let index = 0; index < 20 && !(sim.getState().kpis.blockedTimeByReasonSec['storage-full'] > 0); index += 1) {
      sim.step(0.2);
    }
    const state = sim.getState();

    expectNoTrafficSafetyFailures(state);
    expect(state.tasks).toHaveLength(1);
    expect(state.tasks[0]?.dropoffNodeId).toBe(finalSlotNodeId);
    expect(sim.getDebugState().storageNodeOccupancy).toHaveLength(384);
    expect(state.kpis.blockedTimeByReasonSec['storage-full']).toBeGreaterThan(0);
  });

  it('attributes queued inbound pressure to vehicle availability when every shuttle is already busy', () => {
    const scenario = createDefaultShuttleScenario({
      durationSec: 60,
      vehicles: {
        count: 1,
        emptySpeedMps: 1,
        loadedSpeedMps: 0.8,
        accelerationMps2: 1,
        liftTimeSec: 0.05,
        lowerTimeSec: 0.05
      },
      taskGeneration: {
        inboundRatePerHour: 7200,
        outboundRatePerHour: 0,
        inboundOutboundMix: 1,
        arrivalDistribution: 'deterministic',
        maxTasks: 4
      },
      trafficPolicy: {
        liftApproachCapacity: 1
      }
    });
    const state = runFor(new ShuttleSimCore(scenario), 5);
    const queuedTasks = state.tasks.filter((task) => task.state === 'queued');

    expect(state.kpis.activeTasks).toBe(1);
    expect(queuedTasks.length).toBeGreaterThan(0);
    expect(queuedTasks.every((task) => task.waitReason === 'vehicle-unavailable')).toBe(true);
    expect(state.kpis.blockedTimeByReasonSec['vehicle-unavailable']).toBeGreaterThan(0);
  });

  it('accepts dashboard-style parameter updates through JSON pointers', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario());
    const result = sim.setParam('/physicsParams/loadedSpeedMps', 1.25);

    expect(result.accepted).toBe(true);
    expect(sim.getScenario().physicsParams.loadedSpeedMps).toBe(1.25);
    expect(sim.getEventLog().some((entry) => entry.eventType === 'param-updated')).toBe(true);
  });

  it('keeps explicit node occupancy ownership while vehicles wait and move', () => {
    const sim = new ShuttleSimCore(testScenario({}));

    expect(sim.getDebugState().currentNodeOccupancy).toEqual([
      { nodeId: 'A', vehicleId: 'SH-01' },
      { nodeId: 'B', vehicleId: 'SH-02' }
    ]);

    sim.setVehicleRouteForTest('SH-01', ['A', 'B']);
    sim.step(0.2);

    expect(sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01')?.waitReason).toBe('node-occupied');
    expect(sim.getDebugState().currentNodeOccupancy).toEqual([
      { nodeId: 'A', vehicleId: 'SH-01' },
      { nodeId: 'B', vehicleId: 'SH-02' }
    ]);
  });

  it('aligns four-way shuttle yaw to the active route heading', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'P', type: 'parking', x: -4, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'aisle', x: 4, y: 0, z: 4, noStop: false, noParking: true, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'B-C', noParking: true }
        ],
        zones: []
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C']);

    const observedYaw: Array<number | undefined> = [];
    for (let index = 0; index < 60; index += 1) {
      sim.step(0.2);
      observedYaw.push(sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01')?.yaw);
    }

    expect(observedYaw.every((yaw) => yaw !== undefined && Number.isFinite(yaw))).toBe(true);
    expect(observedYaw.some((yaw) => yaw !== undefined && Math.abs(yaw) < 1e-6)).toBe(true);
    expect(observedYaw.some((yaw) => yaw !== undefined && Math.abs(yaw + Math.PI / 2) < 1e-6)).toBe(true);
    const finalVehicle = sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01');
    expect(finalVehicle).toMatchObject({
      currentNodeId: 'C',
      x: 4,
      z: 4
    });
    expect(finalVehicle?.yaw ?? 0).toBeCloseTo(-Math.PI / 2, 6);
  });

  it('cruises through same-axis cell chains instead of stopping at every grid node', () => {
    const scenario = testScenario({
      durationSec: 20,
      vehicles: {
        count: 1,
        lengthM: 1,
        widthM: 1,
        heightM: 0.2,
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        maxLoadKg: 1000,
        safetyRadiusM: 0.1,
        batteryEnabled: false,
        initialSoc: 1
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      },
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 1.25, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'aisle', x: 2.5, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'D', type: 'parking', x: 3.75, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 1.25, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'row-a', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 1.25, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'row-b', noParking: true },
          { id: 'C-D', from: 'C', to: 'D', lengthM: 1.25, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'row-c', noParking: true }
        ],
        zones: []
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C', 'D']);
    let arrivalTimeSec = Number.POSITIVE_INFINITY;
    for (let index = 0; index < 80; index += 1) {
      sim.step(0.1);
      if (sim.getState().vehicles[0]?.currentNodeId === 'D') {
        arrivalTimeSec = sim.getState().simTimeSec;
        break;
      }
    }

    const fullStopTravelSec = calculateTravelTimeSec(1.25, 2, 1.2) * 3;
    expect(arrivalTimeSec).toBeLessThan(fullStopTravelSec - 0.8);
    expect(sim.getEventLog().some((entry) => entry.eventType === 'reservation-created' && entry.details.motionMode === 'cruise')).toBe(true);
    expectNoTrafficSafetyFailures(sim.getState());
  });

  it('holds a same-lane follower until the leading shuttle clears minimum headway', () => {
    const base = testScenario({});
    const scenario = testScenario({
      vehicles: { ...base.vehicles, count: 2 },
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'parking', x: 1.25, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 2.5, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 1.25, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'row-a', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 1.25, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'row-b', noParking: true }
        ],
        zones: []
      },
      physicsParams: {
        emptySpeedMps: 2.6,
        loadedSpeedMps: 2.2,
        accelerationMps2: 2,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['B', 'C']);
    sim.setVehicleRouteForTest('SH-02', ['A', 'B']);

    sim.step(0.2);
    const follower = sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(follower?.state).toBe('waiting-blocked');
    expect(['min-separation', 'node-clearing']).toContain(follower?.waitReason);
    expect(follower?.blockingVehicleId).toBe('SH-01');
  });

  it('can disable collision avoidance for diagnostic A/B runs while keeping physical audits active', () => {
    const base = testScenario({});
    const scenario = testScenario({
      vehicles: { ...base.vehicles, count: 2 },
      trafficPolicy: { ...base.trafficPolicy, collisionAvoidanceEnabled: false },
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'parking', x: 1, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 2, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true },
          { id: 'C-B', from: 'C', to: 'B', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'C-B', noParking: true }
        ],
        zones: []
      },
      physicsParams: {
        emptySpeedMps: 1,
        loadedSpeedMps: 1,
        accelerationMps2: 4,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B']);
    sim.setVehicleRouteForTest('SH-02', ['C', 'B']);

    const state = runFor(sim, 2, 0.1);

    expect(state.traffic.collisionAvoidanceEnabled).toBe(false);
    expect(state.vehicles.map((vehicle) => vehicle.currentNodeId)).toEqual(['B', 'B']);
    expect(state.traffic.minVehicleSeparationM).toBe(0);
    expect(state.traffic.physicalViolationCount).toBeGreaterThan(0);
  });

  it('accepts collision avoidance toggles through dashboard-style parameter updates', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario());
    const result = sim.setParam('/trafficPolicy/collisionAvoidanceEnabled', false);

    expect(result.accepted).toBe(true);
    expect(sim.getScenario().trafficPolicy.collisionAvoidanceEnabled).toBe(false);
    expect(sim.getState().traffic.collisionAvoidanceEnabled).toBe(false);
  });

  it('requires reset before changing collision avoidance after the run has advanced', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario());
    sim.step(0.1);

    const result = sim.setParam('/trafficPolicy/collisionAvoidanceEnabled', false);

    expect(result).toMatchObject({
      accepted: false,
      path: '/trafficPolicy/collisionAvoidanceEnabled',
      previousValue: true,
      value: false,
      reason: 'reset-required'
    });
    expect(sim.getScenario().trafficPolicy.collisionAvoidanceEnabled).toBe(true);
    expect(sim.getState().traffic.collisionAvoidanceEnabled).toBe(true);
  });

  it('uses local same-row storage grants instead of preauthorizing the whole row', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      }
    }));
    sim.setVehicleRouteForTest('SH-01', [
      'storage-r01-c08',
      'storage-r01-c07',
      'storage-r01-c06',
      'storage-r01-c05',
      'storage-r01-c04',
      'storage-r01-c03',
      'storage-r01-c02',
      'storage-r01-c01'
    ]);

    sim.step(0.2);
    const horizonEvent = sim.getEventLog().find((entry) => entry.eventType === 'reservation-created' && entry.reason === 'route-horizon');

    expect(horizonEvent?.details.horizonLegCount).toBe(1);
    expect(
      sim.getState().reservations.filter((reservation) =>
        reservation.vehicleId === 'SH-01' &&
        reservation.resourceType === 'edge' &&
        reservation.resourceId.startsWith('storage-r01-')
      ).length
    ).toBe(1);

    for (let index = 0; index < 80 && sim.getState().vehicles[0]?.currentNodeId !== 'storage-r01-c01'; index += 1) {
      sim.step(0.1);
    }

    expect(sim.getState().vehicles[0]?.currentNodeId).toBe('storage-r01-c01');
    expectNoTrafficSafetyFailures(sim.getState());
  });

  it('uses a local row-entry storage grant from the infeed side', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({
      vehicles: { count: 1 },
      taskGeneration: {
        inboundRatePerHour: 0,
        outboundRatePerHour: 0,
        inboundOutboundMix: 0.5,
        arrivalDistribution: 'deterministic',
        maxTasks: 1
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1.5,
        accelerationMps2: 1.2,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.05
      }
    }));
    sim.setVehicleRouteForTest('SH-01', [
      'right-row-01',
      'storage-r01-c24',
      'storage-r01-c23',
      'storage-r01-c22',
      'storage-r01-c21',
      'storage-r01-c20',
      'storage-r01-c19',
      'storage-r01-c18'
    ]);

    sim.step(0.2);
    const horizonEvent = sim.getEventLog().find((entry) => entry.eventType === 'reservation-created' && entry.reason === 'route-horizon');

    expect(horizonEvent?.details.horizonLegCount).toBe(1);
    expect(
      sim.getState().reservations.filter((reservation) =>
        reservation.vehicleId === 'SH-01' &&
        reservation.resourceType === 'edge' &&
        (
          reservation.resourceId === 'right-row-01-storage-r01-c24' ||
          reservation.resourceId.startsWith('storage-r01-')
        )
      ).length
    ).toBe(1);
    expectNoTrafficSafetyFailures(sim.getState());
  });

  it('keeps reservation horizons out of main aisles and lift approaches to avoid artificial wait-for cycles', () => {
    const base = testScenario({});
    const sim = new ShuttleSimCore(testScenario({
      vehicles: { ...base.vehicles, count: 1 },
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 1, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'aisle', x: 2, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'D', type: 'parking', x: 3, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'main-a', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'main-b', noParking: true },
          { id: 'C-D', from: 'C', to: 'D', lengthM: 1, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'main-c', noParking: true }
        ],
        zones: []
      }
    }));
    sim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C', 'D']);
    sim.step(0.2);
    const horizonEvent = sim.getEventLog().find((entry) => entry.eventType === 'reservation-created' && entry.reason === 'route-horizon');

    expect(horizonEvent?.details.horizonLegCount).toBe(1);
    expect(sim.getState().reservations.filter((reservation) => reservation.vehicleId === 'SH-01' && reservation.resourceType === 'edge')).toHaveLength(1);
    expectNoTrafficSafetyFailures(sim.getState());
  });

  it('does not add direction-switch dwell to the default four-way shuttle demo', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario());
    sim.runToEnd(90);

    expect(sim.getState().vehicles.every((vehicle) => Number.isFinite(vehicle.yaw))).toBe(true);
    expect(sim.getEventLog().some((entry) => entry.eventType === 'direction-switch-started')).toBe(false);
  });

  it('adds direction-switch time to orthogonal moves without holding the shuttle on the turn node', () => {
    const scenario = testScenario({
      durationSec: 40,
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1,
        accelerationMps2: 2,
        switchDirectionSec: 1,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.2
      },
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'P', type: 'parking', x: -4, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'aisle', x: 4, y: 0, z: 4, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'D', type: 'aisle', x: 8, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true },
          { id: 'B-C', from: 'B', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'B-C', noParking: true },
          { id: 'B-D', from: 'B', to: 'D', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'B-D', noParking: true }
        ],
        zones: []
      }
    });
    const turningSim = new ShuttleSimCore(scenario);
    turningSim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C']);

    for (let index = 0; index < 20; index += 1) {
      turningSim.step(0.2);
    }

    const turningVehicle = turningSim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01');
    expect(turningVehicle).toMatchObject({
      currentNodeId: 'B',
      currentEdgeId: 'B-C'
    });
    expect(turningVehicle?.yaw ?? 0).toBeCloseTo(-Math.PI / 2, 6);
    expect(turningVehicle?.phaseRemainingSec ?? 0).toBe(0);
    expect(turningVehicle?.legTravelSec ?? 0).toBeGreaterThan(3);
    expect(turningSim.getEventLog().some((entry) => entry.eventType === 'direction-switch-started')).toBe(false);

    const noTurnCostScenario = {
      ...scenario,
      physicsParams: {
        ...scenario.physicsParams,
        switchDirectionSec: 0
      }
    };
    const noTurnCostSim = new ShuttleSimCore(noTurnCostScenario);
    noTurnCostSim.setVehicleRouteForTest('SH-01', ['A', 'B', 'C']);
    for (let index = 0; index < 20; index += 1) {
      noTurnCostSim.step(0.2);
    }
    const noTurnCostVehicle = noTurnCostSim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01');
    expect(turningVehicle?.z ?? 0).toBeLessThan(noTurnCostVehicle?.z ?? 0);

    const straightSim = new ShuttleSimCore(scenario);
    straightSim.setVehicleRouteForTest('SH-01', ['A', 'B', 'D']);
    for (let index = 0; index < 28; index += 1) {
      straightSim.step(0.2);
    }
    expect(straightSim.getEventLog().some((entry) => entry.eventType === 'direction-switch-started')).toBe(false);
  });

  it('blocks opposite-direction same-edge movement with an active edge reservation', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true }
        ],
        zones: []
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B']);
    sim.step(0.2);

    expect(sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01')?.currentEdgeId).toBe('A-B');
    expect(sim.getDebugState().currentNodeOccupancy).toEqual([{ nodeId: 'C', vehicleId: 'SH-02' }]);

    sim.setVehicleRouteForTest('SH-02', ['B', 'A']);
    sim.step(0.2);
    const state = sim.getState();
    const secondVehicle = state.vehicles.find((vehicle) => vehicle.id === 'SH-02');

    expect(secondVehicle?.state).toBe('waiting-blocked');
    expect(['edge-reserved', 'node-clearing']).toContain(secondVehicle?.waitReason);
    expect(state.vehicles.filter((vehicle) => vehicle.currentEdgeId === 'A-B')).toHaveLength(1);
    expect(state.traffic.minVehicleSeparationM ?? Infinity).toBeGreaterThanOrEqual(scenario.vehicles.safetyRadiusM * 2);
  });

  it('prevents a target-node occupancy race while the current occupant waits', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'parking', x: 4, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'X', type: 'intersection', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-X', from: 'A', to: 'X', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'west', noParking: true },
          { id: 'X-C', from: 'X', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'east', noParking: true },
          { id: 'B-X', from: 'B', to: 'X', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'north', noParking: true }
        ],
        zones: [{ id: 'zone-x', type: 'intersection', nodeIds: ['X'], edgeIds: ['A-X', 'X-C', 'B-X'], noStop: true, noParking: true, capacity: 1, conflictGroup: 'zone-x' }]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'X', 'C']);
    sim.setVehicleRouteForTest('SH-02', ['B']);
    sim.addReservationForTest({
      resourceType: 'edge',
      resourceId: 'X-C',
      vehicleId: 'external-blocker',
      taskId: null,
      startTimeSec: 0,
      endTimeSec: 60,
      priority: 0,
      conflictGroup: 'east',
      reasonCode: 'test-block'
    });

    for (let index = 0; index < 40; index += 1) {
      sim.step(0.2);
    }

    expect(sim.getDebugState().currentNodeOccupancy).toContainEqual({ nodeId: 'X', vehicleId: 'SH-01' });
    expect(sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01')?.waitReason).toBe('edge-reserved');

    sim.setVehicleRouteForTest('SH-02', ['B', 'X']);
    sim.step(0.2);

    expect(sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-02')?.waitReason).toBe('node-occupied');
    expect(sim.getDebugState().currentNodeOccupancy).toContainEqual({ nodeId: 'X', vehicleId: 'SH-01' });
  });

  it('does not replace active self occupancy grants with partial rolling lookahead grants', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'X', type: 'intersection', x: 4, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'D', type: 'parking', x: 12, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-X', from: 'A', to: 'X', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'west', noParking: true },
          { id: 'X-C', from: 'X', to: 'C', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'east', noParking: true }
        ],
        zones: []
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'X', 'C']);
    sim.setVehicleRouteForTest('SH-02', ['D']);
    sim.addReservationForTest({
      id: 'active-self-edge',
      resourceType: 'edge',
      resourceId: 'A-X',
      vehicleId: 'SH-01',
      taskId: null,
      startTimeSec: 0,
      endTimeSec: 60,
      priority: 0,
      conflictGroup: 'west',
      reasonCode: 'test-active-self'
    });

    const state = sim.step(0.2);
    const selfEdgeReservations = state.reservations.filter(
      (reservation) =>
        reservation.vehicleId === 'SH-01' &&
        reservation.resourceType === 'edge' &&
        reservation.resourceId === 'A-X'
    );

    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-01')?.currentEdgeId).toBe('A-X');
    expect(selfEdgeReservations.map((reservation) => reservation.id)).toEqual(['active-self-edge']);
    expect(selfEdgeReservations[0]).toMatchObject({ startTimeSec: 0, endTimeSec: 60 });
  });

  it('merges active self zone grants by exact resource only', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'parking', x: 4, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'P', type: 'parking', x: 0, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'lane', noParking: true }
        ],
        zones: [
          { id: 'zone-cross', type: 'intersection', nodeIds: [], edgeIds: ['A-B'], noStop: true, noParking: true, capacity: 1, conflictGroup: 'shared-cross' }
        ]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B']);
    sim.setVehicleRouteForTest('SH-02', ['P']);
    sim.addReservationForTest({
      id: 'active-self-zone',
      resourceType: 'zone',
      resourceId: 'zone-cross',
      vehicleId: 'SH-01',
      taskId: null,
      startTimeSec: 0,
      endTimeSec: 60,
      priority: 0,
      conflictGroup: 'shared-cross',
      reasonCode: 'test-active-self-zone'
    });

    const state = sim.step(0.2);
    const selfZoneReservations = state.reservations.filter(
      (reservation) =>
        reservation.vehicleId === 'SH-01' &&
        reservation.resourceType === 'zone' &&
        reservation.resourceId === 'zone-cross'
    );

    expect(selfZoneReservations.map((reservation) => reservation.id)).toEqual(['active-self-zone']);
    expect(selfZoneReservations[0]).toMatchObject({ startTimeSec: 0, endTimeSec: 60 });
  });

  it('merges overlapping candidate grants within one install call', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'X', type: 'intersection', x: 4, y: 0, z: 0, noStop: true, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'P', type: 'parking', x: 0, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-X', from: 'A', to: 'X', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'lane', noParking: true }
        ],
        zones: []
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'X', 'A']);
    sim.setVehicleRouteForTest('SH-02', ['P']);

    const state = sim.step(0.2);
    const selfEdgeReservations = state.reservations.filter(
      (reservation) =>
        reservation.vehicleId === 'SH-01' &&
        reservation.resourceType === 'edge' &&
        reservation.resourceId === 'A-X'
    );

    expect(selfEdgeReservations).toHaveLength(1);
    expect(selfEdgeReservations[0]?.endTimeSec).toBeGreaterThan(4.5);
  });

  it('does not merge same-vehicle grants that only share a conflict group', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'parking', x: 4, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'P', type: 'parking', x: 0, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'lane', noParking: true }
        ],
        zones: [
          { id: 'zone-a', type: 'intersection', nodeIds: [], edgeIds: [], noStop: true, noParking: true, capacity: 1, conflictGroup: 'shared-cross' },
          { id: 'zone-b', type: 'intersection', nodeIds: [], edgeIds: ['A-B'], noStop: true, noParking: true, capacity: 1, conflictGroup: 'shared-cross' }
        ]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B']);
    sim.setVehicleRouteForTest('SH-02', ['P']);
    sim.addReservationForTest({
      id: 'active-self-other-zone',
      resourceType: 'zone',
      resourceId: 'zone-a',
      vehicleId: 'SH-01',
      taskId: null,
      startTimeSec: 0,
      endTimeSec: 60,
      priority: 0,
      conflictGroup: 'shared-cross',
      reasonCode: 'test-active-self-zone'
    });

    const state = sim.step(0.2);
    const selfZoneIds = state.reservations
      .filter((reservation) => reservation.vehicleId === 'SH-01' && reservation.resourceType === 'zone')
      .map((reservation) => reservation.resourceId)
      .sort();

    expect(selfZoneIds).toEqual(['zone-a', 'zone-b']);
  });

  it('rolls back merged self grants by the actually installed reservation id', () => {
    const sim = new ShuttleSimCore(testScenario({}));
    sim.addReservationForTest({
      id: 'active-self-edge',
      resourceType: 'edge',
      resourceId: 'A-B',
      vehicleId: 'SH-01',
      taskId: null,
      startTimeSec: 0,
      endTimeSec: 60,
      priority: 0,
      conflictGroup: 'lane',
      reasonCode: 'test-active-self'
    });
    const candidate: Reservation = {
      id: 'rolling-candidate-edge',
      resourceType: 'edge',
      resourceId: 'A-B',
      vehicleId: 'SH-01',
      taskId: null,
      startTimeSec: 59.8,
      endTimeSec: 61.2,
      priority: 1,
      conflictGroup: 'lane',
      reasonCode: 'edge-reservation'
    };

    const install = sim.installMoveReservationsForTest('SH-01', [candidate]);
    expect(install.installed.map((reservation) => reservation.id)).toEqual(['active-self-edge']);
    expect(sim.getState().reservations).toContainEqual(
      expect.objectContaining({ id: 'active-self-edge', startTimeSec: 0, endTimeSec: 61.2 })
    );

    const rolledBack = sim.rollbackMoveReservationsForTest(install);
    expect(rolledBack.reservations.filter((reservation) => reservation.resourceType === 'edge' && reservation.resourceId === 'A-B')).toEqual([
      expect.objectContaining({ id: 'active-self-edge', startTimeSec: 0, endTimeSec: 60 })
    ]);
  });

  it('serializes crossing paths through a shared zone', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'parking', x: 0, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 6, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'D', type: 'parking', x: 6, y: 0, z: 4, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-C', from: 'A', to: 'C', lengthM: 6, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'south', noParking: true },
          { id: 'B-D', from: 'B', to: 'D', lengthM: 6, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'north', noParking: true }
        ],
        zones: [{ id: 'zone-cross', type: 'intersection', nodeIds: [], edgeIds: ['A-C', 'B-D'], noStop: true, noParking: true, capacity: 1, conflictGroup: 'zone-cross' }]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'C']);
    sim.setVehicleRouteForTest('SH-02', ['B', 'D']);
    sim.step(0.2);

    const state = sim.getState();
    expect(state.reservations.filter((reservation) => reservation.resourceType === 'zone' && reservation.resourceId === 'zone-cross')).toHaveLength(1);
    expect(state.vehicles.find((vehicle) => vehicle.id === 'SH-02')?.waitReason).toBe('zone-reserved');
  });

  it('reserves every matching zone for an edge and target node', () => {
    const scenario = testScenario({
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'edge-a-b', noParking: true }
        ],
        zones: [
          { id: 'zone-edge', type: 'intersection', nodeIds: [], edgeIds: ['A-B'], noStop: true, noParking: true, capacity: 1, conflictGroup: 'zone-edge' },
          { id: 'zone-node', type: 'intersection', nodeIds: ['B'], edgeIds: [], noStop: false, noParking: true, capacity: 1, conflictGroup: 'zone-node' }
        ]
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B']);
    sim.step(0.2);

    const zoneIds = sim
      .getState()
      .reservations.filter((reservation) => reservation.vehicleId === 'SH-01' && reservation.resourceType === 'zone')
      .map((reservation) => reservation.resourceId)
      .sort();
    expect(zoneIds).toEqual(['zone-edge', 'zone-node']);
  });

  it('does not call a linear wait chain a deadlock candidate but reports a wait-for cycle', () => {
    const sim = new ShuttleSimCore(testScenario({}));
    sim.setVehicleWaitingForTest('SH-01', { targetNodeId: 'B', waitReason: 'node-occupied', blockingVehicleId: 'SH-02', waitingSinceSec: 0 });
    expect(sim.getState().traffic.deadlockCandidateVehicleIds).toEqual([]);
    expect(sim.getState().kpis.deadlockCount).toBe(0);

    sim.setVehicleWaitingForTest('SH-02', { targetNodeId: 'A', waitReason: 'node-occupied', blockingVehicleId: 'SH-01', waitingSinceSec: 0 });
    expect(sim.getState().traffic.deadlockCandidateVehicleIds.sort()).toEqual(['SH-01', 'SH-02']);
  });

  it('computes triangular and trapezoidal motion profiles without NaN values', () => {
    expect(calculateTravelTimeSec(1, 2, 1)).toBeCloseTo(2, 6);
    expect(motionProfileAt(1, 1, 2, 1)).toMatchObject({ distanceM: 0.5, speedMps: 1 });

    const trapezoidTravelSec = calculateTravelTimeSec(10, 2, 1);
    expect(trapezoidTravelSec).toBeCloseTo(7, 6);
    expect(motionProfileAt(3, 10, 2, 1)).toMatchObject({ distanceM: 4, speedMps: 2 });

    expect(calculateTravelTimeSec(4, 2, 1)).toBeCloseTo(4, 6);
    expect(motionProfileAt(0, 0, 2, 1)).toEqual({ distanceM: 0, speedMps: 0 });
  });

  it('arrives according to acceleration-aware travel time and loaded speed limits', () => {
    const scenario = testScenario({
      vehicles: {
        count: 2,
        lengthM: 1,
        widthM: 1,
        heightM: 0.2,
        emptySpeedMps: 2,
        loadedSpeedMps: 1,
        accelerationMps2: 1,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        maxLoadKg: 1000,
        safetyRadiusM: 0.4,
        batteryEnabled: false,
        initialSoc: 1
      },
      layout: {
        units: 'meter',
        calibrationProfile: null,
        nodes: [
          { id: 'A', type: 'parking', x: 0, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] },
          { id: 'B', type: 'aisle', x: 4, y: 0, z: 0, noStop: false, noParking: true, capacity: 1, allowedDirections: [] },
          { id: 'C', type: 'parking', x: 8, y: 0, z: 0, noStop: false, noParking: false, capacity: 1, allowedDirections: [] }
        ],
        edges: [
          { id: 'A-B', from: 'A', to: 'B', lengthM: 4, directionMode: 'twoWay', reservationType: 'edge', conflictGroup: 'A-B', noParking: true }
        ],
        zones: []
      },
      physicsParams: {
        emptySpeedMps: 2,
        loadedSpeedMps: 1,
        accelerationMps2: 1,
        switchDirectionSec: 0,
        liftTimeSec: 0,
        lowerTimeSec: 0,
        loadedClearanceM: 0.2,
        reservationClearanceSec: 0.2
      }
    });
    const sim = new ShuttleSimCore(scenario);
    sim.setVehicleRouteForTest('SH-01', ['A', 'B']);
    sim.step(0.2);
    const travelSec = sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01')?.legTravelSec ?? 0;
    expect(travelSec).toBeCloseTo(calculateTravelTimeSec(4, 2, 1), 6);
    while (sim.getState().vehicles.find((vehicle) => vehicle.id === 'SH-01')?.currentEdgeId) {
      sim.step(0.2);
    }
    expect(sim.getState().simTimeSec).toBeCloseTo(travelSec, 0);

    const loadedTravelSec = calculateTravelTimeSec(4, scenario.physicsParams.loadedSpeedMps, scenario.physicsParams.accelerationMps2);
    expect(loadedTravelSec).toBeGreaterThan(travelSec);
  });

  it('reports lightweight inbound theoretical shuttle capacity from the current layout and speed parameters', () => {
    const sim = new ShuttleSimCore(createDefaultShuttleScenario({ vehicles: { count: 12 } }));
    const baseline = sim.getState().kpis.theoreticalCapacity!;

    expect(baseline.kind).toBe('inbound');
    expect(baseline.shuttleCount).toBe(12);
    expect(baseline.singleShuttlePph).toBeGreaterThan(40);
    expect(baseline.fleetPph).toBeCloseTo(baseline.singleShuttlePph * 12, 2);
    expect(baseline.idealCycleSec).toBeCloseTo(
      baseline.loadedTravelSec + baseline.emptyReturnSec + baseline.liftAndLowerSec,
      3
    );
    expect(baseline.averageLoadedDistanceM).toBeGreaterThan(0);
    expect(baseline.averageEmptyReturnDistanceM).toBeGreaterThan(0);
    expect(baseline.assumptions).toContain('inbound-only ideal with unlimited lift-side demand');

    expect(sim.setParam('/physicsParams/loadedSpeedMps', 2).accepted).toBe(true);
    const fasterLoaded = sim.getState().kpis.theoreticalCapacity!;
    expect(fasterLoaded.singleShuttlePph).toBeGreaterThan(baseline.singleShuttlePph);
  });

  it('reports top-lift theoretical capacity as four physical bounds instead of the legacy inbound-only formula', () => {
    const sim = new ShuttleSimCore(createInboundOutboundDemoScenario());
    const theory = sim.getState().kpis.theoreticalCapacity!;

    expect(theory.kind).toBe('top-lift-column');
    expect(theory.formulaVersion).toBe('top-lift-four-bound-v1');
    expect(theory.bounds.map((bound) => bound.kind).sort()).toEqual([
      'column-resource',
      'lift-port',
      'network-min-cut',
      'shuttle-cycle'
    ]);
    expect(theory.limitingBound).toBe(theory.bounds[0]!.kind);
    expect(theory.fleetPph).toBe(theory.bounds[0]!.pph);
    expect(theory.singleShuttlePph).toBeCloseTo(theory.fleetPph / 8, 3);
    expect(theory.achievedOutboundPct).toBe(0);
    expect(theory.achievedTotalPct).toBe(0);
    expect(theory.assumptions.join(' ')).toContain('top-lift four-bound estimate');
  });
});
