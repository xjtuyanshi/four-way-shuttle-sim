{
  "name": "four-way-shuttle-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev:api": "pnpm --filter shuttle-api dev",
    "dev:dashboard": "pnpm --filter shuttle-dashboard dev",
    "shuttle:prereq": "pnpm --filter shuttle-api prereq",
    "shuttle:validate": "pnpm --filter shuttle-api validate:phase0",
    "shuttle:ws-smoke": "node scripts/smoke-shuttle-ws.mjs",
    "test:shuttle": "vitest run packages/shuttle-sim-core/src/index.test.ts",
    "unreal:setup": "node scripts/setup-unreal-visual-twin.mjs",
    "unreal:build": "node scripts/smoke-unreal-visual-twin.mjs --build-only",
    "unreal:smoke": "node scripts/smoke-unreal-visual-twin.mjs",
    "unreal:stage": "node scripts/stage-unreal-visual-twin.mjs",
    "unreal:pixelstreaming:check": "node scripts/check-pixel-streaming-readiness.mjs",
    "build": "pnpm run typecheck && pnpm --filter shuttle-dashboard build"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
